# A8 — QA / 测试缺口审计（R3H-04）

**维度：** A8 · test gap / QA  
**Worktree：** `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3h04`  
**判定：** **PASS_WITH_GAPS**  
**BLOCKING：** 0 · **NON-BLOCKING：** 9

---

## 1. 执行证据

### 1.1 AUDIT.plan.md §1 A8 命令（必跑）

```text
uv run pytest tests/test_prediction_market_adapters.py tests/test_web_evidence_adapter.py \
  tests/test_no_clean_write_for_web_evidence.py tests/test_r3h_source_final_decisions.py \
  -q -k "kalshi or polymarket or web_search or layer or clean or resolve" \
  --basetemp=.audit-sandbox/pytest
```

| 指标 | 结果 |
|------|------|
| 收集 | 36 |
| 通过 | 34 |
| 跳过 | 2（`KALSHI_LIVE_SMOKE` / `POLYMARKET_LIVE_SMOKE` 未设 — Tier B optional） |
| 取消选择 | 9 |
| 退出码 | 0 |
| 耗时 | ~0.8s |

**按文件收集分布：**

| 文件 | `-k` 命中 |
|------|-----------|
| `tests/test_prediction_market_adapters.py` | 14 / 18 |
| `tests/test_web_evidence_adapter.py` | 7 / 9 |
| `tests/test_no_clean_write_for_web_evidence.py` | 15 / 15 |
| `tests/test_r3h_source_final_decisions.py` | **0 / 3** |

> **命令设计缺口：** A8 命令列出 `test_r3h_source_final_decisions.py`，但 `-k` 子串不匹配其 3 个用例名；frozen §12 要求该文件三源段通过，实际不在 A8 选择器覆盖内（见 G1）。

### 1.2 frozen §11 补充命令（A8 范围外 · 已旁证）

```text
uv run pytest tests/test_source_route_planner.py tests/test_source_capabilities.py \
  -q -k "kalshi or polymarket or web_search" --basetemp=.audit-sandbox/pytest
```

| 结果 | 5 passed |
|------|----------|

### 1.3 全库 pytest（若可）

```text
uv run pytest -q --basetemp=.audit-sandbox/pytest
```

| 结果 | **4 FAILED**（与 R3H-04 三源无关） |
|------|-------------------------------------|

失败模块（均非本轨 adapter 测试）：

- `tests/test_layer3_snapshot_builder.py::test_layer3Snapshot_l5BundleOutsideProjectRoot_rejects`
- `tests/test_layer4_market_structure.py::test_marketAdapter_bundleDirOutsideProject_rejects`
- `tests/test_official_macro_adapters.py::test_liveEvidenceBridge_resolveRawPath_rejectsOutsideProjectRoot`
- `tests/test_round3g_sandbox_clean_write_rehearsal.py::test_RehearsalRunner_perSourceDataHealthProfiles`（`FileNotFoundError` rollback artifact）

对比 Execute `9.8-full-pytest.txt`（39 FAIL）：本 worktree 已改善，但 merge gate 仍非全绿。

### 1.4 catalog / docstring 门禁

| 检查 | 结果 |
|------|------|
| `scripts/check_test_catalog.py` | OK: 119 modules |
| `tests/test_docstring_quadruple_coverage.py` | 全绿 |
| 四文件 catalog 登记 | 均已登记（见 §4） |

---

## 2. GitNexus

| 操作 | 结果 |
|------|------|
| `query("kalshi polymarket web_search prediction market clean write manual review")` | 返回 `assert_can_write` / Layer5 validator 等通用流程；**未命中** R3H-04 新 port/normalizer |
| `context("create_kalshi_fetch_port")` | **not found** |
| `context("reject_clean_table_promotion")` | **not found** |
| `impact("EvidenceFoundationValidator.validate_record")` | **not found** |

与 Execute `research/gitnexus-execute-summary.md` 一致：**索引未覆盖 R3H-04 新增符号**（`kalshi_port`、`polymarket_port`、`web_search_evidence_port`、`probability_signal`、`manual_review_staging`）。Audit 无法对新增符号做 upstream blast-radius 追溯；建议在 merge 前 `node .gitnexus/run.cjs analyze`。

`query` 仍确认 Layer5 `EvidenceFoundationValidator.validate_record` 存在于 `backend/app/layer5_evidence/foundation.py`，与 layer smoke 测试对象一致。

---

## 3. 五字段 docstring 与测试目的弱化

### 3.1 五字段合规

抽检四文件全部 `test_*`（含 parametrize 展开）：

| 文件 | `test_*` 数 | 含「覆盖范围」docstring 数 | 判定 |
|------|-------------|---------------------------|------|
| `test_prediction_market_adapters.py` | 18 | 18 | ✅ |
| `test_web_evidence_adapter.py` | 9 | 9 | ✅ |
| `test_no_clean_write_for_web_evidence.py` | 8 函数 → 15 用例 | 8（parametrize 共享） | ✅ |
| `test_r3h_source_final_decisions.py` | 3 | 3 | ✅ |

五字段（覆盖范围 / 测试对象 / 目的/目标 / 验证点 / 失败含义）齐全；模块级 docstring 与 catalog `purpose` 对齐。

### 3.2 测试目的弱化（purpose weakening）

**未发现**为通过 pytest 而削弱 frozen §10 / Red Flag 目的的改判证据：

- `-k resolve` 仍断言 `FORBIDDEN_FIELDS`（含 `resolved_outcome`、`factual_resolution`、`clean_write_target` 等）。
- web_search 仍强制 `need_human_review=true` + `manual_review_state=queued`。
- polymarket `resolution_source` 测 URL 元数据，非事实判定。

**弱断言模式（非 purpose 弱化，但降低回归信号）：**

| 用例 | 问题 |
|------|------|
| `test_kalshi_port_liveWithoutOptIn_blocksUnauthorized` | `pytest.raises((PredictionMarketLiveSmokeError, Exception))` 过宽 |
| `test_polymarket_port_liveWithoutOptIn_blocksUnauthorized` | 同上 |
| `test_layer_smoke_*`（3 个） | 仅 `validate_record()` 不抛异常，无对 provenance/summary 字段的语义断言 |
| `test_bootSkeleton_*`（4 个） | 元测试（模块/docstring 存在性），Execute 9.0 骨架用途合理 |

---

## 4. catalog 登记

| 模块 | type | purpose（摘要） | verifies（specs） |
|------|------|-----------------|------------------|
| `test_prediction_market_adapters.py` | runtime-contract | R3H-04 预测市场适配器 | source_capability/route, layer5_evidence |
| `test_web_evidence_adapter.py` | runtime-contract | R3H-04 网页证据 | layer5_evidence, user_input_privacy |
| `test_no_clean_write_for_web_evidence.py` | negative-runtime | 三源 clean-write / 不得 resolve | source_route, layer5_evidence |
| `test_r3h_source_final_decisions.py` | policy-contract | R3H 任务卡终态门禁 | source_registry |

`specs/verification/contract_coverage.yaml` 已将三源测试挂到 `source_route_contract` 与 `layer5_evidence_contract` requirements。

---

## 5. Red Flag → 测试映射（frozen §13 + §10.1）

| Red Flag / AC | 覆盖 | 证据 |
|---------------|------|------|
| 预测字段 `resolved_outcome` / `fact` / `confirmed` | ✅ 测试 | `-k resolve` + normalizer reject + port 输出负例 |
| web_search 路由选中 clean-table writer | ✅ 测试 | `test_web_search_port_route_validationOnlyPrimaryBlockedWhenEnabled`；registry `fallback_policy=manual_review_required` |
| 修改 R3H-03 CN port/registry | ➖ A8 外 | A3/A5 维；本轨测试未覆盖 |
| 运行时 import OpenBB client | ⚠️ 部分 | artifact 形状测（`test_web_search_staging_openbbWidgetArtifactShape_*`）；**无** pytest 静态 import 扫描（A3 `rg` 负责） |
| kalshi/polymarket route 不得 clean writer（§10.1） | ⚠️ 间接 | registry `fallback_policy` + DISABLED 默认；**缺** READY 路径下「输出类型非 clean」显式断言 |
| polymarket `resolution_source` 非事实 | ✅ 测试 | `test_evidence_contract_polymarketReplay_*` + layer smoke |
| manual_review 不得 bypass | ⚠️ 部分 | `reject_clean_table_promotion` 双测；**缺** WriteManager / DbValidationGate 跨模块 bypass 负例 |
| Resource cap（§7 max_queries / max_window_days） | ❌ 缺口 | 仅有 `max_markets` / `max_results` cap 溢出测；**无** `max_queries`、`max_window_days` |
| mock-first / live user gate | ⚠️ 部分 | kalshi/polymarket liveWithoutOptIn ✅；web_search 无 live 路径（mock-only，可 defer） |
| 三源 `READY_WITH_EVIDENCE` 终态 | ✅ 旁证 | `test_r3h04_predictionWebSources_*`（§11 文件，非 A8 选择器） |
| `test_r3h_source_final_decisions` 三源段 | ✅ 文件存在 | **不在 A8 `-k` 范围**（G1） |

---

## 6. 缺口清单（仅报告）

| ID | 优先级 | 描述 | 建议补测方向（不在本 Audit 执行） |
|----|--------|------|----------------------------------|
| G1 | **High** | A8 命令含 `test_r3h_source_final_decisions.py` 但 `-k` 过滤 0 条；与 frozen §12 脱节 | 调整 A8 命令（去掉该文件或扩展 `-k`）；或单独跑全文件 |
| G2 | **High** | `max_queries`（web_search）与 `max_window_days`（kalshi/polymarket）cap **无**负例测 | 仿 `test_*_capOverflow_*` 模式各 1 条 |
| G3 | **Medium** | polymarket port 缺 `clean_write_target` 输出负例（kalshi 有 `test_no_clean_write_kalshiPortOutput_*`） | 对称 parametrize kalshi/polymarket |
| G4 | **Medium** | kalshi/polymarket READY route 未断言 evidence-only / 非 clean-table writer | 扩展 route planner 断言 `output_kind` 或 contract tag |
| G5 | **Medium** | 无 `DataSourceService.fetch` 三源集成测 | 最低 1 条 sandbox fetch → bundle schema 断言 |
| G6 | **Medium** | Layer smoke 仅「不抛异常」 | 断言 provenance 字段值 / summary 禁止 resolve 关键词 |
| G7 | **Low** | live gate 测试捕获 `Exception` 过宽 | 收窄为 `PredictionMarketLiveSmokeError` |
| G8 | **Low** | GitNexus 未索引 R3H-04 新符号 | merge 前 re-analyze |
| G9 | **Low** | 全库 pytest 4 FAIL（非本轨） | merge coordinator / 他轨 repair |

---

## 7. 覆盖摘要（qa-expert §3.8）

### Current Coverage

- A8 选择器：**34 passed / 2 skipped**，核心 Red Flag（resolve 字段、web manual_review、registry fallback）有测。
- frozen §11 路由/capability 旁证：**5 passed**。
- catalog + 五字段 docstring：**合规**。

### Coverage Gaps

- A8 命令与 `test_r3h_source_final_decisions` 选择器不一致（G1）。
- §7 Resource cap 两维未测（G2）。
- 对称 port 负例与 READY route evidence-only 语义（G3–G4）。
- 跨模块 manual_review bypass / DataSourceService 集成（G4–G5）。

### Priority

- **Critical：** 无（A8 命令全绿，无数据丢失类漏测）。
- **High：** G1、G2。
- **Medium：** G3–G6。
- **Low：** G7–G9。

---

## 8. 结论

**PASS_WITH_GAPS**

- **通过：** AUDIT.plan.md §1 A8 必跑命令全绿；四测试模块 catalog 登记完整；五字段 docstring 合规；未发现测试目的弱化；Red Flag 主路径（禁止 resolve、web manual_review、registry fallback）有回归锚点。
- **保留：** A8 选择器未覆盖 `test_r3h_source_final_decisions`（G1）；cap 维度与部分对称/集成负例缺失（G2–G6）；GitNexus 索引滞后（G8）；全库 pytest 仍有 4 个他轨失败（G9）。

**Repair 建议（主会话，非 A8 执行）：** 优先 G1+G2；G3–G6 可在 §4.3 defer 并注明搜索范围；merge 前 re-index GitNexus。
