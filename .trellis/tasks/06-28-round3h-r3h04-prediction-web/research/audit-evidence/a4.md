# A4 — audit-quality（code-reviewer）

**维度：** A4 · code-review-and-quality + doubt-driven-development  
**任务：** `06-28-round3h-r3h04-prediction-web` (R3H-04)  
**Worktree：** `quant-monitor-desk-wt-r3h04`  
**审计范围：** 三 fetch port · `probability_signal.py` · `manual_review_staging.py` · `prediction_market_live_smoke.py` · registry/capability 三源切片 · `tests/test_prediction_market_adapters.py` · `tests/test_web_evidence_adapter.py` · `tests/test_no_clean_write_for_web_evidence.py` · `tests/test_source_route_planner.py`（R3H-04 增量）  
**权威对照：** frozen §2.8 / §7 / §10.1 · `AUDIT.plan.md` A4 行 · `source_route_contract.yaml` · `layer5_evidence_contract.yaml`  
**判定：** **PASS_WITH_FIXES**  
**OPEN：** **5** · **BLOCKING：** **0**

---

## Review Summary

**Verdict:** PASS_WITH_FIXES

**Overview:** R3H-04 三源实现遵循 per-source port + normalizer/staging 分层，mock/replay-first 与 fail-closed 负例（禁止 resolve 字段、web clean promote 拒绝、validation_only 路由阻断）在**运行时**基本闭合。A4 指定攻击面中，kalshi `max_markets` 与 web `max_results` 有 cap 溢出 pytest；manual_review 读路径会 canonicalize 恶意 fixture；`reject_forbidden_resolution_fields` 可拒 `resolved_outcome`。但 **polymarket cap、三源 `max_window_days`、web `max_queries`、polymarket evidence-only 路由负例、对抗 replay fixture** 缺少登记测试；`resolution_source` 在 normalizer/live 路径**不校验 URL**，可接受含 “resolved” 语义的纯文本——与 frozen §10.1「仅为 URL 元数据」存在语义缺口。A8 子集 pytest 34 passed / 2 skipped（live smoke opt-in）。

---

## 多轴审查（§3.4）

| 轴 | 发现 | 阻塞? | 证据 |
| --- | --- | --- | --- |
| 正确性 | kalshi `max_markets` cap 溢出 → `PortError`（含 `cap`） | 否 | `kalshi_port.py:84` · `test_kalshi_port_capOverflow_blocksOverMaxMarkets` |
| 正确性 | polymarket `max_markets` cap **运行时**拒绝，**无**对等 pytest | 否（测缺口） | `polymarket_port.py:70,148` · 手工：`max_markets=6` → `exceeds cap=5` |
| 正确性 | web `max_results` cap 溢出有测；`max_queries` cap **无**测 | 否（测缺口） | `web_search_evidence_port.py:55-56,82-84` · `test_web_search_port_capOverflow_blocksOverMaxResults` |
| 正确性 | 三源 `max_window_days=30` **均未** pytest；运行时 kalshi 超窗 → `PortError` | 否（测缺口） | `reject_window_span_over_cap` · `kalshi_port.py:85-88` · 手工：74 天 → `exceeds cap=30` |
| 正确性 | web_search yaml primary + `validation_only` → `VALIDATION_ONLY_BLOCKED` | 否 | `test_web_search_port_route_validationOnlyPrimaryBlockedWhenEnabled` |
| 正确性 | polymarket `event_market_contract` yaml primary + `validation_only` **运行时**阻断，**R3H-04 无**登记测 | 否（测缺口） | `source_registry.yaml:249,668` · 手工：`validation_only_cannot_be_primary` |
| 正确性 | 预测 bundle 禁止 `resolved_outcome` 等；normalizer 构造/读路径 fail-closed | 否 | `probability_signal.py:17-46` · `test_resolve_probabilityNormalizer_rejectsForbiddenResolutionField` |
| 正确性 | `resolution_source` **不**校验 URL；可写入 `"Market resolved YES on Jan 1"` | 否（语义风险） | `probability_signal.py:61` · `polymarket_port.py:143` · 手工验证 |
| 错误处理 | web `read_web_evidence_staging_bundle` 忽略 fixture 中 `need_human_review=false`，强制 canonicalize | 否 | `manual_review_staging.py:118-127` · 手工对抗 fixture |
| 错误处理 | `stage_for_manual_review` 拒 `need_human_review=false` | 否 | `manual_review_staging.py:156-159` · `test_web_search_staging_stageForManualReview_neverPermitsCleanWrite` |
| 错误处理 | live gate 测试 `pytest.raises((PredictionMarketLiveSmokeError, Exception))` 过宽 | 否 | `test_prediction_market_adapters.py:390,412` |
| 可维护性 | 三 port 复用 `reject_over_cap` / `finalize_bundle`；未改 `resource_guard.py` | 否 | frozen §2.8 #5 |
| 测试 | 五字段中文 docstring 齐全；A8 子集全绿 | 否 | `tests/test_*` 模块头 + A8 命令输出 |
| 测试 | **无** committed 对抗 replay fixture（仅正向 fixture 3 个） | 否（测缺口） | `tests/fixtures/replay/prediction_market/**` · `web_evidence/**` |
| 安全（局部） | `reject_clean_table_promotion` 任意表名硬拒 | 否 | `manual_review_staging.py:176-180` · `test_no_clean_write_webStaging_rejectsCleanTablePromotion` |
| 性能 | on-demand 小样本；cap 在 port 内（A6 SKIP 合理） | 否 | frozen §7 |

---

## AUDIT.plan A4 攻击面逐项

| 检查项 | 运行时 | pytest / 对抗 fixture | 结论 |
| --- | --- | --- | --- |
| 三源 cap 溢出 | kalshi/poly/web **均有** `reject_over_cap`；poly 构造期不查 `max_markets` 但 fetch 查 | 仅 kalshi `max_markets` + web `max_results`；缺 poly cap、三源 window、web queries | **PASS_WITH_FIXES** |
| evidence-only route 负例 | web + poly（validation_only）运行时 fail-closed | web 有专用测；poly `event_market_contract` 未入 R3H-04 路由测 | **PASS_WITH_FIXES** |
| manual_review 绕过负例 | read canonicalize + stage 校验 + promote 硬拒 | 无对抗 fixture pytest（手工可证） | **PASS_WITH_FIXES** |
| polymarket `resolution_source` 非事实 | 禁 `resolved`/`resolved_outcome` 字段 | **未**约束 URL / _resolution 语义文本 | **PASS_WITH_FIXES** |
| 对抗 fixture | normalizer 读路径可拒篡改字段 | 仓库无 `*_adversarial_*.json` 及对应测 | **PASS_WITH_FIXES** |

---

## DOUBT（错误处理 / 边界）

对抗搜索范围：三 port · 两 normalizer/staging · route planner R3H-04 增量 · replay fixtures · frozen §7 caps · §10.1 负例表。

| ID | 位置 | 疑点 | 结论 | 状态 |
| --- | --- | --- | --- | --- |
| A4-DOUBT-01 | `polymarket_port.py:181-191` | `create_polymarket_fetch_port` 构造期不校验 `max_markets`，仅 `len(market_slugs)` | `fetch_payload` 首行 `reject_over_cap(max_markets)` 闭合；**缺** pytest 回归 | **OPEN**（NON-BLOCKING） |
| A4-DOUBT-02 | 三 port `fetch_payload` | `max_window_days` 仅当 `start_time`+`end_time` 均非空才 enforce | 与 `reject_window_span_over_cap` 设计一致；无 window 参数时 cap 不生效——**计划外边界** | **OPEN**（NON-BLOCKING） |
| A4-DOUBT-03 | `polymarket_port.py:143` | live `resolution_source` fallback 到 `description` 可能非 URL | normalizer 无 URL schema 校验；mock 测仅 assert `https://` | **OPEN**（NON-BLOCKING） |
| A4-DOUBT-04 | `manual_review_staging.py:111-128` | 读路径重建 bundle，fixture 篡改 `need_human_review` 不可持久化 | 安全向 canonicalize；**无** pytest 对抗用例 | **CLOSED**（测缺口） |
| A4-DOUBT-05 | `test_prediction_market_adapters.py:390` | `pytest.raises(..., Exception)` 吞掉非预期异常 | live 路径默认 skip；仍削弱 RED 信号 | **OPEN**（NON-BLOCKING） |

---

## 计划外发现

> 对抗性搜索：对照 frozen §10.2 攻击面 · `source_route_contract` · `rg resolved_outcome|factual_resolution|clean_write` on R3H-04 diff · 手工 runtime 探针（cap / route / adversarial JSON）。

| ID | 严重度 | 发现 | 建议修复 | Audit status |
| --- | --- | --- | --- | --- |
| A4-O1 | NON-BLOCKING | polymarket `max_markets` cap 无 pytest（kalshi 有） | 镜像 `test_kalshi_port_capOverflow_*` 为 polymarket | **OPEN** |
| A4-O2 | NON-BLOCKING | 三源 `max_window_days` cap 无 pytest | 各 port 一条超窗 `FetchRequest` 负例 | **OPEN** |
| A4-O3 | NON-BLOCKING | web `max_queries` cap 无 pytest | `create_web_search_evidence_fetch_port(queries=4×…)` 负例 | **OPEN** |
| A4-O4 | NON-BLOCKING | polymarket `event_market_contract` validation_only primary 阻断未入 R3H-04 路由测 | 扩展 `test_source_route_planner` 或 `test_prediction_market_adapters` | **OPEN** |
| A4-O5 | NON-BLOCKING | `resolution_source` 可接受非 URL 含 resolution 语义文本 | normalizer 增加 `https?://` 校验或 live 路径去掉 `description` fallback | **OPEN** |
| A4-O6 | NON-BLOCKING | 无 committed 对抗 replay fixture + pytest | 如 `polymarket_adversarial_resolved_outcome.json`、`web_adversarial_no_manual_review.json` | **OPEN** |
| A4-O7 | NON-BLOCKING | worktree 变更 largely uncommitted | 合并协调器责任；A4 已审工作区内容 | **CLOSED**（流程） |

**对抗搜索声明：** 已对照设计文档（frozen §7/§10）、契约字段、任务卡 Red Flags、AUDIT.plan A4 矩阵；**未**将 MASTER/implement.jsonl 用例表当作完备性上限。除 CLOSED 项外，上表 OPEN 均为测试/契约语义缺口，**无**已证实的 clean-write 或 factual-resolve 运行时旁路。

---

## Critical / Important / Suggestions（code-reviewer 模板）

### Critical Issues

（无）

### Important Issues

- `tests/test_prediction_market_adapters.py` — 缺 `test_polymarket_port_capOverflow_blocksOverMaxMarkets`；frozen §7 要求三源 cap 对等。
- `tests/test_prediction_market_adapters.py` / `tests/test_web_evidence_adapter.py` — 缺三源 `max_window_days` 超窗负例（对照 R3H-02 market adapters window cap 测模式）。
- `tests/test_web_evidence_adapter.py` — 缺 `max_queries` 溢出负例（`web_search_evidence_port.py:82-83` 已实现）。
- `tests/test_source_route_planner.py` — polymarket `event_market_contract` + enabled 域应断言 `VALIDATION_ONLY_BLOCKED`（web_search 在 adapter 测有，poly 缺对称覆盖）。
- `backend/app/datasources/normalizers/probability_signal.py:61` + `polymarket_port.py:143` — `resolution_source` 无 URL 校验，live 可落盘非 URL / 含 resolution 语义字符串，与 frozen「URL 元数据非事实」不一致。

### Suggestions

- `tests/test_prediction_market_adapters.py:390,412` — 收窄 `pytest.raises` 为 `PredictionMarketLiveSmokeError` only。
- 增加对抗 fixture pytest：`read_web_evidence_staging_bundle` 对 `need_human_review=false` fixture 仍输出 `true/queued`；`read_probability_signal_evidence_bundle` 对含 `resolved_outcome` fixture 抛 `ProbabilitySignalEvidenceError`。
- `web_search_evidence_port` queries 溢出错误文案不含 `cap` 子串，与 kalshi 测试 `match="cap"` 不一致——可统一 `reject_over_cap` 用于 queries 计数。

### What's Done Well

- `probability_signal` + `manual_review_staging` 双 SSOT 清晰；`FORBIDDEN_RESOLUTION_FIELDS` 在 build/read/write 全链复用。
- web_search `reject_clean_table_promotion` + registry `fallback_policy: manual_review_required` 双层防御。
- mock/replay-first 默认 + live smoke env/YAML 双 gate（`prediction_market_live_smoke.py`）。
- 测试模块五字段中文 docstring 完整，符合 testing-guidelines。

### Verification Story

- Tests reviewed: **yes** — A8 子集 `34 passed, 2 skipped`（`--basetemp=.audit-sandbox/pytest`）；另手工 runtime 探针验证 poly cap / window / adversarial JSON / route。
- Build verified: **no** — 未跑全库 pytest / loop_maintain（非 A4 必做）。
- Security checked: **yes（局部）** — 无密钥进 diff；clean promote 硬拒；validation_only 路由阻断；无 OpenBB runtime import。

---

## 结论

**PASS_WITH_FIXES** — 核心安全语义（不写 clean、不 resolve 事实、web 须 manual review）在实现层闭合，A8 子集全绿。合并前建议补齐 A4-O1–O6 测试与 `resolution_source` URL 契约，并将对抗 fixture 纳入 `tests/fixtures/replay/**` 回归，以满足 AUDIT.plan「对抗 fixture」完备性。

**Repair 优先级：** O5（语义）≥ O1–O4（cap/route 测）≥ O6（对抗 fixture 资产）≥ DOUBT-05（断言收窄）。
