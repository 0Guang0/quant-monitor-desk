# Phase C 候选（Phase A 不删文件）。须满足 DEBT.plan.md §3.5 全部条件。
candidates: []
# 评估记录（非执行项）：
# - test_r3x_ponytail_pilot_prep_bucket_a / structural_bucket_b / residual_open_items_closure：
#   catalog type=runtime-contract → 不满足 §3.5.1，不得提议整文件删除。
# - test_audit_fixes / test_audit_remediation：type=protocol 且 verifies 为空，满足 §3.5.1，
#   但各用例仍守卫独立审计回归路径（WriteManager+DbValidationGate、reconcile 三分支、
#   job_event_log 等），无单一 replacement_coverage 可覆盖整模块 → 不列入 candidates。
# - 桶内跨文件断言重叠（如 health_check stub、ADAPTER_DOMAIN_COMPATIBILITY_MAP=={}）
#   映射不同 ADV/POST14 ID，属有意双伞回归，非 §3.5 可删整文件情形。
