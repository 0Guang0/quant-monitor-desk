# A8 — Test gap

## Result

**PASS**

`tests/test_layer3_loader.py` 原 13 用例完整覆盖 `layer3_loader_contract.yaml` 七条硬规则、AC-020-1..5 与 MASTER §5.3 fail-fast 场景；注释均含 `purpose` / `target` / `verifies` / `failure_meaning`（中文），断言为业务语义而非弱空检查。AUDIT 补测 `test_layer3Loader_emptyBundle_rejects`（五表皆空）已绿，未改动既有 13 用例 purpose。`test_batch3_staged_downstream_gate.py` 复跑 2/2 绿。

**证据：** `research/audit-sections/a8-pytest-evidence.txt`（`uv run pytest tests/test_layer3_loader.py tests/test_batch3_staged_downstream_gate.py -q --basetemp=.audit-sandbox/pytest`，16 passed）

---

## §3.8 Red Flag 覆盖

| Red Flag | 覆盖 | 补测/证据 |
| -------- | ---- | --------- |
| 全量 registry 扫描 | Covered（架构 + staged fixture） | `STAGED_LAYER3_BUNDLE_DIR` 固定 `tests/fixtures/layer3_staged_bundle/`；§3.3 forbidden `specs/layer3_global_industry_chains/`；无单测指向生产路径 — **defer §4.3**（slice 禁令 + A1 对照，非 loader 单测职责） |
| 绕过 staged mode | Covered | `test_layer3Loader_nonStagedMode_rejects` |
| event_only 当行情锚 | Covered | `test_layer3Loader_eventOnlyPrivate_notDailyPriceAnchor` |
| 改 layer2/lineage contract | defer | §3.3 forbidden；A3 静态审计，非本文件 pytest 范围 |
| 弱断言假绿 | Covered | 13+1 用例均有语义断言；拒绝路径统一 `IndustryChainLoadError` + `match` |
| production-live 声称 | Covered | `test_layer3Loader_nonStagedMode_rejects` + `test_batch3_staged_downstream_gate.py` 文档 fail-closed token |
| 过度工程 / 重复造轮 | defer | Execute §0.3a ponytail + A2；非 A8 测试矩阵项 |

---

## Contract 七条 ↔ 用例映射

| # | `layer3_loader_contract.yaml` 规则 | 测试 | 判定 |
| - | ---------------------------------- | ---- | ---- |
| 1 | `chain_id` unique | `test_layer3Loader_duplicateChainId_rejects` | PASS |
| 2 | `node_id` unique | `test_layer3Loader_duplicateNodeId_rejects` | PASS |
| 3 | edge `from_node_id` / `to_node_id` exist | `test_layer3Loader_edgeMissingFromNode_rejects`, `test_layer3Loader_edgeMissingToNode_rejects` | PASS |
| 4 | `anchor_id` unique | `test_layer3Loader_duplicateAnchorId_rejects` | PASS |
| 5 | `anchor.node_id` exist | `test_layer3Loader_anchorMissingNode_rejects` | PASS |
| 6 | event_only private 非日度价锚 | `test_layer3Loader_eventOnlyPrivate_notDailyPriceAnchor` | PASS |
| 7 | P0_CORE/P0_EVENT 须有 `source_keys` | `test_layer3Loader_p0Anchor_missingSourceKeys_rejects` | PASS |

**附加（§5.3 / fail-fast / AC）：**

| 场景 | 测试 | 判定 |
| ---- | ---- | ---- |
| AC-020-1 五表加载成功 | `test_layer3Loader_loadsStagedFixture_success` | PASS |
| AC-020-5 staged-only | `test_layer3Loader_nonStagedMode_rejects` | PASS |
| 缺文件 fail-fast | `test_layer3Loader_missingBundleFile_rejects` | PASS |
| 坏 JSON fail-fast | `test_layer3Loader_invalidJson_rejects` | PASS |
| cross-chain 端点存在 | `test_layer3Loader_crossChainMissingNode_rejects`（`to_node_id` 悬空） | PASS |
| **AUDIT 边界：空 bundle** | `test_layer3Loader_emptyBundle_rejects`（A8 新增） | PASS |

---

## 缺口与 defer

| Severity | Gap | §4.3 | Rationale |
| -------- | --- | ---- | --------- |
| LOW | cross-chain `from_node_id` 悬空无独立具名用例 | No | `loader.py` 对称校验；常规 edge `from_node_id` 用例已证同路径；`crossChainMissingNode` 覆盖 `to_node_id` |
| LOW | 单 chain + 其余四表空列表是否应拒绝 | No | contract 七条未要求非 chain 表非空；§6.1 eco「每表 ≥1」为 fixture 体量指引；全空已由 A8 补测覆盖 |
| — | 生产 registry 路径加载 | defer | forbidden + staged-only gate；无 pytest 负例指向 `specs/` — A1/A3 口径 |

无 HIGH/MEDIUM 缺口阻断 Audit PASS。

---

## A8 补测（sandbox，未 commit）

| 用例 | purpose（不变更既有 13 条） | 命令结果 |
| ---- | --------------------------- | -------- |
| `test_layer3Loader_emptyBundle_rejects` | 五表皆为空的 staged bundle 须 `IndustryChainLoadError`，不得返回空 `IndustryChainLoadResult` | GREEN |

实现：`_write_empty_bundle()` 在 `tmp_path` 构造 manifest + `chains: []` + 四 JSON `[]`；断言 `match="non-empty chains|chains"`。

---

## DOUBT 自检

- 每个原始 Red Flag：测试 | explicit defer | §4.3 — **已填表**
- 注释 purpose / verifies / failure_meaning — **13+1 满足**
- `--basetemp` sandbox — **`.audit-sandbox/pytest`**
- 无 tautological 唯一断言 — **通过**
- batch3 gate 与 AC-020-5 联动 — **`test_batch3_staged_downstream_gate.py` 2/2 绿**
