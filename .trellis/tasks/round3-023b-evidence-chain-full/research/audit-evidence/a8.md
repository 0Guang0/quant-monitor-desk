# A8 audit-test-gap — B01-023 Layer5 Evidence Chain Full

**维度：** A8 · `testing-guidelines` + `doubt-driven-development` + `qa-expert`  
**权威：** `agents/audit-adversarial-authority.md`（契约 + 任务卡 Red Flags + 计划外边界）  
**工作区：** `quant-monitor-desk-wt-023-layer5` @ `feature/round3-023b-evidence-chain-full`（Wave D Track B）  
**判定：** **PASS**（2 项 NON-BLOCKING 缺口；无 BLOCKING）

---

## 1. AUDIT.plan §2 A8 验证矩阵

| 检查项 | 命令 / 口径 | 结果 |
| --- | --- | --- |
| pytest-isolated · 五字段 + 边界审阅 | `uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q --basetemp=.trellis/tasks/round3-023b-evidence-chain-full/.audit-sandbox/pytest` | **12 passed** |
| audit-prod-path · batch3 gate | `uv run pytest tests/test_batch3_staged_downstream_gate.py -q`（同上 basetemp + `QMD_DATA_ROOT=.trellis/tasks/round3-023b-evidence-chain-full/.audit-sandbox/data`） | **2 passed** |
| 合并 Tier A 选择器 | playbook §8.4 + MASTER §5.4 L1/L3 | **14 passed**（见 `research/audit-evidence/a8-pytest-layer5-chain-foundation-batch3.txt`） |

> **环境注记：** 首次 `--basetemp` 需先创建 `.trellis/tasks/round3-023b-evidence-chain-full/.audit-sandbox/pytest` 父目录（Windows 下 pytest 不自动 mkdir）；创建后复跑全绿。

---

## 2. MASTER §5.3 / AC-023 用例矩阵

| AC | §5.3 `test_*` | 状态 | 语义评价 |
| --- | --- | --- | --- |
| AC-023-1 | `test_instrumentRegistry_uniqueIds` | 绿 | 重复 `instrument_id` → `InstrumentRegistryError` + `duplicate`；非 tautology |
| AC-023-2 | `test_securityBar_rejectsFutureTradeDate` | 绿（部分） | 未来 `trade_date` → `EvidenceValidationError` + `future`；**event/financial/valuation 无独立 pytest**（见 §8） |
| AC-023-3 | `test_evidenceChain_traceUpstreamSnapshots` | 绿 | upstream ids + layer3/4 context 内容断言 |
| AC-023-3 | `test_evidenceChain_rejectsAgentTextAsFact` | 绿 | builder + `reject_agent_text_as_fact` 双路径 `match=agent` |
| AC-023-4 | `test_evidenceChain_severeConflictQueuesManualReview` | 绿 | severe → `QUEUED`；queued 无 `need_human_review` → 拒绝 |
| AC-023-5 | `test_evidenceReadPort_boundary` | 绿 | fake port 驱动 `build_from_port`；无 port → `EvidenceReadPort required` |
| AC-023-6 | playbook §8.4 + §11 验收 | 绿 | chain + foundation 双文件绿；batch3 gate 未回归 |
| 023A 回归 | `test_layer5_evidence_foundation.py`（6 测） | 绿 | identity / lineage / agent-not-fact 不退化 |

**§5.2 失败语义对照：**

| 能力 | 成功测 | 失败测 | 缺口 |
| --- | --- | --- | --- |
| registry | S1 唯一性 happy | 重复 id | — |
| bar | — | 未来 trade_date | — |
| chain upstream | 非空 ids 保留 | **空 upstream ids** | **实现有、测无**（§8 AA-023-A8-01） |
| chain context | 非空 context | **空 layer3/4 context** | **实现有、测无**（§8 AA-023-A8-02） |
| conflict | severe → queued | queued 无 flag | severe 正向 + 负向均已绑 |
| port | fake port | 无 port | — |

---

## 3. `layer5_evidence_contract.yaml` validation_tests

契约 **无** `validation_tests` 段；质量规则由 `quality_rules` + 行为 pytest 间接覆盖：

| 契约规则 | pytest 映射 | 状态 |
| --- | --- | --- |
| `agent_text_not_fact_source` | `test_evidenceChain_rejectsAgentTextAsFact` + foundation 3 测 | 绿 |
| `evidence_foundation_must_trace_to_fetch_or_hash` | `test_evidenceFoundation_factualRecord_requiresProvenance` | 绿（023A） |
| `evidence_must_reference_structured_context` | `test_evidenceChain_traceUpstreamSnapshots`（非空路径） | 绿；**空 context 拒绝未绑** |
| `event_must_have_source_or_local_path` | — | **无 L5 event validator pytest**（NON-BLOCKING，§8） |
| `high_must_be_greater_or_equal_low` 等 bar OHLC | — | bar validator 存在；仅 future-date 失败路径有测 |

`deferred_to_023b: []` — Execute 已兑现 foundation defer 项。

---

## 4. 五字段 docstring + 通俗中文（GLOBAL_TESTING_POLICY §7）

### `tests/test_layer5_evidence_chain.py`（6/6）

| 测试 | 五字段齐全 | 目的通俗 | 判定 |
| --- | --- | --- | --- |
| `test_instrumentRegistry_uniqueIds` | ✓ | staged 标的 id 不得重复 | PASS |
| `test_securityBar_rejectsFutureTradeDate` | ✓ | 禁止未来行情进入证据 | PASS |
| `test_evidenceChain_traceUpstreamSnapshots` | ✓ | 证据链须能追溯 L3/L4 快照 | PASS |
| `test_evidenceChain_rejectsAgentTextAsFact` | ✓ | 代理文本不能冒充事实 | PASS |
| `test_evidenceChain_severeConflictQueuesManualReview` | ✓ | 严重冲突须进人工复核队列 | PASS |
| `test_evidenceReadPort_boundary` | ✓ | 读 staged 数据须经 port 注入 | PASS |

### `tests/test_layer5_evidence_foundation.py`（6/6）

| 测试 | 五字段齐全 | 目的通俗 | 判定 |
| --- | --- | --- | --- |
| `test_evidenceFoundation_factualRecord_requiresProvenance` | ✓ | 事实证据须有来源指纹 | PASS |
| `test_evidenceFoundation_recordKinds_areDistinct` | ✓ | 证据种类边界清晰 | PASS |
| `test_evidenceFoundation_agentTextNotFactSource_rejectsFactualKind` | ✓ | 代理痕迹不得进事实类 | PASS |
| `test_evidenceFoundation_manualReviewFlag_requiresQueuedState` | ✓ | 需复核须同步入队状态 | PASS |
| `test_evidenceFoundation_identityHash_isDeterministic` | ✓ | 身份哈希用于去重幂等 | PASS |
| `test_layer5Lineage_envelopeMatchesSnapshotContractFields` | ✓ | 第五层血缘字段齐全 | PASS |

### `tests/test_batch3_staged_downstream_gate.py`（2/2）

五字段齐全；目的均为「Batch3 staged-only 门禁闭合、禁止 production-live」。

**门禁自检：** 上述 12 条 layer5 测均含 `覆盖范围` / `测试对象` / `目的/目标` / `验证点` / `失败含义`；无 AC 编号替代人话。

---

## 5. MASTER §7 Red Flags → 测试 / defer

| Red Flag | 覆盖 | 证据 |
| --- | --- | --- |
| 与 023A 重复实现 identity | 测 + 复用 | foundation 6 测绿；chain 调用 `EvidenceFoundationValidator` |
| Agent 文本作事实源 | 测 | chain + foundation 共 4 条 agent 拒绝路径 |
| 混 Batch 01 PR | scope | Track B；batch3 gate 文档测 |
| registry 并发闭合 | scope | 非 A8；单测唯一性 |
| 全量 bar 回填 | 测 + staged | 仅 `security_bar_daily` fixture；batch3 禁止 full-market |
| 改 L3/L4 代码 | scope | §3.3 forbidden |
| 弱断言假绿 | 审 | 无 `assert True` / 仅 `is not None` 单断言 |
| 绕过 as_of / 未来数据 | 测 | `test_securityBar_rejectsFutureTradeDate` |
| 交易动作语义 | doc | `no_trading_action_semantics: true`；builder 无 order 字段 — **NON-BLOCKING** |
| `R3-PARTIAL-4` instant UI | defer | ADR-023 + MASTER §3.2；pytest 冻结 queued 状态非 UI |
| `R2-RISK-2` storage 耦合 | 测 | `test_evidenceReadPort_boundary` |

---

## 6. test catalog / playbook §8.4

| 文件 | `tests/test_catalog.yaml` | 链接契约 |
| --- | --- | --- |
| `test_layer5_evidence_chain.py` | 已登记 · `policy-contract` | **specs 列为空** — hygiene 建议补 `layer5_evidence_contract.yaml` |
| `test_layer5_evidence_foundation.py` | 已登记 · `runtime-contract` | 同上 |
| `test_batch3_staged_downstream_gate.py` | 已登记 · `policy-contract` | `BATCH3_STAGED_DOWNSTREAM_GATE.md` |

playbook §8.4 要求 `uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q` — **已满足**。

---

## 7. 语义断言质量（对抗性）

| 项 | 结论 |
| --- | --- |
| 只 mock 外部 I/O | ✓ chain 测为纯内存 + in-test `_FakeEvidenceReadPort`；无 storage import |
| 失败路径语义 message | ✓ `match=` 覆盖 duplicate / future / agent / queued / port required |
| severe → queued | ✓ `need_human_review=True` + `ManualReviewState.QUEUED` 双断言 |
| severe without queue（queued 无 flag） | ✓ `test_evidenceChain_severeConflictQueuesManualReview` 第二段 |
| 空 upstream_snapshot_ids | **未测** — `evidence_chain.py:49-50` fail-closed |
| 空 layer3/layer4 context | **未测** — `evidence_chain.py:51-52` fail-closed |
| event/financial/valuation staged 行 | **未实现 validator 单测** — contract 有模型定义，023b 仅 bar validator |
| ResourceGuard 全市场扫描 | **无专用 pytest** — eco + 单 fixture；与 021/022 同级 — NON-BLOCKING |

---

## 8. 计划外发现

> 已按 `audit-adversarial-authority.md` 对照 `evidence_chain.py` 全分支、`evidence_validator.py`、`layer5_evidence_contract.yaml`、`023` 任务卡 §13 Red Flags 与 MASTER §5 外路径搜索。

| ID | 级别 | 发现 | 建议 |
| --- | --- | --- | --- |
| AA-023-A8-01 | **NON-BLOCKING** | **空 `upstream_snapshot_ids`** 已实现拒绝（`EvidenceChainError: upstream_snapshot_ids required`），AUDIT.plan §2 A8 点名边界，**无专用 pytest** | hygiene：`LayerContextBundle(upstream_snapshot_ids=())` → `match=upstream_snapshot_ids required` |
| AA-023-A8-02 | **NON-BLOCKING** | **空 `layer3_context` / `layer4_context`** 同上（`layer3_context and layer4_context are required`），测未绑 | 可与 AA-023-A8-01 合并为单测或 defer hygiene |
| AA-023-A8-03 | **NON-BLOCKING** | AC-023-2「bar/event/financial/valuation」仅 **security_bar** 有 validator + 失败测；event 等契约字段无 staged pytest | 若 Batch 6 扩展 validator 再补；本 slice staged-only 可登记 defer |
| — | — | **无 BLOCKING** | — |

**显式声明：** 已搜索 `tests/test_layer5_evidence_chain.py`、`tests/test_layer5_evidence_foundation.py`、`backend/app/layer5_evidence/**`、ADR-023、`UNRESOLVED_ITEM_TASK_COVERAGE.md`（`R3-PARTIAL-4` / `R2-RISK-2`）；除上表外未发现计划外 HIGH 风险未覆盖项。

**AUDIT.plan 点名边界核对：**

| 边界 | 实现 | pytest | 判定 |
| --- | --- | --- | --- |
| 空 upstream ids | ✓ `evidence_chain.py:49-50` | ✗ | AA-023-A8-01 |
| severe without queue | ✓ `_resolve_manual_review` + queued 无 flag 拒绝 | ✓ `test_evidenceChain_severeConflictQueuesManualReview` | **已覆盖** |

---

## 9. AUDIT.plan §3.8 摘要

| Red Flag / AC 缺口 | 覆盖 | 补测 / 证据 |
| --- | --- | --- |
| AC-023-1..5 | 6/6 chain + 6/6 foundation pytest 绿 | 本节 §1 + Execute `9.6-green.txt` |
| AC-023-6 / playbook §8.4 | Tier A 双文件 + batch3 绿 | 本节 §1 |
| 空 upstream / 空 context | 实现有、测无 | AA-023-A8-01/02 → `audit.report` §4.3 |
| event/financial/valuation | contract 有、测无 | AA-023-A8-03 |
| `R3-PARTIAL-4` UI | defer | ADR-023 |
| `R2-RISK-2` | 测 | `test_evidenceReadPort_boundary` |
| 023A 回归 | 6 测绿 | foundation 文件未改 purpose |

---

## 10. 判定与交接

| 项 | 值 |
| --- | --- |
| **A8 总判定** | **PASS** |
| BLOCKING | 0 |
| NON-BLOCKING | 3（AA-023-A8-01..03） |
| 补测 | 本轮 **未** 新增 pytest（缺口记入 `audit.report.md` §4.3 建议列；qa-expert 允许 ≤2 条边界补测，非阻断故 defer hygiene） |
| Execute 对齐 | `research/execute-evidence/full-pytest-green.txt` 全库绿；本 A8 仅复跑 layer5 + batch3 选择器 |

**结论：** B01-023 Execute 测试契约与 playbook §8.4 Tier A 门禁满足；五字段 docstring 与语义断言达标；023A foundation 回归未退化；batch3 staged gate 未回归。AUDIT.plan 点名的 **severe without queue** 已由 `test_evidenceChain_severeConflictQueuesManualReview` 覆盖；**空 upstream ids** 为 hygiene 级缺口，不阻断 Wave D `023b` Audit PASS。
