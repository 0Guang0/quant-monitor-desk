# A5 audit-completion — Wave C 022 Layer4 Market Structure

**维度：** A5 · verification-before-completion + doubt-driven-development  
**协同视角：** `quant-analyst`（证据链 / look-ahead）+ `risk-manager`（fail-closed / defer）+ `audit-adversarial-authority`  
**派发模型：** composer-2.5  
**分支：** `feature/round3-022-layer4-market` @ worktree `quant-monitor-desk-wt-022-layer4`  
**审计时间：** 2026-06-24  
**判定：** **PASS**（AC 闭环完整；playbook Plan 行与步进 green 证据偏弱，sandbox 复跑补偿）

---

## 1. 启动清单

| 项 | 状态 |
| --- | --- |
| `agents/audit-a5-completion.md` | 已读 |
| `agents/audit-adversarial-authority.md` | 已读 |
| `agents/quant-analyst.md` | 已读 |
| `agents/risk-manager.md` | 已读 |
| `AUDIT.plan.md` 全文 | 已读 |
| `MASTER.plan.md` §2 / §8–§10 | 已读 |
| `implement.jsonl` 全读（56 行） | 已读 |
| `022_implement_layer4_market_structure.md` §15 lineage | 已读 |
| `manifest-amend.md` | **不存在** — 无 amend 口径 |
| `research/original-plan-trace.md` | 已读 |

**约束：** 只读；未 `git commit`；未改生产库。

---

## 2. A5 Checklist

| 检查项 | 结果 |
| --- | --- |
| 每条 AC 追溯链 + 1–5 分 | ✅ §3.5 |
| §10 最弱 2 行 sandbox 抽检 | ✅ §4（Tier B 全库 + ruff） |
| `execute-evidence/*-green.txt` 非占位 | ⚠️ §5 — 步进 green 多为进度条；8.0/8.7 有摘要 |
| audit-prod-path（registry §2.1） | ✅ N/A — staged-only，无 prod 数据依赖 |
| registry / defer 项 | ✅ ADV-R3X / R3Y 边界未越界关闭（§8） |

---

## 3. Playbook §8.2 逐行对照

| 维度 | PASS 条件（playbook §8.2） | 审计结果 | 证据 |
| --- | --- | --- | --- |
| **Plan** | `validate-plan-freeze` exit 0；MASTER 含 L4 AC；§3.3 全文已索引；staged-only + `as_of` | **部分 FAIL** | `validate-plan-freeze` **exit 1**（E8：`implement.jsonl` 缺 §6 wiring 四路径）；`validate-execute-handoff` **exit 0**；MASTER §2 AC-022-1..8 齐全；SCI §12 索引 §3.3 |
| **实现** | `market_structure.py` 可运行；经 WriteManager 的 clean 写路径（若触及 DB） | **PASS** | `market_structure.py` + `lineage.py` + `models.py` 存在且 7 测全绿；**本任务无 DB 写**（MASTER §3.2 defer WriteManager sync） |
| **契约** | 符合 `layer4_market_contract.yaml`；不复制 L5 大表；无交易动作语义 | **PASS** | `test_marketBreadth_requiredFieldsPresent`、`test_marketSnapshot_noLayer5HistoryFields`；A1/A4 契约对照 PASS |
| **Lineage** | 快照含任务卡 §15 字段；测试证明 **no-future-data** | **PASS** | §6 字段表；`test_marketSnapshot_lineageRequiredFieldsComplete` + `test_marketSnapshotRejectsFutureInput` |
| **测试** | `test_layer4_market_structure.py` 全绿；五字段 + §2.2.2；ResourceGuard / eco 未触发全市场扫描 | **PASS** | **7 passed** in 0.07s；docstring 五字段齐全；A6 SKIP 已记 plan 外无 hot path |
| **MAP 验证** | layer4 测 + `test_batch3_staged_downstream_gate.py` 绿 | **PASS** | 7 passed + **2 passed**（sandbox 复跑 exit 0） |
| **任务卡验收** | `uv run pytest -q` 全绿；`ruff check` 相关路径无新增 error | **PASS** | 全库 pytest **exit 0**（~125s）；`ruff check backend/app/layer4_markets tests/test_layer4_market_structure.py` → All checks passed |
| **Audit** | A1–A8 各维报告；A6 可 SKIP | **进行中** | `research/audit-evidence/a1..a8.md` 已就绪（a6=SKIP）；本文件补 A5 |
| **边界** | 未改 `staged_pilot.py`、`mutation_proof.py`、`staged_evidence.py`、registry 三件套 | **PASS** | `git diff master` 对上述路径 **无 diff**；变更限于 `layer4_markets/**` + tests/fixtures |

**§8.2 勾销：** 9 行中 **8 行 PASS**；**Plan 行**因 `validate-plan-freeze` E8 暂不能勾满 → 记入 §4.3（NON-BLOCKING，见 §9）。

---

## 4. §3.5 — AC 追溯与评分

| AC# | 追溯链（原始 → MASTER → §8 → 证据） | 分 | 抽检/复验 |
| --- | --- | --- | --- |
| **AC-022-1** | 卡 §1 registry → MASTER §2 → §8.1 Models → `test_marketRegistry_uniqueMarketIds`（8 行种子、CN_A、无重复 id） | **5** | 含于 sandbox **7 passed** |
| **AC-022-2** | module doc calendar PK → MASTER §2 → §8.3 → `test_marketCalendar_rejectsDuplicateTradeDate`（duplicate PK → `Layer4MarketError`） | **5** | 含于 7 passed；非交易日逻辑在代码 `197-200` 但无单测（A4 NB-1，不扣 AC 分） |
| **AC-022-3** | contract breadth `required_fields` → MASTER §2 → §8.3 → `test_marketBreadth_requiredFieldsPresent` | **5** | 含于 7 passed |
| **AC-022-4** | 卡 **§15** lineage 字段 → `LINEAGE_REQUIRED_FIELDS` → §8.4/8.5 → `test_marketSnapshot_lineageRequiredFieldsComplete`（`layer_id=layer4`、hashes、upstream） | **5** | 含于 7 passed；`lineage.py` 复用 `core/snapshot_lineage` |
| **AC-022-5** | 卡 **§15**「as_of 之后未来数据」→ contract `no_future_data` → §8.6 → `test_marketSnapshotRejectsFutureInput`（breadth 未来 `as_of_timestamp` → `match="future"`） | **4** | 含于 7 passed；**calendar 未来观测路径未单测**（`market_structure.py:202-207` 已实现，A4 NB-3） |
| **AC-022-6** | `BATCH3_STAGED_DOWNSTREAM_GATE` + MAP → MASTER §0 → §8.2 → `test_marketAdapter_nonStagedSource_rejects` + batch3 gate | **5** | manifest `production_live` 拒绝；gate **2 passed** |
| **AC-022-7** | D-09 / contract boundaries → MASTER §2 → §8.7 → `test_marketSnapshot_noLayer5HistoryFields` | **5** | `FORBIDDEN_LAYER5/LAYER1` frozenset 扫描 |
| **AC-022-8** | 卡 §11 + playbook §8.2 → MASTER §10 Tier A/B → §8.7 → `8.7-green.txt` + 本审计 sandbox | **4** | Tier A/B 本次独立复绿；Plan freeze 未 0 略降分 |

**均分：** 4.75 / 5 — **PASS**

---

## 5. 任务卡 §15 Lineage 字段对照

| §15 / contract 要求 | 实现落点 | 测试 |
| --- | --- | --- |
| `snapshot_id` | `market_structure.py:222` `l4-market-{market_id}-{trade_date}` | `LINEAGE_REQUIRED_FIELDS` 遍历 |
| `snapshot_type` | `market_breadth_snapshot` | 同上 |
| `layer_id` | `lineage.py:50` `layer4` | `assert envelope.layer_id == "layer4"` |
| `as_of_timestamp` | `lineage.py:51` 来自 build `as_of` | 非空断言 |
| `source_fetch_ids` | manifest → `lineage.py:56` | manifest + envelope 非空 |
| `source_content_hashes` | manifest → `lineage.py:57` | `assert envelope.source_content_hashes` |
| `rule_version` / `code_version` / `parameter_hash` | `lineage.py:58-60` | 字段遍历 |
| `upstream_snapshot_ids` / `is_incremental` | `lineage.py:62-63`；默认 `False` | upstream tuple 透传测 |
| **no-future-data** | `_reject_future_observation` calendar+breadth 双路 | breadth 未来 mutation 测绿 |

**结论：** §15 必填字段与 `snapshot_lineage_contract.yaml` `required_fields` 对齐；**no-future-data** 主路径有测，calendar 未来面为已知缺口（NON-BLOCKING，交 A8）。

---

## 6. §10 最弱 2 行 — sandbox 抽检

选取规则：优先 Tier **B**（非 smoke 单行）。

| # | §10 原文 | 复跑命令 | exit | 与 Execute 一致？ |
| --- | --- | --- | --- | --- |
| 1 | `uv run pytest -q`（Tier B · §8.7 一次） | 同上（A5 sandbox） | **0** | ✅ 与 `8.7-green.txt`「100% passed」一致（全库绿，~125s） |
| 2 | `uv run ruff check backend/app/layer4_markets tests/test_layer4_market_structure.py` | 同上 | **0** | ✅ `All checks passed!` 与 8.7-green 一致 |

### 6.1 派发 sandbox 复跑（AUDIT.plan §2 A5 cli-sandbox）

| 命令 | exit | 输出摘要 |
| --- | --- | --- |
| `uv run pytest tests/test_layer4_market_structure.py -v` | **0** | **7 passed** in 0.07s |
| `uv run pytest tests/test_batch3_staged_downstream_gate.py -v` | **0** | **2 passed** in 0.03s |

与 Execute `8.0-green` / `8.7-green` 声称的 7+2 一致。

---

## 7. execute-evidence 真实性抽检（2 个 green.txt）

| 文件 | 内容摘要 | 判定 |
| --- | --- | --- |
| `execute-evidence/8.3-green.txt` | 含命令行 + `2 passed — exit 0` + pytest 进度条 | **真 GREEN** — 非 TODO；结论由本次 7 passed 补偿 |
| `execute-evidence/8.7-green.txt` | Tier A 三步 + 全库 `100% passed` + ruff `All checks passed!` | **真 GREEN** — 含多命令摘要；全库本次独立 exit 0 验证 |

**对照 RED：** `8.1-red.txt` / `8.6-red.txt` 含真实 FAIL 叙述（`ImportError` / `future observation not rejected`）— TDD 起点可信。

**§4.3 观察（非 FAIL）：** `8.2-green.txt`、`8.4-green.txt` 等为 `.... [100%]` + 单行 PASS 自述；建议后续 Execute 保留 `-v` 摘要。不影响 AC 判定。

---

## 8. audit-prod-path（registry §2.1）

| 项 | 状态 |
| --- | --- |
| `AUDIT_PROD_ROOT` = `.audit-sandbox/r3-022-audit-prod-equiv` | **未创建** — staged-only |
| `QMD_DATA_ROOT` / 生产 DuckDB 写 | **无依赖** |
| AUDIT.plan §2 A5 prod-path 口径 | 「staged N/A；无污染」 |

**处置：** 以 fixture pytest + batch3 gate 复跑替代 prod 数据 hash 校验 — 与 AUDIT.plan 冻结口径一致。未复制/修改生产数据树。

---

## 9. Defer / Registry 边界（risk-manager）

| 项 | Execute 是否越界 | 证据 |
| --- | --- | --- |
| `ADV-R3X-LINEAGE-001` 全量 L4 DB lineage | **否** | 仅 envelope + pytest；无 WriteManager/duckdb import |
| `R3Y-LINEAGE-VR-001` 三 registry | **否** | `git diff master` 对三件套 **空** |
| `snapshot_lineage_contract.yaml` | **否** | 只读引用 |
| production-live 声称 | **否** | manifest 硬拒 `staged_fixture_only` 以外 mode |
| 8× live MarketAdapter | **否** | 仅 `CN_A` staged；其余 registry 种子元数据 defer |

### §4.3 登记（NON-BLOCKING）

| ID | 项 | 等级 | 说明 |
| --- | --- | --- | --- |
| A5-NB-1 | `validate-plan-freeze` E8 | NON-BLOCKING | `implement.jsonl` 未点名 `market_structure.py` / `lineage.py` / `models.py` / `fixtures/.../manifest.yaml`；`validate-execute-handoff` 已通过 — **合并前**应补 manifest 或 amend |
| A5-NB-2 | 核心交付未 commit | NON-BLOCKING | `git status` 显示 `??` 未跟踪实现文件；playbook §6 要求分支提交 — **主会话 merge gate** 项，非 A5 AC 阻断 |
| A5-NB-3 | calendar 未来 `as_of` 无单测 | NON-BLOCKING | 与 A4 NB-3 同源；`no_future_data` 主断言已绿 |

---

## 10. Quant-analyst 视角（方法层）

| 检查项 | 结论 |
| --- | --- |
| 回测/指标可复现命令 + sandbox 路径 | ✅ `tests/fixtures/layer4_staged_market/`；mutation 在 `.audit-sandbox/layer4_market_mutations/` |
| look-ahead / 数据窥探 | ✅ breadth 未来观测拒绝；calendar 路径代码守卫、测缺口见 §9 |
| live 源 DISABLED | ✅ `source_mode` gate + 无 http/fetch import |
| 指标定义与实现一致 | ✅ contract `required_fields` ↔ `MarketBreadthSnapshotRow` |
| sandbox 数据量级下结论成立？ | ✅ 微型 fixture（1 calendar + 1 breadth）；AC 为 **骨架/门禁** 非统计泛化 — 符合 Round3 staged 定位 |

---

## 11. 计划外发现

**显式声明：** 已按 `audit-adversarial-authority.md` A5 最低动作对照任务卡 AC、设计文档行为、MASTER 外 runtime 路径与证据真实性。

| ID | 发现 | 等级 | 证据 / 偿还 |
| --- | --- | --- | --- |
| EXT-1 | `implement.jsonl` 与 MASTER §6 wiring 不一致 → Plan freeze 失败 | NON-BLOCKING | `task.py validate-plan-freeze` exit 1；合并前补 manifest |
| EXT-2 | 实现文件尚未 `git add`/commit | NON-BLOCKING | `git status` `??` 五路径 |
| EXT-3 | `no_future_data` 仅 breadth mutation 覆盖 | NON-BLOCKING | `market_structure.py:202-213`；建议 A8 补 calendar future 测 |
| EXT-4 | 非交易日 `is_trading_day=False` 无单测 | NON-BLOCKING | `market_structure.py:197-200`；A4 NB-1 |
| EXT-5 | L3 同构 manifest/bundle 辅助函数 ~52 行重复 | NON-BLOCKING | A2 建议；可抽 `core/staged_bundle.py`（Batch 6+） |

**BLOCKING：** 0

---

## 12. DOUBT 闭环

| 疑点 | 结论 |
| --- | --- |
| 抽检与 Execute 声称一致？ | ✅ 7+2 passed；Tier B pytest + ruff exit 0 |
| green.txt 是真实终端输出吗？ | ⚠️ 步进文件偏薄；8.0/8.7/8.3 有命令摘要；RED 可信 |
| sandbox 数据量级下 AC 仍成立？ | ✅ staged 骨架 AC；非 prod-equivalent 统计 |
| `manifest-amend.md` 进入追溯链？ | N/A — 不存在 |
| playbook §8.2 Plan 行能否勾满？ | ❌ 直至 plan-freeze E8 修复 |

---

## 13. 判定摘要

- **AC-022-1..8：** 均可从 `022_implement_layer4_market_structure.md` → MASTER §2 → §8 步 → pytest / `execute-evidence` 闭环追溯。
- **§15 lineage + no-future-data：** 字段齐全且有 pytest 守卫；calendar 未来面为已知 NON-BLOCKING 缺口。
- **Playbook §8.2：** 除 **Plan** 行（plan-freeze E8）外，其余维度 sandbox 复验 **PASS**。
- **Sandbox 复跑：** `test_layer4_market_structure.py` **7 passed**；`test_batch3_staged_downstream_gate.py` **2 passed**；全库 pytest **exit 0**。
- **弱点：** plan-freeze E8；未提交 worktree；步进 green 证据偏薄。
- **最终：** **A5 PASS** — 不以自述为 PASS；独立 sandbox 命令已验证核心 AC、lineage §15、no-future-data 主路径与 Tier A/B 门禁。

---

_A5 only · composer-2.5 · 只读 · 无 commit_
