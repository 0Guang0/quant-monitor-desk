# A4 — Code Quality Audit Evidence (R3FR-07)

> **维度：** A4 语义 / 错误模型 / 测试质量 / 文档一致性  
> **分支：** `chore/round3fr-cleanup-rehome`  
> **审查范围：** backend 4 文件 docstring diff + `tests/test_r3fr07_legacy_wrapper_cleanup.py` + 关联 doc 变更  
> **权威：** `agents/audit-adversarial-authority.md` · `frozen/R3FR_07` §9 · `specs/contracts/data_cli_contract.yaml`

## Verdict

**PASS** — 无 BLOCKING 项；redirect 文档与真实调用路径一致；`health_check` 错误模型未变；测试五字段齐全。2 项 NON-BLOCKING 计划外发现。

---

## 检查项矩阵

| ID    | 检查项                                                             | 判定     | 证据                                                                                                                                                                     |
| ----- | ------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A4-01 | `health_check` docstring 与 `run_data_health_profile` 委托路径一致 | **PASS** | `data_commands.py:154-201`：docstring 声明委托；函数体仅新增 docstring，L193 仍调用 `run_data_health_profile`                                                            |
| A4-02 | `check_daily_bars` redirect 语义准确（非误称 delegate）            | **PASS** | `data_health.py:661-665`：标明 evidence-path shim + canonical `market_bar_p0`/`run_data_health_profile`；函数体未改，未虚假声称运行时委托                                |
| A4-03 | TDX probe 模块/类 doc 与 `TdxPytdxFetchPort` 瘦委托一致            | **PASS** | `interface_probe_fetch_ports.py:1-6,77-101`：模块 doc + 类 doc；`fetch_payload` 实例化 `TdxPytdxFetchPort` 并转发                                                        |
| A4-04 | `tdx_manual_probe` 模块 doc 边界准确                               | **PASS** | `tdx_manual_probe.py:1-6`：probe orchestration only；实现经 `TdxPytdxProbeFetchPort` 委托，与 import L26 一致                                                            |
| A4-05 | `health_check` 错误模型未变（对齐 `data_cli_contract`）            | **PASS** | diff 仅 docstring；`CliFailure` + `error_code`/`docs_anchor` 映射（`CAPABILITY_MISSING`/`INVALID_INPUT`）与 HEAD 一致；契约 `must_use: [run_data_health_profile]` 仍满足 |
| A4-06 | `test_r3fr07` 五字段 docstring                                     | **PASS** | 12 个 test 均含：覆盖范围 / 测试对象 / 目的目标 / 验证点 / 失败含义                                                                                                      |
| A4-07 | 测试非纯同义反复（tautological）                                   | **PASS** | 文档 guardrail 用 regex/结构化断言（如 `_assert_no_stale_health_placeholder`、`当前下一执行入口` 分段）；非 `assert doc is not None`                                     |
| A4-08 | 3F-R CLOSED 措辞未过度宣称 production-live                         | **PASS** | `PROJECT_IMPLEMENTATION_ROADMAP.md` L109-110：CLOSED + 3G 为下一入口；仍指向 Batch05 发布门禁；handoff L128-141 无 production-live 暗示                                  |
| A4-09 | AC-07-01 stale placeholder 已清                                    | **PASS** | `rg` 父/batch README 无 `still returns not_implemented_phase_c`；历史卡 R3FR-06 保留 placeholder 语境为已完成叙述                                                        |

---

## §3.4 发现表

| 轴       | 发现                                                         | 阻塞?                 | 证据                                                                                  |
| -------- | ------------------------------------------------------------ | --------------------- | ------------------------------------------------------------------------------------- |
| 正确性   | `health_check` / TDX delegate 文档与代码路径一致             | 否                    | `data_commands.py:193-201`；`interface_probe_fetch_ports.py:95-101`                   |
| 错误模型 | cleanup 未触及 `health_check` 异常映射                       | 否                    | git diff 仅 +docstring；`test_qmd_data_cli.py` 既有 health 错误路径测试仍适用         |
| 可读性   | docstring 风格与 R3FR 批次注释惯例一致                       | 否                    | 四处均为模块/函数顶注，无逻辑膨胀                                                     |
| 测试     | redirect 测试仅验 doc，运行时委托由 `test_qmd_data_cli` 覆盖 | 否（见 NON-BLOCKING） | `test_healthCheck_canonicalRedirectDoc` vs `test_qmdData_health_invokesProfileRunner` |
| 文档     | batch 3G README 与 roadmap/handoff 关门口径一致              | 否                    | `BATCH_3G_SANDBOX_CLEAN_WRITE/README.md` L3-5                                         |

---

## 计划外发现

| 发现                                                                                                       | 严重度           | 说明                                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `R3G_01_SANDBOX_CLEAN_WRITE_REHEARSAL.md` L5 仍写 `blocked by Round 3F-R`                                  | **NON-BLOCKING** | batch README/coordinator 已更新为 condition A satisfied；单卡 status 行未同步 redirect。AC-07-04 仅约束 batch README，不阻塞本维 PASS  |
| `test_healthCheck_canonicalRedirectDoc` / `test_checkDailyBars_canonicalRedirectDoc` 不验运行时 call graph | **NON-BLOCKING** | 符合 AC-07-02 doc redirect 范围；行为回归由 R3FR-06 既有 `test_qmd_data_cli` / `test_ops_data_health` 承担。建议在 A8 矩阵中确认未降级 |

**对抗搜索范围：** `docs/implementation_tasks/ROUND_3_*` stale placeholder；`R3G_*` blocked 措辞；`health_check` diff hunk；`data_cli_contract.yaml` health 段。

---

## DOUBT 闭环

**追问：** 若只跑 `test_r3fr07` doc 测试，能否漏掉 `health_check` 错误模型回归？

**结论：** 能漏 doc 以外的回归，但本 diff **零逻辑变更**；`test_qmd_data_cli.py::{test_qmdData_health_invalidProfile_documentedError, test_qmdData_health_rejectsLiveCleanWriteFullScan}` 仍覆盖 `CliFailure` 契约。A4 判定：错误模型 unchanged = **PASS**。

---

## What's Done Well

- `check_daily_bars` doc 诚实区分 shim vs canonical profile，避免误读为 DRY 委托（frozen §9.2「行为不变」）。
- `test_r3fr07` 五字段中文 docstring 全覆盖，失败含义指向执行风险而非 assert 字面。
- 3F-R CLOSED 与「3G 当前下一入口」并列，未删除 Batch05 / sandbox-only 上限。

---

## Verification Story

| 项          | 状态                                                                       |
| ----------- | -------------------------------------------------------------------------- |
| 测试审阅    | 是 — 12 cases，五字段 + 语义断言                                           |
| 构建/pytest | 未在本维重跑（只读审计）；A8 矩阵命令待主会话                              |
| 安全        | 是 — 无新 trust boundary；doc-only backend diff                            |
| 契约对照    | 是 — `data_cli_contract.yaml` `qmd data health` must_use 与 docstring 一致 |
