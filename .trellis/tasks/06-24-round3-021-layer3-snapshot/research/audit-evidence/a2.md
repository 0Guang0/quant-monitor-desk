# A2 audit-ponytail — 最小实现审查

**维度：** A2 · ponytail-review  
**判定：** PASS（无阻塞候选）

## git diff --stat（tracked）

```
backend/app/layer3_chains/models.py              | 54 +++++++
tests/test_layer3_snapshot_builder.py            | 169 +++++++++++++++++++--
7 files changed, 245 insertions(+), 29 deletions(-)
```

**未跟踪（核心实现）：** `snapshot_builder.py` 248 行、`lineage.py` 69 行、`fixtures/layer3_l5_staged_bars/`

## ponytail 梯级评估

| 文件                  | 评估                                                                | 梯级                |
| --------------------- | ------------------------------------------------------------------- | ------------------- |
| `lineage.py`          | 复用 `core/snapshot_lineage` kernel，镜像 L2 `Layer2LineageBuilder` | 2 复用              |
| `snapshot_builder.py` | 模块级 helper 对齐 `loader.py` 风格；`ponytail:` 注释标明天花板     | 2 复用 + 6 最小新码 |
| `models.py`           | 仅 dataclass 类型扩展（AC 要求）                                    | explicit AC         |
| `test _build()`       | 共享构建入口，避免重复 loader 调用                                  | 2 复用              |

## ≥20 行候选删改

| 候选                                     | 梯级                                        | 阻塞?          |
| ---------------------------------------- | ------------------------------------------- | -------------- |
| `snapshot_builder.py` 模块级函数 ~110 行 | 梯级 2 — 与 loader 同模式；拆类反而增抽象   | 建议（不阻塞） |
| `lineage.py` 独立文件 vs 内联 builder    | 梯级 2 — L2 已有独立 lineage 模块，保持一致 | 不阻塞         |

## DOUBT

未发现「单处调用的新抽象层」或「未请求依赖」。`yaml` 为 loader 同族已有依赖。
