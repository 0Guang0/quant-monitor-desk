## A1 Spec compliance

### Verdict: PASS_WITH_ISSUES

### Findings

- **original-plan-trace.md → AC mapping:** `research/original-plan-trace.md` maps task card `014` to MASTER §2 AC-1…AC-12 (job types/state machine, job_event_log, ResourceGuard, adapter→validator→gate→WriteManager chain, migration tables, no direct clean writes, smoke, registry tombstone, pytest/ruff, scope limits). Table is complete for the single Batch D task card.
- **implement.jsonl — GLOBAL / 014 / contracts / Batch C wiring:** Present — `GLOBAL_*`, `014_implement_data_sync_orchestrator.md`, `sync_job_contract.yaml`, `data_quality_rules.yaml`, `source_conflict_rules.yaml`, and Batch C wiring (`validation_gate.py`, `write_manager.py`, `source_registry.py`, `validators/data_quality.py`, `validators/source_conflict.py`, `004`/`005` migrations). **Gap:** Batch D **deliverables** created during Execute are absent from `implement.jsonl` (see adversarial trigger and O-2).
- **Job types / statuses vs `sync_job_contract.yaml`:** `SyncJobStateMachine.create_job` accepts exactly the six contract `job_types` (`full_load`, `incremental`, `backfill`, `revision_audit`, `reconcile`, `data_quality`). `SYNC_JOB_STATUSES` in `jobs.py` matches contract `statuses.normal` + `statuses.terminal` + `statuses.retryable` (15 values). Tests in `tests/test_sync_jobs.py` exercise `full_load`, `revision_audit`, `data_quality` skeleton paths.
- **VALIDATING / Reconcile vs quality & conflict contracts:** `run_incremental` transitions `STAGED → VALIDATING`, calls `DataQualityValidator` with `rule_set_id="p0_round_1"` (matches `data_quality_rules.yaml` `version`). On `SEVERE_CONFLICT`, transitions to `WAITING_RECONCILE` per `source_conflict_rules.yaml` `resolution_policy.severe_diff`. `run_reconcile` delegates to `SourceConflictValidator.record_unresolved_reconcile` and lands on `MANUAL_REVIEW_REQUIRED` when unresolved.
- **Runtime flow vs architecture docs:** `03_runtime_flows.md` §1 chain (ResourceGuard → DataSyncOrchestrator → Adapter → Staging → DataQualityValidator → SourceConflictValidator → WriteManager → clean) is implemented in `orchestrator.py` `run_incremental` / `begin_fetching`. `04_data_architecture.md` clean/staging boundary respected — clean writes only via `WriteManager.write`, staging via `adapter.fetch(..., con=writer_con, job_id=...)`.
- **check.jsonl coverage:** All A1-listed audit reads present — ROUND README, BATCH_B/C ledgers, `write_manager.md`, `004`/`005` migrations, Batch C wiring modules, `source_registry.yaml`, contracts, architecture docs, `original-plan-trace.md`. All `check.jsonl` paths are also listed in `implement.jsonl` (subset OK).
- **`sync_to_db(tombstone_missing=True)`:** `DataSyncOrchestrator.bootstrap(sync_registry=True)` calls `SourceRegistry.sync_to_db(con, tombstone_missing=True)` — reuses Batch B API; no duplicate tombstone logic in `backend/app/sync/*`.
- **Path normalization:** Implementation lives under `backend/app/sync/orchestrator.py` and `backend/app/sync/jobs.py`; no `backend/sync/*` references in sync package.
- **Functional tests:** `pytest tests/test_sync_migration.py tests/test_sync_jobs.py tests/test_sync_orchestrator.py tests/test_batch_d_orchestration_flow.py -q` — 26 passed, exit 0.

### Commands run

```bash
python .trellis/scripts/task.py validate 06-18-round2-batch-d-orchestrator
```

```
=== Validating Context Files ===
Target dir: .../06-18-round2-batch-d-orchestrator
  implement.jsonl: ✓ (68 entries)
  check.jsonl: ✓ (37 entries)
✓ All validations passed
```

Exit code: **0**

```bash
python .trellis/scripts/task.py validate-plan-freeze 06-18-round2-batch-d-orchestrator
```

```
Plan freeze validation FAILED:
  - E3: MASTER §9/§10 path missing from implement.jsonl: tests/test_batch_d_orchestration_flow.py
```

Exit code: **1**

```bash
pytest tests/test_manifest_protocol.py -q --tb=no
```

```
..FF........F
FAILED test_batch_d_suggest_implement_empty
FAILED test_batch_d_manifest_freeze_passes
FAILED test_validate_manifest_freeze_bundle
```

Exit code: **1** (3 failures — all Batch D manifest gaps tied to O-2)

```bash
pytest tests/test_sync_migration.py tests/test_sync_jobs.py tests/test_sync_orchestrator.py tests/test_batch_d_orchestration_flow.py -q --tb=no
```

```
..........................
```

Exit code: **0** (26 tests)

### Open item adjudication

| Item | A1 scope | Classification | Notes |
|------|----------|----------------|-------|
| **O-1** | Out of scope (note) | — | `tests/test_trellis_validate_plan.py` ruff E501×3 is pre-existing Trellis meta-test lint; not Batch D orchestrator code. Primary owner: **A4** (lint gate) / **A5** (evidence scope). |
| **O-2** | **In scope** | **P1** | `tests/test_batch_d_orchestration_flow.py` exists on disk and is listed in MASTER §9/§10, but is **missing from `implement.jsonl`**. Confirmed by `validate-plan-freeze` E3 and 3× `test_manifest_protocol.py` failures. Runtime tests pass; this is a **manifest/traceability** defect, not a missing implementation. Repair: add one `implement.jsonl` row (and optionally mirror in `check.jsonl` for A4). Not P0 (code + tests green); not P2 (blocks plan-freeze / manifest bundle). |
| **O-3** | Out of scope (note) | — | Execute Tier C evidence used `pytest --ignore` for Trellis meta/manifest tests; strict MASTER §9.3 no-ignore not claimed green. Primary owner: **A4** (test gate honesty) / **A5** (evidence). |

### Adversarial trigger

**Result:** Found **multiple** orchestrator-referenced / Batch D deliverable paths **not** declared in `implement.jsonl`:

| Path | Evidence | In implement.jsonl? |
|------|----------|---------------------|
| `backend/app/sync/jobs.py` | Direct import in `orchestrator.py` L15–20; state machine authority | **No** (marked `deferred` in `original-plan-trace.md` §输入文件 as `backend/app/sync/orchestrator.py` only; `jobs.py` same package, same Execute step) |
| `backend/app/sync/orchestrator.py` | Core deliverable | **No** (`deferred` in trace — acceptable at Plan freeze, but post-Execute should be amended into manifest) |
| `backend/app/db/migrations/006_ingestion_sync.sql` | Creates `data_sync_job` / `job_event_log` tables used by orchestrator SQL | **No** (`migrate.py` listed; migration file itself not) |
| `tests/test_batch_d_orchestration_flow.py` | MASTER §8.5 / §9.1 E2E; import target from `test_sync_orchestrator.py` | **No** — **O-2 / E3** |

Wiring modules the orchestrator imports **are** correctly listed (`resource_guard`, `base_adapter`, `fetch_result`, `connection`, `validation_gate`, `write_manager`, `data_quality`, `source_conflict`, lazy `source_registry`).

**Why not “none found”:** The adversarial condition is satisfied by `backend/app/sync/jobs.py` (runtime import, not in manifest) and `tests/test_batch_d_orchestration_flow.py` (MASTER §9/10 gate failure). The `deferred` label in `original-plan-trace.md` explains Plan-phase omission but does not excuse post-Execute manifest drift for shipped code.
