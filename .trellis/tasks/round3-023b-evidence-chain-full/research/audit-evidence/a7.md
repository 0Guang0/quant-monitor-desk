# A7 audit-ops — round3-023b-evidence-chain-full (B01-023)

> **维度：** A7 · database-administrator + sre-engineer（adjunct）+ devops-incident-responder（RCA 只读）  
> **权威：** `agents/audit-adversarial-authority.md` + `AUDIT.plan.md` §2 A7 + `MASTER.plan.md` §0/§3.2/§3.3  
> **模型：** composer-2.5  
> **分支：** `feature/round3-023b-evidence-chain-full` @ worktree `../quant-monitor-desk-wt-023-layer5`  
> **日期：** 2026-06-25  
> **模式：** Audit（只读，无 commit，无 prod DB 写）

---

## 总判定

| 项 | 值 |
| --- | --- |
| **判定** | **PASS** |
| **BLOCKING** | 0 |
| **AUDIT.plan §2 A7 通过条件** | 无 production DB 写；Track B 未混 Batch 01 PR 声称 production readiness — **满足** |

---

## 1. 审计范围（AUDIT.plan §2）

| 验证类型 | 命令 / 检查 | 通过条件 |
| --- | --- | --- |
| cli-sandbox | 无 production DB 写；Track B 未混 Batch 01 PR 声称 | 无 prod 写 |

本任务为 **staged-only 内存 builder**（`EvidenceChainBuilder` / `InstrumentRegistryValidator` / `StagedEvidenceValidator` + `EvidenceReadPort` protocol）。MASTER §3.2 defer：`WriteManager` clean table 写、全量 DB 持久化（`ADV-R3X-LINEAGE-001`）**未在本分支引入**。A7 重点为 **零 prod DB 接触**、**无 migration 越界**、**Track B 合并轨与 Batch 01 包声明隔离**；init/migrate 两遍与 kill 恢复见 DOUBT §6（不适用）。

---

## 2. migration / schema 检查（database-administrator）

| 步骤 | 命令 / 方法 | exit | 关键证据 | 结果 |
| --- | --- | --- | --- | --- |
| schema diff | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/` | 0 | 无输出 | **无 migration 变更** |
| Layer5 DDL | `rg layer5\|evidence_chain specs/schema/` | 1 | 无匹配 | **无 Layer5 表 DDL** |
| 业务 diff 核心 | `git diff master --name-only` | 0 | L5：`evidence_chain.py`、`instrument_registry.py`、`evidence_validator.py`、`models.py`、`ports.py`、contract、ADR-023、L5 测试/fixture | **未触及 db 包** |
| layer5 DB 引用 | `rg -i "migration\|init_db\|duckdb\|write_manager\|WriteManager\|DbValidationGate\|QMD_DATA_ROOT" backend/app/layer5_evidence/ tests/test_layer5_evidence_*.py` | 1 | 无匹配 | **无 DB 依赖** |
| contract defer | `layer5_evidence_contract.yaml` `deferred_to_023b: []` | — | 023b 项已兑现 | **无挂账 defer 残留** |

**代码路径摘要：**

- `EvidenceChainBuilder.build`：校验 foundation + layer context → 组装 `EvidenceChainRecord` dataclass（**纯内存**）。
- `build_from_port`：经 `EvidenceReadPort` 注入 staged dict；**不 import** `storage/staged_evidence` 或具体 storage 类。
- `InstrumentRegistryValidator` / `StagedEvidenceValidator`：内存行校验；无 DuckDB / WriteManager。
- ADR-023：severe → `manual_review_state=queued`；**无** DB `manual_review_queue` 写入。

**WriteManager clean 写路径（对照，未触及）：**

- 任务卡 §5/§8、`docs/modules/write_manager.md`：clean 表写入须经 `DuckDBWriteManager`。
- 023b 实现 **未 import** `write_manager`、未新增 `*Writer`、未调用 `duckdb.connect` — 符合 MASTER §3.2 defer。

---

## 3. 生产 `data/` 污染与 sandbox（sre-engineer adjunct）

| 步骤 | 命令 | exit | 关键证据 | 结果 |
| --- | --- | --- | --- | --- |
| prod DuckDB | `Test-Path data/duckdb/quant_monitor.duckdb` | — | `False` | **无 prod DB 可污染** |
| 仓库根 `data/` | 枚举 `data/` | — | 仅既有 `data/cache/duckdb_tmp`、`data/logs/failed_write_audit.ndjson` | **审计未新增 prod 路径** |
| 隔离 pytest | `uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q --basetemp=.audit-sandbox/r3-023b/pytest` | 0 | `12 passed` | 与 playbook §8.4 / Execute `full-pytest-green.txt` 一致 |
| L5 测试写路径 | `rg write_text\|write_bytes backend/app/layer5_evidence/` | 1 | 无匹配 | **builder 无文件写** |
| L5 测试 I/O | `test_layer5_evidence_chain.py` | — | 仅 `read_text` 读 `tests/fixtures/layer5_staged_evidence/`；port 用 in-test fake | **无 data/ 写** |
| AUDIT_PROD_ROOT | `Test-Path .audit-sandbox/r3-023b-audit-prod-equiv` | — | 未创建（A5 负责 prod-equiv） | 本维 N/A |

**说明：** 全库 Execute 证据 `full-pytest-green.txt` 显示全绿；本维仅复跑 L5 聚焦集 12/12。

---

## 4. Track B 与 Batch 01 边界（AUDIT.plan A7 特有）

| 检查项 | 证据 | 结果 |
| --- | --- | --- |
| 合并轨声明 | MASTER §0「Track B 单独合并」；PRD §5「不声称 production-live」 | **显式分轨** |
| 提交信息 | `feat(layer5): complete B01-023 evidence chain…`；`docs(trellis): close B01-023 execute handoff` | **无 Batch 01 包 production readiness 声称** |
| 停止条件 | MASTER §1.5 #4：声称 production-live / Batch 01 包 readiness → 中止 | **文档未越界** |
| playbook §8.4 | 验收命令仅 L5 测试 + 全库 pytest/ruff/compileall | **未捆绑 Batch 01 六卡 gate** |

**范围外 diff（见 §8，非 A7 BLOCKING）：** 分支相对 `master` 另含 `tests/test_ops_data_health.py`（fixture 路径）、`tests/fixtures/data_health/v2_integration_bundle/*`、`docs/quality/coordination/BATCH_01_MAIN_SESSION_COORDINATOR_PLAYBOOK.md`（相对 master 为新增）、`docs/generated/docs_specs_index.generated.md`。其中 `test_ops_data_health` 变更仅为 `_V2_EVIDENCE` 指向 repo 内 fixture（`DataHealthService().check_evidence_dir` **只读**），**无 DB 写**；但属 **B01-DH2 文件锁**（playbook §2.5），合并时应由主会话协调，**不得**作为 Track B 与 Batch 01 混 PR 的 readiness 依据。

---

## 5. GitNexus 必用记录

| 工具 | 参数 / 查询 | 要点 | 与本任务关系 |
| --- | --- | --- | --- |
| `query` | `layer5 evidence chain database migration duckdb write WriteManager` | 命中 Layer1/Layer2 `*Writer`、`WriteManager._execute_write` 等 db 写路径 | **layer5_evidence 未出现在 process_symbols** |
| `context` | `WriteManager` | incoming imports：**无** `layer5_evidence/*` | **写路径与 L5 builder 无交叉** |
| `context` | `EvidenceChainBuilder` | 索引尚无该符号（Execute 新建） | 以静态 `rg` + 代码审阅补证；merge 后建议 `node .gitnexus/run.cjs analyze` |

---

## 6. 可靠性 / fail-closed（sre-engineer checklist）

| 检查项 | 证据 | 结果 |
| --- | --- | --- |
| Agent 非事实源 | `test_evidenceChain_rejectsAgentTextAsFact` + `reject_agent_text_as_fact` | fail-closed |
| severe → queued | `test_evidenceChain_severeConflictQueuesManualReview` + ADR-023 | 可观测状态 |
| upstream 必填 | 空 `upstream_snapshot_ids` → `EvidenceChainError` | 无部分链 |
| no-future bar | `test_securityBar_rejectsFutureTradeDate` | as_of 边界 |
| port 边界 | 无 port 时 `build_from_port` → `EvidenceChainError` | 无静默 storage 耦合 |
| ResourceGuard / smoke | 本任务不触及 ResourceGuard、不跑 `production_equivalent_smoke.py` | **N/A**（AUDIT A6 SKIP） |
| init_db 两遍 / kill migrate | 无 schema 变更、L5 测试不调用 `init_db.py` | **N/A** |

---

## 7. DOUBT（doubt-driven-development）

| 疑点 | 结论 |
| --- | --- |
| 第二次 init/migrate 是否仅不报错但数据损坏？ | **不适用** — 零 migration；builder 不写 DuckDB。 |
| kill 后 `schema_version` 与 migration 表是否一致？ | **不适用** — 无 migrate 执行路径。 |
| pytest 是否隐式写生产 `data/`？ | **否** — `prod_db=False`；L5 测试仅读 fixture。 |
| `test_ops_data_health.py` 改动是否引入 DB 写？ | **否** — 仅改 evidence 目录常量；`check_evidence_dir` 只读。 |
| 分支含 Batch 01 playbook 是否等于混 PR 声称 readiness？ | **否** — 文档/协调物同分支携带 ≠ 合并 PR 捆绑声称；MASTER 仍强制 Track B 分轨（§7.3）。 |
| 是否绕过 WriteManager 写 clean？ | **否** — 无 clean 写；持久化 defer 至 Batch 6 / LIN。 |

---

## 8. §3.7 证据表（合并 DBA + SRE）

| 场景 / 步骤 | 命令 | exit | 日志 / evidence |
| --- | --- | --- | --- |
| 无 migration | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/` | 0 | 空 diff |
| 无 L5 DB 写 | `rg` layer5_evidence + L5 tests | 1 | 无匹配 |
| sandbox pytest | `uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q --basetemp=.audit-sandbox/r3-023b/pytest` | 0 | 12 passed |
| prod DB 完整性 | `Test-Path data/duckdb/quant_monitor.duckdb` | — | False |
| GitNexus 隔离 | `query` + `context(WriteManager)` | 0 | L5 未接入写路径 |

---

## 9. 计划外发现

> 对抗性搜索域：MASTER 未列的 DB/migration/污染面、WriteManager 旁路、ops CLI 写库、Track B 文件锁越界。

| ID | 发现 | 严重度 | 处置建议 |
| --- | --- | --- | --- |
| — | 已搜索：`layer5_evidence/**`、L5 测试、`specs/schema/`、`backend/app/db/` diff、WriteManager incoming | — | **无计划外 BLOCKING** |
| NB-1 | 分支 diff 触及 **B01-DH2 拥有** 的 `tests/test_ops_data_health.py` + v2 fixture（非 `worktree-slices.md` allowed） | **NON-BLOCKING（A7）** | 合并前主会话确认 DH2 协调或拆 commit；**不构成 prod DB 写** |
| NB-2 | 分支含 `BATCH_01_MAIN_SESSION_COORDINATOR_PLAYBOOK.md`（相对 master 新增） | **NON-BLOCKING（A7）** | Track B PR 仍应 **单独 merge**（§7.3）；playbook 宜由协调者分支合入 master，非 023 业务依赖 |
| NB-3 | L5 builder **未挂 ResourceGuard**（小 fixture 内存路径） | **NON-BLOCKING** | 与 A6 SKIP / 021–022 模式一致 |
| NB-4 | GitNexus 索引未收录 `EvidenceChainBuilder` 等新符号 | **NON-BLOCKING** | merge 后 `analyze` 刷新 |

**显式声明：** 已对抗搜索 live fetch → DB、direct SQL、init_db 隐式调用、migration 新增、生产 `data/duckdb` 写入、Batch 01 包 production-live 声称 — **均未发现 BLOCKING。**

---

## 10. 结论

B01-023 full Layer5 evidence chain 为 **纯内存、staged-fixture-only、EvidenceReadPort 注入** 实现：

1. **无** `specs/schema/`、`scripts/init_db.py`、`backend/app/db/` 变更；
2. **无** DuckDB / WriteManager / `QMD_DATA_ROOT` 引用（WriteManager clean 写路径未被绕过或提前引入）；
3. **无** 生产 DuckDB 文件（`data/duckdb/quant_monitor.duckdb` 不存在）；
4. **独立复跑** L5 pytest **12/12 绿**（`--basetemp=.audit-sandbox/r3-023b/pytest`）；
5. **Track B** 文档与提交 **未声称** Batch 01 包 production readiness；合并轨与 Batch 01 六卡 **显式分轨**。

**A7 判定：PASS。** 无 BLOCKING；运维面零 prod DB 接触，符合 MASTER §3.2 defer 与 AUDIT.plan §2 A7 通过条件。
