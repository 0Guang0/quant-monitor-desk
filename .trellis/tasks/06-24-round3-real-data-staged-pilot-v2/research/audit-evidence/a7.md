# A7 — audit-ops（DuckDB / 运维）

**维度：** cli-sandbox + audit-prod-path · **判定：** PASS

## 检查项

| 检查 | 命令/证据 | 结果 |
| ---- | --------- | ---- |
| 无 migration 于 diff | `git diff HEAD -- specs/schema` | 无变更 |
| 无 DB clean write | v2 路径无 `writer()` / `apply_migrations` | PASS |
| sandbox 无 `data/` 污染 | 工作区无 `data/duckdb/`；`.audit-sandbox/r3y-pilot-v2-audit` 新建仅 pytest basetemp | PASS |
| audit-prod-path hash | `data/duckdb/quant_monitor.duckdb` 不存在 | N/A — 无可污染对象 |

## DOUBT

- 第二次 init/migrate：本任务未改 schema/migration → 不适用。
- kill 恢复：未触及 migration 路径。

## 结论

staged pilot v2 为证据 JSON + 只读 mutation proof；运维面无 prod 写风险。
