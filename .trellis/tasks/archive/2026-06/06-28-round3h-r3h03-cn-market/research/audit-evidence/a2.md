# A2 Ponytail 审计 — R3H-03 CN Market Adapters

> **维度：** A2 过度工程 / ponytail-review  
> **Worktree：** `feature/round3h-r3h03-cn-market`（未提交变更）  
> **权威：** `frozen/R3H_03_CN_MARKET_ADAPTERS.md` · `agents/audit-a2-ponytail.md` · `agents/audit-adversarial-authority.md`  
> **范围：** 十源 fetch port + `cn_market` normalizer + `cn_trading_calendar` + `cn_market` health profile  
> **动作：** 只报告，不修复

---

## Checklist

- [x] `git diff --stat` 已记录 Lxx / net lines
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象交叉引用（若有）
- [x] 阻塞 vs 建议已区分

---

## Diff 统计

### 已跟踪变更（`git diff --stat`）

| 文件 | + | - |
|------|---|---|
| `calendar_gap_rules.py` | 31 | — |
| `R3H_03_CN_MARKET_ADAPTERS.md` | 139 | — |
| `check_test_catalog.py` | 26 | — |
| `provider_catalog.yaml` | 20 | — |
| `source_capabilities.yaml` | 76 | — |
| `source_registry.yaml` | 20 | — |
| `contract_coverage.yaml` | 2 | — |
| `test_source_capabilities.py` | 64 | — |
| `tests/test_catalog.yaml`（staged） | 24 | — |
| **合计** | **~402** | **~44** |

### 未跟踪新增（Execute 主体）

| 类别 | 行数 |
|------|------|
| 9 × fetch port（baostock…xqshare） | 841 |
| `cn_market.py` normalizer | 236 |
| `license_gate.py` | 45 |
| `cn_market.py` / `cn_trading_calendar.py` health | 169 |
| `test_cn_market_adapters.py` + `test_tdx_provider_port.py` | 757 |
| replay fixtures（未计行） | — |
| **未跟踪合计** | **~2048** |

**净增估算：** ~2400 行（含 registry/spec 与测试）。十源 per-port 架构为 frozen §1.1 选项 B **显式 AC**，port 文件数量本身不算 bloat。

---

## DOUBT（≥20 行可简化？）

**结论：是。** 找到 4 处 ≥20 行候选；另有 2 处 <20 行 shrink。搜索范围：

- `backend/app/datasources/fetch_ports/{baostock,akshare,cninfo,eastmoney,sina_finance,mootdx,ths_ifind,qmt_*}_port.py`
- `backend/app/datasources/normalizers/cn_market.py`
- `backend/app/datasources/auth/license_gate.py`
- `backend/app/ops/data_health_profiles/{cn_market,cn_trading_calendar,calendar_gap_rules}.py`
- `tests/test_cn_market_adapters.py`
- 对照 R3H-02 `market_data.py` / `yahoo_finance_port.py` 既定 per-port 模式

---

## §3.2 候选删改表

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
|----------------------|---------------|----------|
| `cn_trading_calendar.py:44-93` 五组 `_qingming/_dragon_boat/_mid_autumn/_national_day/_spring_festival_holidays` 生成器用固定公历日近似农历节（端午 5/28–30、中秋 9/15–17 与真实 A 股日历偏差大） | 梯级 1（YAGNI 精度）+ 梯级 2（可复用交易所静态 frozenset / 外部权威 CSV，与 spring festival 表同形） | **建议** — G2/G17 日历为 AC，但 ~50 行生成器可换 ~15 行 `ponytail:` 标注的静态日期表；精度问题交 A4/A5 |
| `eastmoney_port.py:27-74` + `sina_finance_port.py:27-74` 两段 mock bar fetch 逻辑 ~95% 重复（仅 `source_id`、OHLCV 常数、`conflict_evidence.validation_source_id` 不同） | 梯级 2 — 提取 `_cn_validation_bar_mock_fetch(source_id, …)` 至 `cn_market.py` 或 `fetch_ports/_cn_mock.py`，两 port 各留 cap + factory | **建议** — 净删 ~55 行；不违反 per-port 文件 AC |
| `qmt_xtdata_port.py:25-67` + `qmt_xqshare_port.py:25-67` QMT 两门 mock fetch 几乎相同（差 `source_id` + `require_license_gate` 键） | 梯级 2 — 共享 `_qmt_daily_bar_mock_port(source_id)`；保留独立 port 文件与 cap 常量 | **建议** — 净删 ~45 行 |
| `akshare_port.py:28-71` 与 eastmoney/sina 同型 mock bar 生成（无 `conflict_evidence`，有 `quality_flags`） | 梯级 2 — 同上共享 helper，三 validation aggregator 共用一个参数化入口 | **建议** — 可与上行合并，额外净删 ~35 行 |
| `cn_market.py:217-218` `cn_market_bundle_layer5_provenance` 单行转发 `bundle_layer5_provenance` | 梯级 2 — 测试直接调 `evidence_bundle.bundle_layer5_provenance` | **不算 ≥20 行** — 对齐 R3H-02 `market_data.py:152-154` 命名约定；§9.9 layer smoke 绑定函数名 |
| `cninfo_port.py:142-156` `create_cninfo_pdf_live_fetch_port` 独立 factory | 梯级 5 — 合并为 `create_cninfo_fetch_port(..., enable_pdf_live=True, pdf_bytes=…)` | **建议** — ~12 行 |
| `mootdx_port.py:20-31` 从 `tdx_pytdx_port` 导入 6 个 `_` 私有符号 | 梯级 2 — 将 cap/guard 升为 `tdx_common` 或 tdx 模块 public API | **建议**（A4 交叉：私有耦合 fragile） |
| `calendar_gap_rules.py:99` `__all__` 重导出 `get_missing_trading_days`（定义在 `cn_trading_calendar`） | 梯级 2 — 调用方直接从 `cn_trading_calendar` import | **建议** — 1 行 indirection |

---

## 计划外发现

| 发现 | 类型 | 说明 |
|------|------|------|
| 农历节用固定公历 generator | 计划外实现选择 | frozen §14 允许 L2 拷改 EasyXT `TradingCalendar`，但当前端午/中秋算法与 spring festival 表精度不一致；非 MASTER 显式要求「五函数生成器」 |
| validation mock 三文件复制 | 计划外重复 | R3H-01/02 亦 per-port 自包含 mock；本任务 eastmoney/sina/akshare 复制度高于 fred/yahoo 对照组 |
| mootdx → tdx 私有 import | 计划外耦合 | frozen §9.5 要求共享 TDX lifecycle，但未要求 cross-module private import |
| layer preview 三函数 | **非计划外** | 镜像 R3H-02 `market_data.py` 模式，属 AC §9.9 |
| `license_gate.py` env-only | **非计划外** | frozen §9.1 + §9.7 显式；已有 `ponytail:` 注释 |
| `cn_market.py` 236 行 | **非计划外** | §9.1 evidence + staged pilot L2 + filings/bars 双域；与 `market_data.py` 155 行同量级 |

**显式声明：** 已对抗搜索 port 目录、normalizer、health profile、测试模块；未发现 catch-all `cn_adapter.py`（frozen 否决项 A 未出现）。

---

## A4 交叉引用

| A2 候选 | A4 关联 |
|---------|---------|
| `cn_trading_calendar` 近似农历 | A4 应验证 FAIL 是否基于错误假日（假阳/假阴） |
| eastmoney/sina/akshare 重复 mock | A4 重复 PortError 文案与 cap 路径；改共享 helper 时须保持负例行为不变 |
| mootdx 私有 import tdx | A4 fragile：tdx_pytdx 重命名 `_reject_*` 即破 mootdx |

---

## 未列为候选（刻意保留）

| 项 | 理由 |
|----|------|
| 10 个独立 port 文件 | frozen §1.1 选项 B 显式 AC |
| `cn_market.py` staged pilot migrators（54-98） | §9.1 L2 迁出 staged_pilot |
| `cninfo_port.py` PDF cap 路径（93-133） | §9.3 + 用户 Q13 Tier-B smoke |
| `test_cn_market_adapters.py` 741 行 | 十源 × route/cap/auth/layer；五字段注释为 policy 要求 |
| `license_gate.py` 45 行 | 三门共享 SSOT；已最简 |
| `cn_market.py` health profile 44 行 | 薄 glue：`calendar_gap_rules` + `ohlcv_rules` |

---

## Ponytail 评分

**net: -~150 行 possible**（eastmoney/sina/akshare/qmt 共享 mock ~135 + cninfo factory ~12 + calendar 生成器精简 ~50 − 新增 helper ~40）。

无 BLOCKING ponytail 项：架构 per-port 与 evidence normalizer 体量符合 frozen AC；候选均为 **建议** 级删改，可在 zero-left repair 或后续 debt-lite 切片处理。

---

## 审计结论

**PASS（ponytail 维度）** — 存在可删重复 mock 与日历生成器精简空间，但不构成合并阻塞；十源 port + cn_market SSOT 为任务显式要求，整体 lean enough to ship，repair 优先处理 validation/QMT mock 去重与 `cn_trading_calendar` 静态化。
