# A7 audit-ops — round3-022-layer4-market

> **维度：** A7 · database-administrator + sre-engineer（adjunct）+ devops-incident-responder（RCA 只读）  
> **权威：** `agents/audit-adversarial-authority.md` + 任务卡 §8/§13 + `docs/modules/write_manager.md`  
> **模型：** composer-2.5  
> **分支：** `feature/round3-022-layer4-market`  
> **日期：** 2026-06-24  
> **模式：** Audit（只读，无 commit）

---

## 总判定

| 项                            | 值                                                                 |
| ----------------------------- | ------------------------------------------------------------------ |
| **判定**                      | **PASS**                                                           |
| **BLOCKING**                  | 0                                                                  |
| **AUDIT.plan §2 A7 通过条件** | 无 migration/DB 写；sandbox 无 `data/` 污染 — **满足**             |

---

## 1. 审计范围（AUDIT.plan §2）

| 验证类型    | 命令 / 检查                                     | 通过条件   |
| ----------- | ----------------------------------------------- | ---------- |
| cli-sandbox | 确认无 migration/DB 写；sandbox 无 `data/` 污染 | 无 prod 写 |

本任务为 **staged-only 内存 builder**（MASTER §3.2 defer：WriteManager DB sync / clean table 写 **可选 sandbox defer**）。A7 重点为 **零 DB 接触**、**无 migration 越界**、**WriteManager clean 写路径未被绕过或提前引入**，而非 init/migrate 两遍或 kill 恢复（见 DOUBT §6）。

---

## 2. migration / schema 检查（database-administrator）

| 步骤           | 命令 / 方法                                                                                                                                 | exit | 关键证据                                                                                                      | 结果                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------- | --------------------- |
| schema diff    | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/`                                                                       | 0    | 无输出                                                                                                        | **无 migration 变更** |
| 三 registry    | `git diff master -- docs/AUDIT_DEFERRED_REGISTRY.md docs/UNRESOLVED_ISSUES_REGISTRY.md docs/RESOLVED_ISSUES_REGISTRY.md`                    | 0    | 无输出                                                                                                        | **defer 边界 intact** |
| 业务 diff 范围 | `git status` + `git diff master --name-only`                                                                                                | 0    | 已跟踪：`__init__.py`、`test_catalog.yaml` 等；未跟踪：`layer4_markets/*.py`、`test_layer4_market_structure.py` | **未触及 db 包**      |
| layer4 DB 引用 | `rg "migration\|init_db\|duckdb\|write_manager\|WriteManager\|DbValidationGate\|QMD_DATA_ROOT" backend/app/layer4_markets/ tests/test_layer4_market_structure.py` | 1    | 无匹配                                                                                                        | **无 DB 依赖**        |
| schema 契约    | `rg "layer4\|market_registry\|market_calendar\|market_breadth" specs/schema/`                                                             | 1    | 无匹配                                                                                                        | **无 Layer4 表 DDL**  |

**代码路径摘要：**

- `MarketStructureBuilder`：读 `tests/fixtures/layer4_staged_market/`（manifest + calendar.json + breadth.json）→ 组装 dataclass → 返回 `MarketStructureBuildResult`（**纯内存**）。
- `Layer4LineageBuilder`：复用 `core/snapshot_lineage.py` 校验字段，无 DuckDB / WriteManager import。
- `seed_registry_rows()` 中 registry 为 **代码内 frozen tuple**，非 DB `market_registry` 表写入。
- manifest 强制 `source_mode == staged_fixture_only`（`market_structure.py:285-289`），fail-closed 拒绝非 staged 源。

**WriteManager clean 写路径（对照，未触及）：**

- 任务卡 §5/§8、`docs/modules/write_manager.md` L4：clean 表写入 **必须** 经 `DuckDBWriteManager`。
- 022 实现 **未 import** `write_manager`、未新增 `*Writer`、未调用 `duckdb.connect` — 符合 MASTER §3.2 defer，**未越界提前持久化**。

---

## 3. 生产 `data/` 污染检查（sre-engineer adjunct）

| 步骤                  | 命令                                                                                                      | exit | 关键证据                                                                 | 结果                                      |
| --------------------- | --------------------------------------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------ | ----------------------------------------- |
| prod DuckDB 存在性    | `Test-Path data/duckdb/quant_monitor.duckdb`                                                              | —    | `False`（本 worktree 无生产库文件）                                      | **无 prod DB 可污染**                     |
| 仓库根 `data/` 枚举   | `Get-ChildItem data -Recurse`                                                                             | —    | 仅 `data/cache/duckdb_tmp`、`data/logs/failed_write_audit.ndjson`（既有） | **pytest 未新增 data/ 文件**              |
| 隔离 pytest           | `New-Item .audit-sandbox/r3-022/pytest -Force`; `uv run pytest tests/test_layer4_market_structure.py -q --basetemp=.audit-sandbox/r3-022/pytest` | 0    | `7 passed`                                                               | 与 Execute §8.7 一致                      |
| 测试写路径            | `rg "write_text\|_MUTATION_ROOT" tests/test_layer4_market_structure.py`                                   | 0    | 写操作仅 `_MUTATION_ROOT = .audit-sandbox/layer4_market_mutations`       | **mutation 隔离于 audit-sandbox**         |
| 生产代码写文件        | `rg "write_text\|write_bytes" backend/app/layer4_markets/`                                                | 1    | 无匹配（仅 `read_text`）                                                 | **builder 只读 fixture**                  |
| AUDIT_PROD_ROOT       | `Test-Path .audit-sandbox/r3-022-audit-prod-equiv`                                                        | —    | `False`（A5 负责 prod-equiv；022 staged N/A）                            | 本维未创建副本                            |

**说明：** mutation 测试在 `.audit-sandbox/layer4_market_mutations/` 留下 fixture 副本（Execute 预期行为），**未**写入仓库根 `data/` 或 `data/duckdb/`。

---

## 4. GitNexus 必用记录

| 工具             | 参数 / 查询                                             | 要点                                                                                         | 与本任务关系                                      |
| ---------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| `query`          | `layer4 market structure database migration duckdb write WriteManager` | 命中 Layer1/Layer2 `*Writer`、`WriteManager._execute_write` 等 **db 写路径**                 | **layer4_markets 未出现在 process_symbols**       |
| `context`        | `WriteManager`                                          | incoming imports：**无** `layer4_markets/*`                                                  | **写路径与 Layer4 builder 无交叉**                |
| `detect_changes` | `scope: all, base_ref: master`                          | `changed_count: 0`（未跟踪文件未入索引）                                                     | 静态 `rg` + diff 补证；**blast radius 可忽略**    |

**索引注记：** merge 后建议 `node .gitnexus/run.cjs analyze` 收录 `MarketStructureBuilder` / `Layer4LineageBuilder`。

---

## 5. 可靠性 / fail-closed（sre-engineer + devops checklist）

| 检查项                      | 证据                                                                                         | 结果                        |
| --------------------------- | -------------------------------------------------------------------------------------------- | --------------------------- |
| 非 staged 源拒绝            | `_read_manifest` 校验 `source_mode == staged_fixture_only`                                   | fail-closed                 |
| 未来数据拒绝                | `_reject_future_observation` → `Layer4MarketError`                                           | 无部分写                    |
| bundle 路径守卫             | `_resolve_bundle_root` 要求 `bundle_dir` 在 `PROJECT_ROOT` 下                                | 防任意路径读                |
| 无静默成功                  | `test_marketAdapter_nonStagedSource_rejects`、`test_marketSnapshotRejectsFutureInput` 等     | 异常可观测                  |
| partial write / 手改生产库  | 无 DB 连接、无 ops CLI 入口                                                                  | **N/A — 无写库面**          |
| ResourceGuard / smoke       | 本任务 **不触及** ResourceGuard、不跑 `production_equivalent_smoke.py`                         | **N/A**（AUDIT A6 SKIP）    |
| init_db 两遍 / kill migrate | 无 schema 变更、测试不调用 `init_db.py`                                                      | **N/A**                     |

---

## 6. DOUBT（doubt-driven-development）

| 疑点                                               | 结论                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 第二次 init/migrate 是否仅不报错但数据损坏？       | **不适用** — 零 migration；builder 不写 DuckDB。                                     |
| kill 后 `schema_version` 与 migration 表是否一致？ | **不适用** — 无 migrate 执行路径。                                                   |
| pytest 是否隐式写生产 `data/`？                    | **否** — worktree 无 `data/duckdb/`；测试写仅限 `.audit-sandbox/layer4_market_mutations`。 |
| 「registry」是否等于三 registry DB 写？            | **否** — `seed_registry_rows()` 为内存元数据；与 MASTER §3.2 R3Y defer 一致。        |
| 是否绕过 WriteManager 写 clean？                   | **否** — 无 clean 写；未来持久化须经 WriteManager（任务卡 §5 已冻结）。             |
| migration 越界（022 偷偷加 Layer4 表）？           | **否** — `specs/schema/` 无 Layer4 相关 DDL；`backend/app/db/` 无 diff。           |

---

## 7. §3.7 证据表（合并 DBA + SRE）

| 场景 / 步骤        | 命令                                                                                          | exit | 日志 / evidence              |
| ------------------ | --------------------------------------------------------------------------------------------- | ---- | ---------------------------- |
| 无 migration       | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/`                         | 0    | 空 diff                      |
| 无 layer4 DB 写    | `rg` layer4_markets + test 文件                                                               | 1    | 无匹配                       |
| sandbox pytest     | `uv run pytest tests/test_layer4_market_structure.py -q --basetemp=.audit-sandbox/r3-022/pytest` | 0    | 7 passed                     |
| prod DB 完整性     | `Test-Path data/duckdb/quant_monitor.duckdb`                                                | —    | False（无库可污染）          |
| GitNexus 隔离      | `query` + `context(WriteManager)`                                                             | 0    | layer4 未接入写路径          |

---

## 8. 计划外发现

> 对抗性搜索域：MASTER 未列的 DB/migration/数据污染面、`WriteManager` 旁路、ops CLI 写库、计划外 schema 演进。

| ID    | 发现                                                                 | 严重度           | 处置建议                                                                 |
| ----- | -------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------ |
| —     | 已搜索：`layer4_markets` 全包、`test_layer4_market_structure.py`、`specs/schema/`、`backend/app/db/` diff、三 registry diff、WriteManager incoming imports | —                | **无计划外 BLOCKING**                                                    |
| NB-1  | 022 builder **未挂 ResourceGuard**（纯内存小 fixture）                 | **NON-BLOCKING** | 与 021/A6 SKIP 一致；live CLI/API/WriteManager 持久化偿还时再挂 guard  |
| NB-2  | mutation 测试遗留 `.audit-sandbox/layer4_market_mutations/*` 目录    | **NON-BLOCKING** | 隔离目录内预期 artifact；非 `data/` 污染                                 |
| NB-3  | 未来 Layer4 clean 持久化须新增 `*Writer` + WriteManager 路由         | **NON-BLOCKING** | 属 MASTER defer 偿还项；022 未提前关闭                                   |
| NB-4  | 仓库既有 `R3Y-STAGED-REG-001`（staged file_registry 绕过 WriteManager） | **NON-BLOCKING** | **非 022 引入**；registry 已登记 β-2 defer；022 diff 未改 `staged_evidence.py` |

**显式声明：** 已对抗搜索 live fetch → DB、direct SQL、init_db 隐式调用、migration 文件新增、三 registry 写回、生产 `data/duckdb` 写入 — **均未发现。**

---

## 9. 结论

022 Layer4 market structure builder 为 **纯内存、staged-fixture-only** 实现：

1. **无** `specs/schema/`、`scripts/init_db.py`、`backend/app/db/` 变更（**无 migration 越界**）；
2. **无** DuckDB / WriteManager / DbValidationGate / `QMD_DATA_ROOT` 引用（**WriteManager clean 写路径未被绕过或提前引入**）；
3. **独立复跑 pytest 7/7 绿**（`--basetemp=.audit-sandbox/r3-022/pytest`），测试写操作仅限 `.audit-sandbox/layer4_market_mutations`；
4. GitNexus 证实 DB 写流程集中在 Layer1/Layer2/db，**layer4_markets 零交叉**。

**A7 判定：PASS。** 无 BLOCKING；运维面零 DB 接触，符合 MASTER §3.2 defer 与任务卡 §8「不得绕过 WriteManager」约束（本阶段无写库行为）。
