# A2 audit-ponytail — Layer 5 证据链增量审查

**维度：** A2 · ponytail-review + doubt-driven-development  
**范围：** `backend/app/layer5_evidence/**` + `tests/test_layer5_evidence_chain.py`（023b 增量；023A `foundation.py` / `lineage.py` 仅作对照）  
**Worktree：** `quant-monitor-desk-wt-023-layer5` · `feature/round3-023b-evidence-chain-full`  
**判定：** **PASS**（0 阻塞候选）

---

## git diff --stat

**相对 `master..HEAD`（commit `a4c59cda` + `ad619101`），本维核心增量：**

```
backend/app/layer5_evidence/evidence_chain.py      | 170 ++++++
backend/app/layer5_evidence/evidence_validator.py  |  34 ++
backend/app/layer5_evidence/instrument_registry.py |  38 ++
backend/app/layer5_evidence/models.py              |  50 ++
backend/app/layer5_evidence/ports.py               |  12 +
tests/test_layer5_evidence_chain.py                | 209 +++++++
tests/fixtures/layer5_staged_evidence/minimal_bundle.json | 29 +
specs/contracts/layer5_evidence_contract.yaml      |   7 +-
docs/adr/ADR-023-layer5-conflict-review-path.md    |  26 +
```

| 类别 | 净增行（约） |
|------|-------------|
| 023b 生产代码（5 文件） | **~304** |
| 023b 测试 + fixture | **~238** |
| 契约 / ADR | ~33 |
| 023A 基线（`foundation.py`、`lineage.py`、models 前半） | 已存在于 master，本分支未改 |

**MASTER §8 触及文件：** `instrument_registry.py`、`evidence_validator.py`（计划名 `evidence_models.py` 已合并进 `models.py`）、`evidence_chain.py`、`ports.py`、`models.py`（扩展）、`test_layer5_evidence_chain.py`、`fixtures/layer5_staged_evidence/**`、`layer5_evidence_contract.yaml`、`ADR-023-layer5-conflict-review-path.md`。

---

## ponytail 梯级评估（文件级）

| 文件 | 评估 | 梯级 |
|------|------|------|
| `ports.py` | 12 行 `Protocol`；ADR + `R2-RISK-2` 显式 port 注入 | 6（最小接口） |
| `instrument_registry.py` | 复用 023A `InstrumentEvidenceRef`；bundle 内 `instrument_id` 唯一 | 2 复用 + 6 |
| `evidence_validator.py` | `SecurityBarDaily` + no-future-data；对齐 L3 `_reject_future_bar` 语义 | 2 复用 + 6 |
| `evidence_chain.py` | `EvidenceChainBuilder` 组合 foundation + layer context；bundle 解析仅服务 `build_from_port` | 2 复用 + 6 |
| `models.py` (+50) | `SecurityBarDaily` / `LayerContextBundle` / `EvidenceChainRecord` 契约 dataclass | explicit AC |
| `lineage.py` / `foundation.py` | 023A 基线；023b 未膨胀 | 2 复用 |
| `reject_agent_text_as_fact` | 模块级薄包装，委托 `EvidenceFoundationValidator` | 5（可一行委托） |
| `validate_bar_from_bundle` | **零调用方**；`build_from_port` 未走 bar 校验 | 1 YAGNI 候选 |
| 测试五字段 docstring | testing-guidelines / A8 要求 | 非 bloat |
| `_FakeEvidenceReadPort` | 已有 `ponytail:` 注释；最小 fake | 6 |

---

## §3.2 ≥20 行候选删改

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
|----------------------|---------------|----------|
| `tests/test_layer5_evidence_chain.py:38-75` 常量 `_REGISTRY_ROW` / `_STAGED_PROVENANCE` / `_LAYER_CONTEXT` + `_factual_record` 与 `test_layer5_evidence_foundation.py:30-62` 及 `minimal_bundle.json` 三重重复（~38 行） | 梯级 2 — 共享 `tests/fixtures/layer5_staged_evidence/conftest_helpers.py` 或仅从 JSON fixture 构造 | **建议** |
| `evidence_chain.py:105-165` `_bar_from_bundle` / `record_trade_date` / `_record_from_bundle` / `_layer_context_from_bundle`（~61 行，仅 `build_from_port` 消费） | 梯级 6 — AC-023-5 要求 port→chain；可保留至真实 storage adapter 出现再抽 `staged_bundle` 内核（比照 L3/L4） | **建议**（跨层抽取 defer，非本切片必删） |
| `foundation.py:74-87` 与 `evidence_chain.py:91-102` `manual_review_state=queued` ↔ `need_human_review` 配对校验重复（~15 行同文案） | 梯级 2 — severe 覆盖后其余交给 `EvidenceFoundationValidator._validate_manual_review` | **建议** |
| `instrument_registry.py:26-29` + `evidence_validator.py:23-26` 相同「遍历 required 字段 + strip 空串」循环（两文件合计 ~72 行，模式 ~15 行可合并） | 梯级 2 — 模块内 `_require_non_empty_fields(obj, names, Error)`；注意勿与 A4 过度抽象冲突 | **建议** |
| `evidence_chain.py:85-89` `validate_bar_from_bundle` + `__init__` 中 `staged_validator` 注入（仅服务上述死方法） | 梯级 1 — 无调用方；删后 `StagedEvidenceValidator` 仅测试直接实例化 | **建议**（单块 <20 行，与上行合并净估 ~25 行） |
| `evidence_chain.py:168-170` `reject_agent_text_as_fact` 模块函数 | 梯级 5 — 测试改调 `EvidenceFoundationValidator().reject_agent_text_as_fact_source` | **建议**（<20 行，附注） |
| `ports.py` / `EvidenceReadPort` | explicit AC-023-5 + `R2-RISK-2` | **不删** |
| `models.py` 新增 dataclass | explicit AC-023-2/3 契约行 | **不删** |
| `ADR-023` | explicit AC-023-4 | **不删** |

**ponytail-review 净估：** `net: -25 ~ -55 lines possible`（死方法 + manual_review 去重 + 测试 helper 合并 + 可选字段校验小函数；bundle 解析抽 core 需跨 L3/L4 协调，非 023b 单切片必做）。

---

## 与 A4 交叉引用

- 四类 `ValueError` 子类（`EvidenceChainError` / `EvidenceFoundationError` / `EvidenceValidationError` / `InstrumentRegistryError`）与 L3/L4 同模式；**重复错误文案**见上表 `manual_review_state=queued` 行 — 属 A4 一致性，A2 建议合并校验路径而非删错误类型。
- `StagedEvidenceValidator` 的 `future trade_date` 与 L3 `as_of` 边界对称；无重复日志样板。
- 若删除 `validate_bar_from_bundle`，须确认 A5 是否要求 `build_from_port` 路径也走 bar 校验（当前 **未接线** — 见计划外发现）。

---

## DOUBT

**≥1 处 ≥20 行可简化：** 是 — 见上表前两行（测试 setup 三重重复 ~38 行；bundle 解析 ~61 行可 defer 抽取）及 manual_review 去重 + 死方法合并。

**搜索范围（已读）：**

- `backend/app/layer5_evidence/*.py` 全文
- `tests/test_layer5_evidence_chain.py` 全文
- 对照 `tests/test_layer5_evidence_foundation.py`、`tests/fixtures/layer5_staged_evidence/minimal_bundle.json`
- 对照 `backend/app/layer3_chains/snapshot_builder.py`、`layer4_markets/market_structure.py`（staged bundle / as_of 模式）
- `docs/modules/layer5_security_evidence.md` §3、`specs/contracts/layer5_evidence_contract.yaml`
- `docs/implementation_tasks/ROUND_3_MODELING_LAYERS/023_implement_layer5_evidence_chain.md`
- `docs/adr/ADR-023-layer5-conflict-review-path.md`
- MASTER §8 切片表 + AC-023-1..5

**未找到：** 无第二消费者的 factory/wrapper、未请求第三方依赖、speculative config 层、单文件净增 ≥20 行且完全可删的契约/schema 行。

---

## 计划外发现

| 发现 | 类型 | 说明 |
|------|------|------|
| `validate_bar_from_bundle` 无调用方 | NON-BLOCKING | MASTER §9.2 仅要求 `StagedEvidenceValidator.validate_bar` 单测；链上 bundle→bar 路径未接入 `build_from_port` |
| 任务卡 §1 列 event/financial/valuation，实现仅 `SecurityBarDaily` | NON-BLOCKING | 属 **欠实现** 非过度工程；`deferred_to_023b: []` 已清空但 contract 仍列 event_registry 等 — 协调者应在 A1/A5 核对 AC-023-2 完备性 |
| `build_from_port` 不调用 `_staged.validate_bar` | NON-BLOCKING | staged bundle 含 `security_bar` 但未校验；与 dead `validate_bar_from_bundle` 并存 — 删死码时应决定是否在 port 路径接线（A5） |
| L5 bundle dict 解析 vs L3/L4 manifest 助手 | NON-BLOCKING | 尚无 ~52 行级同构重复；待 storage adapter 落地后可比照 `core/staged_bundle.py` 抽取 |
| `evidence_chain.py` 缺模块顶 `ponytail:` 块注释 | 信息 | L3 `snapshot_builder.py` 有；非阻塞 |

**对抗搜索声明：** 已对照设计 evidence_chain 表语义、契约 boundaries、`R2-RISK-2` port 要求、023A foundation 边界；无计划外新增抽象层或「仅一处调用却无消费者的 wrapper」（除 `validate_bar_from_bundle` / `reject_agent_text_as_fact` 薄层）。

---

## A2 checklist

- [x] `git diff --stat` 已记录 Lxx / net lines
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象/错误模型交叉引用
- [x] 阻塞 vs 建议已区分
- [x] `## 计划外发现` 已填
