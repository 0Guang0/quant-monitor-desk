# A3 Security Audit — R3FR-07 Legacy Wrapper Cleanup

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 维度     | A3 Security                           |
| 分支     | `chore/round3fr-cleanup-rehome`       |
| 任务     | `06-27-06-27-round3fr-cleanup-rehome` |
| 日期     | 2026-06-27                            |
| **结论** | **PASS**                              |

---

## Summary

| 等级                | 数量 |
| ------------------- | ---- |
| Critical            | 0    |
| High                | 0    |
| Medium              | 0    |
| Low                 | 0    |
| Info (non-blocking) | 1    |

**Blocking findings:** 0  
**Non-blocking findings:** 1（计划外、预存面，非本 diff 引入）

---

## AUDIT.plan §1 A3 冻结检查

| 检查                                                          | 命令 / 动作                                                                                                          | 结果         |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------ |
| 无 stale health placeholder                                   | `rg "still returns.*not_implemented" docs/implementation_tasks/ROUND_3_REFERENCE_ADOPTION_REFACTOR/README.md`        | **0 匹配**   |
| 3G 入口未再被 3F-R 阻塞叙事                                   | `rg "Blocked by.*3F-R" docs/implementation_tasks/ROUND_3_SANDBOX_CLEAN_WRITE/BATCH_3G_SANDBOX_CLEAN_WRITE/README.md` | **0 匹配**   |
| 16 卡治理边界                                                 | `pytest tests/test_reference_adoption_guardrails.py::test_r3fr01DownstreamCardsGovernanceBoundaries -q`              | **1 passed** |
| 无新 live fetch / WriteManager 旁路 / 参考项目 runtime import | diff + rg 见下文                                                                                                     | **PASS**     |
| 无 production-live 宣称                                       | docs diff 审查                                                                                                       | **PASS**     |

---

## Diff 威胁面（点名四文件）

`git diff` 对以下文件**仅增加 docstring / 模块注释**，无逻辑、导入、旗标或 I/O 行为变更：

| 文件                                             | 变更性质                                                               |
| ------------------------------------------------ | ---------------------------------------------------------------------- |
| `backend/app/cli/data_commands.py`               | `health_check` 注明委托 `run_data_health_profile`（R3FR-06 canonical） |
| `backend/app/ops/data_health.py`                 | `check_daily_bars` 注明 evidence-path shim，禁止在此增规则             |
| `backend/app/ops/interface_probe_fetch_ports.py` | 模块 doc 明确 `TdxPytdxProbeFetchPort` 为薄委托                        |
| `backend/app/ops/tdx_manual_probe.py`            | 模块 doc 明确 probe 编排边界                                           |

**结论：** 本任务 diff 不扩大攻击面、不新增写路径、不弱化既有 gate。

---

## 静态基线 rg（security-auditor 模板 + diff 文件）

| 模式                                          | 范围                                                                      | 结果 |
| --------------------------------------------- | ------------------------------------------------------------------------- | ---- |
| `https?://\|api_key\|secret\|token\|password` | 四文件                                                                    | 0    |
| `f".*SELECT\|execute(f`                       | 四文件                                                                    | 0    |
| `参考项目\|sys.path.insert`                   | 四文件                                                                    | 0    |
| `writer(\|INSERT \|UPDATE \|DELETE `          | `data_health.py`, `interface_probe_fetch_ports.py`, `tdx_manual_probe.py` | 0    |
| `subprocess\|eval(\|exec(`                    | `interface_probe_fetch_ports.py`, `tdx_manual_probe.py`                   | 0    |

`data_commands.py` 中 `cm.writer()` / `apply_migrations` 存在于 **`init` 路径**（L112–123），**不在**本次变更的 `health_check`；`health_check` 仍通过 `_HEALTH_FORBIDDEN_FLAGS` 拒绝 `allow_network` / `clean_write` / `full_market_scan` / `full_history`（L132–178，预存、未改动）。

---

## DOUBT 三类对抗搜索

| 类别               | 搜索范围                          | 结论                                 |
| ------------------ | --------------------------------- | ------------------------------------ |
| 硬编码 URL 变体    | diff 四文件 + docs 3F-R/3G README | 无新 URL；docs 仅状态/重定向         |
| JWT / API key 模式 | 同上                              | 无                                   |
| SQL 拼接           | `data_health.py` 全文件 + diff    | 无 `execute(f"...")` / format SELECT |

---

## GitNexus 信任边界

`query({ search_query: "data health CLI read-only trust boundary WriteManager" })` 返回与 `live_pilot_fetch_ports._direct_request`、`test_write_manager` 等相关流程；**未**将本次 docstring 变更与 WriteManager 旁路关联。

`TdxPytdxProbeFetchPort.fetch_payload`（L88–101）仍要求 `authorization`，否则 `USER_AUTH_REQUIRED` — 与 R3FR-03 委托边界一致，diff 未修改该逻辑。

---

## Findings

### [INFO] 预存 AkShare 验证 fetch port（计划外、非阻塞）

- **Location:** `backend/app/ops/interface_probe_fetch_ports.py:27–73` (`AkshareSinaDailyValidationFetchPort`)
- **Description:** 模块内仍有可触发 akshare 网络的 validation port；本次 diff 仅改模块 docstring，未动该类。
- **Impact:** 属 018C sandbox probe 预存面；非 R3FR-07 引入；调用方须显式构造 port，非默认 CLI 路径。
- **Classification:** **NON-BLOCKING** — 记录为 hygiene；3G 前若收紧 probe 面可单独开卡。
- **Recommendation:** 保持 R3G 任务卡对「无默认 live 源」的断言；无需为 R3FR-07 回滚。

---

## 计划外发现

| ID    | 描述                                                                                                | 等级 | Blocking? | 证据                                                |
| ----- | --------------------------------------------------------------------------------------------------- | ---- | --------- | --------------------------------------------------- |
| A3-O1 | `AkshareSinaDailyValidationFetchPort` 预存 live 网络能力                                            | Info | No        | `interface_probe_fetch_ports.py:27–73`；diff 未触及 |
| —     | 已对抗搜索：diff 内无新密钥、SQL 注入、参考项目 import、WriteManager 旁路、production-live 文档宣称 | —    | —         | rg + pytest + docs diff                             |

---

## Positive Observations

- Python 变更严格限于文档化 canonical 边界，符合 BATCH_3FR_HARDENING_RULES §6/§7/§9 cleanup 精神。
- `health_check` CLI 仍 fail-closed 拒绝 live/clean-write 旗标（`_HEALTH_FORBIDDEN_FLAGS`）。
- 3F-R / 3G README 与 `ROUND3_HANDOFF.md` 更新为 CLOSED / 下一入口 3G，**删除** stale `not_implemented` 叙事，**未**添加 production-live readiness 声称。
- `test_r3fr01DownstreamCardsGovernanceBoundaries` 通过，16 卡治理契约仍绿。

---

## Recommendations（非阻塞）

1. R3G 执行时继续以 `reference_adoption_guardrails.yaml` + `test_reference_adoption_guardrails.py` 为 gate，不依赖本 cleanup diff 扩面。
2. 若未来收缩 018C probe 面，对 `AkshareSinaDailyValidationFetchPort` 单独评审授权与 caps（非 R3FR-07 范围）。

---

## Verdict

**PASS** — 无 blocking 安全发现；AUDIT.plan A3 冻结检查全部满足；diff 为只读文档化 cleanup，不引入新 trust-boundary 回归。
