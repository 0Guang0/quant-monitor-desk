# A2 — audit-ponytail（ponytail-review）

**维度：** review-only · **任务：** `round3-readonly-data-health-v1`（Wave C C-20）  
**权威：** `agents/audit-a2-ponytail.md` + `agents/audit-adversarial-authority.md`（设计/R3Y 任务卡 > MASTER §8）  
**范围：** `backend/app/ops/data_health*.py`、`tests/test_ops_data_health.py`、增量 fixtures  
**判定：** **PASS**（0 阻塞候选；2 处 ≥20 行可简化建议）

---

## diff 统计

分支 `feature/round3-readonly-data-health-v1`；业务增量为 **未跟踪新文件**（相对 `master` 无已提交 diff）。

```
backend/app/ops/data_health.py     | 707 +++++++++ (new)
backend/app/ops/data_health_cli.py |  53 ++++ (new)
tests/test_ops_data_health.py      | 380 ++++ (new)
tests/fixtures/data_health/        |  3 files (new)
────────────────────────────────────────────────────
业务净增                           | ~1140 lines
```

**MASTER §8 触及文件：** `data_health.py`、`data_health_cli.py`、`test_ops_data_health.py`、`tests/fixtures/data_health/`（与 §4 代码地图一致）。

**旁路变更（非 A2 主体）：** `docs/generated/project_map.*`、`tests/test_catalog.yaml` — loop_maintain 产物，不计入 ponytail 删改候选。

---

## ponytail 梯级评估（区域摘要）

| 区域 | 行数 | 梯级 | 结论 |
| ---- | ---- | ---- | ---- |
| `data_health_cli.py` 全文 | 53 | 6（R3Y-DH-08 薄 CLI） | 已 lean；MASTER 允许独立 entry |
| `_fail_check` / `_pass_check` | ~38 | 2（共享边界 DRY） | 保留 — 多 rule 复用 |
| `check_daily_bars` / `check_staleness` / lineage 字段检查 | ~200 | 6（§5.3b 13 rule_id AC） | 九切片强制；无 batch6 validator 可复用（全库 grep 无 `INVALID_OHLC` 实现） |
| `evaluate_gate` + report 字段 | ~90 | 6（AC-DH-GATE / R3Y §10 schema） | 不可删 |
| `DataHealthService` | ~33 | 6（任务卡「service entrypoint」） | 外观 yagni，但 R3Y-DH-08 / MASTER §3.1 显式要求 |
| `check_result_to_dict` / `from_dict` | ~28 | 6（§8.1 model roundtrip AC） | 契约校验必要 |
| `ponytail:` 常量 `_MIN_DAILY_BAR_HISTORY` / `_STALE_DATA_DAYS` | 4 | 7（天花板已标注） | 合规 |
| 测试五字段 docstring + `_GOOD_BAR` | 380 | 6（AC-DH-TEST / playbook §2.2） | purpose 齐全；见下方 1 处 setup 膨胀 |

---

## §3.2 ≥20 行候选删改

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
| --------------------- | ------------- | -------- |
| `tests/test_ops_data_health.py:340-360` CLI 测试内联创建 `good_bundle` manifest（与已提交 `tests/fixtures/data_health/good_bundle/` 重复） | 2（复用既有 fixture） | **建议** |
| `backend/app/ops/data_health.py:432-469` `check_lineage_entry` 双段 `reg.get` + 双 `try/except: pass`，`VALIDATION_ONLY_AS_PRIMARY` 逻辑重复 | 5（合并为单次 registry 查询循环） | **建议** |
| `backend/app/ops/data_health.py:571-625` `manifest_entries` 与 `fetches` 双循环 + 后置 dedup（v2 manifest 两数组同源条目） | 5（择一 canonical 数组或 union 后再查，可去掉 dedup 块） | **建议**（边界 ~20 行；需确认 v1 manifest 仅单数组场景） |
| `backend/app/ops/data_health.py:706-707` `default_v2_evidence_dir()` 无调用方（测试用 `_V2_EVIDENCE` 常量） | 1 delete | 建议（<20 行，附记） |

**不算删改（MASTER/R3Y explicit AC）：** `DataHealthReport` schema 字段、`EvidenceBundle` loader、`require_evidence_bundle`（§8.2 测试）、13 rule_id 对应测试、gate 布尔 + rationale。

---

## DOUBT

**≥20 行可简化？** **是** — 至少 1 处明确（CLI 测试 fixture 重复 ~21 行）；另 2 处生产代码各 ~18–25 行可合并（lineage registry 块、bundle 双循环+dedup）。

**搜索范围：**

- `backend/app/ops/data_health.py` 全文（707 行）
- `backend/app/ops/data_health_cli.py` 全文（53 行）
- `tests/test_ops_data_health.py` 全文（380 行）
- `tests/fixtures/data_health/**`
- 复用对照：`backend/app/ops/staged_pilot.py`、`db_inspector.py`（无 manifest loader / OHLC validator 可直引）
- 全库 `INVALID_OHLC` / `data_quality` Python 实现 — **仅 data_health 模块**

---

## 交叉引用 A4

| A2 候选 | A4 关联 |
| ------- | ------- |
| `check_lineage_entry:453-469` 双 `except Exception: pass` | A3 已记 P2 fail-open（`a3.md` OOB-A3-02）；A4 应评错误模型一致性。A2 侧可 **shrink** 合并分支，与 fail-closed 强化正交。 |
| `_checks_from_bundle` dedup | A4 可能关注重复 FAIL 消息语义；删 dedup 前须保证 gate `has_fail` 行为不变。 |
| 测试 fixture 内联 | A8 交界 — fixture SSOT 应单一（`tests/fixtures/data_health/`）。 |

---

## 计划外发现

| ID | 来源 | 发现 | 梯级 | Blocking |
| -- | ---- | ---- | ---- | -------- |
| OOB-A2-01 | 对抗 MASTER §6 evidence 根 vs 实现 | `default_v2_evidence_dir()` 死代码；`_DEFAULT_V2_EVIDENCE` 与测试 `_V2_EVIDENCE` 三处重复锚定 | delete / 2 | NON-BLOCKING |
| OOB-A2-02 | 对抗 playbook §7「测试 fixture 膨胀」 | `test_dataHealthCli_readOnly_exitZero` 运行时 mkdir+写 manifest，与 committed fixture 重复 | 2 | NON-BLOCKING |
| OOB-A2-03 | 对抗 v2 manifest 结构（`raw_evidence_manifest_v2.json` 同时含 `manifest_entries`+`fetches`） | 双循环+dedup 为计划外补偿逻辑；可改为单路径迭代减 ~20 行 | 5 | NON-BLOCKING |
| OOB-A2-04 | AUDIT.plan A2「复用 validators」 | 仓库无既有 OHLC/lineage Python validator；evidence-path v1 新建合理，非重复造 Batch6 平台 | — | N/A |

**对抗搜索声明：** 已对照 `R3Y_readonly_data_health_v1.md` §4–§5、`docs/ops/data_health_cli.md`（Batch6 边界）、`MASTER.plan.md` §3–§5.3b、`specs/contracts/data_quality_rules.yaml` 索引；除上表外未发现计划外 factory/第二消费者 wrapper 或 ≥20 行纯样板层。

---

## ponytail-review 一行式

- `test_ops_data_health.py:L340-360: shrink: 21-line runtime fixture bootstrap. Use tests/fixtures/data_health/good_bundle only.`
- `data_health.py:L432-469: shrink: twin reg.get + pass blocks. Single loop over {resolved_source, primary} unique ids.`
- `data_health.py:L571-625: yagni: dual manifest loops + dedup. One canonical v2 entry source; drop dedup if unified.`
- `data_health.py:L706-707: delete: default_v2_evidence_dir unused. Tests already use _V2_EVIDENCE.`

**net: ~45–55 lines possible**（非阻塞；不损 AC-DH-RULES / gate 字段前提下）。

---

## A2 checklist

- [x] `git diff --stat` 已记录（未跟踪新文件 ~1140 行业务行）
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象/错误处理交叉引用
- [x] 阻塞 vs 建议已区分

---

## 结论

**A2 PASS** — C-20 增量整体符合 ponytail「九切片 AC 产物」预期；CLI 薄包装、rule helper DRY、`ponytail:` 天花板注释到位。存在 **0 项阻塞删改**、**3 项建议性 ≥20 行简化**（测试 fixture 重复为最清晰一刀）。合并前可选收紧，不阻断 staged-only merge。
