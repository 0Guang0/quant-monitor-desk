name: Nightly

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly-${{ github.ref }}
  cancel-in-progress: false

jobs:
  perf-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - run: pip install -e ".[dev]"
      - name: "Perf gate: quick profile (≤180s)"
        run: python scripts/run_perf_gate.py --profile quick
      - name: "Perf gate: full profile (≤300s)"
        run: python scripts/run_perf_gate.py --profile full
      - name: "Perf gate: CI parallel (≤240s)"
        run: python scripts/run_perf_gate.py --profile ci-parallel

  network-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - run: pip install -e ".[dev]"
      - name: Optional live vendor fetch smoke (--run-network)
        env:
          QMD_DATA_ROOT: .audit-sandbox/nightly-network
          QMD_ALLOW_LIVE_FETCH: "1"
        run: pytest -q --run-network -m network tests/test_fred_macro_incremental_e2e.py -k "mock"
