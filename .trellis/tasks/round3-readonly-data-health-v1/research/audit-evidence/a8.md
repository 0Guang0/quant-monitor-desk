# A8 — audit-test-gap（QA / testing-guidelines）

**维度：** pytest-isolated + audit-prod-path（MAP §2.2 C-20）  
**Agent 模板：** `agents/qa-expert.md` + `agents/test-automator.md`  
**对抗性权威：** `agents/audit-adversarial-authority.md`（契约 + R3Y 任务卡 > MASTER §5/§8）  
**模型：** composer-2.5  
**判定：** **PASS**（2 项 NON-BLOCKING 计划外缺口）

---

## 权威对照摘要

| 层级 | 来源 | A8 用法 |
| ---- | ---- | ------- |
| L1 | `specs/contracts/data_quality_rules.yaml` + `R3Y_readonly_data_health_v1.md` §5.1 rule 子集 | rule_id 覆盖矩阵 |
| L1 | `docs/ops/data_health_cli.md` §6 report schema | 只读字段断言 |
| L2 | `agents/qa-expert.md` / `test-automator.md` | 五字段、语义断言、sandbox basetemp |
| L3 | `AUDIT.plan.md` §2 A8 验证矩阵 | 本报告证据 |
| 参考 | `MASTER.plan.md` §5.3 / §5.3b / §7 Red Flags | 执行计划对照，非完备性上限 |

---

## Red Flag / AC 覆盖

| Red Flag / AC | 覆盖 | 证据 |
| ------------- | ---- | ---- |
| CLI 空壳（§7） | 13 条 rule 单测 + CLI subprocess | `test_dataHealthCli_readOnly_exitZero`；§8.3–8.8 |
| 写 DB / source fetch（§7） | 报告硬编码字段断言 | `production_db_mutated` / `source_fetch_performed` false（report + CLI） |
| 弱断言（§7 / AC-DH-TEST） | 17/18 测含 `rule_id` 或 `overall_status`/`returncode` 语义断言 | 见下文「语义断言抽检」 |
| production-live 声称（§7） | gate 字段存在性 | `test_dataHealthIntegration_v2Evidence_bundle` |
| AC-DH-RULES（§5.3b 13 rule_id） | 每条至少 1 个 `test_*` | 矩阵 13/13 |
| R3Y-DH-01..09 九切片 | 18 测映射 §8.1–8.9 | `execute-evidence/8.*-green.txt` |
| AC-DH-MAP | MAP §2.2 双命令 | 见「pytest 复跑」 |
| 只读边界 | 无 prod DB 写测；邻接套件未因 C-20 回归 | 邻接 pytest 绿 |

**frozen rule_id 子集（MASTER §6.1）：** 13/13 有对应失败路径 pytest（`DUPLICATE_PRIMARY_KEY` … `STALE_DATA`）。

---

## 五字段 docstring 抽检

**文件：** `tests/test_ops_data_health.py`  
**方法：** AST 解析全部 `def test_*`，检查 docstring 是否含五字段：`覆盖范围`、`测试对象`、`目的/目标`、`验证点`、`失败含义`。

| 指标 | 结果 |
| ---- | ---- |
| `test_*` 函数总数 | **18** |
| 五字段齐全 | **18/18** |
| 违规 | **0** |

**目的/目标通俗中文抽检（3 例）：**

| 测试 | 目的摘录 | 判定 |
| ---- | -------- | ---- |
| `test_dataHealthDailyBar_invalidOhlc_fails` | 坏 OHLC 必须能被 `INVALID_OHLC` 检出 | PASS |
| `test_dataHealthLineage_validationOnlyMisuse_fails` | validation-only 源不得误作 primary | PASS |
| `test_dataHealthIntegration_v2Evidence_bundle` | v2 evidence 须产出 gate 输入字段 | PASS |

无「仅 AC 编号无人话」、无英文-only purpose。

---

## 语义断言抽检（testing-guidelines §2–§3）

| 类别 | 数量 | 说明 |
| ---- | ---- | ---- |
| `rule_id` + `status == FAIL` | 12 | 数据质量失败路径，非 call-only |
| 模型/报告/CLI 可观察结果 | 4 | roundtrip、`overall_status`、`returncode`、JSON 字段 |
| 弱/宽松断言 | 1 | 见下 |

**需记录的宽松断言（NON-BLOCKING）：**

- `test_dataHealthIntegration_v2Evidence_bundle`：`overall_status in {"PASS","WARN","FAIL"}` 对任意合法实现恒真；同测另断言 `sandbox_clean_write_gate_ready` 类型与 `gate_rationale` 非空，仍保留 gate 契约语义，但未冻结 v2 evidence 的期望 overall 等级。

**无以下违规：**

- 仅 `assertDoesNotThrow` / `assertIsNotNone` 作为唯一断言  
- private 方法 `verify()` / 调用次数断言  
- `assert True` 或空断言体  

`test_dataHealthModel_roundtrip_success` 使用 dataclass 全字段相等，属有效业务语义（序列化 roundtrip）。

---

## AUDIT.plan §2 边界对照

| 场景 | MASTER §5.3 是否列 | Audit 结论 |
| ---- | ------------------ | ---------- |
| 13 rule_id 失败路径 | 是（§5.3b） | 已覆盖 |
| manifest 缺失 FAIL | 是 | `test_dataHealthLoader_missingManifest_fails` |
| v2 evidence 集成 + gate | 是 | `test_dataHealthIntegration_v2Evidence_bundle` |
| CLI 坏 evidence 非零退出 | **未列** | 计划外缺口（见下） |
| metadata 空 date/source/vendor | R3Y-DH-04 任务卡提及，MASTER 仅冻结 title | 计划外缺口（见下） |

**Audit 未新增 pytest**（只读审计；缺口记入本报告供 A9 / Repair 裁决）。

---

## pytest 复跑（MAP §2.2 C-20 + A8 sandbox）

**环境：** `quant-monitor-desk-wt-r3-data-health` · Windows · `uv run pytest`

| 命令（MAP §2.2 / AUDIT §2） | basetemp | 结果 |
| --------------------------- | -------- | ---- |
| `uv run pytest tests/test_ops_data_health.py -q` | `.trellis/tasks/round3-readonly-data-health-v1/.audit-sandbox/pytest` | **18 passed**, exit 0 |
| `uv run pytest tests/test_ops_db_inspector.py tests/test_staged_pilot.py tests/test_data_quality_validator.py -q` | （MAP 原文，无 basetemp） | **101 passed, 1 skipped**, exit 0 |
| 同上邻接命令 | audit-sandbox basetemp | **1 ERROR** — `test_dbInspect_pathOutsideDataRoot_rejectedFromScan`（`tmp_path` 父目录缺失，**C-20 未改** `test_ops_db_inspector.py`；Windows + basetemp 环境脆化） |

**audit-prod-path 判定：** MAP §2.2 字面命令（无 basetemp）**全绿** → **PASS**。  
basetemp 邻接失败记为 **NON-BLOCKING 环境项**（非 C-20 slice 回归）。

**test catalog：** `tests/test_catalog.yaml` 已登记 `tests/test_ops_data_health.py`。

---

## MASTER §5.3 / §5.3b 用例矩阵

| §8 | `test_*` | rule / 语义 | 状态 |
| --- | -------- | ----------- | ---- |
| 8.1 | `test_dataHealthModel_roundtrip_success` | model roundtrip | 绿 |
| 8.2 | `test_dataHealthLoader_missingManifest_fails` | loader FAIL | 绿 |
| 8.3 | `test_dataHealthDailyBar_*`（5 测） | OHLC / dup / volume / history / empty via bars | 绿 |
| 8.4 | `test_dataHealthMetadata_emptyTitle_fails` | `MISSING_REQUIRED_FIELD` | 绿 |
| 8.5 | `test_dataHealthLineage_*`（6 测） | lineage + misuse rules | 绿 |
| 8.6 | `test_dataHealthStaleness_*`（2 测） | `STALE_DATA` / `EMPTY_RESPONSE` | 绿 |
| 8.7 | `test_dataHealthReport_jsonAndText_success` | JSON + text summary | 绿 |
| 8.8 | `test_dataHealthCli_readOnly_exitZero` | CLI exit 0 + 只读字段 | 绿 |
| 8.9 | `test_dataHealthIntegration_v2Evidence_bundle` | v2 evidence + gate | 绿 |

矩阵行 **9/9**（§5.3）；rule_id **13/13**（§5.3b）。

---

## 计划外发现

> 对抗性搜索：对照 R3Y §4–§5、`data_quality_rules.yaml` common_rules、实现 `check_metadata_rows` / CLI，追问「仅按 MASTER §5 测仍可能漏什么」。

| # | 发现 | 严重度 | 说明 |
| - | ---- | ------ | ---- |
| 1 | CLI 坏 evidence / 缺参未断言非零退出 | **NON-BLOCKING** | R3Y-DH-08 要求可操作入口；现仅 happy path exit 0。fail-closed 行为靠 loader 单测，未端到端 CLI。 |
| 2 | metadata 仅测 empty `title` | **NON-BLOCKING** | 任务卡 R3Y-DH-04 列举 empty date/source/vendor；MASTER §6.1 已冻结 v1 最小子集，实现 `check_metadata_rows` 亦仅 title。测试与实现一致，但契约面仍窄。 |
| 3 | v2 集成测 `overall_status` 集合过宽 | **NON-BLOCKING** | 见「语义断言抽检」；建议后续 hygiene 收紧为基于 fixture 的期望等级或 snapshot 子集断言。 |
| 4 | basetemp 下邻接 `test_ops_db_inspector` Windows flake | **NON-BLOCKING** | 非 C-20 diff；A8 sandbox 口径需注意 Windows `tmp_path` 父目录。 |

**已对抗搜索、无 BLOCKING 缺口：** daily bar happy PASS 无独立测（失败路径充分 + `check_daily_bars` 内 `_pass_check`）；`cn_filings` profile 未单列（v1 scope defer）；契约 YAML 无 `validation_tests` 段（以 rule_id 子集 + pytest 参数化覆盖为准）。

---

## DOUBT（qa-expert checklist）

- [x] 每个 MASTER §7 Red Flag：有测试或 explicit NON-BLOCKING 上表  
- [x] 五字段 docstring：18/18  
- [x] `--basetemp=<task>/.audit-sandbox/pytest`：slice 主测已跑（18 绿）  
- [x] 无 tautological 主路径断言（除集成测宽松项已标注）  
- [x] MAP §2.2 邻接命令：exit 0  

---

## 结论

| 检查项 | 判定 |
| ------ | ---- |
| 五字段 docstring | **PASS**（18/18） |
| 语义测试（rule_id / 只读 / gate） | **PASS**（1 测宽松，NON-BLOCKING） |
| MAP §2.2 C-20 verification | **PASS**（双命令绿；basetemp 邻接 Windows 脆化 NON-BLOCKING） |
| **A8 总判定** | **PASS** |
