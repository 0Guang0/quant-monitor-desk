# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Environment
.env
.env.local

# Runtime data (local-first store)
data/duckdb/
data/raw/
data/files/
data/parquet/
data/audit/
data/reports/
data/cache/

# Audit sandbox (complex-task-protocol §2.10; ≠ Execute DATA_ROOT)
.audit-sandbox/
.audit-sandbox*/
.audit-sandboxpytest-*/

# Stray npm cache dirs (invalid path names from misconfigured npm)
=/
frontend/=/

# Code intelligence indexes (local-only; refresh with analyze --index-only / codegraph sync)
.gitnexus/
.codegraph/

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Agent / CLI tool mirrors (keep skills in ~/.codex or ~/.cursor; do not vendor in repo)
.agents/
.claude/
.audit_*/
.audit-sandbox/

# Local npm cache (never commit; Codex git scan was hashing 9k+ files here)
.npm-cache/

# Test / build artifacts
.coverage
frontend/tsconfig.tsbuildinfo

# Temporary agent artifacts (must not ship)
task_plan.md
findings.md
progress.md
scratch/
tmp/
.tmp/
.ai-bridge/
