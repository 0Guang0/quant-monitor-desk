# A8 — Test Gap Audit (R3H-03 CN Market Adapters)

**Task:** `06-28-round3h-r3h03-cn-market`  
**Auditor role:** A8 / QA (test-engineer)  
**Worktree:** `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3h03`  
**Authority:** frozen `R3H_03_CN_MARKET_ADAPTERS.md` §5.1、§10.1、§13 Red Flags · `AUDIT.plan.md` §1 A8 · `testing-guidelines` · `EXECUTION_INDEX.md` §2

---

## Pytest command (AUDIT.plan §1 A8)

```bash
uv run pytest tests/test_cn_market_adapters.py tests/test_tdx_provider_port.py \
  tests/test_source_route_planner.py -q \
  -k "baostock or cninfo or akshare or tdx or mootdx or eastmoney or sina or ifind or qmt or xqshare or layer_cn" \
  --basetemp=.audit-sandbox/pytest
```

### Run 1 — A8 selector（audit-sandbox basetemp）

```
...............................
31 passed in 0.64s
```

| Module | Collected (total) | Selected by `-k` |
| --- | --- | --- |
| `tests/test_cn_market_adapters.py` | 33 | **29** |
| `tests/test_tdx_provider_port.py` | 1 | **1** |
| `tests/test_source_route_planner.py` | 24 | **1** (`test_qmtDisabled_routePlanShowsSkipReason`) |

**A8 selector 合计：31 passed, 0 failed.**

### Run 2 — §9.8 adjunct（plan-external, mandatory for registry AC）

```bash
uv run pytest tests/test_source_capabilities.py -q -k "r3h03"
```

```
..                                                                       [100%]
2 passed
```

### Run 3 — 全库 merge gate（§9.10 / AC-MERGE）

```bash
uv run pytest -q
```

```
exit 0（约 3.5 min；含 5 skipped — live/auth-gated 类用例）
```

### Run 4 — 五字段 docstring 门禁

```bash
uv run pytest tests/test_docstring_quadruple_coverage.py -q
```

```
...                                                                      [100%]
exit 0
```

---

## Catalog & loop_maintain

| Check | Result |
| --- | --- |
| `tests/test_catalog.yaml` — `test_cn_market_adapters.py` | Registered（`runtime-contract`；links `source_capabilities.yaml`, `source_route_contract.yaml`, `layer5_evidence_contract.yaml`） |
| `tests/test_catalog.yaml` — `test_tdx_provider_port.py` | Registered（`runtime-contract`） |
| `tests/test_catalog.yaml` — `test_source_route_planner.py` | Registered（pre-existing） |
| `uv run python scripts/loop_maintain.py` | **OK** |
| `uv run python scripts/check_test_catalog.py` | **OK** — 118 modules |

---

## 五字段 docstring 核查

| 范围 | 结果 |
| --- | --- |
| `test_cn_market_adapters.py` 全部 33 个 `test_*` | **PASS** — 每个含 覆盖范围 / 测试对象 / 目的/目标 / 验证点 / 失败含义 |
| `test_tdx_provider_port.py` 1 个 `test_*` | **PASS** |
| A8 选中 `test_source_route_planner.py` 1 项 | **PASS**（`test_qmtDisabled_routePlanShowsSkipReason`） |
| `tests/test_docstring_quadruple_coverage.py` | **PASS**（全库门禁） |

**Spot-check 结论：** 模块级 docstring 与 R3H-03 十源范围声明一致；目的/目标与失败含义均为通俗中文。

---

## A8 selector 外但仍属 R3H-03 的测试（4 项）

以下 `test_cn_market_adapters.py` 用例**不在** A8 `-k` 内，但属于 Execute 9.0/9.1 关键路径：

| Test | Step | 为何在 selector 外 | 风险 |
| --- | --- | --- | --- |
| `test_bootSkeleton_testModuleLoads` | 9.0 | 名称无 keyword | 低 — boot smoke |
| `test_bootSkeleton_cnMarketNormalizerModuleExists` | 9.0 | 名称无 keyword | 低 |
| `test_evidence_contract_writeReadRoundTrip_fixturePreservesFields` | 9.1 | 名称无 keyword | **中** — G11 核心往返证据 |
| `test_evidence_contract_licenseGateDefaultDisabled` | 9.1 | 名称无 keyword | **中** — 三门 license_gate 默认 DISABLED |

**建议：** 将 AUDIT `-k` 扩为 `... or evidence_contract or bootSkeleton`，或单独在 A8 附录跑 `tests/test_cn_market_adapters.py -k evidence_contract` 作为 adjunct。

---

## Red Flag → coverage matrix（frozen §13）

| Red Flag | A8 selector 覆盖 | 证据位置 | Gap |
| --- | --- | --- | --- |
| Registry note without port | **PASS** | `test_baostock_registry_readyWithEvidenceStatus`（十源循环）+ `test_r3h03_cnMarketSources_readyWithEvidenceStatus`（capabilities，selector 外） | 无 |
| `akshare` promoted to primary | **PASS** | `test_akshare_validationOnlySource_blockedAsPrimaryWhenForced` + registry 双测 | 无 |
| QMT enabled without authorization tests | **PASS** | `test_qmt_port_unauthorizedBlocksByDefault` + `test_xqshare_port_unauthorizedBlocksByDefault` + `test_qmtDisabled_routePlanShowsSkipReason` | 无 |
| Runtime `参考项目` import | **N/A in A8** | A3 维 `rg` 审计 | 由 A3 闭包 |

---

## §10.1 adversarial step map

| Step | Must prove | A8 status | 备注 |
| --- | --- | --- | --- |
| 9.0 | Module skeleton; normalizer import | **Partial** | boot 2 测在 selector 外；`test_tdxProviderPort_mootdxPortModuleExists` 仅 import smoke |
| 9.1 | `cn_market_evidence_v1` E2E; staged pilot L2 迁出 | **Partial** | `writeReadRoundTrip` / `licenseGate` 在 selector 外；staged pilot 形状测在 selector 内 |
| 9.2 | baostock Primary READY + cap overflow | **PASS** | route + `capOverflow` |
| 9.3 | cninfo filings + PDF cap | **PASS** | metadata + PDF cap + live smoke capped |
| 9.4 | akshare cannot primary; `quality_flags` | **PASS** | route 对抗 + flags |
| 9.5 | tdx/mootdx no silent fallback | **Partial** | 独立 source_id + minute 拒绝 + gate auth；**无 tdx/mootdx rows cap overflow pytest** |
| 9.6 | eastmoney/sina conflict evidence | **Partial** | conflict 字段有；**无 cap overflow / route DISABLED pytest** |
| 9.7 | QMT/iFinD/xqshare default disabled | **PASS** | 每源 unauthorized + authorized 正例各 1 |
| 9.8 | Ten-source registry manifest | **Partial in A8** | selector 内 `test_baostock_registry_*`；完整十源在 `test_source_capabilities -k r3h03`（2 passed） |
| 9.9 | Layer CN smoke; provenance missing fails | **Partial** | L2/L4/L5 + calendar/OHLCV health；**缺 Layer3 smoke**（见 G3） |
| 9.10 | Full suite green | **PASS** | 本 run `pytest -q` exit 0 |

---

## Gap table（prioritized）

| ID | Priority | Gap | Why it matters | Suggested test（勿弱化目的） |
| --- | --- | --- | --- | --- |
| G1 | **High** | akshare / eastmoney / sina / cninfo filings **rows cap overflow** 无登记 pytest | frozen §7 + A4-OPEN-02；`reject_over_cap` 已实现仅 baostock/cninfo PDF 有对抗测 | `test_akshare_port_capOverflow_blocksOverMaxRows` 等，mirror `test_baostock_port_capOverflow_*` |
| G2 | **High** | tdx_pytdx / mootdx **symbol/rows cap overflow** 无 pytest | §7「20 list rows / 3 bars」；minute 拒绝有测，cap 溢出无测 | `test_mootdx_port_capOverflow_blocksOverMaxRows` |
| G3 | **Medium** | **Layer3 CN smoke 缺失** | frozen §9.9 要求 Layer3/Layer4/Layer5；`cn_market.py` 无 `layer3_preview` helper | 若产品需要 L3：加 `cn_market_bundle_layer3_preview` + `test_layer_cn_*_layer3*`；否则 frozen 显式 defer §4.3 |
| G4 | **Medium** | Route 对抗面薄：selector 仅 **baostock READY** + **qmt DISABLED** | 十源 × route 闭环表（§5.1）要求每源 route 测；cninfo/eastmoney/sina/ifind/xqshare 无 dedicated route pytest | `test_cninfo_route_primaryReadyOnAnnouncements`；eastmoney/sina `DISABLED_BY_DEFAULT` route 负例 |
| G5 | **Medium** | A8 `-k` 漏掉 **9.1 evidence_contract** 核心 2 测 | G11/G16 证据契约是 AC 根 | 扩 AUDIT `-k` 或 adjunct 命令 |
| G6 | **Medium** | `test_source_capabilities.py -k r3h03` **不在 A8 命令** | §9.8 AC-REGISTRY-10 权威测在此 | A8 附录必跑（本 audit 已跑 2/2 PASS） |
| G7 | **Low** | `test_tdx_provider_port.py` 仅 1 个 boot 测 | 9.5 行为测在 `test_cn_market_adapters`；本文件近乎占位 | 可合并或加 tdx family 专属 cap/route 测 |
| G8 | **Low** | `test_source_route_planner.py` 24 测中仅 1 被 `-k` 选中 | AUDIT 命令 hygiene | 接受（CN route 多在 adapter 模块）；或增 CN 专用 route 文件 |
| G9 | **Low** | Boot / mockFetch 弱断言 | `source_fetch_id` truthy only；boot 查 docstring 子串 | NON-BLOCKING per testing-guidelines §9 boot |

---

## Weak assertions（A8 selector 内）

| Test | Issue | Severity |
| --- | --- | --- |
| `test_bootSkeleton_*` | docstring / import smoke only | Acceptable for 9.0 |
| `test_*_mockFetch_emits*` | `assert body.get("source_fetch_id")` truthy — 非稳定 id | NON-BLOCKING |
| `test_baostock_registry_readyWithEvidenceStatus` | 十源 capabilities 静态读；不跑 fetch | NON-BLOCKING（capabilities  adjunct 补强） |
| `test_eastmoney_port_conflictEvidencePresent` | 断言 primary_source_id 字段存在；未跑 route planner | NON-BLOCKING |
| `test_tdxProviderPort_mootdxPortModuleExists` | `create_mootdx_fetch_port` 非 None only | LOW |

**Positive：** `test_akshare_validationOnlySource_blockedAsPrimaryWhenForced` 使用真实 `SourceRoutePlanner` + `extra_candidates Primary`；Layer smoke 用 `assert_layer5_factual_source_record` 具体字段；calendar/OHLCV health 有 FAIL 级断言。

---

## 十源 × 八项闭环（§5.1 速查）

| source_id | port mock | cap pytest | route pytest | replay | evidence | health/conflict | layer |
| --- | --- | --- | --- | --- | --- | --- | --- |
| baostock | ✓ | ✓ rows | ✓ READY | ✓ | ✓ | ✓ calendar/OHLCV | ✓ L4/L5 |
| cninfo | ✓ | ✓ PDF | **—** | ✓ | ✓ | — | ✓ L2 |
| akshare | ✓ | **—** rows | ✓ validation block | ✓ | ✓ flags | — | — |
| tdx_pytdx | ✓ gate | **—** | **—** | ✓ | ✓ | — | — |
| mootdx | ✓ | **—** | **—** | ✓ | ✓ | minute reject | — |
| eastmoney | ✓ | **—** | **—** | ✓ | ✓ conflict | — | — |
| sina_finance | ✓ | **—** | **—** | ✓ | ✓ conflict | — | — |
| ths_ifind | ✓ auth ± | **—** | **—** | ✓ | ✓ | — | — |
| qmt_xtdata | ✓ auth ± | **—** | ✓ DISABLED | ✓ | ✓ | — | — |
| qmt_xqshare | ✓ auth ± | **—** | **—** | ✓ | ✓ | — | — |

**图例：** ✓ = A8 selector 内有行为测；**—** = 实现/registry 有但缺登记对抗 pytest 或不在 selector。

---

## 计划外发现

| ID | BLOCKING? | Finding | Search scope |
| --- | --- | --- | --- |
| P1 | NON-BLOCKING | `akshare_port` / `eastmoney_port` / `sina_finance_port` 均调用 `reject_over_cap(max_rows)`，**零** overflow pytest | `backend/**/fetch_ports/*` + `tests/test_cn_market_adapters.py` |
| P2 | NON-BLOCKING | `cn_market.py` 提供 layer2/4/5 preview，**无 layer3**；§9.9 文案写 Layer3 | `cn_market.py` + layer tests |
| P3 | NON-BLOCKING | A4 已记 `license_gate` 与 route `_platform_allows` 双轨 — fetch fail-closed，路由未统一 | A4 `a4.md` A4-OPEN-01 |
| P4 | NON-BLOCKING | `test_cninfo_port_pdfLiveSmoke_cappedWithinLimit` 在 selector 内（Tier-B PDF）；非网络 live | cninfo port |
| P5 | NON-BLOCKING | Execute `9.10-full.txt` 注记 round3g rehearsal 缺 sandbox yaml — **本 run 全库仍绿** | 环境漂移风险低 |

_对抗搜索已完成：frozen §5.1/§10.1/§13、三测试模块、capabilities adjunct、catalog/loop、docstring 门禁、port cap 实现对照。_

---

## Verdict

**PASS_WITH_FINDINGS**

| Criterion | Result |
| --- | --- |
| A8 selector pytest | **31/31 PASS** |
| Red Flags — test \| defer \| audit note | **PASS**（§13 四项均有测或它维闭包） |
| 五字段 docstrings | **PASS** |
| test_catalog + loop_maintain | **PASS** |
| 全库 pytest（§9.10） | **PASS**（exit 0） |
| §5.1 十源八项闭环 | **Partial** — cap/route 子集有缺口（G1–G4） |

**Summary：** R3H-03 核心对抗面（akshare 不可 primary、QMT/iFinD/xqshare 未授权阻断、baostock primary route、conflict_evidence、Layer L2/L4/L5 smoke、十源 registry 终态）在 A8 selector + capabilities adjunct 下**可绿且已绿**。最高风险缺口：**G1/G2** — 多源 `reject_over_cap` 已实现但缺 overflow pytest（与 A4-OPEN-02 一致）。**G3** Layer3 smoke 与 frozen §9.9 文案不一致。**G5/G6** 为 AUDIT 命令覆盖 hygiene，不阻塞 selector 绿。

**Recommend follow-up（audit only）：**

1. TDD 补 G1/G2 cap overflow 测（akshare、eastmoney、sina、mootdx）。
2. 决议 Layer3：实现 `layer3_preview` + smoke，或 frozen §9.9 收窄为 L2/L4/L5。
3. 扩 AUDIT A8 `-k` 含 `evidence_contract`；附录固定 `test_source_capabilities -k r3h03`。
4. 可选：cninfo / aggregator route DISABLED 负例（G4）。
