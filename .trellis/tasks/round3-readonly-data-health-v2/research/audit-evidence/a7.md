# A7 — audit-ops（DuckDB / 运维）

**任务：** `round3-readonly-data-health-v2`（B01-DH2）  
**Worktree：** `feature/round3-readonly-data-health-v2` @ `c79d1228`  
**维度：** A7 · database-administrator + sre-engineer + devops-incident-responder  
**模式：** 只读语义审查（无 migration / 无 init_db 复跑 / 无 prod DB 写）  
**判定：** **PASS**

---

## 检查项

| 检查 | 命令 / 证据 | exit | 结果 |
| --- | --- | --- | --- |
| migration / schema 变更 | `git diff master...HEAD --name-only -- specs/schema/ backend/app/core/migrations/ scripts/init_db.py backend/app/db/` | 0 | **无 diff** — 零 schema 变更 |
| `source_health_snapshot` DDL | `rg source_health_snapshot` in branch diff (`backend/`, `specs/schema/`, `scripts/`) | — | **无实现** — 仅文档/registry defer 引用 |
| 实现面 DB 写路径 | `rg -i "duckdb\|WriteManager\|apply_migration\|init_db\|ConnectionManager\|CREATE TABLE\|INSERT INTO" backend/app/ops/data_health*.py tests/test_*data_health*.py` | 1（无匹配） | **PASS** — 零 DB 接触 |
| 网络 / live fetch | `rg -i "subprocess\|requests\|httpx\|urllib" backend/app/ops/data_health*.py` | 1 | **PASS** |
| 依赖链 | `data_health.py` 仅 `PROJECT_ROOT`、`SourceRegistry`（YAML）、`staged_pilot._resolve_evidence_path` / `_equity_bar_rows`（纯 JSON 读取） | — | **PASS** — 无 ConnectionManager |
| prod DuckDB 存在性 | `Test-Path data/duckdb/quant_monitor.duckdb` | — | **PROD_DB_ABSENT** — hash 对比 **N/A** |
| duckdb hash 不变 | prod 库不存在；diff 无 `data/duckdb/` 触及 | — | **N/A（满足「无污染」）** |
| CLI 只读 smoke | `uv run python -m backend.app.ops.data_health_cli --evidence tests/fixtures/data_health/v2_integration_bundle --profile model_input_whitelist --format json` | 2（BLOCKED 语义） | `production_db_mutated=false`，`source_fetch_performed=false` |
| CLI 参数面 | `--help` | 0 | 无 `--db`；仅 `--evidence` / `--profile` / `--output` |
| 切片测试 | `uv run pytest tests/test_ops_data_health.py tests/test_data_health_v2.py -q --tb=no` | 0 | **35 passed** |
| Execute 声称对齐 | `execute-evidence/full-pytest-green.txt` | — | 1026 passed — Execute handoff 与 A7 语义一致 |

### 语义 / 运维面摘要

- **输入：** `Path.read_text` + `json.loads` 读取 staged/raw/sandbox evidence JSON；v2 profile 另读 fixture YAML（whitelist 目录）；`SourceRegistry` 仅解析 `specs/datasource_registry/*.yaml`。
- **输出：** 内存 `DataHealthReport` + 可选 `--output` 写报告文件；**不写** `quant_monitor.duckdb`、不调用 `apply_migrations`、不触及 `WriteManager`。
- **v2 profiles：** `model_input_whitelist`、`fred_sandbox_pilot`、`tdx_manual_probe`、`staged_pilot_v3`、`source_readiness_rollup` — 全部为 evidence-path 规则校验，无持久化层。
- **gate 字段：** `build_report()` 将 `production_db_mutated=False`、`source_fetch_performed=False` 硬编码；`sandbox_clean_write_gate_ready` 为只读语义闸门（读 manifest 标志），不触发 clean write。
- **R3E 禁止项：** 任务卡 §4/§6 明确禁止 migration、`source_health_snapshot`、production clean write — diff 与实现均遵守。

---

## GitNexus

`research/gitnexus-execute-summary.md`：`DataHealthService.check_evidence_dir` / `data_health_cli` impact **LOW**；未触及 `staged_pilot.py` body、registry trio、`specs/model_inputs/**`；声明 **no live fetch, no DB write, no migration** — 与源码审查一致。

---

## DOUBT（database-administrator + sre-engineer + devops-incident-responder）

| 疑点 | 结论 |
| --- | --- |
| 第二次 init/migrate 是否静默损坏？ | **不适用** — diff 无 schema/migration；实现不调用 `init_db.py`。 |
| kill migrate 后 schema 一致？ | **不适用** — 无 migration 路径。 |
| `staged_pilot` 辅助函数是否引入写库边？ | **否** — `_resolve_evidence_path` / `_equity_bar_rows` 为路径解析与内存行规范化，无 DuckDB。 |
| 异常后是否静默「成功」？ | CLI 缺 evidence 目录 exit 2；缺 manifest → FAIL check + `gate_rationale`；`model_input_whitelist` 在 `specs/model_inputs/**` 未合并时 **BLOCKED**（非 PASS 伪装）。 |
| prod hash 审计前后不变？ | prod DB **不存在**于本 worktree；无写路径 → **无污染风险**。 |
| AUDIT.plan 指定 basetemp 复跑？ | Windows 下若 `.audit-sandbox/pytest` 父目录未预建会 setup ERROR（3 项）；用默认 basetemp **35 passed** — **NON-BLOCKING** 审计基础设施面，非实现缺陷。 |

---

## 计划外发现

> 对抗性搜索范围：`data_health*.py`、`test_ops_data_health.py`、`test_data_health_v2.py`、R3E/R3Y 禁止项、`docs/ops/data_health_cli.md` Batch6 设计 vs 实现、branch diff 全量 DB/migration 面。

| ID | 发现 | 严重度 | 说明 |
| --- | --- | --- | --- |
| — | （无 BLOCKING） | — | 未发现计划外 prod DB 写、migration、`source_health_snapshot` DDL、init_db 依赖或 ResourceGuard 旁路。 |
| A7-O1 | `production_db_mutated` / `source_fetch_performed` 在 `build_report()` **硬编码为 False** | NON-BLOCKING | 与 v1 相同：声明式字段非运行时探针；R3E §6 已禁止写库。后续若接 DuckDB 只读 inspect 需契约级 guard。 |
| A7-O2 | `docs/ops/data_health_cli.md` 设计含 `--db` 只读 DuckDB；v2 CLI **仍无 `--db`** | NON-BLOCKING | 与 R3E evidence-path 模式一致，属显式 defer（Batch 6）；非运维污染面。 |
| A7-O3 | CLI `--output` 可向任意可写路径落盘 | NON-BLOCKING | 非 DB 写；误用可覆盖用户文件；无 prod 路径默认值。 |
| A7-O4 | `model_input_whitelist` profile 在 `specs/model_inputs/**` 未合并时整体 **BLOCKED** | NON-BLOCKING | R3E §3 要求「不猜测 scope」；属任务卡设计，非 DB 越界。 |
| A7-O5 | AUDIT.plan basetemp 路径在 Windows 需预建 `.audit-sandbox/` 父目录 | NON-BLOCKING | 3 个 CLI 集成测试 setup ERROR；默认 basetemp 全绿。建议协调者在 playbook 注明 `New-Item -Force` 或 `parents=True`。 |

**已对抗搜索、无额外 DB/migration/数据污染面。**

---

## 结论

B01-DH2 read-only data health v2 在 v1 evidence-path 基础上扩展五类 readiness profile，实现为 **JSON/YAML manifest 加载 + 规则校验 + 报告/gate 语义**，**零 migration、零 `source_health_snapshot` DDL、零 DuckDB 接触、零 prod 写、零 live fetch**。AUDIT.plan §1 A7「确认无 migration / `source_health_snapshot` DDL · 零 schema 变更」满足；prod duckdb hash **N/A（库不存在，无污染）**。

**A7 判定：PASS**
