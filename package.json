{
  "name": "quant-monitor-desk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prepare": "husky",
    "typecheck": "npm run typecheck --prefix frontend",
    "test": "uv run python -m pytest -q",
    "test:quick": "uv run python -m pytest -q -m \"not slow and not network\"",
    "test:full": "uv run python -m pytest -q",
    "test:perf-gate": "uv run python scripts/run_perf_gate.py"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4"
  }
}
