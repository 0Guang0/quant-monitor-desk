# Trellis Audit A2 (Ponytail) — R3H-01

**Verdict:** PASS_WITH_DEBT — 无 A2 阻塞项。

实现方向与 frozen §1.1 方案 B 一致：L2 `fred_port` 基线、ops 仅 orchestration、G10 normalizer SSOT、废弃 bridge FRED sidecar。

## §3.2 候选删改表

| 候选删改（file:line）                                                              | ponytail 梯级                                           | 是否阻塞        |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------- |
| 六源 port 重复 `_bundle_content_hash` + `_finalize_bundle`（各 fetch_ports/\*.py） | 梯级 2（复用 `manifest_content_hash` 或 shared helper） | 建议            |
| 六源 port 重复 `_reject_over_cap`                                                  | 梯级 2（参数化 cap guard）                              | 建议            |
| `official_macro.py:378-393` `read_bis_credit_gap_evidence_bundle` — 零调用方       | 梯级 1（YAGNI）                                         | 建议            |
| `official_macro.py` 五组 test-only `read_*` vs prod `read_fred` 不对称             | 梯级 2（泛型 reader 可省 ~80 行）                       | 建议            |
| `sec_edgar_port.py:148-164` `SecEdgarLiveFetchPort` 占位类                         | 梯级 1（YAGNI）                                         | 建议（A4 交叉） |
| `bis_port.py:21` `MAX_SERIES` 未使用                                               | 梯级 1                                                  | 建议            |
| `live_evidence_bridge.py:14-16` 薄 re-export                                       | 梯级 2                                                  | 建议            |
| route `disabledByDefault` 五源近重复测试 setup                                     | 梯级 2（parametrize）                                   | 建议            |

## 保留（正确）

- `ops/fred_fetch_ports.py` thin re-export
- `materialize_fred_promote_evidence` 去掉 FRED sidecar（frozen §9.1）
- `normalizers/sec_edgar.py` 独立 schema（frozen §9.5）
- `ingestion_evidence.py` layer helpers（frozen §9.7）

## 计划外发现（NON-BLOCKING）

- 六 port hash 复制；`read_bis_credit_gap` 死代码；BIS credit_gap 无 replay；SEC layer smoke 手工拼 macro shape

## DOUBT

≥20 行可简化：**是** — hash/cap 三联复制 ~78 行；test-only read\_\* ~85 行；route 负例重复 ~140 行。均不挡 Audit PASS。
