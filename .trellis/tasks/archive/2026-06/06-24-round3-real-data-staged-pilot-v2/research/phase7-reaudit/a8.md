# A8 — Phase 7 复审计（test-gap / testing-guidelines）

**维度：** A8 · `testing-guidelines` + `doubt-driven-development`  
**仓库：** `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3-pilot-v2`  
**任务：** `06-24-round3-real-data-staged-pilot-v2`（R3Y 任务卡）  
**审计员模板：** `agents/qa-expert.md` + `agents/audit-adversarial-authority.md`（三级权威）  
**AUDIT.plan §2 A8：** pytest-isolated + `test_production_live_pilot_policy.py`（audit-prod-path）  
**模式：** 只读 · 未新增 pytest · 未 commit

---

## 总判定

| 项               | 结果     |
| ---------------- | -------- |
| **A8 判定**      | **FAIL** |
| **BLOCKING**     | 1        |
| **NON-BLOCKING** | 7        |

**阻断根因：** `test_stagedPilotV2_routePreviewMatrixV2_allStatuses` 在 Phase 7 sandbox 复跑 **RED** — `capture_route_preview_matrix_v2` 因缺少 `skipped` route_kind 在断言前 `RuntimeError` 退出。AC-SP2-05 测量级套件当前不可绿。

---

## pytest 复跑（sandbox）

**命令：**

```bash
uv run pytest tests/test_staged_pilot.py -q \
  --basetemp=.trellis/tasks/06-24-round3-real-data-staged-pilot-v2/.audit-sandbox/pytest
```

**结果（2026-06-24 Phase 7）：**

| 套件                                         | collected | passed                                                      | failed | exit  |
| -------------------------------------------- | --------- | ----------------------------------------------------------- | ------ | ----- |
| `tests/test_staged_pilot.py`                 | **41**    | **40**                                                      | **1**  | **1** |
| `tests/test_production_live_pilot_policy.py` | 9         | （本轮未复跑；Execute `8.10-green.txt` / A1 记载 9 passed） | —      | —     |

**失败用例：**

```
test_stagedPilotV2_routePreviewMatrixV2_allStatuses
  → RuntimeError: route_preview_matrix_v2 missing route kinds: ['skipped']
  → staged_pilot.py:1566 capture_route_preview_matrix_v2
```

**与首轮 Audit / Execute 差异：**

| 时点                                 | staged pilot 计数        | 说明                                                                                                       |
| ------------------------------------ | ------------------------ | ---------------------------------------------------------------------------------------------------------- |
| Execute `tier-a-staged-pilot.txt`    | 40 passed                | 无 `closeoutThreeStateSemantics`；route 探针将 `USER_AUTH_REQUIRED` 标为 `skipped`                         |
| 首轮 `research/audit-evidence/a8.md` | 40 passed                | 判定 PASS                                                                                                  |
| **Phase 7 本轮**                     | 40 passed / **1 failed** | 新增第 41 测；实现将 `qmt_xtdata` 改为 `user_auth_required`，与 `required_kinds` 仍要求 `skipped` **冲突** |

**测量级 49 测口径：** 首轮为 `test_staged_pilot.py`（40）+ `test_production_live_pilot_policy.py`（9）= 49。当前 staged 文件已增至 41 项且 1 RED，**不满足** AUDIT.plan「与 Execute 一致 / exit 0」。

---

## AC-SP2 + AC-MUT-001 覆盖矩阵

> 评分：5=pytest 直接断言 AC 语义；4=间接/execute-evidence 补强；3=仅 happy path 或弱断言；2=缺口有 defer；1=无测无 defer。  
> **不得**因 MASTER §5.3 已列用例而自动给 5 分。

| AC             | R3Y / MASTER 追溯          | 冻结 pytest                                                           | 分    | Phase 7 对抗结论                                                                                                                            |
| -------------- | -------------------------- | --------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| **AC-SP2-01**  | R3Y-SP2-01 → §8.1          | `test_stagedPilotV2_capsJson_*` ×2                                    | **4** | caps 正向 + `max_symbols` 负向有测；**无** `max_trade_days` / `max_rows` / `max_network` 超 cap 负向                                        |
| **AC-SP2-02**  | R3Y Q1 → §8.2              | `test_stagedPilotV2_baostockExpanded_writesManifestV2`                | **4** | mock `fetch_runner` 断言 hash/fetch_id/relative_paths；**未**断言 R3Y Q7 的 `vendor_api` / `as_of_timestamp`                                |
| **AC-SP2-03**  | R3Y Q2 → §8.3              | `test_stagedPilotV2_cninfoExpanded_schemaFieldsPresent`               | **4** | schema 字段 + md 落盘；metadata 经 patch 注入，非 live 结构漂移                                                                             |
| **AC-SP2-04**  | R3Y Q3 → §8.4              | `test_stagedPilotV2_akshareValidation_recordsTaxonomy`                | **3** | 断言 `SUCCESS` + `SOURCE_FAILURE` 枚举与 `validation_only`；**未**断言 `re_defer: true` / `NETWORK_ERROR` 文案（execute-evidence 有，测无） |
| **AC-SP2-05**  | R3Y Q4 → §8.5              | `test_stagedPilotV2_routePreviewMatrixV2_allStatuses`                 | **1** | **Phase 7 RED**；实现 `required_kinds` 含 `skipped` 但 `USER_AUTH_REQUIRED` → `user_auth_required` 后无 `skipped` 探针                      |
| **AC-SP2-06**  | R3Y Q5 → §8.6              | `test_stagedPilotV2_validationReportV2_exposesQualityFlags`           | **3** | 断言 `quality_flags` + `row_count`；**未**断言 `source_used` / schema drift 路径                                                            |
| **AC-SP2-07**  | R3Y Q6 → §8.7              | `test_stagedPilotV2_conflictSummaryV2_primaryOrDeferred`              | **4** | status ∈ 两枚举 + `reason` 非空；未区分 PRIMARY compare vs NO_CONFLICT defer 的业务差异                                                     |
| **AC-SP2-08**  | R3Y Q8 → §8.8              | 4× `test_mutationProof_*`（**无** `write_no_mutation_proof_v2` 专测） | **3** | `build_production_mutation_proof` 语义充分；**markdown 工件** `no_mutation_proof_v2.md` 生成/字段无 pytest；prod 存在情形仅单元 tmp DB      |
| **AC-SP2-09**  | R3Y Q9 → §8.9              | `closeoutMatrix` + `closeoutRequiresHash*`                            | **5** | per_source 矩阵 + `production_live_readiness_claim: false` + AUD-08 gate                                                                    |
| **AC-MUT-001** | AUD-04 / R3Y-MUT-PROOF-001 | 4× `test_mutationProof_*` + `closeoutRequiresHash*`                   | **5** | VERIFIED / MUTATION_DETECTED / INCONCLUSIVE 三态 + KEY/非 KEY 变异对抗测齐全                                                                |

**MASTER §7 Red Flags → 测试映射：**

| Red Flag                   | 测试 / defer                                      | 结论          |
| -------------------------- | ------------------------------------------------- | ------------- |
| 九切片合并为单 run         | 各切片独立 `test_stagedPilotV2_*`                 | ✅            |
| proof_status 假安全        | 4× `test_mutationProof_*`                         | ✅            |
| sync `adapter=` 旁路       | 本文件无专测；§8.10 其他模块                      | defer → A3/A1 |
| validation-only 当 primary | `primary_forbidden is True`（akshare 测）         | ✅ 弱         |
| 超 cap 无声扩样            | 仅 `max_symbols` 拒绝                             | ⚠️ 部分       |
| production-live 声称       | closeout `production_live_readiness_claim: false` | ✅            |
| 过度工程                   | N/A（A2 维）                                      | —             |

---

## docstring 四元组抽检

**规范来源：** `testing-guidelines` §9、`qa-expert` checklist — 中文 **目的 / 测试对象 / 验证点 / 失败含义**（本项目 docstring 常用「覆盖范围」≈范围说明）。

### v2 新增/变更测（15 项）

| 四元组字段              | 合规数    | 说明                                                |
| ----------------------- | --------- | --------------------------------------------------- |
| 目的/目标（含 AC 引用） | **15/15** | 均含「目的/目标：AC-\*」或 OOB-1                    |
| 测试对象                | **15/15** | 均含「测试对象：」                                  |
| 验证点                  | **1/15**  | 仅 `test_stagedPilotV2_closeoutThreeStateSemantics` |
| 失败含义                | **1/15**  | 同上                                                |

**抽检结论：** v2 块 **未达四元组完整**（14/15 缺验证点与失败含义）。首轮 `audit-evidence/a8.md` 称「40/40 含目的」——**未审计**验证点/失败含义，本轮对抗升级后判 **NON-BLOCKING 文档债**。

**通俗性示例（合格）：**

- `test_mutationProof_inconclusiveWhenHashChangesKeyCountUnchanged` — 「不得将 hash-only 漂移标为 VERIFIED」非工程师可懂。

**示例（不完整四元组）：**

- `test_stagedPilotV2_routePreviewMatrixV2_allStatuses` — 有目的/对象，无「失败含义：若 skipped 缺失则 matrix 不可落盘」。

### 全文件 `test_staged_pilot.py`（41 项）

| 字段                                      | 合规      |
| ----------------------------------------- | --------- |
| 覆盖范围 + 测试对象 + 目的/目标（三元组） | **41/41** |
| 完整四元组                                | **1/41**  |

---

## 弱断言 / tautological 抽检

| 测                                                          | 风险                                                                     | 严重度       |
| ----------------------------------------------------------- | ------------------------------------------------------------------------ | ------------ |
| `test_stagedPilotV2_conflictSummaryV2_primaryOrDeferred`    | `reason` 仅断言 truthy，任意非空串即过                                   | NON-BLOCKING |
| `test_stagedPilotV2_akshareValidation_recordsTaxonomy`      | 未断言 `re_defer` / NETWORK 文案，回归可静默丢失 re-defer 语义           | NON-BLOCKING |
| `test_stagedPilotV2_validationReportV2_exposesQualityFlags` | `"quality_flags" in item` 未验 flags 业务内容；无 `source_used`          | NON-BLOCKING |
| `test_stagedPilotV2_baostockExpanded_writesManifestV2`      | `required_fields_present is True` 为汇总位，单字段回归可能被内部逻辑掩盖 | NON-BLOCKING |
| v1 `test_stagedPilot_captureRouteMatrix_*`                  | INCONCLUSIVE 分支仅断言 status 字符串，无 hash 明细                      | 已知 v1 债   |

**无** `assert True` / 纯 `is not None` 唯一断言的 v2 测；mock 边界符合 §1（patch fetch / `_load_raw_json_payload` 在 I/O 边界）。

---

## AUDIT.plan §2 A8 边界项

| AUDIT.plan 要求                          | Execute 是否列                                                      | Phase 7 结论                    |
| ---------------------------------------- | ------------------------------------------------------------------- | ------------------------------- |
| 非 KEY 表 count 变 → MUTATION_DETECTED   | `test_mutationProof_mutationDetectedWhenNonKeyTableRowCountChanges` | ✅ 已覆盖                       |
| hash 变 / KEY count 不变 → INCONCLUSIVE  | `test_mutationProof_inconclusiveWhenHashChangesKeyCountUnchanged`   | ✅ 已覆盖                       |
| `write_no_mutation_proof_v2` markdown    | **未列**                                                            | ❌ 无专测（AC-SP2-08 缺口）     |
| closeout `None` vs `false` 三态（OOB-1） | Phase 7 新增 `closeoutThreeStateSemantics`                          | ✅ 部分闭合；JSON 序列化仍见 A4 |

**Audit 补测：** 遵守只读，**未**新增 pytest。

---

## 与首轮 `audit-evidence/a8.md` 对照

| 首轮声称                 | Phase 7 对抗结论                                 |
| ------------------------ | ------------------------------------------------ |
| 判定 PASS                | **推翻** → FAIL（route 测 RED）                  |
| 40/40 docstring 目的合规 | 仍成立；四元组不完整                             |
| 49 passed 合计           | 当前 staged **41 中 1 FAIL**                     |
| 无需补测                 | route 回归 + SP2-08 markdown 缺口需 Execute 修复 |

---

## 计划外发现

> 对抗性搜索范围：`tests/test_staged_pilot.py` v2 块（L632–1170）、`staged_pilot.py` v2 capture 路径、`mutation_proof.py`、`execute-evidence/*_v2.*`、R3Y §3 九问 / §4 九切片、MASTER §5.3 / §7 Red Flags、A4/A5 Phase 7 OOB-1/OOF-1 交叉项。**已显式搜索。**

| ID            | 严重度       | 发现                                                                                                                                                                        | 证据                                                                                                                          | 建议                                                                                                             |
| ------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| **OOF-A8-01** | **BLOCKING** | **AC-SP2-05 测量级 pytest RED**：`USER_AUTH_REQUIRED` 映射为 `user_auth_required` 后，`required_kinds` 仍强制 `skipped`，`capture_route_preview_matrix_v2` 在测试断言前崩溃 | `staged_pilot.py:1505-1506,1563-1566`；pytest 失败栈；`execute-evidence/route_preview_matrix_v2.json` 仍为旧版 `skipped` 产物 | 二选一：① `required_kinds` 加入 `user_auth_required` 并更新 R3Y/契约；② 另增探针保留 `skipped`；修复后重跑 41 测 |
| **OOF-A8-02** | NON-BLOCKING | **无** `write_no_mutation_proof_v2` pytest — AC-SP2-08 markdown 字段（R3Y-MUT-PROOF-001 段落、sha256/counts 表）无自动化回归                                                | `tests/` 无匹配；`staged_pilot.py:1906+`                                                                                      | 增 1 测：`write_no_mutation_proof_v2(tmp_path)` 断言 md 含 `proof_status` + 文件存在                             |
| **OOF-A8-03** | NON-BLOCKING | v2 docstring **四元组** 14/15 缺「验证点」「失败含义」                                                                                                                      | v2 `def test_*` docstring 全文抽检                                                                                            | 按 `testing-guidelines` §9 补齐，不改 purpose                                                                    |
| **OOF-A8-04** | NON-BLOCKING | caps fail-closed 仅测 `max_symbols`；`max_trade_days` / `max_rows` / `max_network` 超界无负向                                                                               | `validate_pilot_v2_authorization` 仅 `capsExceedingMaxSymbols`                                                                | 增参数化负向或 §4.3 defer                                                                                        |
| **OOF-A8-05** | NON-BLOCKING | akshare 测未断言 `re_defer` + NETWORK 文案；与 `akshare_validation_taxonomy_v2.json` 不同步风险                                                                             | 测 L793-798 vs evidence L14-19                                                                                                | 断言 `any(r["re_defer"] for r in records)` + reason 含 NETWORK                                                   |
| **OOF-A8-06** | NON-BLOCKING | validation report v2 未断言 `source_used`（R3Y Q5 显式项）                                                                                                                  | `capture_validation_report_v2` 产出含字段；测 L863-865 未验                                                                   | 补 `assert item["source_used"] == "baostock"`                                                                    |
| **OOF-A8-07** | NON-BLOCKING | baostock manifest 未覆盖 R3Y Q7 `vendor_api` / `as_of_timestamp`                                                                                                            | Q7 vs `test_stagedPilotV2_baostockExpanded_*`                                                                                 | 扩 manifest 断言或 defer 至 live-fetch 切片                                                                      |
| **OOF-A8-08** | NON-BLOCKING | execute-evidence `route_preview_matrix_v2.json` 与当前代码行为**不一致**（coverage 无 `user_auth_required`，qmt 仍为 `skipped`）                                            | evidence L199-204 vs 当前 `staged_pilot.py`                                                                                   | 修复 OOF-A8-01 后重生成 evidence                                                                                 |

**已搜索且无计划外阻断（测试层）的领域：** AC-MUT-001 对抗四测 + closeout gate；AC-SP2-09 per_source 矩阵；production-live 声称；validation-only `primary_forbidden`；tautological-only 断言。

---

## §3.8 汇总（A8 移交 A9）

| Red Flag / AC                  | 覆盖                     | 补测/证据                     |
| ------------------------------ | ------------------------ | ----------------------------- |
| R3Y-MUT-PROOF-001 / AC-MUT-001 | 4 对抗测 + closeout gate | ✅                            |
| AC-SP2-01..04,07,09            | 有 pytest；部分弱        | ⚠️                            |
| **AC-SP2-05**                  | **RED**                  | **OOF-A8-01 须 Execute 修复** |
| AC-SP2-06,08                   | 弱 / 无 markdown 测      | OOF-A8-02,06                  |
| MASTER §7 Red Flags            | 大部分有测               | adapter 旁路 defer A3         |
| docstring 四元组               | 1/41 完整                | OOF-A8-03                     |

**A8 Phase 7 判定：FAIL（1 BLOCKING）。** 修复 `OOF-A8-01` 并复绿 `test_staged_pilot.py` 后，余项可作为 NON-BLOCKING 记入 `audit.report` §4.3，不单独阻断 B-19 测试契约闭合。

---

_Phase 7 复审计 · B-19 A8 test-gap · composer-2.5 · 只读 · 工作区 `wt-r3-pilot-v2` · 无 commit_
