# A7 — audit-ops（SRE / DuckDB / 事故响应）

**维度：** A7 · database-administrator + sre-engineer + devops-incident-responder  
**任务：** `round3-fred-authorized-sandbox-pilot` (B01-FRED)  
**审计范围：** `backend/app/ops/fred_*.py` · registry proposed delta · `execute-evidence/*` · FRED pytest 切片  
**权威对照：** `R3E_fred_authorized_sandbox_pilot.md` · `BATCH_01_HARDENING_RULES.md` · `production_live_pilot_policy.md` · `AUDIT.plan.md` §2 A7  
**模式：** sandbox-only；**无** migration / init_db 复跑 / production DB 写  
**判定：** **PASS**  
**OPEN：** **0** · **BLOCKING：** **0**

---

## §3.7 检查项（冻结命令与证据）

| 检查项 | 命令 / 证据 | exit | 结果 |
| --- | --- | --- | --- |
| migration / schema 变更 | `git diff master -- specs/schema scripts/init_db.py backend/app/core/migrations` | 0 | **无 diff** — 本切片未触及 migration 面 |
| 实现面 DB 写路径 | `rg -i "duckdb\|WriteManager\|apply_migration\|init_db\|writer\(\)\|ConnectionManager" backend/app/ops/fred_sandbox_pilot.py backend/app/ops/fred_fetch_ports.py backend/app/ops/fred_evidence_validator.py` | 1（无匹配） | **PASS** — 三模块零 DuckDB / WriteManager 接触 |
| production clean write 闸门 | `rg "allow_production_clean_write\|production_clean_write\|sandbox_only" backend/app/ops/fred_sandbox_pilot.py` | 0 | `allow_production_clean_write` 为 true 时 **raise**；route/manifest/closeout 均 `production_clean_write: False`；`sandbox_only: True` |
| 授权 YAML 写目标 | `execute-evidence/authorization.yaml` | — | `write_target: raw_or_staging_or_sandbox` · `allow_production_clean_write: false` |
| Execute 证据 JSON | `fred_raw_manifest.json` · `route_preview_fred.json` · `fred_pilot_closeout.json` | — | 全部 `production_clean_write: false`；`production_live_claim: false` |
| prod DuckDB 存在性 | `Test-Path data/duckdb/quant_monitor.duckdb` | — | **PROD_DB_ABSENT**（worktree 无生产库文件） |
| AUDIT_PROD_ROOT | `Test-Path .audit-sandbox/r3e-fred-audit-prod` | — | **ABSENT**（本切片未打开 prod-path 副本） |
| 默认 sandbox 根 | `DEFAULT_SANDBOX_ROOT` → `.audit-sandbox/r3e-fred-sandbox/` | — | 运行时写仅 JSON manifest/evidence 至 sandbox 或 pytest `tmp_path` |
| DISABLED_SOURCE（fail-closed） | `test_fredRegistry_disabledByDefault_notProductionLive` + registry `enabled_by_default: false` | — | fred **disabled-by-default**；`expected_error_before_user_setup: DISABLED_SOURCE` |
| FAIL_AUTH 异常路径 | `test_fredPilot_missingAuth_returnsFailAuth` · `test_fred07_liveFetch_closureClosedSkipOptIn_withoutApiKey` | — | 无授权 / 无 `FRED_API_KEY` → `FRED_PILOT_FAIL_AUTH`；非静默 PASS |
| Tier A FRED pytest | `uv run pytest tests/test_fred_source_registry.py tests/test_fred_sandbox_pilot.py tests/test_fred_staged_semantics.py -q --basetemp=.trellis/tasks/round3-fred-authorized-sandbox-pilot/.audit-sandbox/pytest` | 0 | **16 passed, 1 skipped**（live opt-in 无 key） |
| Execute 声称对齐 | `execute-evidence/9.7-green.txt` · `9.8-green.txt` · `research/execute-evidence/handoff.md` | — | Tier A/B 绿；FRED-07 CLOSED-SKIP-OPT-IN；无 prod 写声称 |

### 运维 / 数据路径语义摘要

| 环节 | B01-FRED 行为 | 生产污染风险 |
| --- | --- | --- |
| Route preview | `SourceRoutePlanner` dry-run；`write_target: sandbox` | 无 DB 写 |
| Mock / live fetch | `FetchPort` → JSON body + hash；**不经** `WriteManager` | 无 DuckDB |
| 证据落盘 | `sandbox_root / fred_raw_manifest.json` 或 `fred_live_fetch_evidence.json` | 仅 sandbox / task evidence 路径 |
| Health | `fred_evidence_validator.py` 纯内存规则 | 无 I/O 写库 |
| Closeout | `fred_pilot_closeout.json` 声明式；`b2_5_o_05_closed: false` | 无 migration |

**依赖链（SRE）：** `fred fetch port` → JSON manifest → pilot-local validator → closeout JSON。**未进入** `validation → write_manager → DuckDB` hop。

---

## A7 异常场景菜单（本任务适用性）

| 场景 | 适用 | 证据 |
| --- | --- | --- |
| Kill migrate | **N/A** | diff 无 migration；实现不调用 `init_db.py` |
| 磁盘满 / 不可写 | **N/A**（未执行混沌） | 写面限于小 JSON；无 prod 路径默认 |
| DISABLED_SOURCE | **PASS** | registry + route preview 测试；fred 默认 disabled |
| Smoke 中断 | **N/A** | 本切片无 `production_equivalent_smoke.py` 路径 |
| ResourceGuard HARD_STOP | **N/A** | 无批处理 ingest 至 DuckDB；caps 在 pilot 层（≤5 series / 100 rows / 10 calls） |
| FAIL_AUTH / NETWORK / SCHEMA | **PASS** | `run_failure_scenario` + live skip 测试；live port `timeout=30` + `PortError` 可观测 |

---

## DOUBT（database-administrator + sre-engineer + incident-responder）

| 疑点 | 结论 |
| --- | --- |
| 第二次 init/migrate 是否静默损坏？ | **不适用** — 零 migration / init_db 依赖 |
| kill migrate 后 schema 一致？ | **不适用** — 无 migration 路径 |
| 异常后是否静默「成功」？ | **否** — FAIL_AUTH / FAIL_NETWORK / FAIL_SCHEMA 显式 status；closeout `production_live_claim: false` |
| `production_clean_write: false` 是否仅为声明？ | **是（声明式）** — 当前无 DB 写路径故不构成污染；见计划外 A7-O1 |
| Execute `*-green.txt` 可独立复现？ | **是** — 上表 pytest 在 audit-sandbox basetemp 复绿 |
| live fetch 无 key 是否误触 prod？ | **否** — `skip_live_fetch_default: true` + pytest skip + FAIL_AUTH 路径 |

---

## GitNexus

- **query（邻接）：** FRED pilot 符号未入索引；邻接 `live_pilot_*` / staged semantics 流程为 **LOW risk**（见 `research/gitnexus-execute-summary.md`）。
- **impact：** 本切片未修改 `ConnectionManager` / `WriteManager` / migration 模块。

---

## 计划外发现

> 对抗性搜索：`fred_*.py` · `R3E` 禁止项（production DB / migration / clean write）· `BATCH_01_HARDENING_RULES` §3–§7 · `rg` 全仓 `fred` + `duckdb|WriteManager` 交叉 · AUDIT.plan 未列的 DB/污染面。

| ID | 严重度 | 发现 | 处置 | Audit status |
| --- | --- | --- | --- | --- |
| — | — | 无 BLOCKING | — | — |
| A7-O1 | NON-BLOCKING | `production_clean_write` / `sandbox_only` 在 manifest 与 closeout 中为**硬编码字段**，非运行时 DuckDB 探针 | R3E §禁止项 + 零 WriteManager 路径已足够；若未来接 clean-write rehearsal 须加契约级 guard | **CLOSED**（sandbox-only 切片范围内可接受） |
| A7-O2 | NON-BLOCKING | 未走 `ResourceGuard`；网络/行数 caps 仅在 `fred_sandbox_pilot.py` 内 | 与 R3E「受控小样本 pilot」一致；Batch 6 live primary 再评估 Guard 对齐 | **CLOSED**（explicit defer to Batch 6） |
| A7-O3 | NON-BLOCKING | 审计时 `.audit-sandbox/r3e-fred-sandbox` 与 `r3e-fred-audit-prod` **不存在**（pytest 用 `tmp_path`） | Execute evidence JSON 已落盘于 `execute-evidence/`；非 prod 污染 | **CLOSED** |
| A7-O4 | NON-BLOCKING | FRED-07 live 默认 skip；B2.5-O-05 live primary 仍 RE-DEFERRED | `research/b01-fred-audit-closures.md` + closure tests | **CLOSED** |
| A7-O5 | NON-BLOCKING | 实现文件 worktree 内 **untracked** | 主会话 merge commit gate；A7 语义已验 | **CLOSED**（流程面，非运维污染） |

**显式非发现（已对抗确认）：**

- `specs/schema` / `scripts/init_db.py` / `backend/app/core/migrations` — master 无 diff。
- `data/duckdb/quant_monitor.duckdb` — worktree 不存在。
- `data_health.py` 主体 — forbidden；本任务 pilot-local validator only。
- registry 三件套直接 commit — 仅 `proposed_registry_delta.yaml`。

---

## 汇总

| 类别 | 数量 |
| --- | --- |
| BLOCKING | **0** |
| OPEN (含 NON-BLOCKING) | **0** |
| CLOSED / N/A / CLOSED-SKIP-OPT-IN | **5** + 5 项 N/A 场景 |

---

## 结论

B01-FRED 为 **authorized sandbox/raw-only** 切片：JSON manifest + pilot-local 校验 + closeout 证据，**零 migration、零 DuckDB 连接、零 production clean write、零 prod DB 文件触碰**。`AUDIT.plan` §2 A7「无 migration/DB clean write · audit-sandbox · 无 prod 写」满足。

**A7 verdict：PASS** — 零遗留，可继续 A8。
