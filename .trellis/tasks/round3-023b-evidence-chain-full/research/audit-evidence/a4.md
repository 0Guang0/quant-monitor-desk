# A4 code-reviewer — B01-023 Layer5 Evidence Chain

> **维度：** A4 · code-review-and-quality + doubt-driven-development  
> **模型：** composer-2.5  
> **分支：** `feature/round3-023b-evidence-chain-full` @ worktree `quant-monitor-desk-wt-023-layer5`  
> **日期：** 2026-06-25  
> **模式：** Audit（只读，无 commit、无改码）

---

## Review Summary

| 项           | 值                                                                                     |
| ------------ | -------------------------------------------------------------------------------------- |
| **判定**     | **PASS**                                                                               |
| **BLOCKING** | 0                                                                                      |
| **Verdict**  | 无 P0 逻辑/安全阻塞；chain trace、Agent-not-fact、ADR-023 severe→queued 主链与契约一致 |

**Overview：** 023b 在 `backend/app/layer5_evidence/` 交付 staged-only 的 `EvidenceChainBuilder`：复用 023A `EvidenceFoundationValidator`、新增 registry/bar 校验器、`EvidenceReadPort` 注入与 ADR-023 冲突路径。6 项 chain 测 + 5 项 foundation 回归全绿（12 passed）。对抗性审阅发现若干**计划外**错误模型/边界测缺口与任务卡 scope 部分兑现项，均登记为 NON-BLOCKING（主责 A8 补测或 Batch 6 偿还）。

---

## 权威层级（`audit-adversarial-authority.md`）

| 级别   | 来源                                                                                  | 审阅对照结论                                                                                  |
| ------ | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 第一级 | `layer5_security_evidence.md` §3.8 evidence_chain、§Agent 非事实源                    | builder 保留 layer context 槽；`reject_agent_text_as_fact` 委托 foundation；无买卖语义          |
| 第一级 | `layer5_evidence_contract.yaml` quality_rules / boundaries                            | agent_text、no-future-data（bar）、structured context（L3/L4 非空）部分执行；见 NB-4、NB-5     |
| 第一级 | `ADR-023-layer5-conflict-review-path.md`                                              | severe → `need_human_review=True` + `manual_review_state=queued`；无 instant UI；与测试一致     |
| 第二级 | `023_implement_layer5_evidence_chain.md` §8–9、§17                                    | trace + Agent-not-fact + conflict ADR 满足；event/financial/valuation 模型未落地（见 NB-5）   |
| 第三级 | `AUDIT.plan.md` §2 A4「chain trace + agent-not-fact + conflict ADR 一致」             | **满足**；Plan 未索引边界见 §计划外发现                                                       |

**对抗性注记：** MASTER §5.3 仅列 6 测；任务卡 §1 列 bar/event/financial/valuation 全族——不得因 MASTER 已绿而视为 event/financial/valuation 已完备。

---

## Chain Trace 审查（integration-ledger + AC-023-3）

| 链路节点              | 实现                                                                 | 测试                                                                 | 结论   |
| --------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------- | ------ |
| L3 upstream id        | `LayerContextBundle.upstream_snapshot_ids` → `EvidenceChainRecord`   | `test_evidenceChain_traceUpstreamSnapshots` 断言 `L3-SNAPSHOT-001`   | OK     |
| L4 upstream id        | 同上                                                                 | 断言 `L4-SNAPSHOT-001`                                               | OK     |
| layer3_context 槽     | JSON 字符串透传                                                      | 断言含 `ANCHOR-CU`                                                   | OK     |
| layer4_context 槽     | JSON 字符串透传                                                      | 断言含 `CN_A`                                                       | OK     |
| layer5_context 默认   | `layer5_context or record.evidence_summary`                          | port 路径经 `minimal_bundle.json` 间接覆盖                            | OK\*   |
| provenance 追溯       | `build()` 前 `foundation.validate_record`                            | foundation 回归 + agent 拒绝测                                       | OK     |
| port 读 staged bundle | `build_from_port` → `_record_from_bundle` + `_layer_context_from_bundle` | `test_evidenceReadPort_boundary`                                     | OK     |

\* `layer5_context` 显式赋值路径无独立断言。

**设计文档缺口（非阻断）：** `layer5_security_evidence.md` §3.8 SQL 未列 `upstream_snapshot_ids` / `manual_review_state`；实现 `EvidenceChainRecord` 已扩展——文档/schema 漂移风险，登记 NB-6。

---

## ADR-023 冲突路径一致性

| ADR 决策                                      | 代码位置                                           | 测试                                                       | 一致? |
| --------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------- | ----- |
| severe → `need_human_review=True`             | `evidence_chain.py:96-97`                          | `test_evidenceChain_severeConflictQueuesManualReview`      | 是    |
| severe → `manual_review_state=queued`         | 同上                                               | 同上                                                       | 是    |
| queued 无 flag → 拒绝                         | `foundation.py:84-87`（先于 chain `_resolve`）     | 同上期望 `EvidenceFoundationError`                         | 是    |
| 不实现 instant severe queue UI                | 无 UI 代码                                         | —                                                          | 是    |
| fail-closed **或** queued 记录                | severe 产出 queued 记录（非 hard reject）          | severe 测断言 queued 而非抛错                              | 是（ADR 允许「或」分支） |

---

## 多轴审查（§3.4）

| 轴          | 发现                                                                                       | 阻塞? | 证据                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------- |
| 正确性      | staged bundle → foundation 校验 → context 槽 + upstream ids 组装                           | 否    | `test_evidenceChain_traceUpstreamSnapshots`、`test_evidenceReadPort_boundary`                     |
| chain trace | L3/L4 snapshot id 与 context 非丢失透传                                                    | 否    | `evidence_chain.py:54-70`；trace 测                                                               |
| Agent-not-fact | `created_by` agent 前缀 / 摘要 marker 拒绝                                              | 否    | `test_evidenceChain_rejectsAgentTextAsFact`；`foundation.py:47-55`                                |
| conflict ADR | severe → queued；queued 无 flag → foundation 拒绝                                          | 否    | `test_evidenceChain_severeConflictQueuesManualReview`                                             |
| registry    | `instrument_id` bundle 内唯一                                                              | 否    | `test_instrumentRegistry_uniqueIds`                                                               |
| bar as_of   | 未来 `trade_date` 拒绝                                                                     | 否    | `test_securityBar_rejectsFutureTradeDate`                                                         |
| 边界闸      | 空 `upstream_snapshot_ids` / 空 L3/L4 context 拒绝                                         | 否\*  | `evidence_chain.py:49-52`；**无单测**（见 DOUBT）                                                 |
| 错误处理    | 域错误 `EvidenceChainError` / `EvidenceFoundationError`；bundle 解析可泄漏 `ValueError`    | 否\*  | port 缺失测；`float()`/`date.fromisoformat` 见 NB-2                                               |
| 架构/复用   | 复用 023A foundation；`EvidenceReadPort` Protocol 解耦 storage                             | 否    | `evidence_chain.py:33-35,72-83`；对齐 MASTER §7                                                   |
| 重复逻辑    | `_resolve_manual_review` queued 检查与 foundation 重复且后段不可达                           | 否    | `evidence_chain.py:98-101` vs `foundation.py:84-87`                                               |
| 风格        | ponytail 注释于 `_FakeEvidenceReadPort`；五字段中文 docstring 齐全                         | 否    | `test_layer5_evidence_chain.py` 全文                                                              |
| 性能        | 纯内存 dataclass；无 DuckDB 热路径                                                         | 否    | 委托 A6 SKIP                                                                                      |

\* 实现存在；测试或错误类型一致性有缺口。

---

## Critical Issues

（无）

---

## Important Issues

| ID   | 位置                                                                 | 描述与建议                                                                                                                                                                                                 | 阻塞?        |
| ---- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| NB-1 | `evidence_chain.py:49-52`                                            | **空 `upstream_snapshot_ids` / 空 layer3/4 context 已实现但无测。** AUDIT.plan A8 已点「空 upstream ids」边界。**建议：** A8 补 mutation → `EvidenceChainError`。                                          | NON-BLOCKING |
| NB-2 | `evidence_chain.py:110-114,128`                                      | **`float()` / `date.fromisoformat()` 失败抛原生 `ValueError`，非域错误。** 畸形 staged bundle 难单类型捕获（对齐 022 NB-2 模式）。**建议：** 包一层转 `EvidenceChainError` / `EvidenceValidationError`。   | NON-BLOCKING |
| NB-3 | `evidence_chain.py:85-89`                                            | **`validate_bar_from_bundle` 无测试调用。** SLICE-MODELS 与 port 路径未串联 bar 校验；`minimal_bundle.json` 含 bar 但未经此方法验证。**建议：** A8 补 port+bar 集成测或删除未用 API。                        | NON-BLOCKING |
| NB-4 | `evidence_validator.py:31-34` vs contract `high_must_be_greater_or_equal_low` | **high&lt;low 校验已实现但仅 direct `validate_bar` 测覆盖 future date。** 无 high&lt;low mutation 测；`validate_bar_from_bundle` 未测。**建议：** A8 补 `high < low` mutation。                            | NON-BLOCKING |
| NB-5 | 任务卡 §1 vs `models.py`                                           | **任务卡列 event/financial/valuation；实现仅 `SecurityBarDaily` + bar validator。** MASTER SLICE-MODELS 以单 bar 测闭合 AC-023-2。**建议：** 主会话登记 defer 或 Batch 6 补模型骨架；勿声称全族已实现。     | NON-BLOCKING |
| NB-6 | `layer5_security_evidence.md` §3.8 vs `EvidenceChainRecord`          | **设计 SQL 缺 `upstream_snapshot_ids`、`manual_review_state`；实现已含。** 契约 `evidence_chain.required_fields` 亦未列 upstream。**建议：** 文档/契约对齐以免下游 schema 漂移。                              | NON-BLOCKING |

---

## Suggestions

| 位置                                | 描述                                                                                                                                           |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `evidence_chain.py:98-101`          | `_resolve_manual_review` 中 queued 检查在 `validate_record` 之后不可达——可删冗余分支或移入 foundation 单一 SSOT。                               |
| `evidence_chain.py:43`              | `confidence` 无范围校验（负值/&gt;1）——staged 可接受；生产化应约束。                                                                             |
| `instrument_registry.py:26-29`      | `validate_row` 仅验非空字符串；`is_active` 无业务语义校验——与 contract 一致，可接受。                                                           |
| `evidence_chain.py:168-170`         | `reject_agent_text_as_fact` 每次 new `EvidenceFoundationValidator()`——可复用实例，非功能问题。                                                   |
| `tests/test_layer5_evidence_chain.py` | severe 测第二断言经 foundation 路径拒绝 queued 无 flag——行为正确但掩盖 chain 层 `_resolve_manual_review` 分支，文档化即可。                      |

---

## DOUBT — 边界遗漏（`file:line`）

1. **`evidence_chain.py:49-50`** — 空 `upstream_snapshot_ids` 抛 `EvidenceChainError`；零测试；chain trace 半链失效时无回归网。
2. **`evidence_chain.py:51-52`** — 空 `layer3_context` / `layer4_context` 拒绝；契约 `evidence_must_reference_structured_context` 依赖此闸但无 mutation 测。
3. **`evidence_chain.py:110-114`** — 畸形 bar 数值 `open: "x"` 经 `_bar_from_bundle` 抛 `ValueError`，非可观测域错误。
4. **`evidence_chain.py:85-89`** — `validate_bar_from_bundle` 公开 API 无测试；port 路径不验证 bundle 内 bar。
5. **搜索范围：** `evidence_chain.py`、`evidence_validator.py`、`instrument_registry.py`、`models.py`、`foundation.py`（只读对照）、`tests/test_layer5_evidence_chain.py`、`layer5_evidence_contract.yaml`、`ADR-023-layer5-conflict-review-path.md`、`integration-ledger.md`；未发现 live fetch、WriteManager 写、storage 硬依赖（转 A3）。

---

## 计划外发现

| ID   | 发现                                       | 严重度           | 说明                                                   |
| ---- | ------------------------------------------ | ---------------- | ------------------------------------------------------ |
| NB-1 | 空 upstream / 空 L3-L4 context 无测        | **NON-BLOCKING** | AUDIT A8 已索引；代码有                                |
| NB-2 | bundle 解析 `ValueError` 泄漏              | **NON-BLOCKING** | 错误模型不一致                                         |
| NB-3 | `validate_bar_from_bundle` 无测            | **NON-BLOCKING** | 未用 API 或缺集成测                                    |
| NB-4 | high&lt;low 契约规则测不全                 | **NON-BLOCKING** | validator 有、测仅 future date                         |
| NB-5 | event/financial/valuation 未实现           | **NON-BLOCKING** | 任务卡 §1 vs MASTER 最小切片张力                       |
| NB-6 | 设计 SQL vs `EvidenceChainRecord` 字段漂移 | **NON-BLOCKING** | upstream_snapshot_ids / manual_review_state            |
| —    | live fetch / production 写 / L3-L4 改      | **无发现**       | diff 范围符合 `worktree-slices.md`                     |

**显式声明：** 已按 `audit-adversarial-authority.md` A4 最低动作对照设计文档语义、契约字段、chain trace、ADR-023 与 MASTER 外边界；**无 BLOCKING 质量问题**。

---

## What's Done Well

- **023A 复用：** `EvidenceChainBuilder.build` 首行 `foundation.validate_record`，无平行 identity 逻辑；对齐 MASTER §7 Red Flag #1。
- **chain trace：** `upstream_snapshot_ids` 与 L3/L4 context 槽完整透传，满足 AC-023-3 与 integration-ledger wiring 意图。
- **ADR-023 闭环：** severe 输入强制 queued；instant UI 未越界实现；与 `data_validation_and_conflict.md` manual_review 语义一致。
- **port 解耦：** `EvidenceReadPort` Protocol + fake 测闭合 `R2-RISK-2` 条件路径，builder 无 concrete storage import。
- **测试 docstring：** 六测五字段中文齐全；mutation 与 business 断言非 tautological。
- **ponytail：** `_FakeEvidenceReadPort` 标明 minimal fake 天花板；无新依赖、无未请求抽象层。

---

## 与 A2 交叉

- `EvidenceChainBuilder` 薄编排层合理；registry/bar validator 分文件对齐 SLICE 边界，非过度抽象。
- `_resolve_manual_review` 与 foundation 重复校验为可删重复（见 Suggestions），非新抽象泛滥。

---

## Verification Story

| 项               | 状态                                                                                                                                    |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Tests reviewed   | **是** — 6 chain + 5 foundation；purpose 与 AC-023-1..5 映射可读                                                                          |
| Build verified   | **是** — `uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q` → **12 passed**（Audit 2026-06-25） |
| Security checked | **是（局部）** — staged dict 输入；port 边界；无密钥进 diff；bundle 解析错误类型见 NB-2                                                  |
| Full pytest      | **未复跑 Tier B** — Execute 证据 `full-pytest-green.txt` 存在；本维聚焦 L5 diff                                                         |

---

## 结论

**A4 审计判定：PASS。**

B01-023 Layer5 evidence chain 实现满足 AUDIT.plan §2 A4「chain trace + agent-not-fact + conflict ADR 一致」与 ADR-023 决策；无 P0 逻辑阻塞。对抗性审阅登记 6 项 **NON-BLOCKING** 计划外缺口（空 upstream/context 测、bundle 错误模型、bar API 覆盖、契约 quality_rules、任务卡模型族、设计文档字段漂移），建议由 **A8** 补测或主会话登记 defer，**不阻断** Wave D Track B merge。
