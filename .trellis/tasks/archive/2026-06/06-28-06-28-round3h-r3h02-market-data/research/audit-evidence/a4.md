# R3H-02 A4 代码质量维审计（对抗性测试 + 契约对齐）

**维度：** A4 — AV 未授权 / cap 溢出 / validation-only primary block / route DISABLED 负例；`source_capabilities.yaml` ↔ ports/normalizers  
**权威：** `AUDIT.plan.md` §1 A4 · `agents/audit-adversarial-authority.md` · `specs/datasource_registry/source_capabilities.yaml` · 活卡 R3H_02 §2.8  
**Diff 范围：** R3H-02 五源 fetch port / normalizer / registry / 测试（未改代码，只读审查）  
**pytest：**

```text
uv run pytest tests/test_market_data_adapters.py \
  tests/test_crypto_market_adapters.py \
  tests/test_source_route_planner.py \
  -q -k "alpha_vantage or stooq or yahoo or deribit or coingecko or cap or validation or disabled" \
  --basetemp=.audit-sandbox/pytest-r3h02-a4

结果: 33 passed (100%) · exit 0
收集: test_market_data_adapters 11 · test_crypto_market_adapters 8 · test_source_route_planner 14
```

---

## Review Summary

**Verdict:** **PASS_WITH_FIXES**

**Overview:** R3H-02 五源 mock/replay 路径在 A4 过滤 pytest 子集上全绿；证据 normalizer 统一 `trade_date` / `market_data_evidence_v1` / `crypto_market_evidence_v1`，AV 无 key 与 Deribit/AV cap 溢出负例 fail-closed。但 **validation-only 路由对抗** 仍缺 R3H 专用 planner 用例、**capabilities 多域/字段声明** 与 port 实现存在可绕过 drift（`max_window_days`、yahoo option_chain、row-level `content_hash`），合并前应闭合 P1。

---

## §3.4 检查清单（AUDIT.plan A4 对抗轴）

| 轴       | 检查项                                         | 结果             | 证据                                                                                                                                                                                                                                                    |
| -------- | ---------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 负例     | AV 未授权 live（无 `ALPHA_VANTAGE_API_KEY`）   | **PASS**         | `alpha_vantage_port.py:146-147` `USER_AUTH_REQUIRED`；`test_market_data_adapters.py:136-153`                                                                                                                                                            |
| 负例     | AV cap 溢出（`max_rows`）                      | **PASS**         | `alpha_vantage_port.py:89` `reject_over_cap`；`test_market_data_adapters.py:156-173`                                                                                                                                                                    |
| 负例     | Deribit cap 溢出（`max_surface_rows`）         | **PASS**         | `deribit_port.py:40-44`；`test_crypto_market_adapters.py:86-111`                                                                                                                                                                                        |
| 负例     | 五源 route `DISABLED_SOURCE`（默认禁用）       | **PASS**         | `test_r3h02_marketRoute_disabledByDefault` 参数化五源；`test_source_route_planner.py:210-242`                                                                                                                                                           |
| 负例     | validation-only **路由** primary block         | **FAIL**         | `route_planner.py:154-158` 有 `validation_only_cannot_be_primary`；R3H 测试仅查 registry 静态绑定（`test_market_data_adapters.py:312-327` 等），**无** yahoo/stooq/coingecko planner 对抗用例（对比 `test_layer1_ingestion_gates.py:223` akshare 模式） |
| 负例     | stooq/yahoo/coingecko cap 溢出                 | **FAIL**         | port 有 `reject_over_cap`（如 `stooq_port.py:58`、`coingecko_port.py:37`），**无**对应 pytest                                                                                                                                                           |
| 负例     | AV `max_symbols` factory 上限                  | **FAIL**         | `alpha_vantage_port.py:163-164` reject；无测试                                                                                                                                                                                                          |
| 契约     | `resource_caps` YAML ↔ port 常量               | **PARTIAL**      | AV/deribit/coingecko 数值对齐 ✓；`max_window_days:120` 在 AV/stooq 有常量**未调用**；stooq yaml 缺 `max_rows`（port `MAX_ROWS=500`）                                                                                                                    |
| 契约     | capabilities `fields` ↔ normalizer/port 行输出 | **PARTIAL**      | bundle 级 `content_hash` ✓；yaml 行级 `content_hash` 未出现在 bars/instruments；coingecko yaml 要求 `market_cap_usd` mock 未产出；deribit yaml 期权 greeks 仅 `mark_iv`                                                                                 |
| 契约     | capabilities 多 operation ↔ port 实现          | **PARTIAL**      | yahoo `fetch_us_option_chain_validation` 已登记，port 仅 daily bar；AV/stooq/deribit/coingecko 多域声明，port 单路径 mock                                                                                                                               |
| 错误模型 | 未授权 vs cap 状态码                           | **PASS（实现）** | 未授权 `USER_AUTH_REQUIRED`；cap `FAILED` + `"cap"`（与 R3H-01 一致）                                                                                                                                                                                   |
| 测试质量 | 五字段 purpose 注释                            | **PASS**         | 三模块测试均含覆盖范围/对象/目的/验证点/失败含义                                                                                                                                                                                                        |

---

## 契约对齐矩阵（source_capabilities ↔ ports ↔ normalizers）

| 源                | resource_caps                                                                | replay_fixture_path                 | 主实现域                                      | 字段/形状对齐                                                                         | 备注                                                                                    |
| ----------------- | ---------------------------------------------------------------------------- | ----------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| **alpha_vantage** | `max_symbols:5` `max_rows:500` `max_option_strikes:20` `max_window_days:120` | ✓ `aapl_daily_replay.json`          | `us_equity_daily_bar` / `us_option_chain`     | bars: `instrument_id,trade_date,OHLCV,source_used` ✓；yaml 行级 `content_hash` **缺** | 7 域声明，port 仅 2 路径；live 有 key 仍委托 mock（`ponytail`，`:148-153`）             |
| **stooq**         | `max_symbols:5` `max_window_days:120`（无 `max_rows`）                       | ✓ `global_daily_replay.json`        | `global_market_daily_bar`                     | bars 形状 ✓；`fx_daily_bar` yaml 用 `pair_id`，normalizer 仅 `instrument_id`          | `MAX_WINDOW_DAYS` 未 enforce                                                            |
| **yahoo_finance** | `max_symbols:3` `max_option_strikes:10` `max_window_days:120`                | ✓ `aapl_validation_replay.json`     | `us_equity_daily_bar`（validation op 名不同） | daily bars ✓                                                                          | **option_chain operation 无 port**；port 无 `MAX_OPTION_STRIKES`/`MAX_WINDOW_DAYS` 使用 |
| **deribit**       | `max_instruments:5` `max_surface_rows:50`                                    | ✓ `btc_options_surface_replay.json` | `crypto_options_surface`                      | 核心字段 ✓；yaml 全 greeks/IV 面 mock 极简                                            | 3 域声明，mock 恒返回 options 行                                                        |
| **coingecko**     | `max_assets:10`                                                              | ✓ `btc_spot_replay.json`            | `crypto_spot_market`                          | `asset_id,price_usd,volume_24h_usd` ✓；**`market_cap_usd` 缺**                        | `crypto_asset_reference` 域未实现                                                       |

**Normalizers SSOT：** `market_data.py` / `crypto_market.py` 在 bundle 级强制 `source_fetch_id` + `content_hash`（`finalize_bundle`），与 replay fixture 往返一致 ✓。行级 yaml `content_hash` 应视为 bundle 级指纹或后续补字段测试。

**Registry / route：** 五源 `READY_WITH_EVIDENCE`（`test_r3h02_marketSources_readyWithEvidenceStatus`）；yahoo/stooq/coingecko `validation_only:true`；`route_planner` Primary 角色阻断 validation-only ✓（静态 registry 测试 + DISABLED 参数化已覆盖默认态）。

---

## 发现项（按严重度）

### Critical（P0 — 合并阻塞）

（无）— mock-first 范围内未发现 validation-only 静默升格 primary 的实现漏洞；`route_planner.py:154-158` 在 Primary 角色检查 `validation_only`。

### Important（P1 — 合并前应修复）

| ID             | 位置                                                                                                           | 描述                                                                                                                                                                                                                                                         | 建议修复                                                                                                                                        |
| -------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| R3H02-A4-P1-01 | `test_market_data_adapters.py:312-327` · `test_crypto_market_adapters.py:159-174` · `route_planner.py:154-189` | **validation-only 路由对抗缺口：** 测试只断言 registry `primary_source_id != yahoo/stooq/coingecko`，未在源启用 + `extra_candidates=[(id,"Primary")]` 时断言 `VALIDATION_ONLY_BLOCKED` / `validation_only_cannot_be_primary`。AUDIT.plan A4 明确要求此负例。 | 为 yahoo/stooq/coingecko 各增 1 条 `SourceRoutePlanner` 测试，对齐 `test_layer1_ingestion_gates.py:223` akshare 模式。                          |
| R3H02-A4-P1-02 | `alpha_vantage_port.py:26` · `stooq_port.py:21` · capabilities `max_window_days:120`                           | **`MAX_WINDOW_DAYS` 声明未 enforce：** 任务卡 §7 ResourceGuard 与 yaml 声明 window cap，fetch 入口未 `reject_over_cap`。                                                                                                                                     | fetch 入口对窗口跨度 enforce，或 yaml 标注 defer；补 window 溢出负例。                                                                          |
| R3H02-A4-P1-03 | `source_capabilities.yaml:286-307` · `yahoo_finance_port.py`（全文）                                           | **yahoo `fetch_us_option_chain_validation` 已 READY 登记，port 未实现 option chain。** route 若选中该 operation 无 fetch 路径。                                                                                                                              | port 增 option 分支（对齐 AV）或 capabilities 降级/移除直至实现切片。                                                                           |
| R3H02-A4-P1-04 | `tests/test_market_data_adapters.py` · `tests/test_crypto_market_adapters.py`                                  | **cap 负例不对称：** 仅 AV `max_rows`、Deribit `max_surface_rows`；stooq/yahoo/coingecko 有 `reject_over_cap` 无 pytest。                                                                                                                                    | 各补 1 条 cap overflow 测试；可选 factory `max_symbols`/`max_assets` 测。                                                                       |
| R3H02-A4-P1-05 | capabilities 行级 `fields` · `market_data.py:22-33` · `crypto_market.py:21-34` · `coingecko_port.py:47-54`     | **契约字段层级混写：** yaml 行级含 `content_hash`（及 coingecko `market_cap_usd`），normalizer/port 行输出未含；bundle 级指纹正确。                                                                                                                          | 二选一：yaml 拆 `bundle_fields` vs `observation_fields`（R3H-01 先例）；或 mock 补字段 + `test_r3h02_capabilityFields_matchPortOutput` 参数化。 |
| R3H02-A4-P1-06 | `alpha_vantage_port.py:163-164`                                                                                | **`MAX_SYMBOLS` factory reject 无测试**；registry `max_symbols:5` 无 CI 锚。                                                                                                                                                                                 | 增 `test_alpha_vantage_port_capOverflow_blocksOverMaxSymbols`。                                                                                 |

### Suggestion（P2）

| ID             | 位置                                         | 描述                                                                                                             |
| -------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| R3H02-A4-P2-01 | `tests/test_source_capabilities.py`          | 无 R3H 版 `test_*_capsMatchRegistry`（对照 `test_tdxPytdx_capsMatchTaskCard`）；yaml↔port 常量漂移无 parity 测。 |
| R3H02-A4-P2-02 | 五源 `*_WHITELIST`                           | unknown symbol/instrument 仅 runtime `PortError`，无对抗 pytest。                                                |
| R3H02-A4-P2-03 | AV/stooq/deribit/coingecko 多域 capabilities | 单路径 mock 可接受本卡 scope；后续域切片须增 operation-domain 矩阵测。                                           |
| R3H02-A4-P2-04 | `alpha_vantage_port.py:148-153`              | live+key 仍 mock 委托；本卡 mock-first 可 defer，live 切片前须 A3 复审。                                         |
| R3H02-A4-P2-05 | `contract_coverage.yaml`                     | 已索引 market/crypto adapter 测试；cap/validation-only route 负例未标 `negative_required`。                      |

### Info（P3）

| ID             | 描述                                                                                                                 |
| -------------- | -------------------------------------------------------------------------------------------------------------------- |
| R3H02-A4-P3-01 | `market_data.py` read 路径仍接受 legacy `date` 别名输入；输出 canonical ✓。                                          |
| R3H02-A4-P3-02 | stooq/coingecko 在 registry domain_roles 为 yaml primary 且 `validation_only:true` — 设计 fail-closed，U-01 交叉态。 |

---

## DOUBT（CLAIM → DOUBT → RECONCILE）

| CLAIM                              | DOUBT                                                 | RECONCILE                                                   |
| ---------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------- |
| AUDIT.plan A4 对抗轴已闭合         | validation-only **planner** 负例缺；三源 cap 未测     | **PARTIAL** — DISABLED/AV 未授权/AV+Deribit cap ✓；P1-01/04 |
| capabilities ↔ port 字段可机读校验 | 行级 `content_hash` / coingecko `market_cap_usd` 漂移 | **PARTIAL** — bundle 级 ✓；P1-05                            |
| validation-only 不得 primary       | 实现有 planner 阻断；R3H 测仅 registry                | **HOLDS（实现）** / **WEAK（测试）** — P1-01                |
| 五源 READY_WITH_EVIDENCE           | `test_r3h02_marketSources_*`                          | **HOLDS**                                                   |
| yahoo 永久 validation-only         | registry + 静态测试                                   | **HOLDS**；路由对抗见 P1-01                                 |

---

## What's Done Well

- **33/33 A4 pytest 全绿**，audit-sandbox basetemp 隔离。
- **DISABLED 负例参数化五源**（`test_r3h02_marketRoute_disabledByDefault`），默认禁用 fail-closed。
- **证据契约：** `trade_date` canonical、replay 往返、`Layer2/4/5` smoke + 缺 `content_hash` 负例。
- **测试文档：** 五字段中文 purpose 注释齐全。
- **Deribit 无 trading/account 语义**（`test_crypto_market_adapters.py:83` 断言 body 无 `account`）。
- **AV 未授权**精确断言 `USER_AUTH_REQUIRED`（非宽松 `FAILED` 备选）。

---

## Verification Story

| 项                 | 状态                                                                       |
| ------------------ | -------------------------------------------------------------------------- |
| Tests reviewed     | **是** — 33 条 A4 过滤用例；对抗缺口 P1-01/04                              |
| Build verified     | **是** — pytest exit 0，`--basetemp=.audit-sandbox/pytest-r3h02-a4`        |
| Security checked   | **局部** — AV 未授权 ✓；whitelist 未测；无密钥进 diff                      |
| Contract alignment | **部分** — replay/schema/registry caps 主路径 ✓；window/多域/行级字段见 P1 |

---

## 计数与 Top Fixes

| 级别 | 数量 |
| ---- | ---- |
| P0   | 0    |
| P1   | 6    |
| P2   | 5    |
| P3   | 2    |

**Top Fixes（合并前优先）：**

1. **P1-01** — yahoo/stooq/coingecko 补 `SourceRoutePlanner` `validation_only_cannot_be_primary` 路由对抗测试。
2. **P1-04** — stooq/yahoo/coingecko cap overflow 负例（与 AV/Deribit 对称）。
3. **P1-02 / P1-03 / P1-05** — enforce `max_window_days` 或 yaml 收敛；yahoo option_chain 实现或 capabilities 降级；capabilities 字段分层或 port 补 `market_cap_usd` 等。

**Overall verdict:** **PASS_WITH_FIXES**（A4 核心 pytest 全绿；合并前建议闭合 P1-01–P1-06，尤其 validation-only 路由对抗与 cap/契约 parity）
