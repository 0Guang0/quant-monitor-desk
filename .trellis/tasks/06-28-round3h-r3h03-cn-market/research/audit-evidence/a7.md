# Trellis Audit A7 (Ops / DB Boundary) — R3H-03 CN Market Adapters

| 字段     | 值                                                                    |
| -------- | --------------------------------------------------------------------- |
| 维度     | A7 Ops（database-administrator + sre）                                |
| 任务     | `06-28-round3h-r3h03-cn-market`                                       |
| 审计日期 | 2026-06-28                                                            |
| 模型     | composer-2.5                                                          |
| Worktree | `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3h03`                  |
| 权威     | `AUDIT.plan.md` §1 · frozen §2.8 · `agents/database-administrator.md` |

---

## Verdict: **PASS**

---

## 审计问题与结论

| 检查项                       | 要求                                              | 结果                                                                                                                                 |
| ---------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 无新增写主库 CLI             | R3H-03 不得引入 `quant_monitor.duckdb` 写路径 CLI | **PASS** — `backend/app/cli/**` 零匹配十源；新增 `fetch_ports/*` 无 CLI/duckdb/WriteManager；`scripts/` 仅 `check_test_catalog.py` 登记 |
| 既有 promote 拒 canonical DB | R3G-03 `_assert_production_db_allowed` 仍拒主库   | **PASS** — 回归 pytest 8/8 绿；`limited_production_entry.py` 本分支无 diff                                                         |
| 任务 diff 边界               | 不得扩 promote CLI / 主库写 orchestration         | **PASS** — 无 `backend/app/cli/` 变更；`limited_production_entry.py` 未改                                                            |

---

## §3.7 命令证据

| 步骤                    | 命令                                                                                                                                                         | exit  | 关键输出                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------ |
| promote 主库硬拒        | `uv run pytest tests/test_round3g_limited_production_clean_write.py -q -k "refusesCanonicalProductionDbPath or PromoteCli" --basetemp=.audit-sandbox/pytest` | **0** | **8 passed**（含 `test_PromoteRunner_refusesCanonicalProductionDbPath`） |
| CLI 无十源接线          | `rg` `backend/app/cli/` 十 source_id                                                                                                                         | —     | **0 matches**                                                            |
| fetch_ports 无 DB 写    | `rg` `backend/app/datasources/fetch_ports/*.py` `duckdb\|quant_monitor\|WriteManager\|INSERT`                                                              | —     | **0 matches**                                                            |
| promote 模块未改        | `git diff master -- backend/app/ops/sandbox_clean_write/limited_production_entry.py`                                                                         | —     | **无变更**                                                               |
| 本任务 committed ops    | `git diff master --name-only -- backend/app/cli backend/app/ops`                                                                                             | —     | 仅 `calendar_gap_rules.py`（日历 gap 规则，无 DB 写）                    |

**canonical 拒绝对齐代码：** `limited_production_entry.py` `_assert_production_db_allowed` — `quant_monitor.duckdb` resolve 后抛 `LimitedProductionEntryError("canonical production DB path refused…")`（`tests/test_round3g_limited_production_clean_write.py:1023–1037`）。

---

## R3H-03 写边界审阅

| 组件                                                                      | 写行为                                         | 主库风险                          |
| ------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------- |
| 十源 `fetch_ports/*_port.py`                                              | 仅返回 `FetchPayload` / replay fixture           | 无                                |
| `cn_market.py` `write_cn_market_evidence_bundle`                          | 写 `out_dir/cn_market_evidence.json`（文件系统） | 无 CLI 暴露；frozen §2.8 禁止主库 |
| `license_gate.py`                                                         | 纯授权决策枚举                                 | 无 DB 写                          |
| `ops/data_health_profiles/cn_market.py` + `cn_trading_calendar.py`      | 内存 bar 健康检查                              | 无 DB 写                          |
| `calendar_gap_rules.py`（committed 变更）                                 | 扩展 `calendar_authority` 分支                 | 无 DB 写                          |
| registry / capabilities yaml                                              | 声明式配置                                     | 无 DB 写                          |
| `scripts/check_test_catalog.py`                                           | catalog 登记                                   | 无 DB 写                          |

**frozen §2.8 gate 2：**「No main DB writes; sandbox/replay/`.audit-sandbox` only」— 实现与 gate 一致。

**既有 ops 引用（非本任务新增）：** `staged_pilot.py` / `r3g03_isolated_pilot_dry_run.py` 等 R3G 路径仍仅覆盖 baostock/cninfo/akshare 等 rehearsal 源；R3H-03 十源新 port **未**接入 promote CLI 候选表（`limited_production_entry.py` `SOURCE_OPERATION_MAP` 未扩）。

---

## GitNexus

`impact(_assert_production_db_allowed)` — 索引未命中（worktree 索引滞后）；以 pytest 回归 + 源码 diff 零变更作为 promote 守卫证据。

---

## 发现项

| ID  | 级别 | 描述     | 阻塞? |
| --- | ---- | -------- | ----- |
| —   | —    | 无 P0/P1 | —     |

---

## 计划外发现

| ID      | 范围                    | 发现                                                                                                                                                         | 严重度               |
| ------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------- |
| U-A7-01 | promote 候选源          | R3G-03 promote 仍仅覆盖 baostock/cninfo/fred/akshare/yahoo 等 rehearsal 源；**R3H-03 七源新 port 未接入 promote CLI** — 符合本卡 scope（L2 证据链 only）       | NON-BLOCKING（预期） |
| U-A7-02 | evidence bundle 写路径  | `write_cn_market_evidence_bundle` 可写任意 `out_dir` — 无 path guard；依赖调用方（测试/tmp/replay）而非 CLI — 与 R3H-01/02 同模式                          | NON-BLOCKING         |
| U-A7-03 | staged pilot 并存       | `ops/staged_pilot_fetch_ports.py` 仍保留 baostock/cninfo/akshare 旧路径；R3H-03 已 L2 迁出至 `fetch_ports/` — 无新 CLI，长期收敛属后续债                    | NON-BLOCKING         |
| U-A7-04 | rehearsal 全量回归      | `test_RehearsalCli_productionDbPathRejected` 本次未复跑；promote 层 `_assert_production_db_allowed` 单测 + PromoteCli 套件已覆盖同一守卫                      | NON-BLOCKING         |

---

## 维度结论

**PASS** — 无新增写主库 CLI；R3G-03 promote canonical DB 硬拒仍有效；R3H-03 变更未触及 ops promote/CLI 写路径，十源 fetch 与 evidence 均为 sandbox/replay 文件写。
