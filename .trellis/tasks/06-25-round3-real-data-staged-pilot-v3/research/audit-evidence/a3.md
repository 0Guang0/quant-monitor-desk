# A3 — audit-security（B01-SP3 staged pilot v3）

**维度：** static · **判定：** PASS  
**模型：** composer-2.5  
**worktree：** `quant-monitor-desk-wt-b01-sp3`  
**分支：** `feature/round3-real-data-staged-pilot-v3`  
**HEAD：** `56d35bc0`  
**任务：** `06-25-round3-real-data-staged-pilot-v3`  
**审查范围：** `git diff master...HEAD`（backend 实质变更 = `staged_pilot.py` v3 段 + WL YAML + v3 测试）；无 clean write / hand-pick / PDF bulk 旁路专项  
**权威：** `agents/security-auditor.md`、`AUDIT.plan.md` §2 A3、`BATCH_01_HARDENING_RULES.md` §3–§6、`R3E_real_data_staged_pilot_v3.md`  
**模式：** 只读，无 commit

---

## 总判定

| 项               | 值       |
| ---------------- | -------- |
| **判定**         | **PASS** |
| **BLOCKING**     | **0**    |
| **NON-BLOCKING** | 3        |

**等级汇总：** P0: 0 · P1: 0 · P2: 2 · P3: 1

---

## 1. 启动记录

| 项               | 内容 |
| ---------------- | ---- |
| 对抗权威         | 已 Read `agents/security-auditor.md`（worktree） |
| AUDIT.plan §2 A3 | `rg` clean write / hand-picked symbols / PDF bulk — 见 §2 |
| GitNexus         | `query(staged pilot v3 authorization whitelist clean write)` → `DbValidationGate._quality_flags_block_clean_write`、`CninfoMetadataStagedFetchPort.fetch_payload` 等；`validate_pilot_v3_authorization` 未入索引（新符号，索引滞后） |
| diff 范围        | `backend/app/ops/staged_pilot.py` +803 行（v3）；`tests/test_real_data_staged_pilot_v3.py` 新增；`specs/model_inputs/**` WL 合并 |

---

## 2. AUDIT.plan §2 A3 验证矩阵

| 检查 | 命令 / 方法 | 结果 | 证据 |
| ---- | ----------- | ---- | ---- |
| production clean write 旁路 | diff `staged_pilot.py`：`allow_clean_write`、`production_clean_write`、`writer(`、`apply_migrations` | **PASS** | `validate_pilot_v3_authorization` 拒绝 `allow_clean_write=True`（`:2389-2392`）；caps `production_clean_write: false`（`:2334`）；conflict `clean_write_attempted: false`（`:2720`）；`cm.writer()` 仅绑定 sandbox DB（`:781`，既有 `run_staged_pilot_raw_only` 路径） |
| hand-picked symbol 旁路 | WL loader + 授权 gate + 测试 | **PASS** | symbol 来自 `load_pilot_v3_whitelist_scope()` / `pilot_v3_*_symbols()`（`:2237-2290`）；未知 symbol 拒绝（`:2423-2432`）；`assert_model_inputs_whitelist_present()` fail-closed（`:2190-2202`）；`test_v3_refuses_missing_whitelist` / `test_v3_whitelist_caps_enforced` |
| PDF / bulk 旁路 | cninfo op 门禁 + 测试 | **PASS** | `CNINFO_FORBIDDEN_OP_FRAGMENTS`（`:2185`）；仅 `CNINFO_METADATA_OPS`（`:2184`）；`fetch_filing_pdf` 被拒（`:2396-2405`）；`test_v3_cninfo_rejects_pdf_expansion` |
| 密钥 / URL | `rg` diff backend 文件（排除 test） | **PASS** | **0 匹配**（`error_redaction.py` 为 redaction 规则，非泄露） |
| 未授权 live 源 | `DISABLED_PILOT_SOURCE_IDS` + diff | **PASS** | fred/qmt/yahoo/tdx 在禁用集（`:58-66`）；diff 无 `enable.qmt` |
| SQL 拼接（v3 新增） | `rg execute(f` on v3 段 | **PASS** | v3 新增段无 SQL；既有 `DELETE FROM {STG_FILE_REGISTRY}`（`:587`）为常量表名 |
| subprocess / eval | `rg` staged_pilot.py | **PASS** | **0 匹配** |

---

## 3. §3.3 威胁面与 DOUBT 三类

### 3.1 硬编码 URL 变体

| 发现 | 无 |
| ---- | -- |
| 搜索范围 | diff backend 文件 + `staged_pilot.py` v3 段 |
| 理由 | 无 `https?://` 新增；fetch 经既有 `staged_pilot_fetch_ports` / `DataSourceService` |

### 3.2 JWT / API key 模式

| 发现 | 无 |
| ---- | -- |
| 搜索范围 | `rg "api[_-]?key\|secret\|token\|password" backend/ scripts/ --glob '!*test*'` on diff |
| 理由 | 0 匹配；授权证据为 repo 内 markdown/YAML，无凭据 |

### 3.3 SQL 拼接

| 发现 | 无（v3 新增） |
| ---- | ------------- |
| 搜索范围 | v3 新增函数 + diff SQL patterns |
| 理由 | v3 路径不写 production DuckDB；冲突 dry-run 用 `SourceConflictValidator.validate_rows` 内存行 |

---

## 4. 写路径与访问控制

| 路径 | 行为 | 判定 |
| ---- | ---- | ---- |
| `run_staged_pilot_raw_only` | sandbox `ConnectionManager` + `WriteManager`；raw 路径 `is_relative_to(sandbox)`（`:801-803`） | PASS |
| `build_production_mutation_proof` / `write_no_mutation_proof_v3` | 只读 production DB 哈希/行数（`:2743-2766`） | PASS |
| `capture_conflict_summary_v3` | dry-run summary JSON；`can_write_primary_value` 来自 validator，无 clean 写（`:2676-2740`） | PASS |
| akshare primary 提升 | `validate_pilot_v3_authorization` validation-only（`:2407-2411`）+ `assert_akshare_not_primary_for_daily_bar`（`:2471-2490`） | PASS |
| registry 关闭 | `_write_registry_proposed_delta_v3` status=`proposed`，Execute 不 commit（`:2804-2836`） | PASS |

---

## 5. 发现（NON-BLOCKING）

### NB-1 · P2 — hardening §3 结构化授权 YAML 未入 runtime gate

| 字段 | 内容 |
| ---- | ---- |
| 威胁 | Live fetch 可能仅依赖 prompt14 markdown，而 BATCH_01 §3 要求的 YAML 字段（`authorization_present`、`max_calls` 等）未被解析 |
| 证据 | `validate_pilot_v3_authorization` 仅校验 `prompt14_user_authorization_*` 文件名 + `"Approved on"`（`:2368-2379`）；`execute-evidence/live_authorization_2026-06-24.yaml` 存在但代码无读取；`rg live_authorization backend/app/ops` → 0 |
| 缓解 | prompt14 用户授权仍 fail-closed；`skip_live_fetch` 测试/mock 路径默认安全；policy 测试 `test_production_live_pilot_policy.py` 仍约束模块级控件 |
| 建议 | A5/A7 跟进：live 路径增加 §3 YAML schema 校验或文档化「prompt14 = 唯一 runtime 授权源」 |

### NB-2 · P2 — v3 编排缺 v2 同级顶层 ResourceGuard 落盘

| 字段 | 内容 |
| ---- | ---- |
| 威胁 | `run_full_staged_pilot_v3` 无 v2 的 `ResourceGuard.check()` + `resource_guard_caps.json`（对比 `run_full_staged_pilot_v2` `:2081-2094` vs v3 `:2891-2917`） |
| 证据 | v3 仅 `reset_network_call_budget`；per-fetch 仍在 `preview_staged_pilot` → `check_resource_guard` HARD_STOP（`:397-399`） |
| 缓解 | 单次 fetch 仍有 guard；network budget 50 cap |
| 建议 | 对齐 v2 顶层 guard 落盘，便于 audit 复验 |

### NB-3 · P3 — conflict dry-run 使用硬编码 fixture symbol

| 字段 | 内容 |
| ---- | ---- |
| 威胁 | 低 — 非 live hand-pick；合成行用于 validator dry-run |
| 证据 | `capture_conflict_summary_v3` rows 硬编码 `sh.600000`（`:2699-2712`）；该 symbol 亦在 WL 内 |
| 建议 | 可选改为 `pilot_v3_baostock_symbols()[0]` 以增强 WL 追溯一致性 |

### 交叉引用（A5，非 A3 BLOCKING）

Execute `no_mutation_proof_v3.md` / `pilot_v3_closeout.json` 为 `proof_status: INCONCLUSIVE`、`closeout_pass: false` — 安全语义正确（未谎称 VERIFIED），证据强度由 A5 验收。

---

## 6. 结论

B01-SP3 v3 在 **无 clean write、无 hand-pick symbol 旁路、无 cninfo PDF/bulk 扩张** 三项 AUDIT §2 A3 专项上 **PASS**。WL-driven 授权、caps、metadata-only cninfo、akshare validation-only 与 sandbox 写路径与任务卡/hardening 一致。遗留 2 项 P2 为授权 schema 与 guard 落盘一致性（非可直接写 production 的旁路），1 项 P3 为 dry-run fixture hygiene。
