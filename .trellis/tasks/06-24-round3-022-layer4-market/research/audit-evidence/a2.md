# A2 audit-ponytail — Layer 4 市场结构增量审查

**维度：** A2 · ponytail-review + doubt-driven-development  
**范围：** `backend/app/layer4_markets/**` + `tests/test_layer4_market_structure.py`（生产与测试同标准）  
**判定：** **PASS**（0 阻塞候选）

---

## git diff --stat

**已跟踪（相对 HEAD）：**

```
backend/app/layer4_markets/__init__.py | 12 ++++++++++++
1 file changed, 12 insertions(+)
```

**未跟踪（本任务核心增量，Execute 未 commit）：**

| 路径 | 行数（约） |
| --- | --- |
| `backend/app/layer4_markets/market_structure.py` | 406 |
| `backend/app/layer4_markets/lineage.py` | 70 |
| `backend/app/layer4_markets/models.py` | 71 |
| `tests/test_layer4_market_structure.py` | 194 |
| `tests/fixtures/layer4_staged_market/**` | fixture |

**合计净增：** ~753 行生产/测试代码 + 12 行 `__init__.py` + fixture（不计 MASTER explicit AC 的 schema/种子元数据行）。

**MASTER §8 触及文件：** `market_structure.py`、`lineage.py`（可选）、`models.py`（可选）、`test_layer4_market_structure.py`、`fixtures/layer4_staged_market/**`。

---

## ponytail 梯级评估（文件级）

| 文件 | 评估 | 梯级 |
| --- | --- | --- |
| `lineage.py` | 与 `layer3_chains/lineage.py` 同构，复用 `core/snapshot_lineage` kernel | 2 复用 |
| `market_structure.py` | 模块级 helper 对齐 L3 `snapshot_builder.py`；`ponytail:` 标明天花板处（FORBIDDEN_*、`collect_result_field_names`） | 2 复用 + 6 最小新码 |
| `models.py` | 契约 dataclass（AC-022-1..3、lineage envelope） | explicit AC |
| `REGISTRY_SEEDS` (50–115) | 8 个 `market_id` 种子，设计文档 §2 + AC-022-1 | explicit AC（不可删） |
| `StagedCNAMarketAdapter` | 022 仅 CN_A staged；设计 §3 要求 adapter 形态 | 6（最小接口） |
| `tests/_build` + `_copy_bundle` | 共享 build 入口；mutation 须落在 `PROJECT_ROOT` 下（`_resolve_bundle_root` 守卫） | 2 复用 + 信任边界 |
| 测试五字段 docstring | AUDIT A8 / testing-guidelines 要求 | 非 bloat |

---

## §3.2 ≥20 行候选删改

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
| --- | --- | --- |
| `market_structure.py:263-391` 与 `layer3_chains/snapshot_builder.py:148-245` 重复的 `_resolve_bundle_root` / `_read_manifest` / `_manifest_provenance` / `_parse_ts`（~52 行同构） | 梯级 2 — 两消费者已存在，可抽到 `core/staged_bundle.py`（`error_type=` 参数化，比照 `snapshot_lineage`） | **建议**（计划外跨层重复；022 单切片内镜像 L3 可接受） |
| `tests/test_layer4_market_structure.py:59-84` 三个 `_mutate_manifest` / `_mutate_calendar` / `_mutate_breadth` 同构 copy→load→mutate→dump | 梯级 5 — 合并为 `_mutate_bundle_file(tmp_path, name, loader, dumper, mutator)`，purpose 不变 | **建议** |
| `market_structure.py:138-147` + `255-260` `StagedCNAMarketAdapter` + `_adapter_for_market` 仅服务 CN_A | 梯级 1 — 第二 adapter 出现前可直调 `_load_*`；但设计 §3 预留 adapter 名 | **建议**（<20 行，附注） |
| `lineage.py` 独立文件 vs 内联 `market_structure.py` | 梯级 2 — L3 已有独立 lineage；MASTER 标 optional inline | **不阻塞** |
| `market_structure.py:50-115` `REGISTRY_SEEDS` 硬编码元组 | explicit AC-022-1 / 设计 §2 | **不删** |
| `market_structure.py:27-48` `FORBIDDEN_*` + `244-252` `collect_result_field_names` | explicit AC-022-7；已有 `ponytail:` 注释与升级路径 | **不阻塞** |

**ponytail-review 净估：** `net: -20 ~ -55 lines possible`（测试 helper 合并 + 跨层 staged_bundle 抽取；后者需协调 L3，非 022 单切片必做）。

---

## 与 A4 交叉引用

- `Layer4MarketError` / `Layer4LineageError` 双 `ValueError` 子类与 L3 同模式；错误文案与 `as_of` 链一致性属 **A4**，非 A2 删改项。
- `_reject_future_observation` 与 L3 `_reject_future_bar` 对称；无重复日志样板需 A2/A4 合并。

---

## DOUBT

**≥1 处 ≥20 行可简化：** 是 — 见上表前两行（跨层 manifest 助手 ~52 行重复；测试 mutation 三件套 ~25 行可合并）。

**搜索范围（已读）：**

- `backend/app/layer4_markets/*.py` 全文
- `tests/test_layer4_market_structure.py` 全文
- 对照 `backend/app/layer3_chains/snapshot_builder.py`、`lineage.py`、`core/snapshot_lineage.py`
- `docs/modules/layer4_market_structure.md` §2–§3
- `specs/contracts/layer4_market_contract.yaml` boundaries
- MASTER §3.2 allowed / optional 文件说明

**未找到：** 单处调用的新 factory/wrapper、未请求第三方依赖、speculative config 层。

---

## 计划外发现

| 发现 | 类型 | 说明 |
| --- | --- | --- |
| L3/L4 staged manifest 解析 ~52 行同构未抽 `core` | NON-BLOCKING | MASTER 未要求；022 镜像 L3 属合理 defer；Wave C 合并协调时可一次性抽取 |
| 8 种子中 7 个 `market_id` 无 staged adapter 实现 | 已对抗 | 设计 §2 元数据 + AC-022-1；`_adapter_for_market` 仅 CN_A，非 YAGNI 越界 |
| `build()` 每次返回全量 `registry_rows` + 全 `calendar_rows` | NON-BLOCKING | 测试仅断言种子唯一性与当日 breadth；若下游只需单日可缩 payload（非 022 AC 硬要求） |
| `market_structure.py` 缺 L3 式模块顶 `ponytail:` 块注释 | 信息 | 非阻塞；关键天花板已在 FORBIDDEN / `collect_result_field_names` 标注 |

**对抗搜索声明：** 已对照设计 §3 完整 `MarketAdapter` 接口（index/sector/rule_events 未实现属正确 defer）、契约 boundaries、L3 同族模式；无计划外新增抽象层或第二消费者缺失的 wrapper。

---

## A2 checklist

- [x] `git diff --stat` 已记录 Lxx / net lines（含 untracked 说明）
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象/错误模型交叉引用
- [x] 阻塞 vs 建议已区分
- [x] `## 计划外发现` 已填
