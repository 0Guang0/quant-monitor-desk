# A1 audit-spec — 规范与 Trace Authority

**维度：** A1 · trellis-check + doubt-driven-development  
**任务：** `06-24-round3-022-layer4-market`  
**审计范围：** `backend/app/layer4_markets/**`、`tests/test_layer4_market_structure.py`  
**权威对照：** `layer4_market_contract.yaml`、`layer4_market_structure.md`、任务卡 `022`、playbook §8.2、MASTER §2/§3.2  
**判定：** PASS  
**OPEN：** 3（NON-BLOCKING） · **BLOCKING：** 0

---

## §3.1 检查项

| 检查项                        | 结果               | 证据                                                                                                                                                                          |
| ----------------------------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| trellis-check 1 变更范围      | PASS               | `git status -sb`：`layer4_markets/{market_structure,models,lineage}.py`、`tests/test_layer4_market_structure.py`、`tests/fixtures/layer4_staged_market/`；未触及 forbidden 包 |
| trellis-check 2 任务工件      | PASS               | `validate-execute-handoff` exit 0                                                                                                                                             |
| trellis-check 3 包上下文      | PASS               | `get_context.py --mode packages` → `Spec layers: backend`                                                                                                                     |
| trellis-check 4 Spec Quality  | PASS（子集）       | contract `models` ↔ `models.py` 字段对齐；`boundaries` ↔ `FORBIDDEN_*` frozenset                                                                                              |
| trellis-check 5 项目检查      | PASS               | `uv run pytest tests/test_layer4_market_structure.py -q` → 7 passed；`ruff check backend/app/layer4_markets tests/test_layer4_market_structure.py` → All checks passed        |
| trellis-check 6 跨层          | PASS               | 仅 `layer4_markets` + `core/snapshot_lineage` 复用；无 layer2/3/5 runtime 侵入；无 import 环                                                                                  |
| trellis-check 7 manifest      | PASS               | `audit.jsonl` 12 条点名文件已读；diff 与 `check.jsonl` 一致                                                                                                                   |
| diff vs audit manifest        | PASS               | 核心交付在 allowed 路径；loop 维护：`check_test_catalog.py`、`test_catalog.yaml`、`project_map.generated.json`（CI 惯例，非 scope 泄漏）                                      |
| Trace Authority / 原始任务卡  | PASS               | `022_implement_layer4_market_structure.md` §1/§15 → MASTER §2 AC-022-1..8；`research/source-index.md` §A 映射完整                                                             |
| Trace Authority / defer 边界  | PASS               | `ADV-R3X` 仅 contract-scoped envelope，无 WriteManager/DB 写回；`R3Y` 三 registry 无 diff；8× live adapter 显式 defer（仅 `CN_A` staged）                                     |
| Trace Authority / staged-only | PASS               | `market_structure.py:284-289` `source_mode != staged_fixture_only` → `Layer4MarketError`；`test_marketAdapter_nonStagedSource_rejects`                                        |
| playbook §8.2 / 契约          | PASS（子集）       | registry/calendar/breadth 模型 + boundaries 对齐；index/sector/rule_event 在 MASTER §3.2 defer                                                                                |
| playbook §8.2 / 边界          | PASS               | 无 `staged_pilot.py`、`mutation_proof.py`、`staged_evidence.py`、registry 三件套变更                                                                                          |
| GitNexus                      | PASS（stale 已记） | `query("MarketStructureBuilder layer4 market staged breadth")` 未收录新符号；`research/gitnexus-execute-summary.md` LOW risk；仅 tests 调用                                   |
| DOUBT / Red Flags             | PASS               | 无 live fetch、无 Agent 源、无交易动作语义；ResourceGuard 未触发                                                                                                              |

---

## Trace Authority 逐步核对

| 条目                      | 核对问题                         | 结果 | 证据                                                  |
| ------------------------- | -------------------------------- | ---- | ----------------------------------------------------- |
| 原始任务卡                | scope/AC/Red Flags 进入 MASTER？ | PASS | `source-index.md` §A + MASTER §2                      |
| task README / input index | Plan 入口合规？                  | PASS | `source-index.md` §B–§C 六类覆盖 [x]                  |
| unresolved coverage       | defer 有 registry 行？           | PASS | MASTER §3.2 `ADV-R3X` / `R3Y` 显式登记                |
| round map                 | batch 与 MASTER §4 一致？        | PASS | Batch 5 / Wave C / `feature/round3-022-layer4-market` |
| source-index              | manifest 血缘完整？              | PASS | `audit.jsonl` ↔ `implement.jsonl` 对齐                |
| omission-check            | 地图倒查无遗漏？                 | PASS | `integration-ledger.md` pointer 与 MASTER §6 一致     |
| integration-ledger        | context packing 一致？           | PASS | contract / wiring / staged gate 条目齐全              |

---

## 契约逐项对照（`layer4_market_contract.yaml`）

### models

| 模型                      | 实现                                             | 判定  | 备注                                                                               |
| ------------------------- | ------------------------------------------------ | ----- | ---------------------------------------------------------------------------------- |
| `market_registry`         | `MarketRegistryRow` + `REGISTRY_SEEDS`（8 行）   | PASS  | PK `market_id`；`test_marketRegistry_uniqueMarketIds`                              |
| `market_calendar`         | `MarketCalendarRow` + `_load_calendar_rows`      | PASS  | PK 去重 `market_structure.py:325-329`；额外 `as_of_timestamp` 用于 no-future guard |
| `market_breadth_snapshot` | `MarketBreadthSnapshotRow` + `_load_breadth_row` | PASS  | `_BREADTH_REQUIRED` 与 contract `required_fields` 一致                             |
| `market_index_snapshot`   | 未实现                                           | DEFER | MASTER §3.2 staged skeleton；非 scope 泄漏                                         |
| `market_sector_snapshot`  | 未实现                                           | DEFER | 同上                                                                               |

### quality_rules

| 规则                                                        | 实现                            | 判定    | 证据                                                                                                        |
| ----------------------------------------------------------- | ------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------- |
| `no_snapshot_on_non_trading_day_unless_session_type_allows` | 非交易日硬拒绝                  | PARTIAL | `market_structure.py:197-200` 拒绝 `is_trading_day=false`；**未**实现 `session_type` 例外路径 → 见计划外 #1 |
| `no_negative_volume_amount`                                 | 未校验                          | GAP     | `total_amount` 可为负；无 builder 守卫 → 见计划外 #2                                                        |
| `high_must_be_greater_or_equal_low`                         | N/A                             | SKIP    | breadth 模型无 high/low；index 模型 defer                                                                   |
| `source_and_quality_flag_required`                          | breadth 强制；calendar 允许空串 | PARTIAL | `_load_breadth_row:363-366` vs `_load_calendar_rows:338-339` 默认 `""` → 见计划外 #3                        |

### boundaries

| 边界                                                | 实现                                           | 判定 | 证据                                        |
| --------------------------------------------------- | ---------------------------------------------- | ---- | ------------------------------------------- |
| `does_not_store_layer5_full_history`                | `FORBIDDEN_LAYER5_HISTORY_FIELDS` + 字段名扫描 | PASS | `test_marketSnapshot_noLayer5HistoryFields` |
| `does_not_materialize_layer1_standardization_suite` | `FORBIDDEN_LAYER1_STANDARDIZATION_FIELDS`      | PASS | 同上                                        |

---

## playbook §8.2 契约行核对

| 子条件                                 | 结果                                  | 证据                                                             |
| -------------------------------------- | ------------------------------------- | ---------------------------------------------------------------- |
| 行为符合 `layer4_market_contract.yaml` | PASS（子集 + quality_rules 缺口见上） | models + boundaries 对齐；quality_rules 三处 PARTIAL/GAP         |
| 不复制 Layer5 大表                     | PASS                                  | boundaries + forbidden field scan                                |
| 不输出交易动作语义                     | PASS                                  | 无 buy/sell/信号字段；lineage `validate_source_dataset_ids` 复用 |

---

## GitNexus

- **query：** `MarketStructureBuilder layer4 market staged breadth` — 索引未收录新符号（greenfield）；返回流程均为 L2/L3 既有模式，无 L4 生产调用链。
- **impact（Execute 预检）：** `MarketStructureBuilder` / `Layer4LineageBuilder` LOW risk，仅 tests。
- **DOUBT：** 索引 stale 不阻断；实现隔离在 `layer4_markets/` + pytest。

---

## 计划外发现

> 对抗性搜索：对照设计文档边界 + 契约 `quality_rules` + 任务卡 Red Flags + MASTER 未索引边界。已执行 `rg` quality_rules/session_type/negative、测试名倒查、git diff forbidden 路径。

| ID  | 严重度       | 发现                                                                      | 证据                                                                                                                                  | 建议归属                                                    |
| --- | ------------ | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| P1  | NON-BLOCKING | `session_type` 允许的非交易日例外路径未实现；当前一律拒绝非交易日         | `market_structure.py:197-200`；contract `no_snapshot_on_non_trading_day_unless_session_type_allows`；`layer4_market_structure.md` §10 | Repair 或登记 defer；A8 补 mutation 测                      |
| P2  | NON-BLOCKING | `no_negative_volume_amount` 未在 builder 强制                             | contract `quality_rules`；`_load_breadth_row` 仅 `int/float` 转换                                                                     | A8 补负值 mutation；或 defer 至 DataQualityValidator 全链路 |
| P3  | NON-BLOCKING | calendar `source`/`quality_flag` 可为空串，breadth 有 required 校验不对称 | `_load_calendar_rows:338-339` vs `_load_breadth_row:363-366`                                                                          | A4/A8 对齐 `source_and_quality_flag_required`               |

**显式非发现（已对抗确认）：**

- `market_index_snapshot` / `market_sector_snapshot` / `market_rule_event` — MASTER §3.2 defer，非越界。
- 仅 `CN_A` staged adapter — MASTER §3.2「registry 种子 + 1× staged adapter」。
- WriteManager DB 写 — MASTER §3.2 defer；builder 无 DB I/O。
- `snapshot_lineage_contract.yaml` — 未修改（§3.3 forbidden）。

---

## 汇总

| 类别                | 数量                                                     |
| ------------------- | -------------------------------------------------------- |
| BLOCKING            | **0**                                                    |
| OPEN (NON-BLOCKING) | **3**                                                    |
| PASS 检查项         | trellis-check 1–7、Trace Authority 全行、boundaries 全行 |

**A1 结论：** staged-only 骨架在 **scope / defer / boundaries / lineage / staged gate** 维度合规；契约 **models** 与 **boundaries** 对齐；**quality_rules** 有三处未完全接线，属 NON-BLOCKING，建议在 Repair 或 A8 闭环。整体 **PASS**。
