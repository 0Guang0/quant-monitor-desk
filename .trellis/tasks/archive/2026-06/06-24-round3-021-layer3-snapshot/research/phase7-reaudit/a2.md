# A2 audit-ponytail — Phase 7 复审计

**维度：** A2 · ponytail-review + doubt-driven-development  
**模型：** composer-2.5  
**分支：** `feature/round3-021-layer3-snapshot`  
**审查范围：** `snapshot_builder.py`、`lineage.py`、`models.py`（仅只读，未改码、未 commit）  
**判定：** **PASS**（无阻塞候选）

---

## 启动记录

### git diff --stat（相对 `master`）

```
backend/app/layer3_chains/models.py | 54 +++++++++++++++++++++++++++++++++++++
1 file changed, 54 insertions(+)
```

**未跟踪（本任务核心实现，计入审查）：**

| 文件                                            | 行数（非空） |
| ----------------------------------------------- | ------------ |
| `backend/app/layer3_chains/snapshot_builder.py` | 214          |
| `backend/app/layer3_chains/lineage.py`          | 59           |
| `tests/fixtures/layer3_l5_staged_bars/`         | fixture      |

**净增估算（三文件 + models）：** ~327 行生产代码；`models.py` +54 为 MASTER §8.1 explicit AC。

### MASTER §8 触及文件

| 步骤                      | 文件                  |
| ------------------------- | --------------------- |
| §8.1 Models               | `models.py`           |
| §8.2 Lineage builder      | `lineage.py`          |
| §8.3–8.5 Snapshot builder | `snapshot_builder.py` |

---

## ponytail 梯级评估

| 文件 / 符号                                         | 评估                                                                                               | 最深梯级                |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------- |
| `Layer3LineageBuilder`                              | 复用 `core/snapshot_lineage` kernel；与 `Layer2LineageBuilder` 同构（`layer2_sensors/lineage.py`） | **2 复用**              |
| `IndustryChainSnapshotBuilder`                      | 对齐 L2 `CrossAssetSnapshotBuilder` 的 builder + 内嵌 lineage 模式                                 | **2 复用**              |
| `snapshot_builder.py` 模块级 `_read_l5_manifest` 等 | 注释已标 `ponytail:` 天花板；风格对齐 `loader.py` 的 `_safe_yaml_load` + manifest 校验             | **2 复用 + 6 最小新码** |
| `Layer3LineageEnvelope` 等 dataclass                | MASTER AC-021-1/2/5 契约字段；非 speculative 抽象                                                  | **explicit AC**         |
| `yaml` 依赖                                         | `loader.py` 同包已用，非新依赖                                                                     | **4 已装依赖**          |
| `_parse_ts`                                         | 7 行 Z 后缀处理；仓库无共享 `parse_iso_ts` util                                                    | **6 最小新码**          |

---

## ponytail-review 逐条（仅过度工程）

`snapshot_builder.py:135-142: shrink: yaml safe_load + YAMLError 与 loader._safe_yaml_load 重复。若需收敛，从 loader 抽 package 级 helper（改错误类型参数），非新建抽象层；净减约 4 行，未达 ≥20。`

`snapshot_builder.py:219-248: shrink: _lineage_for_anchor 为模块函数并传入 builder。可改为 IndustryChainSnapshotBuilder 实例方法，省掉 builder 形参；组织性改动，净减 <5 行。`

`snapshot_builder.py:207-216: shrink: _event_only_snapshot 单处语义包装。可内联 IndustryChainDailySnapshotRow(...) 于 build 循环；净减约 8 行。`

`lineage.py:19-69: yagni? 仅 snapshot_builder + 单测直接调用。对照 L2 独立 lineage 模块为层间惯例，非单点 wrapper；保留。`

`models.py:94-111: yagni? Layer3LineageEnvelope 字段与 L2 同构。按层分类型是契约边界，非可删抽象。`

**net: -15 lines possible**（激进内联小 helper；不建议为省行破坏 L2 镜像可读性）

---

## §3.2 候选删改表（≥20 行门槛）

| 候选删改（file:line）                                                        | ponytail 梯级                                              | 是否阻塞                            |
| ---------------------------------------------------------------------------- | ---------------------------------------------------------- | ----------------------------------- |
| `snapshot_builder.py:135-186` manifest/bar 解析 helper 簇（~52 行）          | 梯级 2 — 与 `loader.py` 同模式；抽共享 factory 反而增文件  | **建议**（不阻塞；维持现状更 lazy） |
| `lineage.py:1-70` 独立 lineage 文件 vs 内联 builder                          | 梯级 2 — L2 已有 `layer2_sensors/lineage.py` 先例          | **不阻塞**                          |
| `snapshot_builder.py:34-132` IndustryChainSnapshotBuilder 类 vs 单函数 build | 梯级 2 — L2 `CrossAssetSnapshotBuilder` 同构；拆类增 DI 面 | **不阻塞**                          |
| `models.py:68-118` 四个新 dataclass                                          | explicit AC（§8.1）；含模块 doc 前向可选字段               | **不算候选**                        |

**阻塞候选：0**

---

## 与 A4 交叉引用

初审计 A4（`research/audit-evidence/a4.md`）结论：**无重复错误处理样板**；`Layer3LineageBuilder` 与 L2 同构，**非过度抽象**。

本复审计一致：

- `Layer3SnapshotError` / `Layer3LineageError` 分层与 L2 的 `CrossAssetObservationError` / `Layer2LineageError` 同模式，非 A4 所称「重复样板」意义上的 bloat。
- `build()` 内 fail-fast（缺 bar、future bar、非 staged manifest）为语义消息，非可删日志层。

---

## DOUBT（≥20 行可简化？）

**CLAIM：** 021 三文件实现已是 staged-only 最小路径，不存在「单块净减 ≥20 行且不改 AC/测试目的」的删改。

**搜索范围：**

1. `snapshot_builder.py`、`lineage.py`、`models.py` 全文
2. 对照 `backend/app/layer3_chains/loader.py`（manifest/yaml 模式）
3. 对照 `backend/app/layer2_sensors/{lineage,snapshot_builder}.py`（层间惯例）
4. 对照 `backend/app/core/snapshot_lineage.py`（lineage kernel）
5. `rg`：`fromisoformat`、`safe_yaml`、`_parse_ts` 于 `backend/`

**结论：** 未找到满足 ≥20 行门槛的必删块。最大可删簇为 manifest helper（~52 行），但属于梯级 2 有意镜像 loader，删则需新共享模块或削弱错误语义，**净复杂度上升**。

**Cross-model：** 非交互子 agent 上下文 — cross-model 跳过（doubt-driven 非交互规则）。

---

## A2 checklist

- [x] `git diff --stat` 已记录（tracked + 未跟踪行数）
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象交叉引用
- [x] 阻塞 vs 建议已区分

---

## 总判定

| 项                    | 值                                                                                                         |
| --------------------- | ---------------------------------------------------------------------------------------------------------- |
| **判定**              | **PASS**                                                                                                   |
| **BLOCKING**          | **0**                                                                                                      |
| **NON-BLOCKING 建议** | 维持现状；若未来 L3 再增第二个 yaml 读者，再考虑把 `_safe_yaml_load` 升为 package 级（一次抽取，多处受益） |

**Lean already. Ship.**（相对 ponytail 维度；正确性/安全由 A3/A4 覆盖。）
