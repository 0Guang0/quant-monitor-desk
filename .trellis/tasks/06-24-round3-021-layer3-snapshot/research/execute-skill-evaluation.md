# Execute skill evaluation — 021 Layer3 snapshot builder

Skills recorded in `execute-skill-reads.jsonl`:

- trellis-execute: Phase 0 boot + §8 RED/GREEN protocol
- test-driven-development: per-step RED FAIL → GREEN PASS with evidence files
- incremental-implementation: models → lineage → builder → as_of → event_only → gates
- karpathy-guidelines: staged-only minimal builder; no DB/live path
- testing-guidelines: semantic assertions + Chinese 覆盖/对象/目的 comments; LINEAGE_REQUIRED_FIELDS loop
- ponytail: L2/core reuse; `_build()` test helper only; ponytail self-check before §8.6
- gitnexus-impact: pre-edit query (new symbol; no upstream callers)
