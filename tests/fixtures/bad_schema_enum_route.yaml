version: "1"
updated_at: "2026-06-26T00:00:00Z"

sources:
  baostock:
    source_name: baostock
    source_type: official_api
    allowed_domains:
      - cn_equity_daily_bar
    trust_level: 2
    license_type: vendor_proprietary_not_in_schema
    official_api: true
    is_enabled: true
    default_priority: 20
    rate_limit_policy: conservative
    auth_required: false
    requires_local_client: false
    expected_frequency: daily
    expected_lag: days
    timezone: Asia/Shanghai
    fallback_allowed: true
    validation_only: false
    notes: route planner schema enum gate fixture

domain_roles:
  cn_equity_daily_bar:
    primary: baostock
    validation: null
    fallback_policy: []
    domain_enabled_by_default: true
