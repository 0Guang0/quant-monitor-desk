# A2 Ponytail 审计证据 — R3FR-05 Provider Catalog

**审计维度：** A2 过度工程 / bloat / 可删路径  
**分支：** `feature/round3fr-provider-catalog` @ `ac924819`  
**diff 范围：** `ab6d97de..ac924819`（R3FR-05 实现文件；不含 `.trellis/` 任务元数据）  
**审计时间：** 2026-06-27  
**审计员：** audit-ponytail (A2，只读)

---

## 总结

| 指标                      | 值                                                               |
| ------------------------- | ---------------------------------------------------------------- |
| diff stat（R3FR-05 实现） | 12 files，约 +1002 / −225 net（含 `test_catalog.yaml` 机械重排） |
| ≥20 行删改候选            | 0 BLOCKING，2 NON-BLOCKING 建议                                  |
| ponytail 最深命中         | 梯级 2（复用既有 path-guard 模式，但未抽共享 helper）            |

**结论：PASS**

实现体量与冻结卡 §4–§5、design §1–§2、EXECUTION_INDEX §2 具名 AC 对齐；无计划外 wrapper/factory、无 `DataSourceService.fetch` 热路径接线。`provider_catalog.yaml` 与 `test_provider_catalog.py` 行数大但属显式 AC 交付，不计入 bloat 候选。

---

## 1. `git diff --stat`（R3FR-05 实现文件）

```
 backend/app/datasources/provider_catalog.py        |  43 +
 backend/app/datasources/service.py                 |   1 +
 specs/contracts/datasource_service_contract.yaml   |   1 +
 specs/contracts/reference_adoption_guardrails.yaml |   1 +
 specs/contracts/source_capability_contract.yaml    |   1 +
 specs/datasource_registry/provider_catalog.yaml    | 543 ++++++++++++++++++++
 specs/datasource_registry/source_capabilities.yaml |  12 +
 specs/datasource_registry/source_registry.yaml     |  37 ++
 tests/test_catalog.yaml                            | 329 +++++++++++---------
 tests/test_provider_catalog.py                     | 332 ++++++++++++++++
 tests/test_r3x_data_source_routing_blockers.py     |   1 +
 tests/test_reference_adoption_guardrails.py        |  41 ++
 docs/generated/docs_specs_index.generated.md       |   1 +
```

| 文件                       | 净增     | 备注                                                           |
| -------------------------- | -------- | -------------------------------------------------------------- |
| `provider_catalog.yaml`    | +543     | 25 provider × 17 必填字段（冻结卡 §4）                         |
| `test_provider_catalog.py` | +332     | EXECUTION_INDEX §2 具名 AC 测试                                |
| `test_catalog.yaml`        | +104 net | `loop_maintain --fix` YAML 折行/缩进 + 登记新模块（design §2） |
| `provider_catalog.py`      | +43      | design §3 最小 loader                                          |
| 其余                       | ≤41      | registry/capability/contract/guardrails 补丁                   |

---

## 2. ponytail 梯级评估（实现面）

| 工件                                | 梯级 | 判定                                                |
| ----------------------------------- | ---- | --------------------------------------------------- |
| `provider_catalog.py`               | 2→6  | 43 行只读 loader；未接 fetch 热路径（冻结卡 §7 #1） |
| `provider_for_source()`             | 2    | design §3 明确要求；非单处无意义 wrapper            |
| `provider_catalog.yaml`             | —    | AC 必填 SSOT；排除 ≥20 规则                         |
| `test_provider_catalog.py`          | —    | 每条对应 plan-adversarial P1-02/07/10 等具名 AC     |
| `test_r3fr05ProviderCatalogClosure` | —    | P1-04 必做 guardrails closure                       |
| `service.py` +1 domain              | 1    | 仅 `provider_metadata_only` 描述映射，无 adapter    |
| `test_catalog.yaml` 重排            | —    | 机械格式化，非业务逻辑膨胀                          |

---

## 3. §3.2 候选删改表

| 候选删改（file:line）                                                                                                                                           | ponytail 梯级                                                                           | 是否阻塞                             |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------ |
| `provider_catalog.py:20-24` 路径 guard 与 `source_registry.py:119-125` `_resolve_registry_path` 逻辑重复                                                        | 梯级 2（可复用既有模式；抽共享 helper 需第二消费者，当前 YAGNI）                        | **建议**（NON-BLOCKING）             |
| `test_reference_adoption_guardrails.py:540-567` 与 `test_provider_catalog.py:94-104,195-203` 部分断言重叠（25 providers、openbb `runtime_source_copy_allowed`） | 梯级 1（guardrails closure 为 P1-04 必做；不宜删一侧）                                  | **建议**（NON-BLOCKING，保留双登记） |
| `test_provider_catalog.py:51-72` `PROPOSED_EXTERNAL_SOURCE_IDS` 硬编码列表                                                                                      | 梯级 2（与冻结卡 §5 逐字对齐；从 YAML 反推会削弱「registry 源必须在 catalog」独立验证） | 建议保留                             |
| `test_provider_catalog.py:281-303` loader 两测可合并为单测                                                                                                      | 梯级 5                                                                                  | 建议（~15 行，未达 ≥20 阈值）        |

**无 BLOCKING 删改候选。**

---

## 4. 与 A4 交叉引用

- `ProviderCatalogError` + `is_relative_to` guard 与 `InvalidRegistryError` / `_resolve_registry_path` 同型（A4 错误模型一致，非重复样板膨胀）。
- `yaml.safe_load` + 浅层 `providers` list 校验：比 `SourceRegistry.load` 更薄，无过度解析层。
- A3 已记：`load_provider_catalog` 缺 outside-root 负向测试（P3 NON-BLOCKING）；A2 不将其升格为 bloat，属测试缺口非实现膨胀。

---

## 5. DOUBT（≥20 行可简化搜索）

**搜索范围：**

- `backend/app/datasources/provider_catalog.py`（全文 43 行）
- `backend/app/datasources/source_registry.py`（对照 `_resolve_registry_path`）
- `tests/test_provider_catalog.py`（全文 332 行）
- `tests/test_reference_adoption_guardrails.py`（`test_r3fr05ProviderCatalogClosure`）
- `specs/datasource_registry/provider_catalog.yaml`（抽样 5 entries）
- `tests/test_catalog.yaml` diff（确认仅为 loop 格式化）

**结论：** 未找到可在 **不违反冻结卡/EXECUTION_INDEX 具名 AC** 前提下净删 ≥20 行的实现或测试块。最大「噪音」来自 `test_catalog.yaml` 全文件 YAML 重排（+329/−225），属 design §2 `loop_maintain --fix` 要求，非 YAGNI 违规。

---

## 6. 计划外发现

| 发现                            | 严重度       | 说明                                       |
| ------------------------------- | ------------ | ------------------------------------------ |
| 无计划外 fetch/adapter 接线     | —            | 已对抗搜索 `service.py`、capabilities stub |
| 无计划外 OpenBB runtime 抽象    | —            | 仅 metadata domain + catalog entry         |
| `test_catalog.yaml` diff 体积大 | NON-BLOCKING | 机械重排稀释 `--stat`；非 R3FR-05 逻辑     |
| path guard 未抽共享 util        | NON-BLOCKING | 等 R3G 第二 YAML loader 再考虑 DRY         |

已对抗搜索；无计划外 BLOCKING bloat。

---

## 7. Checklist

- [x] `git diff --stat` 已记录 Lxx / net lines
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象交叉引用
- [x] 阻塞 vs 建议已区分

---

## 8. 结论

**A2：PASS**

R3FR-05 diff 以契约 YAML + 具名 AC 测试为主，runtime 仅 43 行只读 loader，符合「contract/catalog only; no live fetch」姿态。存在少量与 `source_registry` / guardrails closure 的结构性重复，均属既定模式或 Plan 必做项，不构成阻塞性过度工程。
