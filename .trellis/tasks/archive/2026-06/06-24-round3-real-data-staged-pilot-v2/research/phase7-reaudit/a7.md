# A7 audit-ops — Phase 7 复审计（B-19 staged pilot v2）

> **维度：** A7 · database-administrator + sre-engineer（adjunct）  
> **模型：** composer-2.5  
> **工作区：** `quant-monitor-desk-wt-r3-pilot-v2`  
> **提交：** `fb044931` — `feat(ops): close B-19 staged pilot v2 Execute+Audit`  
> **任务：** `06-24-round3-real-data-staged-pilot-v2`（R3Y 任务卡）  
> **日期：** 2026-06-24  
> **模式：** Audit（只读，无 commit、无业务代码修改）

---

## 总判定

| 项                   | 值                                                                                                        |
| -------------------- | --------------------------------------------------------------------------------------------------------- |
| **判定**             | **PASS**                                                                                                  |
| **BLOCKING**         | 0                                                                                                         |
| **AUDIT.plan §2 A7** | cli-sandbox：无 migration / 无 prod clean write / sandbox 无 `data/` 污染 — **满足**                      |
| **audit-prod-path**  | 本 worktree **无** `data/duckdb/quant_monitor.duckdb` → hash 前后对比 **N/A**（fail-closed 已由单测覆盖） |

---

## 1. 审计范围与权威

| 来源                                              | 用途                                          |
| ------------------------------------------------- | --------------------------------------------- |
| `agents/audit-adversarial-authority.md`           | 对抗性权威层级；须含计划外发现                |
| `agents/database-administrator.md`                | DuckDB / migration / mutation proof checklist |
| `R3Y_real_data_staged_pilot_v2.md` §3 目标 8、§11 | production no-mutation proof v2；staged-only  |
| `AUDIT.plan.md` §2 A7、§3 AUD-04 / AC-MUT-001     | 冻结验证矩阵                                  |
| `backend/app/ops/mutation_proof.py`               | R3Y-MUT-PROOF-001 语义 SSOT                   |

**对抗焦点（相对 MASTER §8）：** mutation proof 在 **prod DB 存在 / 缺失** 两种情形下是否 fail-closed；DuckDB hash 与行数是否被旁路标为 VERIFIED；sandbox `writer()` 是否可能误写 `DEFAULT_PRODUCTION_DB`。

---

## 2. migration / schema（database-administrator）

| 步骤              | 命令 / 方法                                                                        | exit | 关键证据                 | 结果                    |
| ----------------- | ---------------------------------------------------------------------------------- | ---- | ------------------------ | ----------------------- |
| schema diff       | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/`              | 0    | 空 diff                  | **无 migration 变更**   |
| 业务 diff 范围    | `git diff master --name-only`（mutation_proof / staged_pilot / test_staged_pilot） | 0    | 仅 ops + 测试 + 任务工件 | **未改 db 包 / schema** |
| init_db 两遍      | 本分支无 schema 变更                                                               | —    | —                        | **N/A**                 |
| kill migrate 恢复 | 无 migrate 执行路径于 diff                                                         | —    | —                        | **N/A**                 |

**说明：** sandbox 内 `_ensure_sandbox_db` 会在 **sandbox_root** 下对空库调用 `apply_migrations` + `cm.writer()`（`staged_pilot.py:482-493`），对象为 `sandbox_root/data/duckdb/quant_monitor.duckdb`，**非** `DEFAULT_PRODUCTION_DB`（`data/duckdb/quant_monitor.duckdb`）。

---

## 3. mutation proof / DuckDB hash（核心）

### 3.1 `mutation_proof.py` 语义（fb04493）

| 字段 / 行为         | 证据                                                                                  | 评价                                                                     |
| ------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `proof_status` 三态 | `VERIFIED` / `INCONCLUSIVE` / `MUTATION_DETECTED`                                     | 符合 R3Y-MUT-PROOF-001                                                   |
| VERIFIED 条件       | `_derive_proof_status`：hash 不变 **且** KEY 表 count 不变 **且** **全表** count 不变 | 强于仅 KEY 表                                                            |
| DB 缺失             | `reason: production_db_file_missing`；hash/count 字段为 `None`                        | ADV-POST14-A-001 fail-closed                                             |
| hash 比较           | `before_bytes == after_bytes`（全文件字节）                                           | 有效；与 backup manifest 的 `duckdb_hash`（SHA256 字符串）**不同契约面** |

### 3.2 运行时证据（Execute）

| 工件                                                  | 内容                                                          | 解读                                 |
| ----------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------ |
| `execute-evidence/no_mutation_proof_v2.md`            | `proof_status: INCONCLUSIVE`；hash/count 均为 `None`          | 与 worktree **无 prod DB** 一致      |
| `execute-evidence/pilot_v2_closeout.json`             | `closeout_pass: false`；`mutation_proof_status: INCONCLUSIVE` | closeout **未** 误报 production-live |
| `execute-evidence/production_db_no_mutation_proof.md` | 同上 INCONCLUSIVE                                             | v1/v2 叙事一致                       |

### 3.3 Phase 7 独立复验（wt-r3-pilot-v2 @ fb04493）

| 步骤                | 命令                                                                                              | exit  | 输出摘要                                 |
| ------------------- | ------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------- |
| prod DB 存在性      | `Test-Path data/duckdb/quant_monitor.duckdb`                                                      | —     | **MISSING**                              |
| sandbox pytest 全量 | `uv run pytest tests/test_staged_pilot.py -q --basetemp=.trellis/tasks/.../.audit-sandbox/pytest` | **0** | **40 passed**                            |
| mutation 子集       | `uv run pytest tests/test_staged_pilot.py -q -k mutation`                                         | **0** | **6 passed**                             |
| policy 回归         | `uv run pytest tests/test_production_live_pilot_policy.py -q`                                     | **0** | **9 passed**                             |
| prod hash 前后      | certutil SHA256                                                                                   | —     | **N/A**（文件不存在，pytest 后仍不存在） |

### 3.4 mutation 单测覆盖（对抗 AUDIT.plan A8 边界）

| 用例                                                                 | 验证点                                                  |
| -------------------------------------------------------------------- | ------------------------------------------------------- |
| `test_stagedPilot_mutationProof_inconclusiveWhenProductionDbMissing` | 缺失 → INCONCLUSIVE，不得 VERIFIED                      |
| `test_mutationProof_verifiedOnlyWhenHashAndCountsUnchanged`          | 仅 hash∧counts 全不变 → VERIFIED                        |
| `test_mutationProof_mutationDetectedWhenKeyTableRowsChange`          | KEY 表行变 → MUTATION_DETECTED                          |
| `test_mutationProof_mutationDetectedWhenNonKeyTableRowCountChanges`  | 非 KEY 表 count 变 → MUTATION_DETECTED                  |
| `test_mutationProof_inconclusiveWhenHashChangesKeyCountUnchanged`    | hash 变、KEY count 不变 → INCONCLUSIVE（不得 VERIFIED） |
| `test_stagedPilotV2_closeoutRequiresHashAndRowCountsUnchanged`       | closeout gate 要求双 true + VERIFIED                    |

**R3Y 目标 8 结论：** 在 **prod DB 不存在** 情形下，proof **不可** 标 VERIFIED（INCONCLUSIVE + closeout_pass false）— **可信**。在 **prod DB 存在** 情形下，本 worktree **未做** 端到端 hash 抽检；语义由单测 + `build_production_mutation_proof` 保证，**不构成 BLOCKING**（见 `research/audit-prod-path-na.md`）。

---

## 4. 生产 `data/` 污染 / sandbox 隔离（sre-engineer）

| 检查项                   | 证据                                                        | 结果                                        |
| ------------------------ | ----------------------------------------------------------- | ------------------------------------------- |
| `allow_clean_write` 默认 | `staged_pilot` 多处硬编码 `False`；请求为 true 时抛错       | fail-closed                                 |
| live fetch 写库目标      | `cm` 绑定 sandbox DB；mutation 对比 `DEFAULT_PRODUCTION_DB` | prod 与 sandbox 路径分离                    |
| 仓库根 `data/duckdb/`    | worktree 无 prod 文件；pytest 后仍缺失                      | **无 prod 写**                              |
| AUDIT_PROD_ROOT          | `.audit-sandbox/r3y-pilot-v2-audit-prod/`                   | 本复审计未复制 prod 树（与首轮 AR-04 一致） |
| sandbox basetemp         | `.trellis/tasks/.../.audit-sandbox/pytest`                  | 仅 pytest 临时目录                          |

---

## 5. DOUBT（doubt-driven-development）

| 疑点                                           | 结论                                                                                   |
| ---------------------------------------------- | -------------------------------------------------------------------------------------- |
| 第二次 init/migrate 是否静默损坏？             | **N/A** — 无 prod schema 变更；sandbox migrate 仅于空 sandbox 库                       |
| kill 后 migration 表不一致？                   | **N/A** — 未改 prod migration 路径                                                     |
| INCONCLUSIVE 是否被 closeout 当成「通过」？    | **否** — `closeout_pass` 要求 `proof_status == "VERIFIED"` 且 hash/count 双 true       |
| 仅 KEY 表 count 不变但非 KEY 变能否 VERIFIED？ | **否** — `all_table_row_counts` + `MUTATION_DETECTED` 单测                             |
| hash 漂移但 count 不变能否 VERIFIED？          | **否** — `INCONCLUSIVE` 单测                                                           |
| Execute 8.8-green 仅 4 条用例是否代表全量？    | **否** — Phase 7 独立复跑 **40** 条全绿；8.8 为切片步进证据（AR-02 已记 NON-BLOCKING） |

---

## 6. §3.7 证据表（DBA + SRE 合并）

| 场景 / 步骤         | 命令                                                                  | exit | 日志 / evidence                |
| ------------------- | --------------------------------------------------------------------- | ---- | ------------------------------ |
| 无 migration        | `git diff master -- specs/schema/ scripts/init_db.py backend/app/db/` | 0    | 空 diff                        |
| prod DB             | `Test-Path data/duckdb/quant_monitor.duckdb`                          | —    | MISSING                        |
| sandbox 全量 pytest | `uv run pytest tests/test_staged_pilot.py -q --basetemp=...`          | 0    | 40 passed                      |
| mutation 子集       | `uv run pytest ... -k mutation`                                       | 0    | 6 passed                       |
| policy              | `uv run pytest tests/test_production_live_pilot_policy.py -q`         | 0    | 9 passed                       |
| Execute proof 工件  | `no_mutation_proof_v2.md`                                             | —    | INCONCLUSIVE（与缺失 DB 一致） |

---

## 7. 结论

B-19 @ `fb044931` 在 A7 运维面 **PASS**：

1. **无** prod migration / schema 变更；
2. **无** production clean write 默认路径；sandbox 写限于 `sandbox_root`；
3. **mutation_proof** 三态语义与 R3Y-MUT-PROOF-001 / AUD-08 对齐，单测覆盖 prod 缺失、KEY/非 KEY 变异、hash-only 漂移；
4. 本 worktree **无 prod DuckDB**，hash 审计为 N/A，但 fail-closed 行为与 Execute 工件一致；
5. Phase 7 独立复跑 **40 + 6 + 9** pytest，exit 0，未创建仓库根 `data/duckdb/`。

**无 BLOCKING。** 计划外项见下表。

---

## 计划外发现

> 已按 `audit-adversarial-authority.md` 对抗搜索：设计数据路径、mutation 旁路、sandbox writer 误指向、backup `duckdb_hash` 契约、Execute 证据弱断言。

| ID       | 等级             | 发现                                                                                                                                                                       | 证据                                                                | 建议                                                                 |
| -------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------- |
| A7-P7-01 | **NON-BLOCKING** | `build_pilot_v2_closeout` 将 `db_hash_unchanged is True` 映射为布尔；DB 缺失时 proof 为 `None`，closeout JSON 写成 **`false`** 而非 `null`，读者可能误解为「检测到变异」   | `pilot_v2_closeout.json`；`staged_pilot.py:1994-2006`               | closeout 序列化保留三态（`true`/`false`/`null`）或附 `reason`        |
| A7-P7-02 | **NON-BLOCKING** | `no_mutation_proof_v2.md` 仅列 status 三字段，**无** before/after `key_table_counts` / SHA256 摘要；AUDIT.plan A5 要求「hash/count 明细非仅 VERIFIED」在 markdown 层仍偏弱 | `write_no_mutation_proof_v2`                                        | markdown 追加 counts 表或 SHA256（与 `backup_and_recovery.md` 对齐） |
| A7-P7-03 | **NON-BLOCKING** | 代码用 **全文件字节** 相等判 hash；backup manifest 契约字段名为 `duckdb_hash`（SHA256 十六进制）— 两套证据 **未互证**                                                      | `mutation_proof.py:116-121` vs `docs/ops/backup_and_recovery.md` §2 | 文档注明差异，或 proof payload 增加 `sha256` 字段                    |
| A7-P7-04 | **NON-BLOCKING** | 本 worktree **从未** 在 prod DB **存在** 时跑过端到端 pilot v2 + hash 抽检；R3Y 目标 8「两种情形」仅 **缺失情形** 有运行时证据                                             | `audit-prod-path-na.md`；本次 `PROD_DB=MISSING`                     | 合并前于 `AUDIT_PROD_ROOT` 或协调者机器补一次 VERIFIED 抽检（可选）  |
| A7-P7-05 | **NON-BLOCKING** | `_ensure_sandbox_db` 对 sandbox 空库 `apply_migrations`；AUDIT.plan 未列「sandbox 双遍 migrate 幂等」— 若 sandbox 半写损坏，仅影响 pilot 重跑，不触及 prod                 | `staged_pilot.py:482-493`                                           | 未来 pilot 切片可加 sandbox 双遍 smoke（低优先级）                   |

**BLOCKING：无。**

---

_审计员：A7 database-administrator 模板 · Phase 7 复审计 · B-19 · 2026-06-24_
