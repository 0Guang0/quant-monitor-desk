# A2 — audit-ponytail（R3G-03）

**Repo:** quant-monitor-desk
**Task:** `.trellis/tasks/06-27-round3g-limited-production-write`
**Branch:** `feature/round3g-limited-production-write`
**判定：** PASS_WITH_FIXES · **P0: 0 · P1: 0 · P2: 3 · P3: 5**

## Diff 统计

| 范围                   | 文件                                                                                              | 净增行    |
| ---------------------- | ------------------------------------------------------------------------------------------------- | --------- |
| 已跟踪 diff            | `data_commands.py`, `main.py`, 3× tests                                                           | +715      |
| 未跟踪（Execute 核心） | `approval_contract.py` (~270L), `rollback_plan.py` (~118L), `limited_production_entry.py` (~496L) | ~884      |
| **合计**               | 6 tracked + 3 untracked                                                                           | **~1.6k** |

`git diff --stat HEAD` 不含三份新 ops 模块（仍 untracked）。

---

## §3.2 候选删改表

| 候选删改（file:line）                                                                 | ponytail 梯级              | 优先级 | 是否阻塞 |
| ------------------------------------------------------------------------------------- | -------------------------- | ------ | -------- |
| `_production_clean_write` 镜像 `_sandbox_clean_write` 无共享提取、无 `ponytail:` 注释 | 2（复用 rehearsal_runner） | P2     | 建议     |
| 从 `rehearsal_runner` 导入 6 个 `_` 私有符号作门禁链                                  | 2（应 public compose）     | P2     | 建议     |
| `validate_r3g03_source_caps` 与 `rehearsal_plan.validate_source_caps` 平行 ~35 行     | 2（参数化 cap profile）    | P2     | 建议     |
| `_resolve_path` 四文件重复                                                            | 2                          | P3     | 否       |
| `_utc_now_iso` 三文件重复                                                             | 2                          | P3     | 否       |
| `_load_contract_caps` 与 rehearsal_plan 重复                                          | 2                          | P3     | 否       |
| CLI `sandbox_clean_write_promote` 与 runner 双重文件存在性检查                        | 2                          | P3     | 否       |
| `PromoteRequest.allow_live_fetch` / `fred_authorization` 未在 runner 消费             | 1（YAGNI）                 | P3     | 否       |
| `build_before_proof` 未薄包装 `key_table_row_counts`（frozen §4.1 点名复用）          | 2                          | P3     | 否       |

---

## P0 — BLOCKING

**无。**

---

## P1 — HIGH

**无。** 无第三套全库扫描 helper、无无消费者 factory/wrapper 层。

---

## P2 — MEDIUM

### A2-P2-01 — 门禁链镜像缺 Execute SSOT 要求的 ponytail 天花板

- **Location:** `limited_production_entry.py:27-34,320-371,427-429`
- **Evidence:** frozen §4.1 要求「extract shared compose from rehearsal_runner **or** minimal mirror + `ponytail:` ceiling comment」。实现选择镜像 `_sandbox_clean_write` → `_production_clean_write`，但 **全文件零 `ponytail:` 注释**。
- **Impact:** 第三套写编排器（rehearsal_runner / staged_pilot / limited_production_entry）双维护；R3G-04+ 改 gate 链时 promote 易漂移。
- **Fix:** 提取 `compose_clean_write_gates()` 或在 `_production_clean_write` / import 块标 `ponytail: mirrored from rehearsal_runner; dual-maint ceiling`。

### A2-P2-02 — 私有符号跨模块 import（脆弱耦合）

- **Location:** `limited_production_entry.py:27-34` imports `_assert_data_health_admission`, `_coverage_ratio`, `_preview_route`, `_run_source_data_health`, `_validation_status_from_dh`
- **Evidence:** GitNexus 索引未收录新符号；直接依赖 rehearsal_runner 内部 API。
- **Impact:** rehearsal_runner 重命名/拆分即破坏 promote；违反「最小 public 面」。
- **Fix:** 将 5 函数升为 `sandbox_clean_write/gates.py` 或 rehearsal_runner public API。

### A2-P2-03 — r3g03 cap 校验与 r3g01 平行重复

- **Location:** `approval_contract.py:97-147` vs `rehearsal_plan.py:42-126`
- **Evidence:** 同读 `candidate_caps` YAML，domain/symbol/window/metadata 逻辑结构相同，仅 cap 键名不同（`r3g03_*` vs `r3g01_*`）。
- **Impact:** 契约改 caps 需改两处；≥20 行可删改候选。
- **Fix:** `_validate_source_caps(candidate, profile="r3g03")` 单函数 + 错误类型参数。

---

## P3 — LOW / hygiene

### A2-P3-01 — `_resolve_path` 四重复（~24 行）

`approval_contract.py:90`, `rollback_plan.py:21`, `limited_production_entry.py:84`, `rehearsal_runner.py:79`

### A2-P3-02 — `_utc_now_iso` 三重复

`limited_production_entry.py:91`, `rollback_plan.py:28`, `rehearsal_runner.py:75`

### A2-P3-03 — CLI 与 runner 双重 missing-file 检查

`data_commands.py:359-382` 与 `limited_production_entry.py:378-383`；CLI 可仅 catch `LimitedProductionEntryError`。

### A2-P3-04 — 死参数 `allow_live_fetch` / `fred_authorization`

`PromoteRequest:80-81`, `main.py:98-99`, `data_commands.py:343-344` — runner 未读取；frozen §9.4 要求 FRED live fetch 门控，当前为 CLI 占位（A1/A5 应跟踪，A2 记 YAGNI 膨胀）。

### A2-P3-05 — `build_before_proof` 未复用 mutation_proof 行计数

`limited_production_entry.py:130-139` 自写 `_target_table_row_count`；frozen §4.1 点名 `key_table_row_counts`。单表计数可接受，但 `_table_schema_hash` / `_latest_write_operation_id`（114-155）为新增 ~42 行只读 helper — 非 full-DB scan，可留。

### A2-P3-06 — 测试 CLI/runner dry_run 部分重叠

`test_PromoteCli_dryRunDefault` vs `test_PromoteRunner_dryRunProducesReport` — purpose 不同（CLI 默认 vs runner），可共享 fixture 减 setup。

---

## 计划外发现

| ID        | 描述                                                                                 | 优先级 | 阻塞          |
| --------- | ------------------------------------------------------------------------------------ | ------ | ------------- |
| OOB-A2-01 | `_production_clean_write` 跳过 `_ensure_validation_report`（rehearsal 有 DB insert） | P3     | 否（A4 语义） |
| OOB-A2-02 | `rollback_plan.py` 体量 ~118 行，符合 frozen「new small」                            | —      | 否            |
| OOB-A2-03 | `approval_contract.py` ~270 行，AC 必需 quadruple-lock，非过度抽象                   | —      | 否            |

**对抗搜索范围：** `backend/app/ops/sandbox_clean_write/`, `rehearsal_runner.py`, `rehearsal_plan.py`, `staged_pilot.py`, `mutation_proof.py`, `data_commands.py`, `tests/test_round3g_*`

---

## DOUBT（≥20 行可简化）

**是。** 至少 3 处：

1. `_production_clean_write` ~52 行 ↔ `_sandbox_clean_write` ~41 行
2. `validate_r3g03_source_caps` ~35 行 ↔ `validate_source_caps` ~38 行
3. `build_before_proof` 辅助 ~55 行（部分可 mutation_proof 薄包装）

---

## 维度结论

**PASS_WITH_FIXES** — 无 P0/P1 过度工程；rollback/approval 模块体量与 AC 对齐。主要债务：**frozen §4.1 ponytail 复用表未完全落地**（镜像写路径缺注释、未提取共享 compose、cap 校验重复）。Repair 须偿还 P2-01/02/03 及全部 P3。
