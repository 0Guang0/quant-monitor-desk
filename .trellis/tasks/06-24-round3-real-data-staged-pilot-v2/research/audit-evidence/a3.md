# A3 — audit-security（静态威胁面）

**维度：** static · **判定：** PASS

## 任务专属 rg（AUDIT §2）

| 检查 | 命令/范围 | 结果 |
| ---- | --------- | ---- |
| production clean write | `staged_pilot.py` `allow_clean_write` | 多处 `False` + `validate_pilot_v2_authorization` 拒绝 True |
| 新增 `adapter=` | `rg adapter= backend/app/ops/staged_pilot.py` | **0 匹配** |
| 密钥/URL | `rg` secrets pattern on diff 文件 | **0 匹配** |
| 启用 live 源 | `rg enable.qmt` on diff 文件 | **0 匹配** |
| SQL 拼接 | `staged_pilot.py:579` `DELETE FROM {STG_FILE_REGISTRY}` | 常量表名；非用户输入 |

## 威胁面三类 DOUBT

| 类 | 发现 |
| -- | ---- |
| 硬编码 URL | 无发现（diff 文件） |
| API key / token | 无发现 |
| SQL 注入 | `mutation_proof.py` 表名来自 `information_schema` + `KEY_TABLES` 常量；只读 `COUNT(*)` |

## 写路径

- v2 证据捕获写 **task execute-evidence/** 与 **sandbox**；`production_clean_write: false` 于 `pilot_v2_caps.json`。
- `build_production_mutation_proof` 只读 `ConnectionManager.reader()`。
- 无 `apply_migrations` / `writer()` 新增于 v2 路径。

## 等级汇总

P0: 0 · P1: 0 · P2: 0 · P3: 0
