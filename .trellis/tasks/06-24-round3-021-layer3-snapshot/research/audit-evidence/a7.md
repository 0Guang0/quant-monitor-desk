# A7 audit-ops — 运维与数据路径

**维度：** A7 · database-administrator + sre-engineer  
**判定：** PASS

## 检查项

| 检查           | 证据                                                                     | 结果                 |
| -------------- | ------------------------------------------------------------------------ | -------------------- |
| migration 变更 | `git diff specs/schema/` + layer3 diff                                   | 无 migration         |
| DB 写路径      | `rg "duckdb\|WriteManager\|apply_migrations" backend/app/layer3_chains/` | 无匹配               |
| init_db 依赖   | 本任务测试不调用 `scripts/init_db.py`                                    | N/A                  |
| sandbox 污染   | `Test-Path .audit-sandbox/r3-021` → False                                | 无 task sandbox 残留 |
| prod 写        | builder 纯内存 dataclass 组装                                            | 无 prod 写           |

## GitNexus

- `query("layer3 chains database migration duckdb write")` — 返回 WriteManager / Layer1 写路径，**layer3_chains 未参与**。

## DOUBT

- 第二次 init/migrate：**不适用**（本任务无 schema 变更）。
- kill 恢复场景：**不适用**。

**结论：** staged-only 骨架，运维面零 DB 接触，符合 MASTER §3.2 defer（WriteManager DB sync defer）。
