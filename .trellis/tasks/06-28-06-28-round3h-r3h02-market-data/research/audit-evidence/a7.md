# Trellis Audit A7 (Ops / DB Boundary) — R3H-02 Market Data Adapters

| 字段     | 值                                                                    |
| -------- | --------------------------------------------------------------------- |
| 维度     | A7 Ops（database-administrator + sre）                                |
| 任务     | `06-28-06-28-round3h-r3h02-market-data`                               |
| 审计日期 | 2026-06-28                                                            |
| 权威     | `AUDIT.plan.md` §1 · frozen §2.8 · `agents/database-administrator.md` |

---

## Verdict: **PASS**

---

## 审计问题与结论

| 检查项                       | 要求                                              | 结果                                                                                                              |
| ---------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 无新增写主库 CLI             | R3H-02 不得引入 `quant_monitor.duckdb` 写路径 CLI | **PASS** — `backend/app/cli/**` 无五源引用；`rg` 零匹配 `alpha_vantage\|stooq\|yahoo_finance\|deribit\|coingecko` |
| 既有 promote 拒 canonical DB | R3G-03 `_assert_production_db_allowed` 仍拒主库   | **PASS** — 回归 pytest 8/8 绿                                                                                     |
| 任务 diff 边界               | 不得扩 `limited_production_entry` / promote CLI   | **PASS** — 本任务 diff 无 `backend/app/cli/`、`backend/app/ops/` 变更                                             |

---

## §3.7 命令证据

| 步骤                    | 命令                                                                                                                                                         | exit  | 关键输出                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------ |
| promote 主库硬拒        | `uv run pytest tests/test_round3g_limited_production_clean_write.py -q -k "refusesCanonicalProductionDbPath or PromoteCli" --basetemp=.audit-sandbox/pytest` | **0** | **8 passed**（含 `test_PromoteRunner_refusesCanonicalProductionDbPath`） |
| CLI 无五源接线          | `rg` `backend/app/cli/` 五 source_id                                                                                                                         | —     | **0 matches**                                                            |
| 本任务 git diff CLI/ops | `git diff --stat HEAD -- backend/app/cli/ backend/app/ops/`                                                                                                  | —     | **无变更**                                                               |

**canonical 拒绝对齐代码：** `limited_production_entry.py` `_assert_production_db_allowed` — `quant_monitor.duckdb` resolve 后抛 `LimitedProductionEntryError("canonical production DB path refused…")`（`tests/test_round3g_limited_production_clean_write.py:1023–1037`）。

---

## R3H-02 写边界审阅

| 组件                                                                      | 写行为                                         | 主库风险                          |
| ------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------- |
| 五源 fetch_ports                                                          | 仅返回 `FetchPayload`（内存 JSON bytes）       | 无                                |
| `write_daily_bar_evidence_bundle` / `write_crypto_market_evidence_bundle` | 测试/replay `tmp_path` 或 fixture 目录         | 无 CLI 暴露；frozen §2.8 禁止主库 |
| registry / capabilities yaml                                              | 声明式配置                                     | 无 DB 写                          |
| `scripts/check_test_catalog.py`                                           | 本任务唯一 scripts 变更（+28 行 catalog 登记） | 无 DB 写                          |

**frozen §2.8 gate 2：**「No main DB writes; sandbox/replay/`.audit-sandbox` only」— 实现与 gate 一致。

---

## 发现项

| ID  | 级别 | 描述     | 阻塞? |
| --- | ---- | -------- | ----- |
| —   | —    | 无 P0/P1 | —     |

---

## 计划外发现

| ID      | 范围             | 发现                                                                                                                                                    | 严重度               |
| ------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| U-A7-01 | promote 候选源   | R3G-03 promote 仍仅覆盖 baostock/cninfo/fred/akshare/yahoo 等 R3G rehearsal 源；**R3H-02 五源未接入 promote CLI** — 符合本卡 scope（证据链 only）       | NON-BLOCKING（预期） |
| U-A7-02 | yahoo            | registry 中 yahoo 已有 R3G pilot closeout fixture；R3H-02 port 为 L2 证据 SSOT，与 promote 路径分离                                                     | NON-BLOCKING         |
| U-A7-03 | normalizer write | `write_*_evidence_bundle` 可写任意 `out_dir` — 无 path guard；依赖调用方（测试/tmp）而非 CLI — 与 R3H-01 official_macro 同模式                          | NON-BLOCKING         |
| U-A7-04 | rehearsal 回归   | `test_RehearsalCli_productionDbPathRejected` 本次未复跑（audit-sandbox basetemp 文件锁）；promote 层 `_assert_production_db_allowed` 单测已覆盖同一守卫 | NON-BLOCKING         |

---

## 维度结论

**PASS** — 无新增写主库 CLI；R3G-03 promote canonical DB 硬拒仍有效；R3H-02 变更未触及 ops/CLI 写路径。
