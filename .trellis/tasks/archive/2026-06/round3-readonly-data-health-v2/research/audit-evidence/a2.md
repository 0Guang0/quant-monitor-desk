# A2 — audit-ponytail（ponytail-review）

**维度：** review-only · **任务：** `round3-readonly-data-health-v2`（B01-DH2）  
**权威：** `agents/audit-a2-ponytail.md` + `agents/audit-adversarial-authority.md`（R3E 任务卡 / 设计 > MASTER §8）  
**范围：** `backend/app/ops/data_health.py`、`backend/app/ops/data_health_cli.py`（diff vs `master`）  
**判定：** **PASS**（0 阻塞候选；1 处 ≥20 行可简化建议）

---

## diff 统计

```
backend/app/ops/data_health.py     | 429 ++++++++++++++++++++++++++++++++++++-
backend/app/ops/data_health_cli.py |  22 +-
────────────────────────────────────────────────────
2 files changed, 445 insertions(+), 6 deletions(-)
```

**MASTER §8 触及文件：** `data_health.py`（DH2-01..06 五 profile + rollup + gate）、`data_health_cli.py`（DH2-07 `--profile` 可选）。测试增量 `tests/test_data_health_v2.py` 不在本 diff 主体，但已 spot-check：全用 committed fixtures + 模块级 `_SERVICE`，无运行时 manifest 膨胀。

---

## 新增/修改符号 — ponytail 梯级

| 符号                                      | 行区           | 梯级                                        | 结论                                                      |
| ----------------------------------------- | -------------- | ------------------------------------------- | --------------------------------------------------------- |
| `BLOCKED` / `VALID_CHECK_STATUSES` 扩展   | L17-23         | 6（R3E §6 四态 AC）                         | 不可删                                                    |
| `V2_PROFILES` / `MODEL_INPUTS_DIR`        | L25-34         | 6（R3E §6 五 profile 名）                   | 不可删；CLI 复用避免双份名单                              |
| `_V3_DEFAULT_CAPS` + `ponytail:` 注释     | L35-40         | 7（天花板已标注）                           | 合规                                                      |
| `_blocked_check`                          | L187-203       | 2（与 `_pass/_warn/_fail_check` 同模式）    | 保留 — frozen dataclass 需独立工厂                        |
| `_aggregate_status` BLOCKED 分支          | L622-625       | 6（rollup/BLOCKED AC）                      | 不可删                                                    |
| `_read_profile_json`                      | L836-844       | 2（复用 `_find_first` + `_read_json` 语义） | 5 处调用，lean helper                                     |
| `_checks_model_input_whitelist`           | L847-876       | 6（DH2-01 / AC-DH2-BLOCKED）                | 不可删                                                    |
| `_checks_fred_sandbox`                    | L879-957       | 6（DH2-02 字段 AC）                         | 冗长但为显式 rule_id，非 wrapper 层                       |
| `_checks_tdx_manual_probe`                | L960-1003      | 6（DH2-03）                                 | 不可删                                                    |
| `_checks_staged_pilot_v3`                 | L1006-1070     | 6（DH2-04 caps/role）                       | 不可删                                                    |
| `_evaluate_rollup_gate`                   | L1073-1105     | 2/5（与 `evaluate_gate` 平行）              | 见 §3.2 — 可 shrink，非新抽象                             |
| `_checks_source_readiness_rollup`         | L1108-1162     | 6（DH2-05 递归 rollup）                     | 递归 `DataHealthService()` 为 AC 需要，非单消费者 factory |
| `_evaluate_profile_gate`                  | L1165-1179     | 6（DH2-06 gate 文案）                       | 不可删                                                    |
| `_PROFILE_CHECKERS` + `_check_v2_profile` | L1182-1212     | 2（五 profile 分发）                        | 非 yagni — 五消费者                                       |
| `check_evidence_dir(..., profile=)` 路由  | L1194-1200     | 6（v1 bundle 向后兼容）                     | 最小 dispatch                                             |
| CLI `--profile` / `V2_PROFILES` import    | L33-65, L83-84 | 6（DH2-07 可选 AC）                         | 薄包装，已 lean                                           |

---

## §3.2 ≥20 行候选删改

| 候选删改（file:line）                                                                                                                                                             | ponytail 梯级                                                           | 是否阻塞                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ----------------------- |
| `data_health.py:794-827` + `1073-1105` — `evaluate_gate` 与 `_evaluate_rollup_gate` 平行三段（has_fail → allow_clean_write → sandbox_clean_write_rehearsal）及近似 rationale 文案 | 2（提取已有文件内 `_clean_write_rehearsal_gate(...)` 或内联共享布尔链） | **建议**                |
| `data_health.py:857` — `glob("*.yaml")` + `glob("**/*.yaml")` 重复枚举同一目录                                                                                                    | 5 shrink                                                                | 建议（<20 行，附记）    |
| `data_health.py:1110-1172` — rollup 路径二次 `_read_profile_json(rollup_manifest.json)`（checker 与 `_evaluate_profile_gate` 各读一次）                                           | 2（checker 返回 manifest 或 gate 入参传递）                             | 建议（<20 行，附记）    |
| `data_health.py:1125-1139` — rollup 子报告手写 `DataHealthCheckResult` 而非薄 summary helper                                                                                      | 5 shrink                                                                | 建议（~15 行；未达 20） |

**不算删改（MASTER/R3E explicit AC）：** 五 profile checker 主体、BLOCKED 状态、`_PROFILE_CHECKERS` 表、CLI profile 暴露、`ponytail:` cap 默认值、v1 `_check_staged_pilot_bundle` 保留路径。

---

## DOUBT

**≥20 行可简化？** **是** — `evaluate_gate` 与 `_evaluate_rollup_gate` 合计约 **22–28 行** 可合并为单条共享 gate 决策链（rollup 仅多 `safety_evidence` 四键校验，宜作可选前缀而非整函数复制）。

**搜索范围：**

- `backend/app/ops/data_health.py` 全文（v2 增量区 L830-1212 + 既有 gate L794-827）
- `backend/app/ops/data_health_cli.py` 全文（90 行）
- `tests/test_data_health_v2.py` 全文（fixture 驱动，无内联 manifest 膨胀）
- 复用对照：`load_evidence_bundle` / `_find_first` / `evaluate_gate`（v1 模式）
- 全库 grep：无第二套 profile checker / data_health v2 模块可直引

---

## 交叉引用 A4

| A2 候选                                             | A4 关联                                                                                                      |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 双 gate 函数平行结构                                | A4 应核对 rationale 字符串与 `sandbox_clean_write_gate_ready` 语义一致；合并 gate 时勿改 FAIL/BLOCKED 优先级 |
| `_read_profile_json` 吞 `JSONDecodeError` 返 `None` | A4 错误模型：与 v1 `load_evidence_bundle` fail-fast 不同；属 profile 路径「缺文件=FAIL」设计，非 A2 删改目标 |
| rollup 子报告 `type: ignore[arg-type]` on status    | A4 类型/状态映射边界；shrink 手写 `DataHealthCheckResult` 前须保持 WARN/FAIL/BLOCKED 映射                    |

---

## 计划外发现

| ID        | 来源                         | 发现                                                                        | 梯级 | Blocking     |
| --------- | ---------------------------- | --------------------------------------------------------------------------- | ---- | ------------ |
| OOB-A2-01 | 对抗 MASTER §8 切片表        | 未发现计划外第六 profile、registry writer 或 fetch 旁路                     | —    | N/A          |
| OOB-A2-02 | 对抗 v1 `evaluate_gate` 模式 | v2 rollup gate 复制 v1 三段决策链而非抽取 — 重复非新抽象层                  | 2    | NON-BLOCKING |
| OOB-A2-03 | 对抗 R3E §7「无递归 scan」   | rollup 仅遍历 manifest `profiles` 键，非目录递归；合规                      | —    | N/A          |
| OOB-A2-04 | 对抗「单消费者 wrapper」     | `_check_v2_profile` / `_PROFILE_CHECKERS` 有五 profile + CLI 消费；非 yagni | —    | N/A          |

**对抗搜索声明：** 已对照 `R3E_readonly_data_health_v2.md` §6–§10、`MASTER.plan.md` §8–§9、`BATCH_01_HARDENING_RULES.md` 索引、`load_evidence_bundle` / `evaluate_gate` v1 既定模式；除上表外未发现计划外 factory、单调用 wrapper 或 ≥20 行纯样板层（profile 校验行数属 AC 产物）。

---

## ponytail-review 一行式

- `data_health.py:L794-827+L1073-1105: shrink: twin clean-write gate chains (~25 lines). Shared helper with optional safety_evidence prefix; keep distinct success strings.`
- `data_health.py:L857: shrink: duplicate yaml globs. evidence_dir.glob("**/*.yaml") only.`
- `data_health.py:L1110+L1171: shrink: rollup_manifest.json read twice. Pass dict from checker to gate.`

**net: ~22–30 lines possible**（非阻塞；不损五 profile AC / BLOCKED / gate 字段前提下）。

---

## A2 checklist

- [x] `git diff --stat` 已记录（+445 / -6；`data_health.py` +429）
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象/错误模型交叉引用
- [x] 阻塞 vs 建议已区分

---

## 结论

**A2 PASS** — B01-DH2 增量符合 ponytail「五 profile + rollup + 薄 CLI」预期；无计划外抽象层、无单消费者 factory、测试 fixture SSOT 干净。`+429` 行业务行主要为 R3E 显式 AC 校验逻辑，非过度工程。存在 **0 项阻塞删改**、**1 项 ≥20 行建议性简化**（双 gate 函数合并）。合并前可选收紧，不阻断 staged-only merge。
