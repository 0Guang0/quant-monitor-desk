# A3 — audit-security（security-and-hardening + DOUBT）

**维度：** A3 · security-and-hardening + doubt-driven-development  
**任务：** `round3-fred-authorized-sandbox-pilot` (B01-FRED)  
**审计范围：** `backend/app/ops/fred_*.py` · `execute-evidence/authorization.yaml` · fred registry/capability 行 · `tests/test_fred_*.py`  
**权威对照：** `BATCH_01_HARDENING_RULES.md` §3–§6 · `R3E_fred_authorized_sandbox_pilot.md` · MASTER §0 授权模板 · `datasource_service_contract.yaml`  
**判定：** **PASS**  
**OPEN：** **0** · **BLOCKING：** **0**

---

## §3.3 威胁面与发现

| 威胁面                               | 等级 | 发现       | 证据                                                                                                                                                                                      | 处置 |
| ------------------------------------ | ---- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 密钥进 repo / 日志泄露               | —    | **无发现** | `rg` `fred_*.py`：仅 `FRED_API_KEY` env 引用；无 `api_key="..."` / JWT / bearer 硬编码                                                                                                    | —    |
| 未授权 live / production clean write | —    | **无发现** | `validate_authorization` L113–114 拒绝 `allow_production_clean_write`；全路径 `production_clean_write=False`；registry `enabled_by_default=false` · capability `production_default=false` | —    |
| SQL 注入 / DuckDB 变异               | —    | **无发现** | fred 模块无 SQL/`execute(f"`；无 `writer`/`INSERT`/`migration`                                                                                                                            | —    |
| 旁路 fetch（不经授权门）             | —    | **无发现** | `backend/app/datasources/**` 无 fred adapter 接线；无 API/ops CLI fred live 入口；仅 `fred_sandbox_pilot.run_live_fetch`                                                                  | —    |
| 命令注入 / 任意执行                  | —    | **无发现** | fred 模块无 `subprocess`/`eval`/`exec`；`series_id` 经 `urllib.parse.urlencode`                                                                                                           | —    |
| 写路径越界                           | —    | **无发现** | manifest/evidence 仅写 `sandbox_root` 或默认 `.audit-sandbox/r3e-fred-sandbox/`                                                                                                           | —    |

---

## 静态扫描（AUDIT.plan §2 A3 + security-auditor 基线）

| 命令 / 检查                                                                  | 范围           | 结果                                                                                                                                                                 |
| ---------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `rg "https?://\|api_key\|secret\|token\|password" backend/app/ops/fred_*.py` | fred ops       | 命中均为 env/官方 FRED URL（`fred_fetch_ports.py:19–92`）；无硬编码密钥                                                                                              |
| `rg "f\".*SELECT\|execute\(f" backend/app/ops/fred_*.py`                     | SQL 拼接       | **0 命中**                                                                                                                                                           |
| `rg "subprocess\|eval\|exec" backend/app/ops/fred_*.py`                      | 任意执行       | **0 命中**                                                                                                                                                           |
| `rg "writer\|INSERT\|UPDATE\|DELETE\|apply_migrations" fred_*.py`            | DB 写          | **0 命中**                                                                                                                                                           |
| `rg "enable.qmt\|enable.xqshare" fred_*`                                     | 未授权 live 源 | **0 命中**                                                                                                                                                           |
| registry/capability fred 行                                                  | SSOT           | `enabled_by_default: false` · `status: sandbox_candidate` · `requires_auth: true` · `api_key_env: FRED_API_KEY`                                                      |
| `authorization.yaml` vs MASTER §0 模板                                       | 授权证据       | 字段对齐；`allow_production_clean_write: false` · `skip_live_fetch_default: true` · caps 5/100/10                                                                    |
| pytest 安全边界                                                              | CI/audit       | `uv run pytest tests/test_fred_source_registry.py tests/test_fred_sandbox_pilot.py tests/test_fred_staged_semantics.py -q` → **16 passed, 1 skipped**（live opt-in） |

---

## 信任边界核对（fetch / auth）

| 边界               | 实现                                                                                            | 结果                                                     |
| ------------------ | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 默认 fail-closed   | `FredPilotRequest.skip_live_fetch=True` · `use_mock_port=True`；mock 路径不经网络               | PASS                                                     |
| Live 三门          | 须 `authorization_evidence` + yaml `authorization_present` + `FRED_API_KEY`（或 `api_key_env`） | PASS — `fred_sandbox_pilot.py:104–118`                   |
| source_id 绑定     | `auth.source_id` 须为 `fred`                                                                    | PASS — L111–112                                          |
| clean write 拒绝   | `allow_production_clean_write` 为真 → `FredPilotAuthorizationError`                             | PASS — L113–114                                          |
| Series 上限        | `P0_SERIES_WHITELIST` + `MAX_SERIES=5`；超集拒绝                                                | PASS — L94–101                                           |
| Row cap            | `max_rows_per_series` 1..100；live 取 `min(request, auth.max_rows)`                             | PASS — L170–172, L259                                    |
| Network cap        | live 循环 `network_calls >= auth.max_calls`（模板 10）                                          | PASS — L264–268                                          |
| 官方 endpoint 固定 | `https://api.stlouisfed.org/fred/series/observations` + urlencode                               | PASS — `fred_fetch_ports.py:82–92`                       |
| YAML 解析          | `yaml.safe_load`                                                                                | PASS — L88                                               |
| 错误分类不静默     | 缺授权 → `FRED_PILOT_FAIL_AUTH`；网络 → `FRED_PILOT_FAIL_NETWORK`                               | PASS — 测试 `test_fredPilot_missingAuth_returnsFailAuth` |

---

## DOUBT 三类对抗（每类须有发现或「无发现 + 范围」）

| 类                    | 搜索范围                                                | 结论                                                                                                 |
| --------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 1. 硬编码 URL 变体    | `fred_fetch_ports.py` · `fred_sandbox_pilot.py` · tests | **无发现** — 仅单一官方 FRED observations URL；无 mirror/redirect/用户可控 base URL                  |
| 2. JWT / API key 模式 | 上述 + registry `api_key_env`                           | **无发现** — 密钥仅 `os.environ.get`；URL 参数含 key 但无 log/print；错误 `PortError` 不拼接完整 URL |
| 3. SQL 拼接           | fred 全模块                                             | **无发现** — 无 DuckDB/SQL 路径；JSON manifest + sandbox 文件写                                      |

---

## GitNexus

- **query：** `fred sandbox pilot authorization live fetch` — 索引未收录新 `fred_sandbox_pilot` 符号；邻接 `live_pilot_auth.validate_authorization` / `staged_pilot` 授权模式；**LOW** 外溢风险（fred 未接入 `DataSourceService` fetch 链）。
- **DOUBT：** 索引 stale 不阻断；静态 diff + rg 已覆盖 mutation/密钥面。

---

## 计划外发现

> 对抗性搜索：hardening §3 授权字段 vs `validate_authorization` 实现 · MASTER 未写的 bypass · `live_pilot_auth` 对照。

| ID               | 严重度            | 发现                                                                            | 处置                                                                                                                                                        | Audit status           |
| ---------------- | ----------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| A3-AUTH-SYMBOLS  | NON-BLOCKING (P3) | `validate_authorization` 未校验 `request.series_ids` ⊆ `auth.symbols_or_series` | 协调者 `authorization.yaml` 与 `P0_SERIES_WHITELIST` 冻结为同一五元组；`validate_series_whitelist` 仍 fail-closed；sandbox-only，无 production adapter 旁路 | **CLOSED**             |
| A3-AUTH-MAXCALLS | NON-BLOCKING (P3) | `max_calls` 取自 auth yaml，未 `min(..., MAX_NETWORK_CALLS_PER_RUN)`            | 模板 `max_calls: 10` 落盘；系列 ≤5 物理上限网络调用；证据篡改属 coordinator 治理面                                                                          | **CLOSED**             |
| A3-FRED-07       | NON-BLOCKING      | 无 `FRED_API_KEY` 时 live slice 易标 OPEN                                       | `b01-fred-audit-closures.md` + `test_fred07_liveFetch_closureClosedSkipOptIn_withoutApiKey`                                                                 | **CLOSED-SKIP-OPT-IN** |
| A3-DIRECT-PORT   | NON-BLOCKING (P3) | `FredLiveFetchPort` 可被测试/内部直接实例化，不经 `run_live_fetch`              | 无 API/CLI/Service 暴露；契约要求经 `DataSourceService` 的生产路径未接线 fred；pilot 隔离符合 BATCH_01 sandbox 角色                                         | **CLOSED**             |

**显式非发现（已对抗确认）：**

- `data_health.py` 主体 — forbidden；fred 使用独立 `fred_evidence_validator.py`。
- production DB / migration — diff 无写库路径。
- qmt/xqshare 默认启用 — 无相关代码。
- macro 关闭 B2.5-O-05 — 安全语义由 staged tests + closeout 守卫，非密钥面但已绿。

---

## AC-FRED 安全相关验收

| AC         | 安全要点                                     | 结果                                                                                     |
| ---------- | -------------------------------------------- | ---------------------------------------------------------------------------------------- |
| AC-FRED-01 | fred disabled-by-default，非 production-live | PASS                                                                                     |
| AC-FRED-04 | 缺授权 → `FRED_PILOT_FAIL_AUTH`              | PASS                                                                                     |
| AC-FRED-07 | 授权 live 尊重 caps；仅 sandbox 写           | **CLOSED-SKIP-OPT-IN**（无 key）；有 key 时 `test_fredLiveFetch_authorized_respectsCaps` |

---

## 汇总

| 类别                        | 数量  |
| --------------------------- | ----- |
| BLOCKING (P0/P1)            | **0** |
| OPEN (含 NON-BLOCKING)      | **0** |
| CLOSED / CLOSED-SKIP-OPT-IN | **4** |

**A3 verdict：PASS** — 无 P0/P1 密钥、mutation 或未授权 live 面；计划外项均已闭合。可继续 A4（audit-quality）。
