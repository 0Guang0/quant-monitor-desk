# A7 — Ops

## Result

**PASS（ops gate）**

本任务为 **staged-only 配置 loader**（`IndustryChainLoader`），无 schema migration、无 `init_db` / DuckDB 写路径、无 production data 变更。AUDIT.plan §2.2 已声明 A6 SKIP；A7 聚焦 **无 DB 写** 与 **`data/duckdb/` hash 审计前后不变**。

---

## §3.7 命令证据

| 步骤 | 命令 | exit | 关键日志/证据 |
| ---- | ---- | ---- | ------------- |
| 1. diff 范围无 migration/DB | `git status --short` + 人工核对变更集 | 0 | 仅 `layer3_chains/*`、`tests/test_layer3_loader.py`、`tests/fixtures/layer3_staged_bundle/`、`test_catalog`；**无** `specs/schema/`、`backend/app/db/`、`scripts/init_db.py`、migration 文件 |
| 2. 静态 DB 引用扫描 | `rg duckdb\|init_db\|write_manager\|DbValidationGate\|ConnectionManager\|migration backend/app/layer3_chains tests/test_layer3_loader.py tests/fixtures/layer3_staged_bundle` | 1（无匹配） | loader/models/tests/fixtures **零** DuckDB/migration 引用 |
| 3. prod `data/duckdb/` hash（审计前） | Python 目录 hash（`rel\|size\|content` 复合） | 0 | `HASH_BEFORE=555631e98e1370cc09a185ddf1b8a08ddc6a23a6830e78cd05f64238c988b0cf`；文件：`.gitkeep`（0B）、`quant_monitor.duckdb`（4468736B）；路径：`C:\Users\Guang\Desktop\quant-monitor-desk\data\duckdb`（worktree 无 `data/`，以主仓 prod-path 副本为准） |
| 4. cli-sandbox 复跑 loader 测试 | `QMD_DATA_ROOT=<wt>/.audit-sandbox/r3-020/data uv run pytest tests/test_layer3_loader.py -q --basetemp=.audit-sandbox/r3-020/pytest` | 0 | `14 passed`；sandbox **未创建** `data/` 子树（纯文件读 fixture + pytest `tmp_path`） |
| 5. prod `data/duckdb/` hash（审计后） | 同步骤 3 | 0 | `HASH_AFTER=555631e98e1370cc09a185ddf1b8a08ddc6a23a6830e78cd05f64238c988b0cf`；与 BEFORE **一致** |
| 6. init/migrate 幂等（本任务 N/A） | — | SKIP | MASTER §0/§1.3 与 AUDIT.plan §2.2：纯配置解析，**无** migration diff；不适用两遍 `init_db.py` |

---

## Ops Checklist

| 检查项 | 判定 | 证据与说明 |
| ------ | ---- | ---------- |
| 无 migration / schema 变更 | PASS | 分支/工作区 diff 不含 `specs/schema/`、migration 模块；回滚策略为删 `layer3_chains/*.py` + tests（MASTER §0 Failure modes） |
| 无 production DB 写 | PASS | `loader.py` 仅 `yaml.safe_load` / `json.loads` 读 staged bundle；`loader_mode != staged_fixture_only` 时 `IndustryChainLoadError` fail-closed |
| Sandbox 无污染 prod data | PASS | `QMD_DATA_ROOT` 指向 `.audit-sandbox/r3-020/data`；pytest 后该目录未创建；未写 `C:\Users\Guang\Desktop\quant-monitor-desk\data\` |
| `data/duckdb/` hash 不变 | PASS | BEFORE == AFTER == `555631e98e1370cc09a185ddf1b8a08ddc6a23a6830e78cd05f64238c988b0cf` |
| 失败可观测（loader 域） | PASS | 校验失败统一 `IndustryChainLoadError`；无部分 `IndustryChainLoadResult`；测试覆盖 duplicate/missing/invalid JSON/空 bundle 等 fail-fast |
| init/migrate 幂等 / kill 恢复 | N/A | 本任务无 DB 生命周期；由 AUDIT.plan 明示跳过 |

---

## 异常场景（loader 域，非 DB）

| 场景 | fail-closed | 说明 |
| ---- | ----------- | ---- |
| 非 `staged_fixture_only` | PASS | `test_layer3Loader_nonStagedMode_rejects`；错误信息含 `staged_fixture_only` |
| 缺 manifest / 数据文件 | PASS | `test_layer3Loader_missingBundleFile_rejects` |
| 空 chains bundle | PASS | `test_layer3Loader_emptyBundle_rejects`（A8 边界） |
| 图引用悬空 / 重复 ID | PASS | 对应 negative tests 全绿 |

---

## Doubt-Driven Note

**CLAIM:** 020 不触及 DuckDB，A7 只需证明无 migration、无 prod 写、hash 不变。

**Adversarial self-check（degraded）：** 是否因「未跑两遍 init_db」而应 FAIL？**否** — AUDIT.plan 与 MASTER 均冻结本任务为 staged config loader；强行跑 init 会引入与 diff 无关的 ops 噪声。是否 pytest import 链隐式写 DB？**否** — sandbox 无 `data/` 目录生成，且静态扫描无 DB 符号；14 测绿仅读写 fixture/`tmp_path`。

**Residual（不进 §4.3）：** `AUDIT_PROD_ROOT`（`.audit-sandbox/r3-020-audit-prod-equiv`）尚未物化；本 A7 以主仓 `data/duckdb` hash 作 audit-prod-path 等价验证。完整 prod-path 复制树由 A5 维度负责。

---

## 审计约束遵守

- 未 `git commit`
- 未写 production `data/`（只读 hash）
- 唯一写入：本文件
