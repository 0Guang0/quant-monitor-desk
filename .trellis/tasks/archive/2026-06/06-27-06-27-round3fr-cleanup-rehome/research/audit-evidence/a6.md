# A6 audit-perf — R3FR-07 Legacy Wrapper Cleanup

> **维度：** A6 · performance-engineer + doubt-driven-development  
> **任务：** `06-27-06-27-round3fr-cleanup-rehome` (R3FR-07)  
> **分支：** `chore/round3fr-cleanup-rehome`  
> **日期：** 2026-06-27  
> **模式：** Audit（只读，无 commit、无改码）

---

## 总判定

| 项                   | 值                                                               |
| -------------------- | ---------------------------------------------------------------- |
| **判定**             | **SKIP**                                                         |
| **BLOCKING**         | 0                                                                |
| **NON-BLOCKING**     | 2                                                                |
| **AUDIT.plan §1 A6** | 本任务跳过 — cleanup 无 perf 契约变更；仅 docstring/规划叙事清理 |

---

## Scorecard（静态审计 · 未做 Lighthouse/CrUX/smoke）

| 指标                     | 值           | 来源                | 目标     | 状态        |
| ------------------------ | ------------ | ------------------- | -------- | ----------- |
| LCP / INP / CLS          | not measured | —                   | CWV 阈值 | —（无 Web） |
| smoke 端到端耗时         | not measured | Plan 未冻结         | —        | SKIP        |
| ResourceGuard 触发       | not measured | diff 无调用路径变更 | —        | SKIP        |
| `test_r3fr07_*` 模块耗时 | not measured | Plan 未冻结         | —        | SKIP        |
| 生产 hot path 行为变更   | **零**       | `git diff` 审阅     | 无变更   | **PASS**    |

> **Artifacts used:** none — source analysis + `git diff HEAD` on 4 backend files + `tests/test_r3fr07_legacy_wrapper_cleanup.py`  
> **Stack detected:** Python 单机 pipeline；本切片为 docstring/文档 guardrail；非 Web 应用

---

## 1. 权威层级（`audit-adversarial-authority.md`）

| 级别   | 来源                                                              | 与本维结论                                                                   |
| ------ | ----------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 第一级 | frozen `R3FR_07` §6–§7 技术/资源约束、`GLOBAL_RESOURCE_LIMITS.md` | §7 冻结：**无 live fetch、无 DB 写入、无全市场扫描**；cleanup/hardening only |
| 第二级 | `agents/performance-engineer.md`、`AUDIT.plan.md` §1 A6 SKIP      | 无 smoke/ResourceGuard/durations 冻结行；A6 显式 SKIP                        |
| 第三级 | `implement.jsonl`、`EXECUTION_INDEX`                              | 步骤 9.2–9.4 为 redirect docstring；无 perf AC                               |

**对抗性注记：** 任务卡 §9.2 提及「可选 DRY 委托 `data_health_profiles`」；实际 diff **仅**增加 `check_daily_bars` docstring，**未**改变调用图或运行时路径 → 无 perf 回归面，亦无 perf 收益面。

---

## 2. SKIP 理由（§3.6 等价）

### 2.1 AUDIT.plan 冻结理由（复述 + 证实）

1. **cleanup/docstring only** — `git diff HEAD` 对 4 个 backend 文件均为 **纯 docstring/模块注释** 增量；零逻辑、零 import、零 I/O 路径变更。
2. **无 perf 契约变更** — frozen §7 与 AUDIT.plan 均未新增/修改 smoke、ResourceGuard、内存峰值或 `--durations` 门禁。
3. **无新 runtime 行为** — `health_check`、`check_daily_bars`、`TdxPytdxProbeFetchPort` 委托链与 R3FR-02/03/06 合并后行为一致；本切片不触碰执行体。
4. **新增测试为文档契约** — `tests/test_r3fr07_legacy_wrapper_cleanup.py` 仅 `read_text` 小型 markdown + `inspect.getdoc`；无网络、无 DuckDB、无 subprocess CLI 压测。
5. **无生产 hot path SLA** — 变更不挂载 FastAPI、调度器或 `production_equivalent_smoke.py`。

### 2.2 Diff 审阅（热路径 / I/O / 无界扫描）

| 文件                                             | 变更类型                     | hot path 影响     | I/O 影响            | 无界扫描          |
| ------------------------------------------------ | ---------------------------- | ----------------- | ------------------- | ----------------- |
| `backend/app/cli/data_commands.py`               | `health_check` docstring     | **无**            | **无**              | **无**            |
| `backend/app/ops/data_health.py`                 | `check_daily_bars` docstring | **无**            | **无**              | **无**            |
| `backend/app/ops/interface_probe_fetch_ports.py` | 模块 docstring               | **无**            | **无**              | **无**            |
| `backend/app/ops/tdx_manual_probe.py`            | 模块 docstring               | **无**            | **无**              | **无**            |
| `tests/test_r3fr07_legacy_wrapper_cleanup.py`    | 新增（未跟踪→工作区）        | **否**（CI only） | 读 ≤12 个小 md 文件 | **有界**（KB 级） |
| `docs/**`、`MODULE_COMPLETION_RATING.md` 等      | 规划叙事                     | **无**            | **无**              | **无**            |

**结论：** diff **未引入** hot path 变更、额外 I/O 或无界扫描。

### 2.3 performance-engineer checklist（Audit 模式）

| 检查项                       | 状态        | 说明                                     |
| ---------------------------- | ----------- | ---------------------------------------- |
| Baseline 有证据来源          | **N/A**     | Plan 未冻结 perf 命令                    |
| EXPLAIN/profile/smoke        | **N/A**     | 无 SQL/逻辑变更；无 smoke 挂载点变更     |
| 优化后同一命令对比           | **N/A**     | 无优化项                                 |
| sandbox 数据量级与 Plan 一致 | **PASS**    | frozen §7 禁止 live/DB/全市场；diff 遵守 |
| 全量 pytest 无无关回归       | **委托 A8** | 本维不重复跑全量                         |

---

## 3. 计划外 perf 风险扫描

> 按 `audit-adversarial-authority.md` A6：**即使 SKIP，仍须扫描** hot path、无界 I/O、批大小/内存尖峰。

### 3.1 Hot path

| 路径                                                     | 本切片是否改动 | 证据                             | 风险    |
| -------------------------------------------------------- | -------------- | -------------------------------- | ------- |
| `run_data_health_profile` / `market_bar_p0`（canonical） | **否**         | docstring 仅指向                 | 无变更  |
| `check_daily_bars`（evidence shim）                      | **否**（逻辑） | 仍 O(n) 行扫；pre-existing       | 见 NB-1 |
| `health_check` CLI                                       | **否**         | 仍委托 `run_data_health_profile` | 无变更  |
| TDX probe / fetch ports                                  | **否**         | 模块注释 only                    | 无变更  |

**结论：** 本分支**不存在**由 R3FR-07 引入的生产 perf 回归；SKIP 合理。

### 3.2 无界 I/O

| 面                      | 扫描                      | 本切片发现                 | 评级       |
| ----------------------- | ------------------------- | -------------------------- | ---------- |
| 网络 / live fetch       | diff + frozen §7          | **无新增**                 | **无**     |
| DuckDB / Parquet 全表扫 | diff                      | **无引用变更**             | **无**     |
| evidence JSON 读取      | `data_health.py` 未改逻辑 | pre-existing bounded pilot | **无新增** |
| 测试读 markdown         | `test_r3fr07_*`           | ≤12 文件 `read_text`       | **有界**   |

### 3.3 批大小 / 内存尖峰

| 面                                 | 本切片行为                            | 风险                    |
| ---------------------------------- | ------------------------------------- | ----------------------- |
| docstring 字符串                   | 运行时零开销（仅 import 时 doc 对象） | **无**                  |
| `inspect.getdoc` 测试              | 微秒级                                | **无**                  |
| 双轨 OHLCV 规则（shim vs profile） | 未合并；doc 指向 canonical            | 见 NB-1（pre-existing） |

---

## 4. DOUBT（doubt-driven-development）

| 疑点                                    | 结论                                                          |
| --------------------------------------- | ------------------------------------------------------------- |
| SKIP 是否遗漏 smoke？                   | **否** — AUDIT.plan §1 A6 冻结 SKIP；无 perf 阈值可 PASS/FAIL |
| docstring alone 是否可能掩盖逻辑变更？  | **否** — `git diff` 证实 4 文件仅注释块增量                   |
| §9.2「可选 DRY」未做是否 perf 债务？    | **NON-BLOCKING** — 行为未变；DRY 为可维护性，非本切片 AC      |
| 新测试多次 `read_text` 同文件是否浪费？ | **否** — CI 毫秒级；非生产路径                                |

---

## 5. §3.6 证据表（SKIP 专用）

| 指标                      | 阈值（Plan 冻结） | 实测       | 证据                            |
| ------------------------- | ----------------- | ---------- | ------------------------------- |
| 生产 hot path 逻辑变更    | 0                 | **0**      | `git diff HEAD` 4 backend files |
| smoke 端到端              | **未冻结**        | **未测**   | SKIP                            |
| ResourceGuard             | **未冻结**        | **未触及** | diff 无 RG 路径                 |
| 内存峰值 MB               | **未冻结**        | **未测**   | docstring-only                  |
| `test_r3fr07_*` durations | **未冻结**        | **未测**   | 委托 A8                         |

---

## 6. 计划外发现

| ID      | 发现                                                                                                           | 严重度               | 说明                                                                            |
| ------- | -------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------- |
| A6-NB-1 | `check_daily_bars` 与 `run_profile_ohlcv_rules` **双轨** OHLCV 逻辑仍存在；docstring 已指向 canonical 但未 DRY | **NON-BLOCKING**     | pre-existing；本切片 scope 为 redirect 文档；未来合并可减少维护面，非 perf 回归 |
| A6-NB-2 | `_checks_source_readiness_rollup` 递归 `DataHealthService.check_evidence_dir`                                  | **NON-BLOCKING**     | pre-existing；rollup profile 规模受 staged caps；本切片未触碰                   |
| —       | 本 diff 引入 hot path / 无界 I/O / 批大小尖峰                                                                  | **无 BLOCKING 发现** | 已对抗审阅 diff、`test_r3fr07_legacy_wrapper_cleanup.py`、frozen §7             |

**显式声明：** 已对照 `audit-adversarial-authority.md` A6、`agents/performance-engineer.md`、`AUDIT.plan.md` §1、`frozen/R3FR_07` §6–§7、`git diff HEAD` 全变更文件列表；**无 BLOCKING perf 项**。

---

## 7. 结论

**A6 审计判定：SKIP（维持）。**

理由摘要：R3FR-07 为 **legacy wrapper docstring redirect + 规划叙事清理**；diff 对 runtime **零逻辑变更**；frozen §7 禁止 live fetch/DB 写/全市场扫描且本切片遵守。AUDIT.plan 冻结 A6 SKIP，无 smoke/ResourceGuard/时长阈值可供 PASS/FAIL。计划外扫描登记 2 项 **NON-BLOCKING** pre-existing 备忘（双轨 OHLCV、rollup 递归），**不阻断** merge。

**零遗留：** OPEN **0** · BLOCKING **0**
