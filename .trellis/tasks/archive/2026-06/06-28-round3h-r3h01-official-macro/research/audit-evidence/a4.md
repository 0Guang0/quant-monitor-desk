# R3H-01 A4 代码质量维审计（audit-quality）

**维度：** A4 — 设计语义 + 契约字段一致性；计划未写的错误模型、脆弱断言  
**权威：** `agents/audit-adversarial-authority.md` · `AUDIT.plan.md` §1 A4 · `specs/datasource_registry/source_capabilities.yaml` · `specs/contracts/source_capability_contract.yaml`  
**Diff 范围：** `feature/round3h-r3h01-official-macro` vs `master`（六源 fetch port / normalizer / registry / 测试）  
**pytest：** `tests/test_official_macro_adapters.py` + `test_sec_edgar_adapter.py` + `test_source_route_planner.py` `-k "fred or treasury or sec_edgar or cftc or bis or world_bank or evidence or layer"` `--basetemp=.audit-sandbox/pytest` → **49 passed**（2026-06-28 A4）

---

## Review Summary

**Verdict:** PASS_WITH_FIXES

**Overview:** R3H-01 六源适配器在 FRED 未授权、行数 cap 溢出、route `DISABLED_SOURCE` 三条 AUDIT.plan A4 对抗轴上已有可执行负例；证据 normalizer 统一 `observation_date`、G10 无 bridge sidecar 路径清晰。但 `source_capabilities.yaml` 与 port 输出字段存在层级混写漂移、三源（cftc/bis/world_bank）缺 cap 负例、registry `resource_caps` 与 port 常量无 parity 测，以及若干宽松断言与 reader 静默回填，合并前应闭合 P1。

---

## §3.4 检查清单（AUDIT.plan A4 对抗轴）

| 轴       | 检查项                                        | 结果        | 证据                                                                                                                                                      |
| -------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 负例     | FRED 未授权 live（无 `FRED_API_KEY`）         | **PASS**    | `fred_port.py:126-128` `USER_AUTH_REQUIRED`；`test_official_macro_adapters.py:186-203`                                                                    |
| 负例     | FRED cap 溢出（`max_rows`）                   | **PASS**    | `fred_port.py:45-50`；`test_official_macro_adapters.py:206-225`                                                                                           |
| 负例     | FRED route `DISABLED_SOURCE`                  | **PASS**    | `test_official_macro_adapters.py:248-274`；`test_source_route_planner.py:185-207`（参数化六源）                                                           |
| 负例     | 其余五源 route DISABLED                       | **PASS**    | 各源 `*_route_disabledByDefault_unauthorized` + `test_r3h01_officialMacroRoute_disabledByDefault`                                                         |
| 负例     | Treasury / SEC cap 溢出                       | **PASS**    | `test_us_treasury_port_capOverflow_*`；`test_secEdgar_port_cikCap_*` / `filingCap_*`                                                                      |
| 负例     | CFTC / BIS / World Bank cap 溢出              | **FAIL**    | port 实现有 `_reject_over_cap` / factory 上限（如 `cftc_cot_port.py:27-29,88-90`），**无**对应 pytest                                                     |
| 契约     | `source_capabilities.yaml` fields ↔ port 输出 | **PARTIAL** | FRED 观测行缺 `source_used`；us_treasury yield 观测行缺契约列出的 `source_used`/`source_fetch_id`/`content_hash`；FRED fields 列表混写 bundle 级与 row 级 |
| 契约     | `resource_caps` YAML ↔ port 常量              | **PARTIAL** | YAML 已声明（如 fred L319-322）；无类似 `test_tdxPytdx_capsMatchTaskCard` 的 R3H parity 测                                                                |
| 错误模型 | 未授权 vs cap 状态码分层                      | **PARTIAL** | 未授权 `USER_AUTH_REQUIRED` 正确；cap 统一 `FAILED` + `"exceeds cap"`（含 `max_rows<=0`，`fred_port.py:46`）                                              |
| 测试质量 | 脆弱 / 过宽断言                               | **PARTIAL** | 未授权测允许 `FAILED`（`test_official_macro_adapters.py:202`）；`read_fred_evidence_bundle` 静默 `"fred-unknown-hash"`（`official_macro.py:77-78`）       |
| 架构     | 重复 route 负例                               | **INFO**    | 同逻辑在 `test_official_macro_adapters.py` 与 `test_source_route_planner.py` 双份维护                                                                     |

---

## 发现项（按严重度）

### Critical（P0 — 合并阻塞）

（无）

### Important（P1 — 合并前应修复）

| ID             | 位置                                                                                                      | 描述                                                                                                                                                                                                                            | 建议修复                                                                                                                                                                                |
| -------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R3H01-A4-P1-01 | `source_capabilities.yaml:328-336` · `official_macro.py:16-22` · `fred_port.py:76-82`                     | **契约字段漂移：** `fetch_macro_series.fields` 含 `source_used`、`source_fetch_id`、`content_hash` 等，但 FRED 观测行仅 `series_id`/`observation_date`/`value`；bundle 级指纹在包顶。下游若按 YAML row fields 校验会误报 FAIL。 | 二选一：（a）YAML 拆 `bundle_fields` vs `observation_fields`；（b）normalizer 观测行补 `source_used`（至少 mock/replay）。增 `test_r3h01_capabilityFields_matchPortOutput` 参数化六源。 |
| R3H01-A4-P1-02 | `tests/test_official_macro_adapters.py`（缺） · `cftc_cot_port.py` / `bis_port.py` / `world_bank_port.py` | **计划外 cap 负例缺口：** CFTC/BIS/World Bank 有 `MAX_ROWS`/`MAX_MARKETS` 等硬编码与 YAML `resource_caps`，但无 `capOverflow` pytest（对比 fred/treasury/sec 已覆盖）。                                                         | 为每源增 `*_capOverflow_blocksOverMaxRows` + factory 维 cap（markets/countries/indicators）测，对齐 `test_us_treasury_port_capOverflow_blocksOverMaxTenors` 模式。                      |
| R3H01-A4-P1-03 | `tests/test_official_macro_adapters.py`（缺） · `fred_port.py:200-201`                                    | **FRED `MAX_SERIES` 无负例：** `create_fred_fetch_port` 在 `len(series_ids)>10` 时 reject，无测试；registry `max_series: 10` 无法被 CI 钉死。                                                                                   | 增 `test_fred_port_capOverflow_blocksOverMaxSeries`；可选 YAML↔`MAX_SERIES` parity 测。                                                                                                 |
| R3H01-A4-P1-04 | `tests/test_official_macro_adapters.py:202` · `tests/test_sec_edgar_adapter.py:129`                       | **脆弱断言 — 未授权状态过宽：** 实现固定 `USER_AUTH_REQUIRED`（`fred_port.py:128`、`sec_edgar_port.py:159-160`），测试却 `status in {USER_AUTH_REQUIRED, FAILED}`，回归若误改为 `FAILED` 仍绿。                                 | 收紧为 `pytest.raises(PortError)` + `assert exc_info.value.status == "USER_AUTH_REQUIRED"` + 精确 message match。                                                                       |
| R3H01-A4-P1-05 | `official_macro.py:73-80`                                                                                 | **Reader 静默回填指纹：** `read_fred_evidence_bundle` 缺 `content_hash`/`source_fetch_id` 时写入 `"fred-unknown-hash"` / `"fred-unknown"`，损坏证据可伪装完整 bundle 通过只查 `observation_date` 的 smoke。                     | 生产路径 raise `OfficialMacroEvidenceError`；仅 legacy round-trip fixture 显式 opt-in 默认值。补 corrupt-bundle 负例。                                                                  |
| R3H01-A4-P1-06 | `tests/test_official_macro_adapters.py:915-937` · `823-849` · `724-749`                                   | **route DISABLED 断言不一致：** FRED/Treasury 断言 `selected_source_id is None` + `candidate.enabled is False`；world_bank/bis/cftc 部分仅 `route_status==DISABLED_SOURCE`，漏检「误选 primary」。                              | 六源 route 负例统一三断言；或删 per-port 重复，只保留 `test_r3h01_officialMacroRoute_disabledByDefault` 并扩展断言。                                                                    |

### Suggestion（P2 — 建议改进）

| ID             | 位置                                                               | 描述                                                                                                                                                    |
| -------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R3H01-A4-P2-01 | `tests/test_source_capabilities.py`（缺） · 各 `*_port.py`         | 无 R3H 版 `test_*_capsMatchRegistry`（对照 `test_tdxPytdx_capsMatchTaskCard`）；YAML `resource_caps` 与 port 常量漂移无 CI 锚。                         |
| R3H01-A4-P2-02 | `fred_port.py:45-50` 等                                            | Cap 拒绝统一 `PortError("FAILED", "...exceeds cap")`；`max_rows<=0` 消息语义不准；无 `RESOURCE_LIMIT` 类 status，与 route `USER_AUTH_REQUIRED` 不对称。 |
| R3H01-A4-P2-03 | `us_treasury_port.py:75-80` · `source_capabilities.yaml:475-483`   | Yield curve mock 行无 `source_used`；契约 fields 含 `source_used`/`source_fetch_id`/`content_hash`。                                                    |
| R3H01-A4-P2-04 | `test_official_macro_adapters.py` + `test_source_route_planner.py` | 六源 route DISABLED 逻辑重复 ~120 行；后续 registry 变更需双处改。                                                                                      |
| R3H01-A4-P2-05 | `contract_coverage.yaml:245`                                       | `SOURCE_CAPABILITY` 条目 `negative_required: false`；官方源 cap/未授权负例未纳入契约覆盖矩阵强制项。                                                    |

### Info（P3 — 可选）

| ID             | 位置                                      | 描述                                                                                 |
| -------------- | ----------------------------------------- | ------------------------------------------------------------------------------------ |
| R3H01-A4-P3-01 | `fred_port.py:121-123`                    | `MAX_WINDOW_DAYS` 有常量与注释，无独立负例或 YAML parity（`max_window_days: 120`）。 |
| R3H01-A4-P3-02 | `test_evidence_contract_livePayload_*:52` | `len(bundle["observations"]) == 3` 硬编码，fixture 变更易碎。                        |
| R3H01-A4-P3-03 | `ops/fred_fetch_ports.py`                 | Thin re-export 正确；旧 ops 测试仍覆盖，无质量回归。                                 |

---

## DOUBT（CLAIM → DOUBT → RECONCILE）

| CLAIM                                             | DOUBT                                                    | RECONCILE                                    |
| ------------------------------------------------- | -------------------------------------------------------- | -------------------------------------------- |
| AUDIT.plan A4 三项 FRED 负例已闭合                | 仅 `max_rows` cap；`MAX_SERIES`/`max_rows<=0` 未测       | **PARTIAL** — 主路径有；series 维 cap P1-03  |
| `source_capabilities.fields` 可机读校验 port 输出 | FRED/us_treasury 观测行缺 `source_used` 等               | **FAIL** — P1-01；需 YAML 分层或补字段       |
| 六源 cap 对称硬化                                 | cftc/bis/world_bank 无 cap pytest                        | **FAIL** — P1-02；实现有、测试无             |
| 未授权 fail-closed                                | 测允许 `FAILED` 备选                                     | **HOLDS（实现）** / **WEAK（测试）** — P1-04 |
| route 默认 DISABLED 不会被选 primary              | world_bank 等断言偏弱                                    | **PARTIAL** — P1-06                          |
| G10 证据无 bridge sidecar                         | `test_evidence_contract_livePayload` 断言无 sidecar 文件 | **HOLDS**                                    |
| registry READY 非 proposed_disabled               | `test_r3h01_officialMacroSources_*`                      | **HOLDS**                                    |

---

## 计划外发现

已对抗搜索：活卡 §7 caps、`source_capabilities.yaml` 六源 `resource_caps`/`fields`、`PortError` 状态枚举、`test_tdxPytdx_capsMatchTaskCard` 先例、`contract_coverage.yaml` negative 标记、plan 未写的 `MAX_SERIES`/`max_window_days`、reader 默认 hash、六源 route 断言对称性。

| ID             | 阻塞?        | 摘要                                                   |
| -------------- | ------------ | ------------------------------------------------------ |
| R3H01-A4-OO-01 | NON-BLOCKING | 三源 cap 负例缺失（P1-02）                             |
| R3H01-A4-OO-02 | NON-BLOCKING | capability fields 与 evidence bundle 层级混写（P1-01） |
| R3H01-A4-OO-03 | NON-BLOCKING | 无 R3H registry↔port cap parity 测（P2-01）            |
| R3H01-A4-OO-04 | NON-BLOCKING | `read_fred_evidence_bundle` 静默 unknown hash（P1-05） |

---

## What's Done Well

- FRED 三条 AUDIT.plan A4 核心负例（未授权、行 cap、route DISABLED）均有行为测且实现 fail-closed（`USER_AUTH_REQUIRED` / `PortError` / `DISABLED_SOURCE`）。
- 证据契约 G10：`official_macro_evidence_v1`、`observation_date` 端到端、无 bridge sidecar；Layer1/L5 smoke 绑定 replay 指纹。
- 测试普遍含五字段中文 purpose 注释，对齐 `GLOBAL_TESTING_POLICY`。
- `ops/fred_fetch_ports.py` 薄 re-export 至 L2 port，避免双实现漂移。
- SEC 双域（filings/Form4）+ identity header 负例 + filing/CIK cap 覆盖优于宏观三源。
- `test_r3h01_officialMacroRoute_disabledByDefault` 参数化六源，route 回归入口清晰。

---

## Verification Story

| 项               | 状态                                                                          |
| ---------------- | ----------------------------------------------------------------------------- |
| Tests reviewed   | yes — 49 项 A4/A8 子集全绿；已读 fred/treasury/sec 负例与 cftc/bis/wb 缺口    |
| Build verified   | yes — 上述 pytest exit 0                                                      |
| Security checked | partial — 未授权路径 fail-closed；reader 静默 hash 见 P1-05（A3 专审 SEC UA） |

---

## 计数与裁决

| 级别 | 数量 |
| ---- | ---- |
| P0   | 0    |
| P1   | 6    |
| P2   | 5    |
| P3   | 3    |

**Overall verdict:** **PASS_WITH_FIXES**（AUDIT.plan A4 FRED 对抗轴已覆盖；合并前建议闭合 P1-01–P1-06，尤其契约字段 parity 与三源 cap 负例）
