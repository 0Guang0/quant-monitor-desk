/**
 * Placeholder dashboard shell.
 * Layout and visual design are NOT final — user must confirm before production UI.
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Quant Monitor Desk</h1>
      <p>Local-first quantitative monitoring console (scaffold).</p>
      <p>
        Five layers: Regime → Cross-asset → Shock anchors → Markets → Evidence
      </p>
    </main>
  );
}
