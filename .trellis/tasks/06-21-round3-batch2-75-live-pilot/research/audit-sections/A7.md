# A7 Ops / Idempotency / Logging Adversarial Audit

## 结论

**FAIL（ops gate）**

A7 未发现 production DB 已被 live pilot 路径破坏：Phase 1/2/3/4 evidence 均记录 production DB hash/row-count unchanged，Phase 4 `allow_clean_write=false` / `clean_write_performed=false`，Request 2 未被 sidecar evidence 冒充为 pass。

但 A7 不能 PASS：幂等与失败可恢复证据不足，失败路径缺少结构化 failure artifact，Batch 2.75 计划中的 `routeNotReady` / sandbox fallback 对抗测试未完整落地，Phase 4.5 smoke/re-defer 的 ops 证据也未闭合。这些会影响重复运行、异常恢复、后续 Batch 3 引用边界与审计可观测性。

本审计只做只读核对，未修改业务代码、未提交、未写 `audit.report.md`。除本文件外未写其他产物。

## Ops Checklist

| 检查项                        | 判定                               | 证据与说明                                                                                                                                                                                                                                                                                                                       |
| ----------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production DB 不被默认写入    | PASS                               | `phase1_no_mutation_proof.md`、`phase2_route_preview_matrix.json`、`phase3_no_production_mutation_proof.md`、`phase4_no_production_mutation_proof.md` 均记录 production DB hash/row-count unchanged；`phase4_validation_report.json` 顶层 `allow_clean_write=false`、`clean_write_performed=false`。                             |
| Sandbox-only raw evidence     | PASS with caveat                   | `run_live_pilot_raw_only()` 使用 per-request sandbox root，并在 adapter fetch 后检查 `raw_file_paths` 必须位于 sandbox 下；实际 Phase 3 evidence 的三条 raw paths 均在 `.audit-sandbox/batch275-live-pilot/req-*` 下。Caveat：越界检查发生在 fetch 后，若 adapter 已写出异常路径，会抛错但不会生成结构化恢复 artifact。          |
| 授权 / HITL / route gate 顺序 | PASS with coverage gap             | `validate_authorization()`、`require_hitl_confirmation()`、`preview_live_pilot()` 在 fetch 前执行；route 非 READY 时 `LivePilotRouteNotReadyError` 阻断。实际 Batch 2.75 gate test 缺少计划中的 `test_livePilot_routeNotReady_blocksBeforeFetch`。                                                                               |
| ResourceGuard 可观测性        | PARTIAL                            | Phase 2/3 route preview evidence 包含 `resource_guard_decision` / `resource_guard_reason`，实际三请求均为 `OK`。但 ResourceGuard HARD_STOP 只抛 `RuntimeError`，没有 task-local failure artifact；Phase 4.5 smoke 未运行，且 closure test 不带 `--use-service-path`，无法证明 smoke 中 ResourceGuard 被 exercise。               |
| Raw fetch 日志 / 可观测性     | PASS for success, FAIL for failure | 成功路径包含 `content_hash`、`schema_hash`、`fetch_time`、`latency_ms`、`retry_count`、`row_count`、`run_id`、`raw_file_paths`，且 sandbox DB 内通过 adapter `record_fetch_log=True` 记录。失败路径没有统一 `try/except` 包装，fetch exception 会中断整个 `capture_phase3_raw_evidence()`，不会持久化 per-request failure JSON。 |
| 幂等 / 重复运行               | FAIL evidence gap                  | 代码会复用既有 sandbox DB，固定 `run_id` / `pilot_request_id`，并覆写 task evidence JSON/MD；未见 rerun proof 证明重复执行不会产生重复 sandbox registry/fetch_log 行或覆盖原始 raw evidence。production DB 安全性有证明，sandbox evidence 幂等性没有证明。                                                                       |
| clean write fail-closed       | PARTIAL                            | 默认 clean write 关闭且未执行。但 PH-B4 已指出 severe validation / conflict block 没有被 Phase 4 artifact 直接证明，且 Phase 4 未走 `DataQualityValidator` / `SourceConflictValidator` manifest 路径。                                                                                                                           |
| db inspector 只读与路径缺失   | PASS with caveat                   | `DbInspector` 使用 `ConnectionManager.reader()`，missing DB 返回 `FAIL`，symlink/outside data-root scan 被排除。missing data-root 被记录为 `exists=false` 但不是 hard FAIL；作为 read-only inventory 可接受，但不应被解释为 live sandbox 可用性证明。                                                                            |
| perf smoke / ops runbook      | FAIL                               | `production_equivalent_smoke.py` 能设置 isolated `QMD_DATA_ROOT`，但接受任意 `--data-root`；本轮 `phase45_perf_budget.json` 为 `RE_DEFERRED`，无 smoke log，`AUDIT_DEFERRED` registry row 不闭合，且 closure command 未限定到 A7 指定 `.audit-sandbox/r3b275-audit`。                                                            |

## 异常场景判定

| 场景                            | fail closed 判定                         | A7 说明                                                                                                                                                                                                                                                                    |
| ------------------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| route 非 READY                  | **代码 PASS / 证据 GAP**                 | `run_live_pilot_raw_only()` 在 fetch 前调用 `preview_live_pilot()`，`explicit_source_route_status != READY` 时抛 `LivePilotRouteNotReadyError`。但 `tests/test_batch275_live_pilot_gate.py` 未包含计划中的 route-not-ready batch-local test，也没有本轮 failure artifact。 |
| ResourceGuard HARD_STOP         | **代码 PASS / 可观测性 GAP**             | `preview_live_pilot()` 在 route preview 前检查 guard，HARD_STOP 抛错并停止。缺少结构化 `pilot_request_id` failure evidence，后续 operator 需要从异常而非 artifact 恢复。                                                                                                   |
| fetch 失败 / vendor unreachable | **production fail-closed / 恢复性 FAIL** | 一般 fetch exception 会中断 `capture_phase3_raw_evidence()`，不会写入 partial failure JSON 或 no-mutation proof；Request 2 的 Eastmoney failure 是通过补充 verdict/reconciliation 纠偏，而不是通用失败恢复机制。                                                           |
| sandbox 路径缺失                | **部分 PASS**                            | `_ensure_sandbox_db()` 会创建 sandbox DB 目录；路径不可写时会抛异常并停止，production DB 不应被写入。缺少结构化 failure artifact。                                                                                                                                         |
| raw path 越出 sandbox           | **代码 PASS / 时序 caveat**              | raw path 检查会在 fetch 后执行，发现越界会抛 `LivePilotAuthorizationError`。但检查发生在 adapter 已返回后，不是写入前拦截；若 adapter 已落盘越界文件，本路径只会报错，不会自动清理或记录恢复步骤。                                                                         |
| fixture / staged fallback       | **部分 PASS / 证据 GAP**                 | `_assert_live_fetch_port()` 拒绝 `StubFetchPort` 与 `LocalFixtureFetchPort`，测试只覆盖 `StubFetchPort`；计划中的 `test_livePilot_noFixtureFallbackSatisfiesEvidence` 未落地，Batch 2.75 缺少 LocalFixture/staged service 直接对抗测试。                                   |
| 重复运行 Phase 3                | **FAIL evidence gap**                    | production DB proof 表示安全；但 sandbox DB 复用、固定 run IDs、evidence file 覆写、fetch_log/file_registry 是否去重未被证明。                                                                                                                                             |
| Phase 4.5 smoke 路径错误        | **FAIL**                                 | script 接受任意 `--data-root` 并创建目录；本审计未运行 smoke。按用户限制，只能使用 `.audit-sandbox/r3b275-audit`，而 closeout/closure_test 指向 `.audit-sandbox/batch275-perf-smoke`。                                                                                     |

## 发现项

| 严重级别 | 发现                                                                                                                                                                       | 是否进 §4.3 | 理由                                                                                                       |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| HIGH     | Phase 3 通用 fetch failure 没有结构化 failure artifact；异常会中断 `capture_phase3_raw_evidence()`，无法保证 partial failure、no-mutation proof、rerun recovery 被持久化。 | 是          | A7 核心失败可恢复 / 日志对抗项；Request 2 依赖后补 reconciliation 才纠偏，说明通用机制不足。               |
| HIGH     | 幂等 / 重复运行未被证明：固定 run IDs、复用 sandbox DB、覆写 evidence JSON/MD，未见 rerun proof 证明 sandbox fetch_log/file_registry/raw evidence 不会重复或覆盖。         | 是          | 不破坏 production DB，但会破坏或污染 audit evidence 与 sandbox 状态，影响 ops recovery。                   |
| HIGH     | Phase 4.5 smoke/re-defer ops 证据不闭合：无 smoke log，`AUDIT_DEFERRED` row 缺失，closure command 不在 A7 指定 audit sandbox，且 ResourceGuard exercise 不成立。           | 是          | 直接影响 AUDIT.plan B5-2/B5-3 和 ops runbook 可信度。                                                      |
| MEDIUM   | Batch-local route-not-ready 测试缺失；代码静态 fail-closed，但没有计划中 `test_livePilot_routeNotReady_blocksBeforeFetch` 或 failure artifact。                            | 是          | 异常场景是 A7 明确要求项；generic route/service coverage 不能完全替代 Batch 2.75 gate。                    |
| MEDIUM   | Fixture fallback 对抗测试不完整：实现拒绝 Stub/LocalFixture，但 Batch 2.75 测试只覆盖 StubFetchPort，未覆盖 LocalFixture/staged service。                                  | 是          | live pilot 最重要的反模式之一；需要进入 §4.3 防止 staged evidence 冒充 live。                              |
| MEDIUM   | Phase 4 severe/conflict clean-write block 未被当前 artifact 证明。                                                                                                         | 是          | PH-B4 已判 FAIL；A7 继承其 ops 风险，因为 clean-write fail-closed 不能只靠默认 `allow_clean_write=false`。 |
| LOW      | production no-mutation proofs 多为 boolean，缺少具体 before/after hash 值。                                                                                                | 否          | 不阻断 A7 主结论；建议 A5/prod-path hash proof 使用具体 hash。                                             |
| LOW      | `DbInspector` missing data-root 不 hard FAIL，只记录 `exists=false` 后继续 DB inspect。                                                                                    | 否          | 作为 read-only inventory 可接受；仅提醒不要把它当作 live sandbox 可用性证明。                              |

## 命令运行限制

未运行 pytest、live fetch、prod-equivalent smoke 或任何会写 data-root 的动态命令。原因：用户要求需要运行命令时只能使用 `.audit-sandbox/r3b275-audit` 且不得写 `production/data`；现有 Phase 3/4.5 closure paths 指向 `.audit-sandbox/batch275-live-pilot` / `.audit-sandbox/batch275-perf-smoke`，动态复跑会写入非指定 audit sandbox，并可能触发网络/package/cache 写入。A7 因此做 read-only 判定。

本次实际执行的命令均为只读文件读取、`rg` 搜索与 MCP resource listing；唯一写入是本 A7 审计文件。

## GitNexus 可用性限制

`research/gitnexus-audit-summary.md` 记录本 Codex session 未暴露 GitNexus MCP resources；`node .gitnexus/run.cjs status` 在 network sandbox 下通过 `npx` 访问 npm registry 失败（`EACCES`）。本审计再次调用 MCP resource listing，结果为空。

因此 live `query()` / `impact()` / `detect_changes()` 不可用。A7 结论基于冻结 GitNexus index 摘要、源码静态读取、Execute evidence、PH-B0–PH-B6 审计产物与策略文件；任何依赖 fresh graph traversal 的判断均未被当作 green evidence。

## Doubt-Driven Note

CLAIM: A7 必须 FAIL，因为 production mutation 虽被控制，但 ops/idempotency/failure-observability gates 未闭合。

Adversarial self-check（degraded, no fresh subagent/tool available）重点反驳点：是否应因 production DB 不变而 PASS？结论：不应。A7 的 contract 明确包含幂等、日志/可观测性、失败可恢复、重复运行与异常场景 fail-closed；当前缺口均在这些维度内，并且至少三项应进 §4.3。Cross-model review 未运行，因为本任务禁止额外动态/外部命令且 GitNexus/live MCP 不可用。
