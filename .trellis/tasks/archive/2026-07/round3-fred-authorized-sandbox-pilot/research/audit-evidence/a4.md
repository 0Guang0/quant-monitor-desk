# A4 — audit-quality（code-reviewer）

**维度：** A4 · code-review-and-quality + doubt-driven-development  
**任务：** `round3-fred-authorized-sandbox-pilot` (B01-FRED)  
**审计范围：** `backend/app/ops/fred_*.py` · `backend/app/datasources/service.py` (+1) · registry/capability/matrix proposed delta · `tests/test_fred_*.py` · `tests/test_fred_staged_semantics.py` 扩展  
**权威对照：** R3E 任务卡 §6–§8 · `datasource_service_contract` fetch 路径 · MASTER §2 AC-FRED-01..07 · `BATCH_01_HARDENING_RULES.md` §3  
**判定：** **PASS**  
**OPEN：** **0** · **BLOCKING：** **0**

---

## Review Summary

**Verdict:** PASS

**Overview:** FRED sandbox pilot 实现体量克制（`fred_sandbox_pilot` / `fred_fetch_ports` / `fred_evidence_validator` 三文件），复用 `SourceRegistry` + `SourceRoutePlanner` + `FetchPort` 邻接模式；失败分类枚举与 R3E 一致；pilot-local health 未侵入 `data_health.py` 主体。测试五字段 docstring 齐全，mock/授权/closeout 路径有业务安全断言。对抗性搜索发现的 orchestration 层边界缺口均在 port 层或 opt-in 范围内闭合，不构成 merge 阻断。

---

## 多轴审查（§3.4）

| 轴              | 发现                                                                                                                                | 阻塞? | 证据                                                                                               |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------- |
| 正确性          | P0 白名单 + ≤5 series cap；route preview `sandbox_only=True` · `production_clean_write=False`；closeout `b2_5_o_05_closed` 恒 false | 否    | `fred_sandbox_pilot.py:94-101,120-160,319-336` · `test_fredRoutePreview_*` · `test_fredCloseout_*` |
| 正确性          | Mock manifest 含 `source_fetch_id` / `content_hash` / `as_of_timestamp`；缺 `source_fetch_id` → `FAIL_SCHEMA`                       | 否    | `fred_sandbox_pilot.py:201-207` · `test_fredMockFetch_*`                                           |
| 正确性          | `macro_supplementary` / akshare 证据不得关闭 B2.5-O-05                                                                              | 否    | `build_pilot_closeout` · `test_macroSupplementary_cannotCloseB250o05` · `fred_pilot_closeout.json` |
| 错误处理        | 缺授权 / 缺 API key → `FRED_PILOT_FAIL_AUTH`（fail-closed）                                                                         | 否    | `fred_sandbox_pilot.py:104-118,247-254` · `test_fredPilot_missingAuth_*`                           |
| 错误处理        | network/schema/validation 失败有显式枚举（非静默 PASS）                                                                             | 否    | `run_failure_scenario` · `test_fredPilot_failureTaxonomy_*`                                        |
| 错误处理        | orchestration `json.loads` 未捕获 `JSONDecodeError`（见 DOUBT）                                                                     | 否    | **CLOSED** — port 层已处理；见下文                                                                 |
| 可维护性        | 三模块边界清晰；未改 `data_health.py`；`service.py` 仅补 `macro_series` default op 映射                                             | 否    | diff `service.py:297`；`rg data_health` fred 包无引用                                              |
| 可维护性        | mock/live manifest 构建有重复（~15 行）                                                                                             | 否    | A2 范畴；当前可读性可接受                                                                          |
| 风格 / ponytail | 无新依赖；stdlib `urllib` live fetch；`ponytail:` 标注 3y 窗口粗算天花板                                                            | 否    | `fred_fetch_ports.py:72-74`                                                                        |
| 测试            | 五字段中文 docstring 齐全；拒绝 tautological；mock 隔离网络                                                                         | 否    | `tests/test_fred_*.py` 全模块                                                                      |
| 测试            | FRED-07 live opt-in：`skipif` + `CLOSED-SKIP-OPT-IN` 闭合                                                                           | 否    | `test_fred07_*` · `b01-fred-audit-closures.md`                                                     |
| 安全（局部）    | 无密钥进 diff；`allow_production_clean_write` 授权门禁；API key 仅 env                                                              | 否    | `fred_sandbox_pilot.py:113-117` · `fred_fetch_ports.py:19-20`                                      |
| 安全（局部）    | `yaml.safe_load` 读 authorization；路径 resolve 相对 PROJECT_ROOT                                                                   | 否    | `fred_sandbox_pilot.py:77-91`                                                                      |
| 性能            | 受控小样本；无 DuckDB/无界 I/O（A6 SKIP 合理）                                                                                      | 否    | caps 5/100/10                                                                                      |
| Registry 语义   | fred `enabled_by_default=false`；capability 无 `production_default`                                                                 | 否    | `test_fredRegistry_*` · proposed yaml delta                                                        |

---

## Code review checklist

- [x] 无 P0 逻辑/安全阻塞
- [x] 错误处理可观测（FAIL\_\* 枚举 + pytest）
- [x] 风格与邻近模块一致（staged_pilot fetch port / route planner 模式）
- [x] 测试变更保留 purpose（中文五字段）
- [x] 判定基于 diff 与 pytest，非覆盖率 KPI 自述

---

## DOUBT（错误处理 / 边界）

对抗搜索范围：`fred_sandbox_pilot.py` · `fred_fetch_ports.py` · `fred_evidence_validator.py` · 对照 R3E §6 状态枚举与 staged_pilot fetch port 错误模型。

| ID          | 位置                               | 疑点                                                                   | 结论                                                                                                                                  | Audit status               |
| ----------- | ---------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| A4-DOUBT-01 | `fred_sandbox_pilot.py:199,283`    | `json.loads(payload.content)` 未映射 `JSONDecodeError` → `FAIL_SCHEMA` | `FredLiveFetchPort` 已在 `fred_fetch_ports.py:98-99` 将 `JSONDecodeError` 转为 `PortError`；mock port 恒生成合法 JSON；live 为 opt-in | **CLOSED**                 |
| A4-DOUBT-02 | `fred_sandbox_pilot.py:283-288`    | live 路径未像 mock（201-207）显式校验 `source_fetch_id`                | live port 在 `fred_fetch_ports.py:117-124` 恒设 `source_fetch_id`；缺字段会 `KeyError` 仅影响 opt-in live，CI 默认 skip               | **CLOSED**                 |
| A4-DOUBT-03 | `fred_sandbox_pilot.py:191-192`    | `FAIL_SCHEMA` 靠 `"schema" in str(exc)` 子串匹配                       | 仅 mock fetch `PortError` 分支；FRED-04 显式 taxonomy 走 `run_failure_scenario`，不依赖该启发式                                       | **CLOSED**（NON-BLOCKING） |
| A4-DOUBT-04 | `fred_evidence_validator.py:70-71` | stale 检查假定 `rows[0]` 为最新观测                                    | mock/live port 均 `sort_order=desc` / 按 offset 降序生成；未排序输入为 pilot 外契约                                                   | **CLOSED**（NON-BLOCKING） |
| A4-DOUBT-05 | `fred_fetch_ports.py:34`           | mock 最多 3 行，与 `max_rows` cap 100 不一致                           | ponytail 天花板：mock 仅需 lineage 字段证据；非 live 容量契约；测试断言字段存在而非行数                                               | **CLOSED**（NON-BLOCKING） |

**显式非发现（已对抗确认）：**

- `data_health.py` 主体 — forbidden；`test_ops_data_health.py` 回归绿（37+1 skip 批次含本模块）。
- production clean write — 全路径 `production_clean_write=False`。
- 静默 PASS — 缺授权 / 失败场景均有显式 status 断言。
- registry 三件套直接 commit — worktree yaml 为 proposed delta；coordinator Track A #3。

---

## 计划外发现

> 对抗性搜索：对照 R3E Red Flags · hardening §3 授权 · MASTER §3.3 forbidden · `rg production_live|clean_write|data_health` on fred diff。

| ID           | 严重度       | 发现                                                  | 处置                                                                                        | Audit status                 |
| ------------ | ------------ | ----------------------------------------------------- | ------------------------------------------------------------------------------------------- | ---------------------------- |
| A4-FRED-07   | NON-BLOCKING | FRED-07 live 无 `FRED_API_KEY` 时 slice 可能被标 OPEN | `b01-fred-audit-closures.md` + `test_fred07_liveFetch_closureClosedSkipOptIn_withoutApiKey` | **CLOSED-SKIP-OPT-IN**       |
| A4-B250-O05  | NON-BLOCKING | B2.5-O-05 需书面 re-defer + closure test              | `b01-fred-audit-closures.md` + `test_b250o05_reDeferred_closureRowClosed` + closeout JSON   | **CLOSED**                   |
| A4-ORCH-JSON | NON-BLOCKING | orchestration 层 `json.loads` 无 try/except           | port 层已 fail-closed；mock 路径全绿；见 A4-DOUBT-01                                        | **CLOSED**                   |
| A4-UNCM      | NON-BLOCKING | worktree 变更未 commit 到分支                         | Audit 验功能；合并 gate 主会话                                                              | **CLOSED**（Audit 范围已验） |

**对抗搜索声明：** 已对照设计文档（R3E §6–§8）、契约（fetch port / registry）、任务卡 Red Flags、MASTER §2 AC 矩阵；**未**将 MASTER §8 用例表当作完备性上限。除上表 CLOSED 项外无新增 OPEN。

---

## AC-FRED 代码质量链（MASTER §2）

| AC         | 代码/测试证据                                           | 结果                   |
| ---------- | ------------------------------------------------------- | ---------------------- |
| AC-FRED-01 | registry tests + yaml `enabled_by_default=false`        | PASS                   |
| AC-FRED-02 | `build_route_preview` + whitelist reject                | PASS                   |
| AC-FRED-03 | `run_mock_fetch` manifest fields + hash                 | PASS                   |
| AC-FRED-04 | `FredPilotStatus` enum + failure parametrize            | PASS                   |
| AC-FRED-05 | `validate_fred_evidence_health` stale/missing/malformed | PASS                   |
| AC-FRED-06 | `build_pilot_closeout` + staged semantics               | PASS                   |
| AC-FRED-07 | authorization.yaml + FAIL_AUTH without key              | **CLOSED-SKIP-OPT-IN** |

---

## What's Done Well

- **最小 orchestration：** 三文件 ~500 行，无框架膨胀；复用 registry/route planner/fetch port 而非新 ingestion 管线。
- **失败模型完整：** 七种 `FRED_PILOT_*` 状态与 R3E 枚举一一对应；`run_failure_scenario` 供 evidence 落盘。
- **B2.5-O-05 语义硬编码：** `b2_5_o_05_closed` 恒 false + `macro_supplementary_cannot_close=true`，防止 Request 3 误读。
- **测试纪律：** 五字段中文 docstring；closeout/live-skip 测试直接锚定 `b01-fred-audit-closures.md` 零 OPEN 策略。
- **边界干净：** 无 `data_health.py` 主体；`macro_series` service 映射一行；sandbox 写路径隔离在 `.audit-sandbox/`。

---

## Verification Story

| 项               | 结果                                                                                                                                                                                         |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tests reviewed   | **是** — `test_fred_source_registry` · `test_fred_sandbox_pilot` · `test_fred_staged_semantics` · `test_ops_data_health` 五字段齐全                                                          |
| Build verified   | **是** — `uv run pytest tests/test_fred_source_registry.py tests/test_fred_sandbox_pilot.py tests/test_fred_staged_semantics.py tests/test_ops_data_health.py -q` → **37 passed, 1 skipped** |
| Security checked | **是** — 无密钥泄露；授权 fail-closed；`allow_production_clean_write` 拒绝                                                                                                                   |
| Diff reviewed    | **是** — staged + unstaged fred 交付物与 a1 manifest 一致                                                                                                                                    |

---

## 汇总

| 类别                        | 数量                                           |
| --------------------------- | ---------------------------------------------- |
| BLOCKING                    | **0**                                          |
| OPEN (含 NON-BLOCKING)      | **0**                                          |
| CLOSED / CLOSED-SKIP-OPT-IN | **5**（DOUBT 表）+ **4**（计划外表，含交叉项） |

**A4 verdict：PASS** — 可继续 A5（audit-completion）。
