# A2 — Ponytail

**Result:** PASS

> **Task:** `06-23-round3-020-layer3-loader`  
> **Scope:** `backend/app/layer3_chains/**`（对照 `backend/app/layer2_sensors/sensor_loader.py`）  
> **Skills:** ponytail-review + doubt-driven-development（梯级 1–7 内联评估）

---

## 概览

`layer3_chains` 为 staged-only 五表 loader，整体形态对齐 019 `sensor_loader.py`（常量默认路径、`staged_fixture_only` gate、`ValueError` 子类、frozen dataclass result）。**无** factory/registry/plugin 层；`_load_json_list` / `_resolve_manifest_file` 为合理复用而非新框架。净增 ~357 LOC（`loader.py` 265 + `models.py` 64 + `__init__.py` 28）与 AC-020 五表 + contract 硬校验体量相称。存在若干可选收缩点（死常量、边引用校验重复、测试样板重复），**均不阻塞** finish-work / §4.3。

---

## git diff --stat（记录）

| 路径 | 状态 | 行数（审阅时读码） |
| ---- | ---- | ------------------ |
| `backend/app/layer3_chains/loader.py` | 新增 | 265 |
| `backend/app/layer3_chains/models.py` | 新增 | 64 |
| `backend/app/layer3_chains/__init__.py` | 新增 | 28 |
| **layer3_chains 小计** | | **~357 net** |

注：Audit 沙箱内 `git diff`/`git diff --stat` 未返回可用输出；上表为对 worktree 源文件逐读计数。分支信封：`feature/round3-020-layer3-loader`（`audit-dispatch.md`）。

---

## A2 checklist

- [x] `git diff --stat` 已记录（手动 LOC；git 命令不可用见上）
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 交叉引用（边引用重复校验，见 A2-02）
- [x] 阻塞 vs 建议已区分

---

## DOUBT

**≥1 处 ≥20 行可简化？** **是。**

| 位置 | 估算 | 说明 |
| ---- | ---- | ---- |
| `loader.py:69-96` | ~28 → ~14 | 普通 edge 与 cross-chain edge 的 `from_node_id`/`to_node_id` 存在性校验结构相同 |
| `tests/test_layer3_loader.py:65-107` | ~43 → ~22 | 四个 duplicate* 用例共享 `_copy_bundle` + 变异 + `pytest.raises` 样板 |

搜索范围：已读 `backend/app/layer3_chains/*.py`、`tests/test_layer3_loader.py`、`sensor_loader.py` L60–97/220–255、`MASTER.plan.md` §6.1/§8、`layer3_loader_contract.yaml`。

---

## §3.2 候选删改表

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
| --------------------- | ------------- | -------- |
| `loader.py:22-28` 未引用常量 `_MANIFEST_FILE_KEYS`（死代码；manifest 路径由各 `_load_*` 字面量传入） | 梯级 1（YAGNI — 不必建） | 建议 |
| `loader.py:69-96` 边端点引用校验对 `edges` / `cross_chain_edges` 复制两段循环；可统一为 `for coll, label in ((edges,"edge"),(cross_chain_edges,"cross-chain edge")):` + 单循环 | 梯级 2（复用同一校验形态；Execute 8.5 已记「no new helper」— 内联合并优于新函数） | 建议 |
| `loader.py:207-243` `_load_edges` 与 `_load_cross_chain_edges` 同构（json list → require `edge_id` → dataclass）；可内联共享 `for item in _load_json_list(...)` 骨架或抽 **模块内** `_require_edge_id` 一行函数（非 factory） | 梯级 2（对齐 `sensor_loader._parse_asset` 单入口思路，但五表 AC 允许分函数） | 建议 |
| `tests/test_layer3_loader.py:65-107` 四个 duplicate 测试可 `@pytest.mark.parametrize` 合并（purpose 不变：各测唯一性字段） | 梯级 2（复用 `_copy_bundle` / `_write_json`） | 建议 |
| `models.py` 六 dataclass + `IndustryChainLoadResult` | — | **不算**（MASTER §8.1 / AC-020-1 显式要求） |
| `IndustryChainLoader` 类 + `load()` | — | **不算**（对齐 `CrossAssetRegistryLoader` §6.1） |

---

## Findings（ID | Sev | Finding | §4.3?）

| ID | Sev | Finding | §4.3? |
| -- | --- | ------- | ----- |
| A2-01 | Suggestion | `loader.py:22-28` — `_MANIFEST_FILE_KEYS` **从未引用**（rg 仅定义处命中）。删 7 行或改为 manifest 完整性自检时再用。 | No |
| A2-02 | Suggestion | `loader.py:69-96` — 边 / 跨链边 **重复** `node_ids` 成员检查（~28 行，最大生产侧 ponytail 块）。合并循环可净减 ~14 行；与 A4「校验链一致性」同址，勿改错误文案。 | No |
| A2-03 | Suggestion | `loader.py:207-243` — `_load_edges` / `_load_cross_chain_edges` 结构重复（各 ~17 行）。字段差异仅 `chain_id` vs `from_chain_id`/`to_chain_id`；可共享 `edge_id` 校验而不引入泛型 parser。 | No |
| A2-04 | Suggestion | `tests/test_layer3_loader.py:65-107` — 四个 duplicate* 测试 **setup 膨胀**（≥20 行候选）；parametrize 可减 ~20 行且不改测试 purpose。 | No |
| A2-05 | Suggestion | `loader.py` 全文件无 `ponytail:` 天花板注释；§0.3a 要求有意简化处标注（如五表分函数、未抽 edge helper）。Execute 证据有 self-check，源码可补一行 `ponytail: five registry loaders; no generic parser`。 | No |

**§4.3 计数（A2）：0** — 均为可选收缩，无阻塞型过度工程。

---

## 与 sensor_loader 对照（ponytail 梯级 2）

| 019 模式 | 020 对齐 | 判断 |
| -------- | -------- | ---- |
| `STAGED_REGISTRY_FIXTURE` + `mode != staged_fixture_only` gate | `STAGED_LAYER3_BUNDLE_DIR` + manifest `loader_mode` gate（§6.1 有意差异） | 合理 |
| 单 YAML `assets` 列表 + `_parse_asset` | 五文件 + `_load_json_list` + 分表 `_load_*` | AC 驱动，非过度抽象 |
| `_assert_unique_instrument_ids` | `_assert_unique` 泛化版 | 更短，可接受 |
| `CrossAssetRegistryWriter`（DB） | **无** writer | 正确 YAGNI（020 无 DB 写） |

---

## What's done well

- **Staged-only 边界清晰**：manifest gate 与 019 L72–75 语义一致，未引入 production registry 路径。
- **`_load_json_list` / `_resolve_manifest_file`**：JSON 四表共享读取与 fail-fast，避免五份复制粘贴式 `open/read/parse`。
- **`models.py`** 仅 frozen dataclass，无行为类/继承树。
- **测试** docstring 含 purpose/target/verifies/failure_meaning，符合 testing-guidelines；未为 loader 造 mock 框架。

---

## Verification story

| Check | Result |
| ----- | ------ |
| Tests reviewed | **Yes** — `tests/test_layer3_loader.py` 13 用例覆盖 AC-020-1..5；意图与 contract 一致 |
| Build verified | **No**（A2 review-only） |
| Security checked | **N/A**（A3） |
| GitNexus live | **未执行**（Audit 只读；Execute 已有 `research/gitnexus-execute-summary.md`） |

---

## 具体 refactor 建议（Audit 不落地）

1. **A2-01：** 删除 `_MANIFEST_FILE_KEYS`（−7 LOC）。
2. **A2-02：** 将 `loader.py:69-96` 合并为单循环遍历 `(edges, "edge")` 与 `(cross_chain_edges, "cross-chain edge")`（−~14 LOC）。
3. **A2-03 / A2-04：** 边加载函数共享 `edge_id` 校验；测试 duplicate 块 parametrized（合计可再 −~25–35 LOC）。

**估算净收缩（若 A2-01–04 全做）：** ~45–55 LOC（~12–15% of layer3_chains 包）。
