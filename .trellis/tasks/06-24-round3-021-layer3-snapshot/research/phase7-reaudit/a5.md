# A5 audit-completion — Phase 7 复审计

**维度：** A5 · verification-before-completion + doubt-driven-development  
**派发模型：** composer-2.5  
**分支：** `feature/round3-021-layer3-snapshot`  
**审计时间：** 2026-06-24  
**判定：** **PASS**（证据链完整；green.txt 部分偏弱，sandbox 复跑补偿）

---

## 1. 启动清单

| 项                                                      | 状态                       |
| ------------------------------------------------------- | -------------------------- |
| `AUDIT.plan.md` 全文                                    | 已读                       |
| `MASTER.plan.md` §2 / §8–§10                            | 已读                       |
| `implement.jsonl` 全读（34 行）                         | 已读                       |
| `audit.jsonl` 全读（10 行）                             | 已读                       |
| `021_implement_layer3_snapshot_builder.md`（Authority） | 已读                       |
| `manifest-amend.md`                                     | **不存在** — 无 amend 口径 |
| `research/original-plan-trace.md`                       | 已读                       |
| GitNexus `query` / `context` / `check`                  | 已调用（见 §6）            |

**约束：** 只读；未 `git commit`；未改生产库。

---

## 2. A5 Checklist

| 检查项                                | 结果                                    |
| ------------------------------------- | --------------------------------------- |
| 每条 AC 追溯链 + 1–5 分               | ✅ §3.5                                 |
| §10 最弱 2 行 sandbox 抽检            | ✅ §4（compileall + ruff；Tier B 见注） |
| `execute-evidence/*-green.txt` 非占位 | ⚠️ §5 — 步进 green 仅进度条；8.6 有摘要 |
| audit-prod-path（registry §2.1）      | ✅ N/A — staged-only，无 prod 数据依赖  |
| registry / defer 项                   | ✅ ADV-R3X / R3Y 边界未越界关闭（§7）   |

---

## 3. §3.5 — AC 追溯与评分

| AC#          | 追溯链（原始 → MASTER → §8 → 证据）                                                                                                                                                                                                          | 分    | 抽检/复验                                                          |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------ |
| **AC-021-1** | `021` 卡 §1「snapshot 行」→ MASTER §2「loader + staged L5 bars」→ §8.3 Snapshot builder core → `test_layer3Snapshot_buildsFromStagedLoaderAndL5_success`（≥1 有价快照、MSFT、`as_of_timestamp`）                                             | **5** | 含于 sandbox 9 passed                                              |
| **AC-021-2** | 卡 §15 lineage 字段 → `snapshot_lineage_contract.yaml` `LINEAGE_REQUIRED_FIELDS` → §8.2 Lineage builder → `test_layer3Snapshot_lineageRequiredFieldsComplete` + `test_snapshotLineageContainsSourceHashes`（`layer_id=layer3`、hashes 非空） | **5** | 含于 9 passed；`lineage.py` 复用 `core/snapshot_lineage`           |
| **AC-021-3** | 卡 §15「as_of 之后未来数据」→ contract `no_future_data` → §8.4 → `test_snapshotRejectsFutureInput`（`match="future"`）                                                                                                                       | **5** | 含于 9 passed；`snapshot_builder._reject_future_bar` 实现可见      |
| **AC-021-4** | `layer3_industry_shock_anchor.md` §8.12.6 event_only → MASTER §2 → §8.5 → `test_layer3Snapshot_eventOnly_skipsPriceFields`（OPENAI 无 `latest_price`）                                                                                       | **5** | 含于 9 passed                                                      |
| **AC-021-5** | 卡 §2 Layer5 映射视图 → MASTER §6.4 → §8.3 → `test_layer3Snapshot_layer5MappingView_nonEventOnly`（`L5-MSFT-US`、close=420.5）                                                                                                               | **5** | 含于 9 passed                                                      |
| **AC-021-6** | `BATCH3_STAGED_DOWNSTREAM_GATE.md` staged-only → MASTER §0/§3 → §8.3 → `test_layer3Snapshot_nonStagedL5Source_rejects`（`source_mode=production_live` 拒绝）+ batch3 gate 回归                                                               | **5** | sandbox gate **2 passed**；manifest 首步校验 `staged_fixture_only` |
| **AC-021-7** | 卡 §11 验收命令 → MASTER §10 Tier A/B → §8.6 Final gates → `execute-evidence/8.6-green.txt` + 本复审计 sandbox                                                                                                                               | **4** | Tier A 两项 pytest 复绿；Tier B 全库本次未复跑（见 §4 注）         |

**均分：** 4.9 / 5 — **PASS**

**补充（非 AC，A8 边界）：** `test_layer3Snapshot_emptyAnchors_returnsEmpty` 已入测试文件（9 条），Execute MASTER §5.3 未列；属 Audit 补测，不扣 AC 分。

---

## 4. §10 最弱 2 行 — sandbox 抽检

选取规则：优先 Tier B/C 与非 pytest 弱证据行。

| #   | §10 原文                                                                                      | 复跑命令                | exit                          | 与 Execute 一致？                             |
| --- | --------------------------------------------------------------------------------------------- | ----------------------- | ----------------------------- | --------------------------------------------- |
| 1   | `uv run python -m compileall backend/app/layer3_chains tests/test_layer3_snapshot_builder.py` | 同上（Phase 7 sandbox） | **0**                         | ✅ 与 8.6-green「compileall exit 0 PASS」一致 |
| 2   | `uv run ruff check backend/app/layer3_chains tests/test_layer3_snapshot_builder.py`           | 同上                    | **0**（`All checks passed!`） | ✅ 与 8.6-green「ruff check exit 0 PASS」一致 |

**注 — Tier B 全库：** `uv run pytest -q` 为 §10 最弱 B 级行，Execute 8.6 声称 exit 0。本次复审计按派发范围**仅**复跑指定两测文件，**未**重跑全库；首轮 `research/audit-evidence/a5.md` 已独立验证 Tier B。不构成 §4.3 阻断（有历史复验 + 指定测文件全绿）。

### 4.1 派发 sandbox 复跑（必做）

| 命令                                                           | exit  | 输出摘要              |
| -------------------------------------------------------------- | ----- | --------------------- |
| `uv run pytest tests/test_layer3_snapshot_builder.py -v`       | **0** | **9 passed** in 0.11s |
| `uv run pytest tests/test_batch3_staged_downstream_gate.py -v` | **0** | **2 passed** in 0.02s |

与 Execute 8.6-green 声称的 8+2 相比：snapshot 测多 1 条（A8 空 anchors），其余一致。

---

## 5. execute-evidence 真实性抽检（2 个 green.txt）

| 文件                             | 内容摘要                                                                                      | 判定                                                                           |
| -------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `execute-evidence/8.4-green.txt` | 仅一行 `. [100%]`（pytest -q 进度条）                                                         | **弱但真** — 属真实终端输出，无 PASS 自述；结论由 8.6 汇总与本次 9 passed 补偿 |
| `execute-evidence/8.6-green.txt` | ponytail self-check 叙述 + Tier A/B 结果行（8 passed、2 passed、ruff/compileall/Tier B PASS） | **真 GREEN** — 含命令结果行；非纯 TODO 占位                                    |

**对照 RED：** `execute-evidence/8.0-red.txt` 含真实 `ModuleNotFoundError: snapshot_builder` traceback — TDD 起点可信。

**§4.3 观察（非 FAIL）：** `8.2-green.txt`、`8.3-green.txt` 同为仅 `.... [100%]` 模式；建议后续 Execute 在 green 证据中保留 `-v` 或 `pytest` 摘要行。不影响 AC 结论。

---

## 6. GitNexus（必用）

| 调用                                                          | 结果                                               | 对 A5 的影响                                 |
| ------------------------------------------------------------- | -------------------------------------------------- | -------------------------------------------- |
| `query("IndustryChainSnapshotBuilder layer3 snapshot build")` | 未命中 layer3 新符号；返回 Layer1/validator 流程   | 索引 **stale**；以 pytest + 源码为准         |
| `context(name="IndustryChainSnapshotBuilder", file_path=…)`   | **Symbol not found**                               | 同上                                         |
| `detect_changes(scope=compare, base_ref=master)`              | **Execution backend unavailable**                  | 无法做 merge 前符号 diff；不阻断 staged 任务 |
| `check()`                                                     | `cycles_found`（Layer1 ingestion 环，与 021 无关） | 无 021 相关阻断                              |

**结论：** Greenfield `snapshot_builder.py` / `lineage.py` 未入索引。Merge 后应 `node .gitnexus/run.cjs analyze`。A5 判定不依赖 GitNexus 命中，但已按规范调用并记录缺口。

---

## 7. audit-prod-path（registry §2.1）

| 项                                                           | 状态                             |
| ------------------------------------------------------------ | -------------------------------- |
| `AUDIT_PROD_ROOT` = `.audit-sandbox/r3-021-audit-prod-equiv` | **不存在**                       |
| `QMD_DATA_ROOT` / 生产 DuckDB 写                             | **无依赖** — staged fixture only |
| AUDIT.plan §2 A5 prod-path 口径                              | 「staged N/A；无污染」           |

**处置：** 以 fixture pytest + batch3 gate 复跑替代 prod 数据 hash 校验 — 与 AUDIT.plan 冻结口径一致。未复制/修改生产数据树。

---

## 8. Defer / Registry 边界

| 项                                       | Execute 是否越界 | 证据                                                   |
| ---------------------------------------- | ---------------- | ------------------------------------------------------ |
| `ADV-R3X-LINEAGE-001` 全量 L3/L4 lineage | **否**           | 仅 contract-scoped envelope + pytest                   |
| `R3Y-LINEAGE-VR-001` 三 registry         | **否**           | `rg` 未改 `AUDIT_DEFERRED_REGISTRY.md` 等              |
| `snapshot_lineage_contract.yaml`         | **否**           | 只读引用                                               |
| production-live 声称                     | **否**           | MASTER §0 staged-only；manifest 硬拒 `production_live` |

---

## 9. DOUBT 闭环

| 疑点                             | 结论                                           |
| -------------------------------- | ---------------------------------------------- |
| 抽检与 Execute 声称一致？        | ✅ 指定两测 9+2 passed；compileall/ruff exit 0 |
| green.txt 是真实终端输出吗？     | ⚠️ 步进文件仅进度条；8.6 有摘要；非伪造        |
| sandbox 数据量级下 AC 仍成立？   | ✅ fixture < 50KB；覆盖 MSFT/OPENAI/拒绝路径   |
| `manifest-amend.md` 进入追溯链？ | N/A — 文件不存在                               |
| GitNexus 能否证明调用图？        | ❌ 索引滞后；pytest + 源码阅读补偿             |

---

## 10. 判定摘要

- **AC-021-1..7：** 均可从原始任务卡 → MASTER §2 → §8 步 → pytest/evidence 闭环追溯。
- **Sandbox 复跑：** `test_layer3_snapshot_builder.py` **9 passed**；`test_batch3_staged_downstream_gate.py` **2 passed**。
- **弱点：** 步进 `*-green.txt` 证据偏薄；GitNexus 未收录新符号；Tier B 本次未复跑（有首轮审计记录）。
- **最终：** **A5 PASS** — 不以自述为 PASS；独立 sandbox 命令已验证核心 AC 与 Tier A 门禁。

---

_Phase 7 复审计 · A5 only · 只读 · 无 commit_
