# A2 — audit-ponytail（ponytail-review）

**维度：** review-only · **判定：** PASS（0 阻塞候选）

## diff 统计

```
backend/app/ops/mutation_proof.py |  66 ++
backend/app/ops/staged_pilot.py   | 831 +++++++++++++++++
tests/test_staged_pilot.py        | 488 +++++++++++++++++
3 files changed, 1379 insertions(+), 6 deletions(-)
```

## ponytail 梯级评估

| 区域 | 净增 | 梯级 | 结论 |
| ---- | ---- | ---- | ---- |
| `mutation_proof.py` `all_table_row_counts` + `_derive_proof_status` | ~60 | 2（复用 `key_table_row_counts` 模式） | AC-MUT-001 要求；不可删 |
| `staged_pilot.py` v2 常量 + 9 捕获函数 | ~700 | 6（MASTER explicit AC 产物） | 九切片各一函数，无多余 factory |
| `test_staged_pilot.py` v2 块 | ~488 | 6（§5.3 冻结用例） | purpose 中文齐全 |

## ≥20 行候选删改

| 候选 | 梯级 | 阻塞? |
| ---- | ---- | ----- |
| 无 | — | — |

**DOUBT 搜索范围：** `staged_pilot.py:1310-2074`、`mutation_proof.py` 全文；未发现单处可删 ≥20 行而不损 AC。

## 交叉引用 A4

v2 函数命名与 v1 `capture_*` 对称，无重复错误处理样板需合并。
