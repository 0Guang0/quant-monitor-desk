# A2 — audit-ponytail（ponytail-review）

**维度：** review-only · **模型：** composer-2.5  
**任务：** `06-25-round3-real-data-staged-pilot-v3`（B01-SP3 / B01-C04）  
**分支：** `feature/round3-real-data-staged-pilot-v3` @ `../quant-monitor-desk-wt-b01-sp3`  
**范围：** `backend/app/ops/staged_pilot.py` **v3 段**（`L2146–L2937`，`master...HEAD` diff）  
**日期：** 2026-06-25  
**模式：** Audit（只读；无 commit、无改码）

---

## 总判定

| 项 | 值 |
| --- | --- |
| **判定** | **PASS** |
| **BLOCKING** | 0 |
| **NON-BLOCKING 建议** | 3 |
| **net 可删行** | **约 −35～−50 行**（非必须；见 §3.2） |

---

## diff 统计

```
 backend/app/ops/staged_pilot.py | 805 +++++++++++++++++++++++++++++++++++++++-
 1 file changed, 802 insertions(+), 3 deletions(-)
```

| 子范围 | 行号 | 净增（约） | 说明 |
| --- | --- | --- | --- |
| 顶部 import 调整 | L5–L36 | +4 | `hashlib`/`yaml`/`SavedFile` 重排；v3 必需 |
| **v3 段本体** | L2146–L2937 | **+792** | WL 驱动 pilot；平行 v2，不破坏 v2 API |
| v3 新增符号（公开） | 22 | — | gate / WL / capture / closeout / orchestrator |
| v3 新增符号（私有） | 4 | — | `_v3_row_caps_for_source` 等 |

**MASTER §8 触及：** `staged_pilot.py` v3 orchestration（R3E-SP3-01..06）；未扩 `staged_pilot_fetch_ports.py`（diff 无变更）。

---

## ponytail 梯级评估（v3 段分区）

| 区域 | 行号 | 净增 | 梯级 | 结论 |
| --- | --- | --- | --- | --- |
| WL 常量 + caps | L2148–L2187 | ~40 | 6（R3E explicit AC） | 证据文件名/caps 冻结；不可删 |
| `assert_model_inputs_whitelist_present` + `compute_whitelist_ref` | L2190–L2234 | ~45 | 6（AC-SP3-01 fail-closed + lineage） | B01-WL 门禁；不可删 |
| `load_pilot_v3_whitelist_scope` + symbol 提取 | L2237–L2290 | ~54 | 2→6 | WL 加载为 v3 核心；symbol 子集逻辑为 AC |
| `pilot_v3_caps_payload` / `write_*` | L2316–L2361 | ~46 | 6 | `pilot_v3_caps.json` + `whitelist_ref.json` 双 artifact |
| `validate_pilot_v3_authorization` | L2364–L2432 | ~69 | **2**（与 v2 共享前段） | v3 增 WL/cninfo-PDF/akshare 专检；见候选 NB-1 |
| `approved_pilot_v3_requests` | L2435–L2468 | ~34 | 6 | WL 驱动 request 信封；替代 v2 硬编码 |
| `assert_akshare_not_primary_for_daily_bar` | L2471–L2490 | ~20 | 6（AC-SP3-04） | v2 无对等；route guard 为任务卡要求 |
| `capture_*_v3` ×4 | L2493–L2740 | ~248 | 2→6 | 平行 v2 `capture_*_v2`；复用 `run_staged_pilot_raw_only` |
| closeout / matrix / registry delta | L2743–L2888 | ~146 | 6（AC-SP3-06） | 六切片 closeout 产物；MASTER §3.3 不 commit registry |
| `run_full_staged_pilot_v3` | L2891–L2936 | ~46 | 6 | 端到端编排；与 v2 `run_full_staged_pilot_v2` 对称 |

**梯级注记：** 实现已沿 v2 平行路径扩展（MASTER §4「平行 v2，不破坏 v2 API」），多数净增属 explicit AC 而非 speculative abstraction。

---

## A2 checklist

- [x] `git diff --stat` 已记录：`staged_pilot.py` **+802 / −3**（`master...HEAD`）
- [x] 每候选附 `file:line` + ponytail 梯级（§3.2）
- [x] 与 A4 交叉引用（§4）
- [x] 阻塞 vs 建议已区分

---

## §3.2 候选删改表

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 |
| --- | --- | --- |
| `staged_pilot.py:2368-2394` 与 `validate_pilot_v2_authorization` `1348-1378` 重复的 auth/raw_only/sandbox/clean_write 前段（~27 行） | 梯级 2（提取 `_validate_staged_pilot_authorization_core(request, *, max_rows, max_symbols)` 供 v2/v3 共用） | **建议** |
| `staged_pilot.py:2519,2616,2664,2718,2774,2871` 等同次 `run_full_staged_pilot_v3` 内 7× 调用 `compute_whitelist_ref()`（重复读 5 个 YAML + 重算 hash） | 梯级 5（`run_full` 开头算一次 `wl_ref=`，下传 capture/closeout） | **建议** |
| `staged_pilot.py:2252-2280` `pilot_v3_baostock_symbols` / `pilot_v3_cninfo_symbols` 同构去重循环 | 梯级 5（`_v3_symbols(source_id, row_filter)` 或单次 scope 扫描分桶） | **建议** |
| `staged_pilot.py:2679` `capture_conflict_summary_v3(..., validation_report=None)` 未使用形参（v2 签名遗留） | 梯级 1（`delete:` 删未用参数） | **建议**（<20 行） |
| `staged_pilot.py:2341-2349` `write_whitelist_ref` 仅 `write_pilot_v3_caps` 单调用 | 梯级 1（可内联 ~8 行） | 不列 ≥20；**不阻塞** |
| v3 整段删减重写为 v2 薄包装 | 梯级 1 | **否** — 破坏 WL 驱动 AC |

**ponytail-review 逐条（diff 内）：**

- `L2368-2394: shrink` auth 前段与 v2 逐行重复。提取 shared core，v3 仅保留 WL symbol/cninfo-PDF/akshare 专检（~−27 行于 v3）。
- `L2519+: shrink` 单次 run 内 7× `compute_whitelist_ref()`。`wl_ref` 线程传参（~−0 行净增但去冗余 I/O；call site 可略减）。
- `L2252-2280: shrink` 两个 symbol 提取函数。合并过滤谓词（~−12 行）。
- `L2679: delete` 未使用的 `validation_report` 形参。无替代。
- `L2283-2290: yagni` 已标 `ponytail:` 注释说明 akshare⊂baostock；**保留**（AC 可追溯，非 bloat）。

`net: -35～-50 lines possible.`（auth core 提取贡献最大；其余为 hygiene）

---

## DOUBT（doubt-driven-development）

| 疑点 | 搜索范围 | 结论 |
| --- | --- | --- |
| 是否存在单处 ≥20 行可删而不损 AC？ | `staged_pilot.py:2146-2937` 全文；对照 v2 段 `1260-2143`；`tests/test_real_data_staged_pilot_v3.py` | **有 1 处**：auth 前段与 v2 重复 ~27 行（NB-1）；其余块为 WL/capture/closeout AC 产物 |
| v3 是否应复用 `test_model_input_whitelist.py` 的 `load_yaml`？ | `tests/test_model_input_whitelist.py`、`backend/app/**` | 测试 helper 在 `tests/`；runtime 仅 `staged_pilot` 消费 — **暂不抽共享模块**（梯级 1：YAGNI，无第二 runtime 消费者） |
| `write_whitelist_ref` + caps 内嵌 `whitelist_ref` 是否重复 artifact？ | `execute-evidence/whitelist_ref.json`、`pilot_v3_caps.json` | **否** — 独立 `whitelist_ref.json` 为证据链字段；caps 内嵌为 manifest 自描述 |
| v3 792 行是否可改为调用 v2 + 参数？ | R3E v3 卡 §3–§6、MASTER §4 | **否** — v2 为硬编码 envelope；v3 为 WL 驱动 + 更大 caps + cninfo PDF 拒止 |

---

## 4. 与 A4 交叉引用

| 面 | A2（本维） | A4 可能重叠 |
| --- | --- | --- |
| `validate_pilot_v3` vs `validate_pilot_v2` 错误文案 | 重复 auth 块建议提取 | A4 可统一异常消息/分支顺序 |
| `capture_conflict_summary_v3` 未用 `validation_report` | delete 形参 | A4 可标 API 卫生 |
| v3 capture 与 v2 对称命名 | 有意平行；非过度抽象 | A4 应检 closeout 字段一致性（非本维） |

本维 **无 BLOCKING** 质量问题需 A4 升级。

---

## 5. 计划外发现

| ID | 发现 | 严重度 | 说明 |
| --- | --- | --- | --- |
| NB-1 | v3 auth 前段 ~27 行与 v2 逐行重复 | **NON-BLOCKING** | 梯级 2：提取 shared core；跨 v2 改动可放 debt-lite 或 SP3 后清理 |
| NB-2 | 单次 v3 run 内 7× `compute_whitelist_ref()` | **NON-BLOCKING** | 梯级 5：线程 `wl_ref`；evidence 正确性不变 |
| NB-3 | `capture_conflict_summary_v3` 死参数 `validation_report` | **NON-BLOCKING** | 梯级 1：删 1 形参即可 |
| — | v3 平行 v2 整段 fork | **非发现** | MASTER explicit「平行 v2」；非计划外 bloat |

**显式声明：** 已对抗搜索 `staged_pilot.py` v3 段、`validate_pilot_v2_authorization`、v2 capture 对称段、`tests/test_real_data_staged_pilot_v3.py`；**无 BLOCKING ponytail 项**。

---

## 6. 结论

**A2 审计判定：PASS。**

v3 段 **+792 行**主要为 WL 驱动门禁、六切片 evidence capture 与 closeout——与 R3E-SP3 AC 及 v2 平行模式一致，无 speculative factory/wrapper。登记 **3 项 NON-BLOCKING** 收缩建议（auth 共享核心、whitelist_ref 单次计算、symbol 提取去重），合计约 **−35～−50 行** 可选优化，**不阻断** merge。
