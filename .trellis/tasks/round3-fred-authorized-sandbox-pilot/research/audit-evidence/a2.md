# A2 — audit-ponytail（ponytail-review + doubt-driven-development）

**维度：** A2 · audit-ponytail  
**任务：** `round3-fred-authorized-sandbox-pilot` (B01-FRED)  
**审计范围：** `backend/app/ops/fred_*.py` · `tests/test_fred_*.py` · registry proposed delta（只读对照）  
**权威对照：** R3E 任务卡 · MASTER §0.3a Ponytail · `staged_pilot.py` / `staged_pilot_fetch_ports.py` 邻接模式 · A4 交叉引用  
**判定：** **PASS**  
**OPEN：** **0** · **BLOCKING：** **0**

---

## git diff 基线

| 范围 | 统计 |
| --- | --- |
| 已跟踪 vs `master` | 8 files · **+142 / −2** |
| 未跟踪（FRED 核心交付） | `fred_sandbox_pilot.py` (334L) · `fred_fetch_ports.py` (126L) · `fred_evidence_validator.py` (98L) · `test_fred_sandbox_pilot.py` (231L) · `test_fred_source_registry.py` (46L) |
| **净增（估算）** | **~977 行**（含 registry/matrix/catalog 维护 ~142L） |
| 新依赖 | **无**（stdlib `urllib` + 既有 `yaml` / `FetchPort`） |
| forbidden | `data_health.py` 主体 **未改** |

**pytest（A2 抽样）：** `uv run pytest tests/test_fred_source_registry.py tests/test_fred_sandbox_pilot.py tests/test_fred_staged_semantics.py -q` → **16 passed, 1 skipped** (FRED-07 opt-in)

---

## A2 checklist

- [x] `git diff --stat` 已记录 Lxx / net lines
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象交叉引用（`a4.md` A4-DOUBT-03/05 · mock/live 重复行）
- [x] 阻塞 vs 建议已区分；**全部当场 CLOSED**

---

## ponytail 梯级评估（最深命中）

| 文件 | 最深梯级 | 结论 |
| --- | --- | --- |
| `fred_sandbox_pilot.py` | **梯级 2**（复用 `SourceRoutePlanner` / `FetchPort` / `live_pilot_constants`） | 未并入 2139L `staged_pilot.py` — 正确隔离 |
| `fred_fetch_ports.py` | **梯级 3–4**（stdlib `urllib`；模式对齐 `staged_pilot_fetch_ports`） | 独立 macro port 合理 |
| `fred_evidence_validator.py` | **梯级 1 豁免** | MASTER AC-FRED-05 显式要求 pilot-local health，非 `data_health.py` |
| `tests/test_fred_*.py` | **梯级 2** | 复用 `contract_gate_support` · 五字段 docstring 无 fixture 膨胀 |

---

## §3.2 候选删改

| 候选删改（file:line） | ponytail 梯级 | 是否阻塞 | 处置 |
| --- | --- | --- | --- |
| `fred_sandbox_pilot.py:167-242` + `245-312` mock/live 双路径 manifest 循环结构重复（~55 行重叠） | 梯级 2（可抽 `_collect_series_manifest`） | **建议** | **CLOSED** — 两路径授权/状态/写盘语义不同；抽 helper 收益 < 可读性；A4 已记 NON-BLOCKING |
| `fred_fetch_ports.py:23-61` + `64-135` `FredMockFetchPort` / `FredLiveFetchPort` 平行类 | 梯级 2（对齐 `staged_pilot_fetch_ports` 一源一 port） | **建议** | **CLOSED** — 与 staged 邻接模式一致；合并会混 macro/CN equity 域 |
| `fred_fetch_ports.py:72-74` `_window_start` 粗算 3y，未复用 `live_pilot_fetch_ports._window_start_for_label` | 梯级 2 | **建议** | **CLOSED** — 已有 `ponytail:` 注释标天花板与升级路径；FRED pilot 默认 `3y` 与 authorization 模板一致 |
| `fred_sandbox_pilot.py:350-361` `run_failure_scenario` network/schema 为硬编码桩，非真实 port 路径 | 梯级 1（YAGNI 质疑） | **建议** | **CLOSED** — FRED-04 证据 `fred_pilot_status_report.json` 需稳定 taxonomy；真实 network/schema 由 mock `PortError`（191-192）与 validator（362-368）覆盖 |
| `fred_sandbox_pilot.py:315-316` `build_pilot_status_report` 仅 2 行包装、无测试直接调用 | 梯级 1 | **建议** | **CLOSED** — 非 ≥20 行候选；Execute 证据 JSON 组装锚点，删之无收益 |
| 未扩展 `staged_pilot.py` / `staged_pilot_fetch_ports.py` 而新建 `fred_*` | 梯级 1（需要建吗？） | — | **CLOSED（非候选）** — MASTER §3.1 允许新建；并入 staged 会违反 Batch 01 文件锁与 2k+ 行耦合 |

**不算候选（规则豁免）：** registry yaml delta · `service.py` +1 `macro_series` · `fred_evidence_validator.py` 全文（AC-FRED-05 契约行）

---

## DOUBT（≥20 行可简化？）

**搜索范围：** `backend/app/ops/fred_*.py` · `tests/test_fred_*.py` · 对照 `staged_pilot.py` · `staged_pilot_fetch_ports.py` · `live_pilot_fetch_ports.py` · `rg "fred_" backend/app/ops`

**结论：** 找到 **1 处** ≥20 行可简化候选（mock/live manifest 循环，`fred_sandbox_pilot.py:167-312`）。经梯级 2 评估：**不阻塞 merge** — 当前重复为 opt-in live vs default mock 的有意分叉；进一步抽象违反 ponytail「无未请求抽象」。

---

## 与 A4 交叉引用

| A2 候选 | A4 相关项 | 关系 |
| --- | --- | --- |
| mock/live manifest 重复 | A4 可维护性行「mock/live ~15 行」 | A2 计全循环 ~55 行；A4 记字段级重复；合并判定：建议级 NON-BLOCKING |
| `_window_start` 粗算 | A4 风格/ponytail 行 | 同一 `ponytail:` 注释；A2 梯级 2 复用缺口已闭合 |
| `run_failure_scenario` 桩 | A4-DOUBT-03 schema 子串启发式 | 互补：A4 审错误模型；A2 审 taxonomy 桩是否 YAGNI — 均 CLOSED |

---

## 计划外发现

> 对抗性搜索：对照 R3E Ponytail 约束 · MASTER §0.3a「复用 staged_pilot / fetch port 模式」· `rg "import requests|httpx|aiohttp" backend/app/ops/fred_*` · 计划外 bloat/重复路径。

| ID | 严重度 | 发现 | 处置 | Audit status |
| --- | --- | --- | --- | --- |
| A2-DUP-01 | NON-BLOCKING | mock/live manifest 收集循环重复 | 保持现状；理由见 §3.2 首行 | **CLOSED** |
| A2-WIN-01 | NON-BLOCKING | live FRED 未复用 `_window_start_for_label` | `ponytail:` 注释 + authorization `window: "3y"` 对齐 | **CLOSED** |
| A2-TAX-01 | NON-BLOCKING | failure taxonomy network/schema 为模拟桩 | FRED-04 证据 + mock PortError 真路径并存 | **CLOSED** |
| A2-DEAD-01 | NON-BLOCKING | `build_pilot_status_report` 无单测直连 | 证据 JSON 结构稳定；2 行不构成 bloat | **CLOSED** |

**显式非发现（已对抗确认）：**

- 新第三方依赖 — 无；live 用 stdlib `urllib`。
- 并入 `staged_pilot.py` — 会扩大 forbidden 邻接面；当前三文件边界符合 Batch 01 文件锁。
- `fred_evidence_validator` 可内联 — AC-FRED-05 要求 pilot-local，与 `data_health.py` 分离。
- 测试 setup 膨胀 — closure 测试读 `b01-fred-audit-closures.md` 为 A1 零遗留契约，purpose 不可删。

---

## 汇总

| 类别 | 数量 |
| --- | --- |
| BLOCKING | **0** |
| OPEN (含 NON-BLOCKING) | **0** |
| CLOSED（建议级） | **4** |

**A2 verdict：PASS** — 实现符合 ponytail 最小 diff 与 staged 邻接模式；≥20 行重复候选已评估并闭合为 NON-BLOCKING；可继续 A3+。
