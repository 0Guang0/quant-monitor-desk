# A4 — Quality

**Result: PASS**

> 维度：loader 校验链、`IndustryChainLoadError` 一致性、测试中文注释、错误处理边界  
> 范围：`backend/app/layer3_chains/**`、`tests/test_layer3_loader.py`、`tests/fixtures/layer3_staged_bundle/**`  
> Skills：code-review-and-quality + doubt-driven-development  
> 对照：`specs/contracts/layer3_loader_contract.yaml` 七条硬规则、`MASTER.plan.md` §2 AC-020-1..6、`sensor_loader.py` staged gate 形态

---

## Review Summary

**Verdict:** PASS（无 P0/P1 阻断；2 项 Important 建议合入前或 021 前硬化）

**Overview：** `IndustryChainLoader` 完整实现 contract 七条硬规则 + `staged_fixture_only` gate，错误类型与 019 `CrossAssetRegistryLoadError` 对齐（均继承 `ValueError`）。校验顺序为「五表解析 → 唯一性 → 图引用 → anchor 语义」，违反时统一 `IndustryChainLoadError`，无部分结果返回。14 条 pytest 全绿（含 A8 空 bundle 边界），测试 docstring 均含 `purpose` / `target` / `verifies` / `failure_meaning` 中文四元组。

---

## 轴评分（1–5）

| 轴           | 分  | 理由                                                                                            |
| ------------ | --- | ----------------------------------------------------------------------------------------------- |
| Correctness  | 4   | contract 硬规则与 AC 用例对齐；YAML 坏数据与「仅 chains 非空、余表空」边界未 fail-fast          |
| Readability  | 4   | 与 `sensor_loader.py` 同构；`_load_*` 拆分清晰；`_MANIFEST_FILE_KEYS` 未使用                    |
| Architecture | 5   | 薄 loader + frozen dataclass；无越界依赖；staged-only 常量与 manifest 协议符合 §6.1             |
| Security     | 4   | 本地 fixture 解析；`bundle_dir.resolve()` 无 containment 校验（staged 测试场景可接受；A3 专审） |
| Performance  | 5   | 小型 bundle 全内存解析；无 I/O 热路径（A6 SKIP 合理）                                           |

---

## §3.4 多轴发现

| 轴           | 发现                                                                                                                                                                                                                                          | 阻塞?      | 证据                                                                                 |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------ |
| Correctness  | **YAML 解析异常未包装为 `IndustryChainLoadError`**：`manifest` 与 chain registry 的 `yaml.safe_load` 坏文档抛 `yaml.parser.ParserError`，与 JSON 路径（`loader.py:120-123` 已 wrap）不一致，违反 MASTER §5.2 fail-fast 统一错误类型           | 否（P2）   | `loader.py:46-47,136`；探针：坏 manifest → `ParserError` 非 `IndustryChainLoadError` |
| Correctness  | **部分空 bundle 可通过 loader**：`chains` 非空而 `nodes/anchors/edges/cross_chain` 全 `[]` 仍返回 `IndustryChainLoadResult`；与 AC-020-1「五类集合非空」及 §6.1「每表 ≥1 条」fixture 约定不一致（仅 success 测试断言 fixture，loader 未强制） | 否（P2）   | `loader.py:140-141` 只校验 chains 非空；探针 `PARTIAL_EMPTY_LOADED 1 0 0`            |
| Correctness  | **cross-chain `from_node_id` 悬空无具名测试**：实现已校验（`loader.py:87-91`），但 §5.3 仅 `test_layer3Loader_crossChainMissingNode_rejects` 变异 `to_node_id`                                                                                | 否（P3）   | `tests/test_layer3_loader.py:220-232`；手工变异 `from_node_id` → 正确抛错            |
| Readability  | **`_MANIFEST_FILE_KEYS` 死代码**：元组定义后未被引用，manifest 键靠字符串字面量散落                                                                                                                                                           | 否（P3）   | `loader.py:22-28` vs `_resolve_manifest_file` 字面量 key                             |
| Readability  | **测试函数体 2 空格缩进**：与同目录 `test_layer1_axis_loader.py` 等 4 空格不一致；文件内自洽                                                                                                                                                  | 否（INFO） | `tests/test_layer3_loader.py:31-44`                                                  |
| Architecture | **`IndustryChainLoadError(ValueError)` + `loader_mode` gate** 与 019 模式一致；`__init__.py` 导出完整                                                                                                                                         | 否         | `loader.py:33-34,50-55`；`sensor_loader.py:56-77`                                    |
| Security     | `source_keys` 非字符串元素被 `str(k)` 静默 coercion，可能掩盖脏数据                                                                                                                                                                           | 否（P3）   | `loader.py:199`                                                                      |
| Performance  | —                                                                                                                                                                                                                                             | —          | 无发现                                                                               |

---

## DOUBT（doubt-driven，≥1）

| 攻击面                                      | 假设                                   | 结果                                                                |
| ------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------- |
| 「fail-fast 统一 `IndustryChainLoadError`」 | 注入坏 YAML manifest                   | **缺口** — `ParserError` 泄漏（§3.4 Correctness P2）                |
| 「AC-020-1 五表非空」                       | chains=[{id}]，余表 `[]`               | **缺口** — loader 接受空 nodes/anchors/edges（§3.4 Correctness P2） |
| 「cross-chain 双端引用」                    | 仅测 to_node                           | **实现成立、测试未对称** — from_node 无具名用例（P3）               |
| 「P0 source_keys」                          | 清空 `anchors[0]`（P0_CORE）           | **成立** — `test_layer3Loader_p0Anchor_missingSourceKeys_rejects`   |
| 「event_only private」                      | `private_company` + `event_only=false` | **成立** — `test_layer3Loader_eventOnlyPrivate_notDailyPriceAnchor` |
| 「空 bundle」                               | 五表皆空                               | **成立** — A8 `test_layer3Loader_emptyBundle_rejects`（14/14 绿）   |

---

## Critical Issues

（无）

---

## Important Issues

| ID    | 位置                      | 描述与建议                                                                                                                                                                                                                                              |
| ----- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A4-01 | `loader.py:46-47,134-141` | **YAML 错误类型不一致。** 建议对 manifest / chain YAML 的 `read_text` + `safe_load` 包 `try/except yaml.YAMLError`，raise `IndustryChainLoadError(f"invalid yaml in {key}: {path}") from exc`；补 `test_layer3Loader_invalidYaml_rejects`。             |
| A4-02 | `loader.py:161-176` 等    | **非 chains 表可为空。** 若需严格 AC-020-1 loader 级保证，在 `_load_nodes/_load_anchors/_load_edges/_load_cross_chain_edges` 后断言 `len >= 1`，或在 `load()` 汇总检查；否则在 MASTER/contract 注明「非空由 fixture 约定，loader 仅保证 chains 非空」。 |

---

## Suggestions

| ID    | 位置                          | 描述                                                                              |
| ----- | ----------------------------- | --------------------------------------------------------------------------------- |
| A4-03 | `loader.py:22-28`             | 删除 `_MANIFEST_FILE_KEYS` 或改为迭代 manifest 必填键，消除 dead code。           |
| A4-04 | `tests/test_layer3_loader.py` | 增加 `test_layer3Loader_crossChainMissingFromNode_rejects`（对称 to_node 用例）。 |
| A4-05 | `loader.py:199`               | `source_keys` 元素非 `str` 时显式拒绝，避免静默 coercion。                        |
| A4-06 | `tests/test_layer3_loader.py` | 测试体缩进对齐仓库 4 空格惯例（可选 ruff format）。                               |

---

## What's Done Well

- **校验链完整覆盖 contract 七条硬规则**：唯一性（chain/node/anchor）、边/锚点/跨链节点引用、event_only 私有公司、P0 `source_keys` 均在 `load()` 末段集中断言，逻辑易追踪。
- **`IndustryChainLoadError` 使用一致**：成功路径返回 typed `IndustryChainLoadResult`；失败路径无吞异常；与 019 `CrossAssetRegistryLoadError` 命名与基类对齐。
- **测试注释规范**：14 条测试均含中文 `purpose/target/verifies/failure_meaning`；失败路径 `pytest.raises(IndustryChainLoadError, match=...)` 含规则语义。
- **Ponytail 对齐**：复用 staged gate 文案形态；无多余抽象层；frozen dataclass 模型边界清晰。
- **A8 空 bundle 已补测**：`test_layer3Loader_emptyBundle_rejects` 验证空 chains fail-fast。

---

## Verification Story

| 检查项           | 结果                                                                                                 |
| ---------------- | ---------------------------------------------------------------------------------------------------- |
| Tests reviewed   | **是** — 14 条；语义断言（MSFT 字段、match 规则、tmp_path 变异）非 tautological                      |
| Build verified   | **是** — `uv run pytest tests/test_layer3_loader.py -q` → **14 passed**（A4 复跑 2026-06-23）        |
| Execute evidence | **是** — `research/execute-evidence/8.6-green.txt` 声称 13 passed（A8 补测后现为 14）；Tier B 全库绿 |
| Security checked | **局部** — 无 DB/网络；路径穿越未阻断（staged 范围）；详情交 A3                                      |
| Contract 对照    | **是** — `layer3_loader_contract.yaml` 七条硬规则均有实现 + 对应测试（cross-chain from 仅缺具名测）  |

---

## §4.3 建议跟踪（非 020 阻断）

| ID    | 优先级 | 动作                           |
| ----- | ------ | ------------------------------ |
| A4-01 | P2     | YAML parse wrap + 测试         |
| A4-02 | P2     | 明确/强制五表非空策略          |
| A4-03 | P3     | 清理 dead constant             |
| A4-04 | P3     | cross-chain from_node 对称测试 |

**P0：0 · P1：0 · P2：2 · P3：2**
