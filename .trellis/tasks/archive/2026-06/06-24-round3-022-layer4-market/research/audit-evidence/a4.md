# A4 code-reviewer — Wave C 022 Layer4 Market Structure

> **维度：** A4 · code-review-and-quality + doubt-driven-development  
> **模型：** composer-2.5  
> **分支：** `feature/round3-022-layer4-market` @ worktree `quant-monitor-desk-wt-022-layer4`  
> **日期：** 2026-06-24  
> **模式：** Audit（只读，无 commit、无改码）

---

## Review Summary

| 项           | 值                                                                   |
| ------------ | -------------------------------------------------------------------- |
| **判定**     | **PASS**                                                             |
| **BLOCKING** | 0                                                                    |
| **Verdict**  | 无 P0 逻辑/安全阻塞；as_of / breadth / lineage 主链与 L2/L3 模式一致 |

**Overview：** 022 在 `backend/app/layer4_markets/` 交付 staged-only 的 `MarketStructureBuilder`：registry 种子、calendar/breadth 加载、manifest 血缘与 `no_future_data` 双观测点校验。实现对齐 `layer4_market_contract.yaml` 边界与 `snapshot_lineage_contract.yaml` 必填字段；7 项 pytest 全绿。对抗性审阅发现若干**计划外**错误模型/契约质量规则缺口，均登记为 NON-BLOCKING（测试偿还主责 A8）。

---

## 权威层级（`audit-adversarial-authority.md`）

| 级别   | 来源                                                                         | 审阅对照结论                                                                     |
| ------ | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 第一级 | `layer4_market_structure.md` §3 MarketAdapter、`layer4_market_contract.yaml` | staged CN_A adapter + 8 行 registry 元数据；不物化 L5 历史 / L1 suite            |
| 第一级 | `snapshot_lineage_contract.yaml` `no_future_data` / `required_fields`        | lineage 复用 `assert_lineage_fields_complete`；双路 `_reject_future_observation` |
| 第二级 | `022_implement_layer4_market_structure.md` §8–15 Red Flags                   | 无 live fetch、无 WriteManager 写、无交易语义                                    |
| 第三级 | `AUDIT.plan.md` §2 A4「as_of / breadth / lineage 链一致」                    | **满足**；但 Plan 未索引的边界见 §计划外发现                                     |

**对抗性注记：** MASTER §8 仅列 7 测；AC-022-2「非交易日规则」在代码已实现、**测未覆盖**——不得因 MASTER 已列 duplicate PK 用例而视为完备。

---

## 多轴审查（§3.4）

| 轴          | 发现                                                                          | 阻塞? | 证据                                                                                            |
| ----------- | ----------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| 正确性      | staged bundle → registry + calendar + breadth + lineage 四元组一致            | 否    | `test_marketBreadth_requiredFieldsPresent`、`test_marketSnapshot_lineageRequiredFieldsComplete` |
| as_of 边界  | breadth **与** calendar 观测均 `> as_of` 时拒绝                               | 否    | `market_structure.py:202-213`；测仅覆盖 breadth：`test_marketSnapshotRejectsFutureInput`        |
| lineage 链  | `layer_id=layer4`；`LINEAGE_REQUIRED_FIELDS` 完整；upstream 可透传            | 否    | `lineage.py:47-67`；`test_marketSnapshot_lineageRequiredFieldsComplete`                         |
| staged-only | `source_mode != staged_fixture_only` 拒绝                                     | 否    | `_read_manifest:284-289`；`test_marketAdapter_nonStagedSource_rejects`                          |
| 非交易日    | `is_trading_day=False` 时 `build()` 拒绝                                      | 否\*  | `market_structure.py:197-200`；**无单测**（见 DOUBT）                                           |
| 边界 D-09   | 产出字段名与 L5 历史 / L1 标准化 forbidden 集合无交集                         | 否    | `test_marketSnapshot_noLayer5HistoryFields`                                                     |
| 错误处理    | 主路径 `Layer4MarketError` + 语义 message；manifest/calendar/breadth 分层校验 | 否\*  | 各 mutation 测；数值解析泄漏见 NB-2                                                             |
| 路径安全    | `bundle_dir` 须 `relative_to(PROJECT_ROOT)`                                   | 否    | `market_structure.py:263-271`                                                                   |
| 风格/架构   | 对齐 L3 `snapshot_builder`：manifest 血缘、`_parse_ts`、future 拒绝命名       | 否    | 与 `layer3_chains/snapshot_builder.py` 对照                                                     |
| 性能        | O(calendar_rows) 微型 fixture；无 DuckDB 热路径                               | 否    | 委托 A6 SKIP                                                                                    |

\* 实现存在；测试或错误类型一致性有缺口，见计划外发现。

---

## Critical Issues

（无）

---

## Important Issues

| ID   | 位置                                                                       | 描述与建议                                                                                                                                                                                                                                              | 阻塞?        |
| ---- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| NB-1 | `market_structure.py:197-200`                                              | **非交易日拒绝已实现但无测。** AC-022-2 / `quality_rules.no_snapshot_on_non_trading_day_unless_session_type_allows` 语义依赖此分支；MASTER 仅验 duplicate PK。**建议：** A8 补 `is_trading_day=false` mutation → `Layer4MarketError` 含 `non-trading`。 | NON-BLOCKING |
| NB-2 | `market_structure.py:371-373`                                              | **数值字段 `int()`/`float()` 失败抛原生 `ValueError`，非 `Layer4MarketError`。** 与 manifest/calendar 统一错误模型不一致，调用方难单类型捕获。**建议：** 包一层 `try/except` 转语义错误（对齐 L3 `_parse_bar_numeric`）。                               | NON-BLOCKING |
| NB-3 | `market_structure.py:202-207` vs `test_layer4_market_structure.py:169-180` | **`no_future_data` 仅测 breadth 未来观测；calendar `as_of_timestamp` 未来路径未测。** 契约 `validation_tests` 名 `test_snapshotRejectsFutureInput` 暗示双输入面。**建议：** A8 补 calendar future mutation。                                            | NON-BLOCKING |
| NB-4 | `layer4_market_contract.yaml:20-24` vs `market_structure.py:346-378`       | **契约 `quality_rules.no_negative_volume_amount` 未在 loader 执行。** 负 `advancers`/`decliners`/`total_amount` 可进入 snapshot。**建议：** Batch 6 前在 `_load_breadth_row` 加非负校验或登记 defer。                                                   | NON-BLOCKING |

---

## Suggestions

| 位置                                | 描述                                                                                                                                           |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `market_structure.py:217-220`       | `parameter_hash_for` 仅含 `advancers`，不含 breadth 全字段；`deterministic_rebuild` 在字段变更时可能欠敏感——staged 可接受，生产化应扩 inputs。 |
| `layer4_market_contract.yaml:10,19` | `required_fields` 未列 `as_of_timestamp`，但模型与 `no_future_data` 强依赖；文档/契约可对齐以免下游 schema 漂移。                              |
| `market_structure.py:255-260`       | 非 `CN_A` market 在 registry 存在但 adapter 拒绝——行为正确；可补单测明确「种子元数据 ≠ staged 可构建」以免误用。                               |
| `market_structure.py:357-361`       | `breadth trade_date` 与请求不一致有拒绝逻辑，无单测。                                                                                          |
| `market_structure.py:381-391`       | naive timestamp 拒绝有逻辑，无单测。                                                                                                           |

---

## DOUBT — 边界遗漏（`file:line`）

1. **`market_structure.py:197-200`** — 非交易日 `Layer4MarketError` 分支零测试覆盖；若回归为静默通过，AC-022-2 半条失效。
2. **`market_structure.py:371-373`** — 畸形 `advancers: "x"` 抛 `ValueError`，错误不可观测为 Layer4 域错误。
3. **`market_structure.py:202-207`** — calendar 未来观测与 breadth 对称实现，但测试只变异 breadth JSON。
4. **搜索范围：** `market_structure.py`、`lineage.py`、`models.py`、`tests/test_layer4_market_structure.py` 全文 + `layer4_market_contract.yaml` quality_rules；未发现路径穿越、密钥、live fetch（转 A3）。

---

## 计划外发现

| ID   | 发现                                       | 严重度           | 说明                                                   |
| ---- | ------------------------------------------ | ---------------- | ------------------------------------------------------ |
| NB-1 | 非交易日拒绝无测                           | **NON-BLOCKING** | 代码有、MASTER §8 未列；AUDIT.plan A8 已点「非交易日」 |
| NB-2 | 数值解析 `ValueError` 泄漏                 | **NON-BLOCKING** | 错误模型不一致                                         |
| NB-3 | calendar 未来 as_of 未测                   | **NON-BLOCKING** | 双观测点仅测一半                                       |
| NB-4 | `no_negative_volume_amount` 未执行         | **NON-BLOCKING** | 契约 quality_rules vs 实现缺口                         |
| —    | live fetch / WriteManager / 三 registry 写 | **无发现**       | 静态审阅 + staged manifest 闸                          |

**显式声明：** 已按 `audit-adversarial-authority.md` A4 最低动作对照设计文档语义、契约字段、`snapshot_lineage` 链与 MASTER 外边界；**无 BLOCKING 质量问题**。

---

## What's Done Well

- **L3 同构：** `staged_fixture_only` manifest、provenance tuple、`Layer4LineageBuilder` 复用 `backend/app/core/snapshot_lineage.py` 内核，模块边界清晰。
- **as_of 双闸：** calendar 与 breadth 均经 `_reject_future_observation`，优于仅校验单一输入面。
- **AC-022-7：** `FORBIDDEN_*` frozenset + `collect_result_field_names` 字段名扫描，ponytail 注释标明 O(rows×fields) 天花板。
- **测试 docstring：** 五字段中文（覆盖范围/对象/目的/验证点/失败含义）齐全，mutation 助手 `_mutate_*` 隔离 fixture。
- **路径守卫：** `bundle_dir` 必须位于 `PROJECT_ROOT` 下，mutation 测经 `_copy_bundle` 合规复制。

---

## 与 A2 交叉

- 无重复 manifest 解析样板（单文件 `market_structure.py`）；`Layer4LineageBuilder` 与 L3 `Layer3LineageBuilder` 同构，非过度抽象。
- `REGISTRY_SEEDS` 常量表为设计 §2 八市场元数据，非无请求抽象。

---

## Verification Story

| 项               | 状态                                                                                                    |
| ---------------- | ------------------------------------------------------------------------------------------------------- |
| Tests reviewed   | **是** — 7/7 测 purpose 与 AC 映射可读；mutation 覆盖 staged/duplicate/future/forbidden                 |
| Build verified   | **是** — `uv run pytest tests/test_layer4_market_structure.py -q` → **7 passed**（Audit 2026-06-24）    |
| Security checked | **是（局部）** — 输入为 staged JSON/YAML；`bundle_dir` 路径约束；无密钥进 diff；畸形数值错误类型见 NB-2 |
| Full pytest      | **未复跑 Tier B** — 委托 A5/A8；本维聚焦 layer4 diff                                                    |

---

## 结论

**A4 审计判定：PASS。**

022 Layer4 market structure 实现满足 AUDIT.plan §2 A4「as_of / breadth / lineage 链一致」与任务卡 staged-only 边界；无 P0 逻辑阻塞。对抗性审阅登记 4 项 **NON-BLOCKING** 计划外缺口（非交易日测、calendar 未来 as_of 测、数值错误模型、契约 quality_rules），建议由 **A8** 补测或 Batch 6 偿还，**不阻断** 022 merge。
