# A3 — Security

**Result: PASS**

A3 静态审计未发现 P0/P1 项。`backend/app/layer3_chains/` 与 `tests/test_layer3_loader.py` 为纯只读 staged fixture 解析器：无 DuckDB/WriteManager、无 SQL、无 live 源、无生产 `data/` 写路径。`loader_mode != staged_fixture_only` 在入口 fail-closed（`loader.py:50-55`）。2 项 P3 防御性建议（manifest 路径约束、错误信息绝对路径）不阻断发布。

---

## 威胁检查表

| 检查项 | 结论 | 证据 |
| ------ | ---- | ---- |
| 威胁面（信任边界） | PASS | 唯一入口 `IndustryChainLoader.load(*, bundle_dir)`；默认 `STAGED_LAYER3_BUNDLE_DIR` 指向 `tests/fixtures/layer3_staged_bundle`；无 HTTP/CLI/DB 暴露面 |
| 生产 DB / migration 写 | PASS | `rg` 于 `backend/app/layer3_chains`、`tests/test_layer3_loader.py` 无 `writer(`、`apply_migrations`、`INSERT`/`UPDATE`/`DELETE`、`duckdb`、`ConnectionManager`、`WriteManager`、`data/` 命中 |
| forbidden 路径 | PASS | 未 import `layer2_sensors`、`datasources`、`db`、`ops`；对照 MASTER §1.3 forbidden 列表无越界 |
| staged-only gate（AC-020-5） | PASS | `loader_mode` 非 `staged_fixture_only` 抛 `IndustryChainLoadError` 并明示禁止 production-live（`loader.py:50-55`）；`test_layer3Loader_nonStagedMode_rejects` 覆盖 |
| 硬编码 URL 变体（DOUBT-1） | PASS（无发现） | `rg -n "https?://"` 于 `backend/app/layer3_chains` → 0 命中；搜索范围含 loader/models/__init__ |
| JWT / API key 模式（DOUBT-2） | PASS（无发现） | `rg -ni "api[_-]?key\|secret\|token\|password\|jwt\|bearer\|AKIA\|sk-"` 于 `backend/app/layer3_chains` → 0 命中 |
| SQL 拼接（DOUBT-3） | PASS（无发现） | `rg -n "execute\(f\|f\".*SELECT\|f'.*SELECT\|\.format\(.*SELECT"` 于 `backend/app/layer3_chains` → 0 命中；本模块无 SQL 层 |
| subprocess / eval | PASS（无发现） | `rg -n "subprocess\|os\.system\|eval(\|exec("` 于 diff 范围 → 0 命中 |
| live 源启用 | PASS（无发现） | `rg -n "enable\.qmt\|enable\.xqshare\|--enable-qmt"` 于 diff 范围 → 0 命中 |
| 反序列化 | PASS | 仅用 `yaml.safe_load`（`loader.py:46,136`）与 `json.loads`（`loader.py:121`）；无 `yaml.load`/`pickle` |
| 对照 019 写库面 | PASS with note | `sensor_loader.py` 含 DuckDB staging 写路径；020 刻意只读、defer 至 `021`，攻击面更小 |

---

## 发现项

| 严重级别 | 发现 | 证据路径 | 是否进 §4.3 |
| -------- | ---- | -------- | ----------- |
| **P3** | manifest 相对路径未做 bundle 根目录约束：`_resolve_manifest_file` 执行 `path = root / str(rel)` 后未验证 `path.resolve().is_relative_to(root.resolve())`。若未来调用方传入攻击者控制的 `bundle_dir` + 含 `../../../` 的 manifest，可读取 bundle 外任意可读 JSON/YAML 文件（只读泄露，非写库）。当前 fixture manifest 均为同级文件名（`bundle_manifest.yaml`），测试仅用 `tmp_path` 复制 bundle，无利用链。 | `loader.py:108-115`；`tests/fixtures/layer3_staged_bundle/bundle_manifest.yaml` | 否 — 建议 `021` 接线前加 containment 断言或单测 |
| **P3** | 错误信息含解析后的绝对路径（如 `missing bundle manifest: {manifest_path}`），日志/异常面向运维可接受，但若未来经 API 外泄需收敛。 | `loader.py:44,114,123` | 否 |
| INFO | 无生产 mutation、无密钥、无 SQL 注入面、无未授权 live 源。AUDIT.plan §2 A3「`rg` 直写 DB / forbidden · 无 mutation」满足。 | 全文 `loader.py`/`models.py`/`__init__.py`；`tests/test_layer3_loader.py` | 否 |

---

## 正向观察

- **Fail-closed staged gate**：与 019 `CrossAssetRegistryLoader` 同模式，显式拒绝 `production_live`（`loader.py:50-55`）。
- **只读、无持久化**：相较 `sensor_loader.py` 的 `WriteManager` 路径，020 范围不引入写库攻击面。
- **契约硬校验**：唯一性、图引用、P0 `source_keys`、`private_company`/`event_only` 规则在加载期阻断脏配置，降低下游误用风险。
- **安全解析**：`yaml.safe_load` + 结构化类型检查（`isinstance` 门控），无动态代码执行。

---

## GitNexus

| 调用 | 结果 |
| ---- | ---- |
| `context({name: "IndustryChainLoader", file_path: "backend/app/layer3_chains/loader.py"})` | Symbol not found（索引未收录新包，与 `research/gitnexus-execute-summary.md` 一致） |
| `query({query: "layer3 industry chain loader staged fixture"})` | 无 layer3_chains 符号；返回 layer1/staged fixture 邻域流程 |
| Plan 预判 | `research/gitnexus-summary.md`：新建符号 LOW risk，无 upstream caller |

结论基于冻结 GitNexus 摘要 + 本审阅静态 `rg`/读码；A3 未改业务代码，不适用 `detect_changes()`。

---

## 建议（非阻断）

1. **`021` 接线前**：在 `_resolve_manifest_file` 增加 `path.resolve().is_relative_to(root.resolve())` 校验；补一条 manifest `../` 逃逸负例测。
2. **保持 staged-only**：任何扩展至 `specs/layer3_global_industry_chains/` 生产路径须新 MASTER 授权 + 独立安全审阅（路径与 mode 双门控）。
