# A2 audit-ponytail — Phase 7 复审计

**维度：** A2 · ponytail-review + doubt-driven-development  
**模型：** composer-2.5  
**worktree：** `quant-monitor-desk-wt-r3-pilot-v2`  
**post-commit：** `fb04493`  
**任务：** B-19 `06-24-round3-real-data-staged-pilot-v2`  
**审查范围（只读）：** `backend/app/ops/mutation_proof.py`、`backend/app/ops/staged_pilot.py`  
**权威：** `R3Y_real_data_staged_pilot_v2.md`、MASTER §2 AC、`agents/audit-adversarial-authority.md`  
**判定：** **PASS**（0 阻塞候选；2 项 NON-BLOCKING 收敛建议）

---

## 启动记录

### git diff --stat（`master...fb04493`，目标文件）

```
 backend/app/ops/mutation_proof.py |  66 ++-
 backend/app/ops/staged_pilot.py   | 831 +++++++++++++++++++++++++++++++++++++-
 2 files changed, 891 insertions(+), 6 deletions(-)
```

| 文件                | 净增             | 说明                                                                                   |
| ------------------- | ---------------- | -------------------------------------------------------------------------------------- |
| `mutation_proof.py` | +60 行（含改型） | R3Y-MUT-PROOF-001：`all_table_row_counts`、`_derive_proof_status`、`MUTATION_DETECTED` |
| `staged_pilot.py`   | +825 行          | v2 块 `1251–2073`（PROMPT_19 / R3Y-SP2-01…09）                                         |

**行数（worktree 实测）：** `mutation_proof.py` 140 行；`staged_pilot.py` 2074 行（v1 基线 ~1249 + v2 ~825）。

### MASTER §8 触及文件

| 步骤                     | 文件                                                          |
| ------------------------ | ------------------------------------------------------------- |
| §8.1–8.2 caps / mutation | `mutation_proof.py`、`staged_pilot.py`（v2 常量）             |
| §8.3–8.9 九切片 evidence | `staged_pilot.py` `capture_*_v2` / `run_full_staged_pilot_v2` |
| §8.10 回归               | 间接（未审测试体，仅交叉引用首轮 A2）                         |

---

## ponytail 梯级评估

| 区域                                                                              | 评估                                                                                           | 最深梯级                         |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------- |
| `mutation_proof.all_table_row_counts`                                             | 闭合 AUD-04 row-count-only 漏洞；与 `key_table_row_counts` 同 ConnectionManager 只读模式       | **2 复用**（模式一致，实现重复） |
| `_derive_proof_status`                                                            | 三元 `proof_status` 为 AC 要求；7 行纯逻辑                                                     | **6 最小新码**                   |
| v2 `authorization_gate` 注入 `preview_staged_pilot` / `run_staged_pilot_raw_only` | 避免 fork v1 fetch 路径；ponytail 正确方向                                                     | **2 复用**                       |
| `validate_pilot_v2_authorization`                                                 | 与 `validate_authorization` 授权文件名/三元组/sandbox 检查 **逐行镜像**，仅 caps/envelope 不同 | **2 应复用未复用**               |
| 九函数 `capture_*_v2`                                                             | R3Y §4 九垂直切片各需独立 evidence 文件名与 done criteria；非单点 factory                      | **explicit AC**                  |
| `_route_status_examples_for_v2`                                                   | SP2-05 要求 selected/skipped/disabled/validation-only 全覆盖                                   | **explicit AC**                  |
| `capture_akshare_validation_taxonomy_v2` 默认 `records`                           | 无 live fetch 的 taxonomy 占位；任务卡允许 re-defer 记录                                       | **1? YAGNI 边缘**（见计划外）    |

---

## ponytail-review 逐条（过度工程 / 可删路径）

`staged_pilot.py:1337-1402: shrink: validate_pilot_v2_authorization 与 validate_authorization:201-260 约 50 行重复。可抽 _validate_authorization_core(request, *, max_rows, max_symbols, envelopes) 由 v1/v2 传入 caps；净减 ~45 行。`

`mutation_proof.py:14-61: shrink: key_table_row_counts 与 all_table_row_counts 共享 information_schema + COUNT 循环。可内联 _table_row_counts(db_path, table_names) 一处实现；净减 ~22 行。`

`staged_pilot.py:1626-1687 + 1690-1752: shrink: capture_baostock_evidence_v2 与 capture_cninfo_evidence_v2 结构同构（validate → reset budget → fetch_runner/run_staged_pilot_raw_only → 写 manifest）。可合并为 _capture_live_source_v2(request, *, manifest_writer=None)；净减 ~35–45 行，但降低九切片函数名与 MASTER trace 可读性。`

`staged_pilot.py:1536-1595: shrink: capture_route_preview_matrix_v2 与 capture_route_preview_matrix:422-469 预览+mutation 骨架重复。v2 增量主要是 route_status_examples + caps；可考虑 v1 加可选 hook 而非整段复制；净减 ~25 行。`

`staged_pilot.py:1907-1934: shrink: write_no_mutation_proof_v2 与 capture_validation_report 内 NO_MUTATION_MD 块:1099-1127 重复 markdown 模式；可共用 _format_mutation_proof_md(proof, pilot_id)；净减 ~15 行（未达 ≥20 单独计）。`

**net（激进收敛）：约 -120 行可能**，代价是授权/capture 函数签名变复杂；当前形态更利于 R3Y 九切片逐 issue 审查。

---

## §3.2 候选删改表（≥20 行门槛）

| 候选删改（file:line）                                                          | ponytail 梯级                                                | 是否阻塞     |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------ | ------------ |
| `staged_pilot.py:1337-1402` `validate_pilot_v2_authorization` 整段镜像 v1 授权 | 梯级 **2**（应抽 shared core）                               | **建议**     |
| `mutation_proof.py:14-61` 双 COUNT 实现                                        | 梯级 **2**（单 helper + KEY_TABLES / all tables）            | **建议**     |
| `staged_pilot.py:1626-1752` baostock/cninfo capture 同构对                     | 梯级 **2**（可合并，损 issue 可追溯性）                      | **不阻塞**   |
| `staged_pilot.py:1251-2073` v2 整块 vs 独立 `staged_pilot_v2.py`               | 梯级 **1?** 单文件 2k+ 行；但 v1 已 1.2k，拆文件增 import 面 | **不阻塞**   |
| `mutation_proof.py:40-61` `all_table_row_counts`                               | explicit AC（R3Y-MUT-PROOF-001 / AUD-04）                    | **不算候选** |
| 九 `capture_*_v2` 命名函数                                                     | explicit AC（R3Y §4 表）                                     | **不算候选** |

**阻塞候选：0**

---

## 与 A4 交叉引用

首轮 A4（`research/audit-evidence/a4.md`）称 v2 与 v1 `capture_*` 对称、无重复错误处理样板。

本复审计 **部分同意、补充一处**：

- **同意：** 无大面积 try/except 日志样板；`StagedPilotAuthorizationError` 复用合理。
- **补充（偏 A4，非 A2 阻塞）：** `run_staged_pilot_raw_only:708-711` 调用 `preview_staged_pilot` 时 **未传入** `authorization_gate`，v2 live 路径（`fetch_runner=None`）会在 preview 阶段走 v1 `validate_authorization` 并与扩样 envelope 冲突。测试用 `fetch_runner` 注入绕过（`test_staged_pilot.py:737-740`）。属正确性缺口，非 ponytail bloat；建议 A4/A5 跟进。

---

## DOUBT（≥20 行可简化？）

**CLAIM：** 存在 ≥20 行可删改簇（授权镜像、双 COUNT），但均属 **梯级 2 收敛**；删而不抽 shared helper 会损 fail-closed 语义或 AC trace。

**搜索范围：**

1. `mutation_proof.py` 全文（140 行）
2. `staged_pilot.py` v2 块 `1251–2073` 及 v1 对照 `validate_authorization`、`capture_route_preview_matrix`、`capture_validation_report`
3. 对照 `backend/app/ops/db_inspector.py`（`KEY_TABLES` SSOT）
4. 对照 `live_pilot_phase*.py` / `interface_probe.py` 对 `mutation_proof` 的消费（确认 `all_table_row_counts` 非单点死代码）
5. 首轮 `research/audit-evidence/a2.md`（对抗性复核，非降级依据）

**结论：** 找到 **2 处** 满足 ≥20 行且不改 AC 的收敛点（授权 core、COUNT helper）；均标 **NON-BLOCKING**。九切片独立函数为任务卡强制，不宜为省行合并成单脚本。

---

## A2 checklist

- [x] `git diff --stat` 已记录 Lxx / net lines
- [x] 每候选附 `file:line` + ponytail 梯级
- [x] 与 A4 过度抽象/样板交叉引用
- [x] 阻塞 vs 建议已区分

---

## 计划外发现

> 对抗性搜索：对照 R3Y 任务卡 §3–§9、设计 `docs/modules/data_validation_and_conflict.md`、`production_live_pilot_policy.md`；不限于 MASTER §8 矩阵。

| ID      | 发现                                                                                                                       | 严重度                            | 说明                                                                                            |
| ------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------- |
| U-A2-01 | `authorization_gate` 未传入 `preview_staged_pilot`（`staged_pilot.py:708-711`）                                            | **NON-BLOCKING**（A4/A5）         | v2 live fetch 默认路径可能 preview 阶段被 v1 envelope 拒绝；evidence 测试靠 `fetch_runner` mock |
| U-A2-02 | `capture_akshare_validation_taxonomy_v2` 默认 `records` 为静态 SUCCESS/NETWORK_ERROR 对（`1770-1786`），非 live retry 产出 | **NON-BLOCKING**（A5 证据真实性） | 符合任务卡「失败则 re-defer」叙事，但计划外 live 路径未实现 retry 逻辑                          |
| U-A2-03 | `validate_pilot_v2_authorization` 仍校验 v1 `APPROVED_PILOT_REQUESTS`（`1362-1366`）再校验 v2 envelope                     | **NON-BLOCKING**                  | 冗余守卫，不增安全，略增维护面；可随 U-A2-01 收敛时删除 v1 triple 检查                          |
| U-A2-04 | `staged_pilot.py` 单文件 2074 行                                                                                           | **NON-BLOCKING**                  | 超常规模块长度；v2 未拆 `staged_pilot_v2.py` 是 ponytail「少文件」与可读性折中                  |

**已对抗搜索、无新增 BLOCKING。**

---

## 总判定

| 项                    | 值                                                                                                                                                                                 |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **判定**              | **PASS**                                                                                                                                                                           |
| **BLOCKING**          | **0**                                                                                                                                                                              |
| **NON-BLOCKING 建议** | （1）抽 `_validate_authorization_core` 消 v1/v2 镜像；（2）`mutation_proof` 合并 COUNT 循环；（3）live v2 时 `preview_staged_pilot(..., authorization_gate=...)` 一行修复（交 A4） |

**Lean enough for staged pilot v2 scope.** 九切片函数名为审查可追溯性服务；最大可删簇为授权重复，建议在 debt-lite 切片收敛，不阻 Phase 7 合并。
