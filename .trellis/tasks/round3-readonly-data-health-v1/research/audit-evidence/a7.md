# A7 — audit-ops（DuckDB / 运维）

**任务：** `round3-readonly-data-health-v1`（Wave C · C-20）  
**维度：** A7 · database-administrator + sre-engineer + devops-incident-responder  
**模式：** 只读语义审查（无 migration / 无 init_db 复跑）  
**判定：** **PASS**

---

## 检查项

| 检查 | 命令 / 证据 | exit | 结果 |
| --- | --- | --- | --- |
| migration / schema 变更 | `git diff master -- specs/schema scripts/init_db.py backend/app/core/migrations` | 0 | 无 diff |
| 实现面 DB 写路径 | `rg -i "duckdb\|WriteManager\|apply_migration\|init_db\|writer\(\)" backend/app/ops/data_health*.py` | 1（无匹配） | PASS — 零 DB 接触 |
| 网络 / live fetch | `rg -i "subprocess\|requests\|httpx\|urllib" backend/app/ops/data_health*.py` | 1 | PASS |
| 依赖链 | `data_health.py` 仅 `PROJECT_ROOT` + `SourceRegistry`（YAML 契约） | — | PASS — 无 ConnectionManager |
| prod DuckDB 存在性 | `Test-Path data/duckdb/quant_monitor.duckdb` | — | **PROD_DB_ABSENT** |
| CLI 只读 smoke | `uv run python -m backend.app.ops.data_health_cli --evidence tests/fixtures/data_health/good_bundle --format json` | 0 | `production_db_mutated=false`，`source_fetch_performed=false` |
| Tier A 切片测试 | `uv run pytest tests/test_ops_data_health.py -q --basetemp=.trellis/tasks/round3-readonly-data-health-v1/.audit-sandbox/pytest` | 0 | **18 passed** |
| MAP 邻接回归 | `uv run pytest tests/test_ops_db_inspector.py tests/test_staged_pilot.py tests/test_data_quality_validator.py -q`（同 basetemp） | 0 | 全绿（1 skipped 为既有） |
| Execute 声称对齐 | `research/execute-evidence/8.10-green.txt` | — | 18 passed / ruff 绿 — 与 Audit 复跑一致 |

### 语义 / 运维面摘要

- **输入：** 仅 `Path.read_text` 读取 staged pilot evidence JSON（manifest / validation_report / closeout）；无 DuckDB 连接。
- **输出：** 内存 `DataHealthReport` + 可选 `--output` 写报告文件；**不写** `quant_monitor.duckdb`、不调用 `apply_migrations`、不触及 `WriteManager`。
- **gate 字段：** `evaluate_gate()` / `sandbox_clean_write_gate_ready` 为**只读语义闸门**（读 `allow_clean_write` / `sandbox_clean_write_rehearsal` 标志），不触发 clean write。
- **v2 集成：** `_DEFAULT_V2_EVIDENCE` 指向 archived `06-24-round3-real-data-staged-pilot-v2/execute-evidence`，只读遍历。

---

## GitNexus

`query("data_health duckdb write migration init_db")` — 返回 `ConnectionManager.writer`、`init_db`、`DbInspector.inspect` 等**邻接** ops/ingest 流，**未索引** `data_health` / `DataHealthService` 参与任何写库 execution flow（新模块尚未入索引或确实无写边）。

---

## DOUBT（database-administrator + sre-engineer）

| 疑点 | 结论 |
| --- | --- |
| 第二次 init/migrate 是否静默损坏？ | **不适用** — diff 无 schema/migration；实现不调用 `init_db.py`。 |
| kill migrate 后 schema 一致？ | **不适用** — 无 migration 路径。 |
| 异常后是否静默「成功」？ | CLI 缺 manifest 时 `load_error` → FAIL check + `gate_rationale` 可解释；pytest `test_dataHealthLoader_missingManifest_fails` 覆盖。 |
| smoke 输出可独立复现？ | 上表 CLI + pytest 命令在 audit-sandbox basetemp 下复现绿。 |

---

## 计划外发现

> 对抗性搜索范围：`data_health*.py`、`test_ops_data_health.py`、`R3Y` 禁止项、`docs/ops/data_health_cli.md` Batch6 设计 vs v1 实现、计划未列的 DB/污染面。

| ID | 发现 | 严重度 | 说明 |
| --- | --- | --- | --- |
| — | （无 BLOCKING） | — | 未发现计划外 prod DB 写、migration、init_db 依赖或 ResourceGuard 旁路。 |
| A7-O1 | `production_db_mutated` / `source_fetch_performed` 在 `build_report()` **硬编码为 False** | NON-BLOCKING | 报告字段为声明式，非运行时探针；若未来有人加 DB 写而未改字段，审计报告仍可能显示 false。任务卡 §5.2 已禁止写库；建议后续 Batch 若接 DuckDB 再加契约级 guard 或 hook。 |
| A7-O2 | `docs/ops/data_health_cli.md` 设计含 `--db` 只读 DuckDB；v1 CLI **无 `--db` 参数** | NON-BLOCKING | 与 R3Y §5.2 / MASTER「evidence-path 模式」一致，属显式 defer；非运维污染面。 |
| A7-O3 | CLI `--output` 可向任意可写路径落盘 | NON-BLOCKING | 非 DB 写；误用可覆盖用户文件。Batch6 设计亦允许 optional report file；无 prod 路径默认值。 |
| A7-O4 | 实现文件审计时为 **untracked**（`?? data_health*.py`, `test_ops_data_health.py`） | NON-BLOCKING | A7 语义 PASS；合并前须由主会话 commit，属 A5/流程面。 |

**已对抗搜索、无额外 DB/migration/数据污染面。**

---

## 结论

C-20 read-only data health 为 **evidence-path 纯读** 实现：JSON manifest 加载 + 规则校验 + 报告/gate 语义，**零 migration、零 DuckDB、零 prod 写、零 live fetch**。AUDIT.plan §2 A7「无 migration/DB 写 · 无 prod 写」满足。

**A7 判定：PASS**
