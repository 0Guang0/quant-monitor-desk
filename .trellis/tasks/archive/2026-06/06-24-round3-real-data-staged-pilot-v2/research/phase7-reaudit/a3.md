# A3 audit-security — Phase 7 复审计（B-19 staged pilot v2）

**维度：** A3 · security-and-hardening + doubt-driven-development  
**模型：** composer-2.5  
**worktree：** `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3-pilot-v2`  
**分支：** `feature/round3-real-data-staged-pilot-v2`  
**HEAD：** `fb044931`（`feat(ops): close B-19 staged pilot v2 Execute+Audit`）  
**任务：** `06-24-round3-real-data-staged-pilot-v2`  
**审查范围：** `mutation_proof.py`、`staged_pilot.py`、`test_staged_pilot.py` diff vs `master`  
**权威：** `agents/audit-adversarial-authority.md`（主仓）、`agents/security-auditor.md`、`R3Y_real_data_staged_pilot_v2.md` §9、`AUDIT.plan.md` §2 A3、AUD-08  
**模式：** 只读，无 commit

---

## 总判定

| 项               | 值       |
| ---------------- | -------- |
| **判定**         | **PASS** |
| **BLOCKING**     | **0**    |
| **NON-BLOCKING** | 3        |

---

## 1. 启动记录

| 项               | 内容                                                                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 对抗权威         | 已 Read `agents/audit-adversarial-authority.md`（主仓）、`agents/security-auditor.md`                                                 |
| 任务卡           | `R3Y_real_data_staged_pilot_v2.md` §6 caps、§9 禁止项                                                                                 |
| AUDIT.plan §2 A3 | `rg` production clean write / sync `adapter=` **新增** — 见 §2                                                                        |
| GitNexus         | `query(staged pilot production mutation proof)` + `context(build_production_mutation_proof)` — 索引行号略滞后于 fb04493，符号关系可用 |
| diff 范围        | `backend/app/ops/mutation_proof.py` +66/-?；`staged_pilot.py` +831；`test_staged_pilot.py` +488（与 A1 一致）                         |

---

## 2. AUDIT.plan §2 A3 验证矩阵

| 检查                        | 命令 / 方法                                                                                | 结果                | 证据                                                                                                                                                |
| --------------------------- | ------------------------------------------------------------------------------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| production clean write 旁路 | `rg` diff 三文件：`clean_write`、`writer(`、`apply_migrations`、`INSERT`/`UPDATE`/`DELETE` | **PASS**            | 生产路径仅 `build_production_mutation_proof` / `ConnectionManager.reader()`；`cm.writer()` 仅绑定 `sandbox_db`（`staged_pilot.py:482-493,747-773`） |
| sync `adapter=` 新增        | `rg adapter=` 三文件                                                                       | **PASS**            | 零匹配；本 diff **未引入** adapter 旁路                                                                                                             |
| R3Y §9 禁止项               | 静态对照 §9 列表                                                                           | **PASS**            | 见 §3.1                                                                                                                                             |
| AUD-08 控件                 | closeout + mutation proof 语义                                                             | **PASS**            | AUD-04 HIGH（假 VERIFIED）已在 `mutation_proof.py` 收紧；closeout `closeout_pass` 须 `VERIFIED`∧hash∧counts（`staged_pilot.py:2036-2041`）          |
| caps                        | v2 caps JSON vs 运行时门禁                                                                 | **PASS（附 NB-1）** | caps 冻结于 `pilot_v2_caps_payload`；授权 gate 校验 envelope（`validate_pilot_v2_authorization`）                                                   |
| 无 production clean write   | 写路径 + 测试                                                                              | **PASS**            | `allow_clean_write` 双 gate 拒绝；`production_clean_write: false`；`sandbox_clean_write_rehearsal: false`                                           |

---

## 3. §3.3 威胁面与发现

### 3.1 R3Y §9 禁止项对照（diff 内实现）

| 禁止项                                   | 结论                  | 证据                                                                                                                                                                                       |
| ---------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| production clean write by default        | **未违反**            | `allow_clean_write=False` 默认；`validate_authorization` / `validate_pilot_v2_authorization` 显式拒绝 `True`（`236-238,1372-1375`）；staging manifest `allow_clean_write: false`（`1672`） |
| production migration                     | **未违反**            | diff 无 `specs/schema` / `init_db`；`apply_migrations` 仅在 `_ensure_sandbox_db` 对 **sandbox** 空库（`488-493`）                                                                          |
| full market scan / full history backfill | **未违反**            | `full_market_scan: false`、`full_history_backfill: false`（`1320-1321`）；`APPROVED_PILOT_V2_REQUEST_ENVELOPES` 冻结小样本                                                                 |
| unbounded network calls                  | **未违反（附 NB-1）** | `_NetworkCallBudget` + `StagedPilotNetworkCapExceededError`（`153-163`）；v2 各 capture 前 `reset_network_call_budget()`                                                                   |
| TDX live fetch                           | **未违反**            | `tdx_pytdx` ∈ `DISABLED_PILOT_SOURCE_IDS`（`58`）；route matrix 仅 dry-run probe（`1493`）                                                                                                 |
| QMT / xqshare enablement                 | **未违反**            | `qmt_xtdata` / `qmt_xqshare` ∈ `DISABLED_PILOT_SOURCE_IDS`（`55-56`）                                                                                                                      |
| validation-only 作唯一事实源             | **未违反**            | akshare 强制 `fetch_daily_bar_validation`（`1400-1403,1764-1767`）；taxonomy `validation_only: true`、`primary_forbidden: true`（`1795-1796`）                                             |
| live FRED                                | **未违反**            | `fred` ∈ `DISABLED_PILOT_SOURCE_IDS`（`54`）                                                                                                                                               |
| production-live readiness 声称           | **未违反**            | `production_live_readiness_claim: false`（`2047`）；closeout 测试断言（`test_staged_pilot.py:1127`）                                                                                       |

### 3.2 信任边界（生产 vs sandbox）

| 边界              | 结论     | 证据                                                                                                                                          |
| ----------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 生产 DuckDB 只读  | **闭合** | `DEFAULT_PRODUCTION_DB = DATA_ROOT / "duckdb" / "quant_monitor.duckdb"`（`36`）；mutation proof 用 `cm.reader()`（`mutation_proof.py:20,46`） |
| sandbox 写隔离    | **闭合** | `run_staged_pilot_raw_only` 在 `sandbox_root` 下 `_ensure_sandbox_db`；raw 路径 `is_relative_to(sandbox_resolved)` 校验（`793-795`）          |
| WriteManager 路径 | **受控** | `_StagedPilotValidationGate` 仅允许 metadata-only + `can_write_clean=false` 的 `append_only`（`621-661`）；非 clean table                     |
| fixture 端口禁止  | **闭合** | `_assert_real_fetch_port` 拒绝 `StubFetchPort` / `LocalFixtureFetchPort`（`473-479`）                                                         |

### 3.3 AUD-08 / AC-MUT-001（mutation proof v2）

| 控件                       | 结论                        | 证据                                                                                                                                                                 |
| -------------------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| VERIFIED 语义收紧          | **满足 AUD-08 P1 修复方向** | `_derive_proof_status`：KEY + **全表** count 不变且 hash 不变才 `VERIFIED`（`mutation_proof.py:64-75,125-129`）                                                      |
| DB 缺失 fail-closed        | **满足**                    | `INCONCLUSIVE` + `reason: production_db_file_missing`（`97-108`）                                                                                                    |
| closeout 不得仅凭 VERIFIED | **满足**                    | `closeout_pass = hash_ok ∧ counts_ok ∧ proof_status=="VERIFIED"`（`staged_pilot.py:2036-2041`）；单测 `test_stagedPilotV2_closeoutRequiresHashAndRowCountsUnchanged` |
| hash/count 明细            | **满足**                    | `no_mutation_proof_v2.md` 含 `proof_status`、key/all table counts 段（`write_no_mutation_proof_v2` `1923-1974`）                                                     |

### 3.4 静态 rg 基线（A3 checklist · 范围 = diff 三文件）

| 模式                                          | 范围                                                           | 结果                                                                                                             |
| --------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `https?://\|api_key\|secret\|token\|password` | `mutation_proof.py`、`staged_pilot.py`、`test_staged_pilot.py` | **0 匹配**                                                                                                       |
| `f\".*SELECT\|execute\(f`                     | 同上                                                           | **2 处** — `mutation_proof.py:35,59`；表名来自 `KEY_TABLES` / `information_schema`，**非用户输入**（见 DOUBT-3） |
| `enable.qmt\|enable.xqshare`                  | 同上                                                           | **0 匹配**                                                                                                       |
| `subprocess\|os.system\|eval(\|exec(`         | 同上                                                           | **0 匹配**                                                                                                       |
| `adapter=`                                    | 同上                                                           | **0 匹配**                                                                                                       |

---

## 4. DOUBT（三类隐蔽威胁）

| 类                        | 搜索范围                                               | 结论                                                                                                                                                                                 |
| ------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **DOUBT-1 硬编码 URL**    | diff 三文件 `rg https?://`                             | **无发现**                                                                                                                                                                           |
| **DOUBT-2 JWT / API key** | diff 三文件 `rg api_key\|secret\|token\|password`      | **无发现**                                                                                                                                                                           |
| **DOUBT-3 SQL 拼接**      | `mutation_proof.py` `f'SELECT COUNT(*) FROM "{name}"'` | **无用户可控注入面** — `name` 源自 `KEY_TABLES` 常量或 DuckDB `information_schema`；`staged_pilot.py` 写路径 `INSERT` 使用 `?` 占位符（`581-600`）。等级 **P3 hygiene**，非 BLOCKING |

---

## 5. 测试安全覆盖（`test_staged_pilot.py` diff）

| 主题                     | 测试                                                                        | 评价                                                                                                       |
| ------------------------ | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `allow_clean_write` 拒绝 | `test_stagedPilot_authorization_rejectsExpandedEnvelope`（v1）              | v2 无独立 `allow_clean_write=True` 用例，但 **同一 gate 逻辑**于 `validate_pilot_v2_authorization`（NB-2） |
| 禁用源                   | `test_stagedPilot_disabledSource_rejectsFetch`                              | PASS                                                                                                       |
| 网络 cap                 | `test_stagedPilot_networkCallCapEnforced`                                   | 覆盖 v1 `MAX_NETWORK_CALLS_PER_RUN=10`，**未**覆盖 v2 caps=25 叙事（NB-1）                                 |
| mutation 三态            | `test_mutationProof_*` ×5 + v1 inconclusive                                 | PASS — 含非 KEY 表变异（`974-1014`）                                                                       |
| AUD-08 closeout gate     | `test_stagedPilotV2_closeoutRequiresHashAndRowCountsUnchanged`              | PASS                                                                                                       |
| sandbox 路径             | 代码级 `is_relative_to(sandbox_resolved)`（`793-795`）；测试多用 mock fetch | **无**独立「路径越界」单测；mock 路径在 `.audit-sandbox/mock/`（NB-2 测试缺口）                            |

---

## 计划外发现

> 对抗性搜索：契约信任边界 + 任务卡 §9 + MASTER/AUDIT **未写**的 mutation/bypass/资源上限旁路。

| ID           | 等级              | 描述                                                                                                                                                                                                                                                                                                      | 证据                                                                                                                                                                                                               | 阻塞? |
| ------------ | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- |
| **OOB-A3-1** | P2 · NON-BLOCKING | **v2 caps JSON 声明 `max_network_calls_per_run=25`，运行时 `_NetworkCallBudget` 默认仍为 `MAX_NETWORK_CALLS_PER_RUN=10`**；且 baostock/cninfo/akshare 各 capture 前 `reset_network_call_budget()`，整次 `run_full_staged_pilot_v2` 理论上可消耗 **3×10** 次 budget slot，与 R3Y §6 单 run 25 次叙事不一致 | `MAX_NETWORK_CALLS_V2=25`（`1271,1318`）vs `_NetworkCallBudget(limit=MAX_NETWORK_CALLS_PER_RUN)`（`153-154`）；`reset_network_call_budget()`（`1634,1698`）；caps 测仅断言 JSON 字段（`test_staged_pilot.py:701`） | 否    |
| **OOB-A3-2** | P2 · NON-BLOCKING | **网络 budget 粒度 = 每次 `run_staged_pilot_raw_only` 调用 1 次 `consume()`**，非每次 HTTP；若 `create_staged_fetch_port` 内部多轮请求，可能低于 R3Y「unbounded network」意图的实际约束强度                                                                                                               | `staged_pilot.py:736` 单次 `consume()`；fetch 仅取 `symbols_or_indicators[0]`（`763`）— 扩样符号在 envelope 内但单次 fetch 未遍历                                                                                  | 否    |
| **OOB-A3-3** | P3 · NON-BLOCKING | **仓库级 AUD-08 残留：sync `adapter=` 旁路**（`run_incremental`/`reconcile` 等）不在本 diff，但 R3Y §9 / AUD-08 要求扩样前确认无生产 CLI 经此路径；**本任务未新增旁路，亦未修复全局旁路**                                                                                                                 | `R3Y-AUD-08-go-no-go.md` §跨模块 HIGH #1；本 diff `rg adapter=` 零匹配                                                                                                                                             | 否    |

**显式声明：** 已对上述计划外项做静态搜索与代码路径追踪；**无 BLOCKING 级 mutation 旁路或 production clean write**。

---

## 6. GitNexus

| 调用                                             | 摘要                                                                                                                                     |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `query` — staged pilot production mutation proof | 返回 `capture_validation_report`、`build_production_mutation_proof` 等相关流；v2 符号 `capture_route_preview_matrix_v2` 等可能未完全索引 |
| `context(build_production_mutation_proof)`       | 上游：`capture_route_preview_matrix`、`run_staged_pilot_raw_only`、`capture_validation_report` 等；**无**直接 `writer()` 调用            |

---

## 7. 结论与建议

**PASS（BLOCKING = 0）。** 本 diff 在 staged-only / sandbox-first 信任边界内实现 v2 扩样与 mutation proof 收紧：生产库只读、写路径隔离于 sandbox WriteManager、`allow_clean_write` 与 R3Y §9 禁止项均有 fail-closed gate；AUD-04 类「假 VERIFIED」问题已在 `mutation_proof.py` 修复并由单测锁定。

**建议（非阻断，可记入后续 debt-lite）：**

1. 将 `_NetworkCallBudget` 默认 limit 与 `MAX_NETWORK_CALLS_V2` 对齐，或整次 v2 run 共享单一 budget（修复 OOB-A3-1）。
2. 为 `validate_pilot_v2_authorization` 增加 `allow_clean_write=True` 拒绝用例（与 v1 对称）。
3. 若未来 live fetch 遍历多 symbol，评估 per-HTTP `consume()` 或 adapter 层 cap（OOB-A3-2）。

---

**产出绝对路径：** `C:\Users\Guang\Desktop\quant-monitor-desk-wt-r3-pilot-v2\.trellis\tasks\06-24-round3-real-data-staged-pilot-v2\research\phase7-reaudit\a3.md`
