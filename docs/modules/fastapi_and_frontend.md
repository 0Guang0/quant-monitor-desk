# FastAPI 与前端模块兼容索引

> 本文件保留用于兼容旧引用；不再作为权威实现文件。

权威实现文件已经拆分为：

```text
docs/modules/fastapi_backend.md
docs/modules/frontend_dashboard.md
```

后续 Claude Code / Codex 实现时，应分别读取这两个文件，而不是继续把后端和前端混在一个文件中。
