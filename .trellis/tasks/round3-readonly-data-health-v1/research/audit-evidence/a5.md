# A5 audit-completion — Wave C C-20

**维度：** A5 · verification-before-completion + doubt-driven-development  
**派发模型：** composer-2.5  
**工作区：** `quant-monitor-desk-wt-r3-data-health`  
**分支：** `feature/round3-readonly-data-health-v1` @ `555fd33b`（实现文件 **未提交**）  
**审计时间：** 2026-06-24  
**判定：** **FAIL**（AC 追溯链存在 BLOCKING 缺口；playbook §8.1 未全行满足）

---

## 1. 启动清单

| 项 | 状态 |
| --- | --- |
| `agents/audit-adversarial-authority.md` | 已读 |
| `agents/audit-a5-completion.md` | 已读 |
| `agents/quant-analyst.md` | 已读（方法层抽检） |
| `agents/risk-manager.md` | 已读（fail-closed / defer 抽检） |
| `R3Y_readonly_data_health_v1.md`（任务卡） | 已读（经 MASTER §2 / audit.jsonl 索引） |
| `WAVE_C_MAIN_SESSION_COORDINATOR_PLAYBOOK.md` §8.1 | 已读 |
| `AUDIT.plan.md` 全文 | 已读 |
| `MASTER.plan.md` §2 / §8–§10 | 已读 |
| `implement.jsonl` 全读（69 行） | 已读 |
| `audit.jsonl` | 已读（10 行） |
| `manifest-amend.md` | **不存在** |

**约束：** 只读审计；未 `git commit`；未改生产库；未改 Execute 验收库。

---

## 2. A5 Checklist

| 检查项 | 结果 |
| --- | --- |
| 每条 AC 追溯链 + 1–5 分 | ✅ §3.5 |
| §10 最弱 2 行 sandbox 抽检 | ✅ §4（Tier B 红；Tier A 邻接绿） |
| `execute-evidence/*-green.txt` 非占位 | ⚠️ §5（有 exit 摘要但缺完整 pytest session → §4.3） |
| audit-prod-path（registry §2.1） | ✅ N/A — 只读 evidence 路径；见 §7 |
| registry / defer 项 | ✅ 未改 registry；`R3-B2.75-REQ2-EM` 保持 DEFERRED |
| `validate-execute-handoff` | ✅ exit 0（2026-06-24 复验） |
| `validate-plan-freeze` | ❌ exit 1 — 见 §4.3 A5-B02 |

---

## 3. Playbook §8.1 / MASTER §2 — 逐行 PASS 对照

| Playbook §8.1 维度 | MASTER AC | 判定 | 阻断原因 |
| --- | --- | --- | --- |
| **Plan** | AC-DH-PLAN | **FAIL** | `validate-plan-freeze` 失败：`test_ops_data_health.py` 未入 `test_catalog`；`data_health*.py` 未入 `implement.jsonl` |
| **实现** | AC-DH-IMPL | **PASS** | `data_health.py` + `data_health_cli.py` 存在；只读；无 prod DB 写 / fetch / migration |
| **业务 AC** | AC-DH-BIZ | **FAIL** | `_checks_from_bundle` 未读 bar/metadata 载荷（A1-01）；v2 集成测不验证日 K/metadata 规则曾运行 |
| **规则面** | AC-DH-RULES | **PASS**（单元） | 13/13 `rule_id` 有独立 `test_*`；但 evidence 集成路径未接线 |
| **切片证据** | AC-DH-SLICE | **PASS**（薄） | §8.0–8.9 RED/GREEN 文件齐；green 输出过简（§5） |
| **测试** | AC-DH-TEST | **FAIL** | Tier B `uv run pytest -q` **5 failed**（loop/trellis 元测）；`test_catalog` 缺口 |
| **MAP 验证** | AC-DH-MAP | **部分** | Tier A 切片 + 邻接 + ruff 绿；Tier B 全库未绿 |
| **Audit** | AC-DH-AUDIT | **进行中** | 本维 A5；A1 已 FAIL（BLOCKING） |
| **边界** | AC-DH-BOUND | **PASS** | 未改 forbidden；无 production-live 声称 |
| **门禁陈述** | AC-DH-GATE | **PASS**（字段） | `sandbox_clean_write_gate_ready` + `gate_rationale` 存在；gate 逻辑 fail-closed 诚实 |

**结论：** playbook §8.1 **不能**标 PASS — Plan、业务 AC、测试/MAP 三行未满足。

---

## 4. §3.5 — AC 追溯与评分

| AC# | 追溯链（原始 → MASTER → §8 → 证据） | 分 | 抽检/复验 |
| --- | --- | --- | --- |
| **AC-DH-PLAN** | R3Y 九切片 → MASTER §2 → freeze / `implement.jsonl` → `plan.freeze.md` | **3** | `validate-plan-freeze` **FAIL**（catalog + implement 缺口） |
| **AC-DH-IMPL** | R3Y §3 只读 → §8.8 → `data_health*.py` + CLI 测 | **4** | ruff 绿；`production_db_mutated`/`source_fetch_performed` 硬编码 false |
| **AC-DH-BIZ** | R3Y §3(2)(3)(9) → §8.9 → `test_dataHealthIntegration_v2Evidence_bundle` | **2** | sandbox 1 passed；**不读** manifest bar 文件（A1-01） |
| **AC-DH-RULES** | R3Y §5.1 + MASTER §6.1 → §8.3–8.6 → 13× `test_*` | **4** | 单元路径 13/13；集成路径未跑日 K/metadata |
| **AC-DH-SLICE** | R3Y-DH-01..09 → §8.1–8.9 → `research/execute-evidence/8.*` | **4** | RED 含真实 FAIL；GREEN 仅摘要行 |
| **AC-DH-TEST** | playbook §2.2 → §10 → `test_ops_data_health.py` | **3** | 18/18 绿；Tier B 全库 5 failed |
| **AC-DH-MAP** | MAP §2.2 C-20 → §10 Tier A/B | **3** | Tier A 邻接 145 collected 绿；Tier B 未绿 |
| **AC-DH-AUDIT** | playbook §8.1 Audit 行 → A1–A8 | **—** | A1 FAIL BLOCKING；本维 A5 FAIL |
| **AC-DH-BOUND** | MAP forbidden + `production_live_pilot_policy` → §8.8–8.9 | **5** | `git diff master` 无 layer4/registry；gate 未声称 live |
| **AC-DH-GATE** | grill-me Q4 → §8.9 → `evaluate_gate` + 集成测 | **4** | 字段齐；v2 closeout `sandbox_clean_write_rehearsal=false` 时 rationale 诚实 |

**均分：** 3.4 / 5 — **FAIL**（< 4.0 且含 AC-DH-BIZ=2）

### MASTER §8.1（R3Y-DH-01 切片）专项

| 字段 | Execute 声称 | A5 复验 |
| --- | --- | --- |
| RED | `execute-evidence/8.1-red.txt` — Plan stub FAIL | 与 stub 阶段一致 |
| GREEN | `8.1-green.txt` — `1 passed exit code: 0` | `uv run pytest …::test_dataHealthModel_roundtrip_success -q` → **1 passed, exit 0** ✅ |
| 语义 | severity/rule_id roundtrip | `check_result_to_dict` / `check_result_from_dict` 往返 — 与测一致 |

**§8.1 切片本身：** 实现 + sandbox 复绿 ✅；证据文件真实性 ⚠️（见 §5）。

---

## 5. §10 最弱 2 行 — sandbox 抽检

选取：Tier **B**（全库，Execute 自述 blocked）+ Tier A **邻接**（间接、非切片本体）。

| # | §10 原文 | 复跑命令 | exit | 与 Execute 一致？ |
| --- | --- | --- | --- | --- |
| 1 | **B** `uv run pytest -q`（仅 §8.10） | 同上 | **1** | ❌ Execute `8.10-green.txt` 自述 Tier B blocked；A5 复跑 **5 failed**（全为 loop/trellis 元测） |
| 2 | **A** 邻接：`test_ops_db_inspector` + `test_staged_pilot` + `test_data_quality_validator` + `test_source_conflict_validator` + `test_db_validation_gate` + `test_raw_store` | 同上 `-q` | **0** | ✅ 145 collected（1 skip），与 Tier A 口径一致 |

**补充 — 切片套件（必做）：**

| 命令 | exit | 摘要 |
| --- | --- | --- |
| `uv run pytest tests/test_ops_data_health.py -q` | **0** | **18 passed** |
| `uv run pytest tests/test_ops_data_health.py -k dataHealthLineage -q` | **0** | **6 passed**（§8.5） |
| `uv run pytest tests/test_ops_data_health.py::test_dataHealthIntegration_v2Evidence_bundle -v` | **0** | **1 passed in 0.10s**（含完整 session 头） |

**排除元测后的全库：** `uv run pytest -q --ignore=tests/test_loop_engineering_flow.py --ignore=tests/test_manifest_protocol.py --ignore=tests/test_trellis_validate_plan.py` → **exit 0**（~115s）。说明业务测已绿，阻断来自 **loop engineering 登记**，非 data_health 回归。

---

## 6. execute-evidence 真实性抽检（2 个 green.txt）

| 文件 | 内容摘要 | 判定 |
| --- | --- | --- |
| `research/execute-evidence/8.1-green.txt` | `1 passed` + `exit code: 0`（无 pytest session 头/耗时） | **薄 GREEN** — 可复现但不符合「完整终端输出」最佳实践 → §4.3 NB |
| `research/execute-evidence/8.9-green.txt` | 同上 | **薄 GREEN** — 集成步最关键却最简；A5 独立 `-v` 复跑有完整 session |

| 文件 | 内容摘要 | 判定 |
| --- | --- | --- |
| `research/execute-evidence/8.1-red.txt` | `pytest.fail Plan stub` + exit 1 | **真 RED** |
| `research/execute-evidence/8.9-red.txt` | `pytest.fail` 集成 stub + exit 1 | **真 RED** |
| `research/execute-evidence/8.10-green.txt` | Tier A 18 passed + ruff；**Tier B note: full pytest blocked** | **诚实自述** — 与 A5 Tier B 失败一致 |

---

## 7. audit-prod-path（AUDIT.plan §0 · registry §2.1）

| 项 | 状态 |
| --- | --- |
| `AUDIT_PROD_ROOT` = `.audit-sandbox/r3-dh-audit-prod-equiv` | **未创建** |
| 任务性质 | 只读 staged evidence；零写路径（`write_contract.yaml`） |
| prod DuckDB / `QMD_DATA_ROOT` 变异 | **不适用** |
| 替代验证 | v2 archive evidence 路径只读集成测 + 邻接 pytest + 边界字段断言 |

**判定：** N/A（与 B-19 pilot v2 A5 先例一致）；不得以此跳过 AC-DH-BIZ 集成规则缺口。

---

## 8. quant-analyst 视角（方法层）

| 检查 | 结果 |
| --- | --- |
| 指标/规则有可复现命令 | ✅ 18 测 + 明确 `-k` / node id |
| sandbox 数据路径 | ✅ `tests/fixtures/data_health/` + v2 archive evidence |
| evidence 是 fixture 还是 prod-equivalent | ⚠️ v2 为 staged mock 路径；集成不测 bar 载荷 → **结论在 sandbox 量级下不足以支撑「evidence 数据健康」声称** |
| look-ahead / 多重试参 | N/A（非 alpha 任务） |
| 指标定义与代码一致 | ✅ 单元层 `rule_id` 与 MASTER §6.1 一致 |

**量化结论：** 单元规则可信；**集成层证据链不足以支撑 AC-DH-BIZ**（对齐 A1-01）。

---

## 9. risk-manager 视角（fail-closed）

| 检查 | 结果 |
| --- | --- |
| DEFERRED / REQ2-EM | ✅ 未作 live unblock 依据 |
| live 源默认 DISABLED | ✅ `DISABLED_SOURCE_USED` 单测 + `SourceRegistry` 只读对照 |
| 写库 / fetch | ✅ 报告硬编码 `production_db_mutated=false`、`source_fetch_performed=false` |
| gate fail-closed | ✅ `evaluate_gate` 在 FAIL checks / `allow_clean_write=false` 时 `sandbox_clean_write_gate_ready=false` |
| 未提交实现 | ⚠️ merge 前无版本化锚点 — 操作风险 |

**风险结论：** 边界字段诚实；**业务集成半壳**为 P0 模型风险（非 cyber 层）。

---

## 10. §4.3 — Findings

### [A5-B01] Tier B 全库 pytest 未绿 — **BLOCKING**

- **Location:** `tests/test_catalog.yaml`；`tests/test_loop_engineering_flow.py`
- **Evidence:** `uv run pytest -q` → 5 failed，均因 `unregistered test module: tests/test_ops_data_health.py`
- **AC:** AC-DH-TEST、AC-DH-MAP；playbook §8.1「测试」「MAP 验证」行
- **Fix:** `uv run python scripts/loop_maintain.py --fix`（登记 catalog + verification matrix）；复跑 Tier B

### [A5-B02] validate-plan-freeze 失败 — **BLOCKING**

- **Evidence:** `python .trellis/scripts/task.py validate-plan-freeze` → E8 + catalog/matrix 错误（同 B01）
- **AC:** AC-DH-PLAN

### [A5-B03] evidence 集成路径未执行日 K/metadata（继承 A1-01）— **BLOCKING**

- **Location:** `data_health.py:565-636` `_checks_from_bundle`
- **Evidence:** 仅 lineage + staleness + staging 缺失；不读 `raw_file_paths`
- **AC:** AC-DH-BIZ；playbook §8.1「业务 AC」行
- **Cross-ref:** `research/audit-evidence/a1.md` [A1-01]

### [A5-B04] 实现与任务工件未提交 — **BLOCKING（Execute 合并门）**

- **Evidence:** `git status` → `?? backend/app/ops/data_health*.py`、`?? tests/test_ops_data_health.py`、`?? .trellis/tasks/round3-readonly-data-health-v1/`
- **Impact:** 分支 HEAD 仍为 docs commit；Audit 证据无法锚定到可合并 SHA

### [A5-NB01] GREEN 证据缺少完整 pytest session — **NON-BLOCKING**

- **Evidence:** `8.1-green.txt`、`8.9-green.txt` 仅一行 pass 摘要
- **Recommendation:** Repair 阶段补 `-v` 或 `--tb=no` 完整 session 到 `execute-evidence/`

### [A5-NB02] 集成测 `overall_status` 断言过宽 — **NON-BLOCKING**

- **Cross-ref:** A8 已记；`in {PASS,WARN,FAIL}` 对任意合法实现恒真

---

## 11. 计划外发现

| ID | 来源 | 发现 | 等级 | Blocking |
| --- | --- | --- | --- | --- |
| OOB-A5-01 | 对抗 R3Y §3 vs `_checks_from_bundle` | evidence 路径不跑日 K/metadata | P0 | **BLOCKING**（= A1-01） |
| OOB-A5-02 | §10 Tier B vs 排除元测全库 | 业务测全绿但 loop 登记阻断正式 Tier B | P1 | **BLOCKING**（= A5-B01） |
| OOB-A5-03 | git status | 实现未提交 | P1 | **BLOCKING**（= A5-B04） |
| OOB-A5-04 | green.txt 格式 | 无完整 pytest session | P3 | NON-BLOCKING |
| OOB-A5-05 | `implement.jsonl` vs §4 代码地图 | 未索引 `data_health.py` / `data_health_cli.py` | P2 | NON-BLOCKING（叠加 B02） |

**对抗搜索声明：** 已对照 R3Y 任务卡、playbook §8.1、MASTER §8.1/§10、`data_quality_rules.yaml`、`production_live_pilot_policy.md`、A1/A8 首轮报告及 `_checks_from_bundle` 运行时路径；除上表外未发现新的 forbidden 触及或 production-live 声称。

---

## 12. 总判定与 Repair 最小路径

| 项 | 判定 |
| --- | --- |
| **A5 维度** | **FAIL** |
| **playbook §8.1** | **FAIL**（Plan / 业务 AC / 测试 未全行绿） |
| **MASTER §8.1 切片（R3Y-DH-01）** | 实现 ✅；证据薄 ⚠️ |
| **BLOCKING 数** | **4**（B01–B04） |
| **NON-BLOCKING** | **2**（NB01–NB02） |

**Repair 最小顺序（不扩 scope）：**

1. `_checks_from_bundle` 只读加载 manifest 指向的 bar/metadata 文件并调用既有 `check_daily_bars` / `check_metadata_rows`（闭合 A1-01 / A5-B03）
2. `loop_maintain.py --fix` + Tier B `uv run pytest -q` 绿（闭合 A5-B01/B02）
3. 补 integration 断言（至少一条经 service 路径的坏 bar FAIL）
4. 提交 allowed 文件 + 任务工件 + 刷新 `execute-evidence/*-green.txt`（闭合 A5-B04/NB01）
5. 复跑 `validate-plan-freeze` + `validate-execute-handoff` → A5 复审计

**不以自述为 PASS** — Execute `8.10-green.txt` 已诚实标注 Tier B blocked；A5 sandbox 独立复验确认该阻断仍成立。
