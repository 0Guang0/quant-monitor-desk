# Agent 工具链路由（指针）

> **SSOT：** [`../agent-toolchain.md`](../agent-toolchain.md)（仓库根）  
> **禁止裸执行**：非一问一答任务前必须先 Read SSOT，按需加载 / `@` skill。  
> 本文件仅作 `task/` 内快捷入口；**修改请只改根目录 `agent-toolchain.md`**。

完整个人路由见桌面 `skill路由.txt`。
