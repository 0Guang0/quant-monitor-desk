## A5 Traceability

### Verdict

**PASS_WITH_FIXES** — AC-1…AC-10 and AC-12 trace cleanly (implementation → test → execute evidence) at score ≥4. **AC-11** is capped at **4** because strict MASTER §9.3 `pytest -q` (no `--ignore`) still fails on six Trellis meta tests; Execute disclosed this as **O-3** and did not claim literal §9.3 green. **O-2** (manifest / `implement.jsonl` gap) is **closed** in the current tree; **O-1** (ruff E501 in Trellis meta-test) remains unrelated infrastructure debt.

---

### AC scorecard (1–5)

| AC | 预期结果 (MASTER §2) | Impl | Test | Evidence | Score | Notes |
|----|---------------------|------|------|----------|-------|-------|
| AC-1 | 六种 `job_type` 可 `create_job` 且状态转移符合契约 | `backend/app/sync/jobs.py` (`create_job`, `_BASE_TRANSITIONS`, per-type overrides L271–276) | `tests/test_sync_jobs.py` (`full_load`, `incremental`, `revision_audit`, `data_quality`); `tests/test_sync_orchestrator.py` (`backfill` L237+, `reconcile` via `run_reconcile` L395+) | `8.2-green.txt` (12 passed); `tier-a-latest.txt` exit 0 | **5** | All six types exercised; `reconcile` created inside `run_reconcile` per §4.2 |
| AC-2 | 六种 `job_type` 各有语义测试（骨架边界） | Same + `orchestrator.py` (`run_incremental`, `run_backfill`, `run_reconcile`) | Jobs skeleton: `test_syncJob_*` L80–127; Backfill: `test_backfillJob_*` L219–319; Reconcile: `test_reconcileJob_*` L395–427; Incremental E2E: `test_batch_d_orchestration_flow.py` (3 tests) | `8.5-green.txt`, `8.6-green.txt`, `8.7-green.txt`, Tier A audit 26 passed | **5** | FullLoad/RevisionAudit skeleton-only per §4.2; Incremental full E2E in §8.5 |
| AC-3 | Backfill eco 自动分片 ≤31 天/task | `jobs.py` `plan_backfill_shards` (`ECO_MAX_BACKFILL_DAYS_PER_TASK = 31`) | `test_backfillJob_largeRange_splitsIntoTasks`, `test_backfillJob_eachShard_callsResourceGuardBeforeFetching` | `8.6-green.txt` (7 passed) | **5** | Shard loop + mid-shard failure preservation tested |
| AC-4 | `data_sync_job` + `job_event_log` 持久化 run/job/task | `jobs.py` `_transition_on_con`; `orchestrator.py` `emit_event` | `test_syncJob_transition_createdToPlanned_recordsEvent`; `test_orchestrator_emitEvent_linksRunJobTask`; migration asserts tables | `8.1-green.txt`, `8.3-green.txt` | **5** | IDs flow through create → transition → event log |
| AC-5 | FETCHING 前 `ResourceGuard.check()`；暂停 → `FAILED_RETRYABLE` + message 含 `RESOURCE_GUARD_PAUSED` | `orchestrator.py` `begin_fetching` L55–77 | `test_orchestrator_fetchBlockedWhenGuardPaused_setsFailedRetryable`; `test_backfillJob_eachShard_callsResourceGuardBeforeFetching` | `8.4-green.txt` (2 passed) | **5** | Status stays contract-valid (`FAILED_RETRYABLE`, not invented status) |
| AC-6 | Incremental: Adapter→staging→DQ→SC→Gate→WriteManager | `orchestrator.py` `run_incremental` L125–260 | `test_incrementalJob_happyPath_writesCleanAndCompletes`; validation-fail + idempotency tests | `8.5-green.txt` (3 passed); `8.9-smoke.txt` | **5** | `adapter.fetch(..., con=, job_id=)` on writer `con` (A2 verified) |
| AC-7 | migration 006 对齐 `schema.sql` L73–113 | `backend/app/db/migrations/006_ingestion_sync.sql` | `tests/test_sync_migration.py` (fresh DB, idempotent init, 004/005 checksum) | `8.1-green.txt` (3 passed) | **5** | `006_ingestion_sync` in `schema_version` |
| AC-8 | Orchestrator 不直写 clean；不经 WriteManager 不写 clean | `orchestrator.py` L229–241 `WriteManager.write` only | `test_incrementalJob_happyPath_*`; Tier B `tests/test_write_manager.py` regression | `8.5-green.txt`; A2 static review PASS | **5** | No clean-table INSERT in orchestrator SQL |
| AC-9 | `ci_ingestion_smoke.py` 覆盖 orchestrator 路径 (GPT-P3-6) | `scripts/ci_ingestion_smoke.py` `_orchestrator_smoke` | Smoke self-test + audit-prod-path re-run | `8.9-green.txt`, `8.11-final-gates.txt` L8 | **5** | `orchestrator_smoke: ok` on `.audit-sandbox/prod-path-data` |
| AC-10 | `sync_registry.py` / bootstrap YAML→DB (`tombstone_missing=True`) | `scripts/sync_registry.py`; `orchestrator.py` `bootstrap` | `test_syncRegistry_cli_syncsYamlToDb`; `test_orchestratorBootstrap_callsSyncToDb` | `8.8-green.txt`; `8.10-green.txt` | **5** | Reuses `SourceRegistry.sync_to_db`, no duplicate tombstone |
| AC-11 | 全库 pytest、ruff、compileall、production_gate 通过 | Delivered sync stack + gates | Tier A/B/C + `production_gate.py` | `8.11-final-gates.txt`, `8.11-handoff.txt` | **4** | **Gap:** strict §9.3 `pytest -q --cov=backend --cov-fail-under=75` → **6 FAIL** (Trellis meta only); `ruff check .` → **5 errors** (O-1, pre-existing). Excluding Trellis meta: **329 passed**, cov **93.80%**, compileall/production_gate/doc_links/smoke **PASS** |
| AC-12 | 不实现 Layer/API·前端/Agent/真实 Port/全量 security CI | Scope limited to `backend/app/sync/*`, scripts, tests | No `frontend/*` / Layer edits; `implement.jsonl` defers Round 3–5 items | `original-plan-trace.md` deferred table; MASTER §3.2 | **5** | Batch D backend-only boundary respected |

**Score summary:** 11×5, 1×4 (AC-11). All AC ≥4 → traceability bar for PASS met; AC-11 fix items routed to §4.3 (O-1, O-3).

**`original-plan-trace.md`:** Task card `014` → AC-1…AC-12 mapping present and matches MASTER §2 verification chain (§8.x + §10 tiers).

---

### Evidence抽检 — Execute §10 Tier B（最弱 2 行）

MASTER §9.2 Tier B 共四行；与 Batch D 编排距离最远、仅作回归的两行为 **§9.2 第 3、4 行**（WriteManager / Adapter 契约，继承自 Batch A/B）。

| §9.2 line | Command (audit-sandbox) | Execute claim | Audit re-run (2026-06-19) |
|-----------|-------------------------|---------------|---------------------------|
| L3 | `pytest tests/test_write_manager.py -q` | (bundled in Tier B; no standalone tier-b file) | **PASS** — exit 0 (combined with L4) |
| L4 | `pytest tests/test_data_adapter_contract.py -q` | same | **PASS** — 64 tests, exit 0 |

```text
# Audit command (combined weakest two lines)
pytest tests/test_write_manager.py tests/test_data_adapter_contract.py -q
# → ................................................................ [100%]  exit 0
```

**Context:** Tier B lines 1–2 (`test_batch_c_validation_flow.py`, `test_source_registry.py`) also re-run green (36 tests, exit 0) but are closer to ingestion wiring; not selected as “weakest.”

---

### 证据真实性 — `*-green.txt` 抽检

| File | Execute content | Placeholder? | Audit verification |
|------|-----------------|--------------|-------------------|
| `8.5-green.txt` | `=== 8.5 GREEN ===` / `3 passed` / `exit 0` | **No** — minimal but real | Re-ran `pytest tests/test_batch_d_orchestration_flow.py -q` → **3 passed**, exit 0 ✓ |
| `8.8-green.txt` | `=== 8.8 GREEN ===` / `2 passed` / `exit: 0` | **No** — real step marker, not TBD/stub | Re-ran `pytest tests/test_sync_orchestrator.py -k registry -q` → **1 passed**, exit 0. **Count mismatch:** evidence says 2, current tree has 1 test matching `-k registry` (`test_syncRegistry_cli_syncsYamlToDb`). Likely stale count or deselection drift; command and exit code are authentic. |

Additional spot-check: `8.11-green.txt` references handoff validation + cov 93.80% — consistent with `8.11-final-gates.txt` and audit-prod-path Tier C (excluding Trellis meta).

---

### audit-prod-path（AUDIT.plan §A5）

**Setup (Windows):**

```powershell
Copy-Item -Recurse data .audit-sandbox/prod-path-data
$env:QMD_DATA_ROOT = ".audit-sandbox/prod-path-data"
```

| Step | Command | Result |
|------|---------|--------|
| 1. Data copy | `Copy-Item -Recurse data .audit-sandbox/prod-path-data` | OK |
| 2. `QMD_DATA_ROOT` | `python -c "from backend.app.config import DATA_ROOT; assert '.audit-sandbox' in str(DATA_ROOT)"` | **PASS** — `DATA_ROOT OK: .audit-sandbox\prod-path-data` |
| 3. Tier C pytest (strict §9.3) | `pytest -q --cov=backend --cov-fail-under=75` | **FAIL exit 1** — cov **93.80%** ≥75% but **6 failed**: `test_trellis_validate_execute.py` (2), `test_trellis_validate_plan.py` (4). **329 passed** otherwise. |
| 3b. Tier C (Trellis meta excluded) | same + `--ignore=tests/test_trellis_validate_execute.py --ignore=tests/test_trellis_validate_plan.py` | **PASS exit 0**, cov **93.80%** |
| 4. Smoke | `python scripts/ci_ingestion_smoke.py` | **PASS exit 0** — `ci_ingestion_smoke: ok tables=[...]` `orchestrator_smoke: ok` |

**Tier A (reference):** `pytest tests/test_sync_migration.py tests/test_sync_jobs.py tests/test_sync_orchestrator.py tests/test_batch_d_orchestration_flow.py -q` → **26 passed**, exit 0.

---

### O-3 裁定 — Tier C `--ignore`

| 项 | Execute 陈述 | Audit 独立结论 |
|----|-------------|----------------|
| **现象** | `8.11-final-gates.txt` / handoff **O-3**: Tier C used `--ignore` trellis meta **+** manifest | Confirmed in evidence; handoff **did not** claim strict §9.3 no-ignore green |
| **根因 (Execute)** | Trellis meta `ImportError` on `validate_plan_freeze` relative import (`8.0-baseline.txt`); manifest failures (**O-2**) | **O-2 resolved** in current tree: `implement.jsonl` L69 lists `tests/test_batch_d_orchestration_flow.py`; `pytest tests/test_manifest_protocol.py -k batch_d` → **7 passed** |
| **当前 strict §9.3** | — | Fails **only** on 6 Trellis meta tests; manifest **no longer** a blocker |
| **业务测试 + cov** | 326 passed (Execute, with both ignores) | **329 passed** (audit, trellis-ignore only); cov **93.80%** |
| **裁定** | — | **ACCEPTABLE disclosure, not evidence fraud.** Execute honestly flagged O-3. For Batch D **business** traceability, Tier C is green once Trellis infrastructure tests are excluded (pre-existing, out of §8 scope). **Recommendation:** §4.3 P2 — fix Trellis meta import OR document §9.3 Tier C exception for meta tests; do **not** require manifest ignore going forward. Strict literal §9.3 remains **open** until meta tests fixed → AC-11 stays at **4**. |

### Related open items (traceability impact)

| ID | Status @ audit | A5 impact |
|----|----------------|-----------|
| O-1 ruff E501 | **open** — 5 errors (3× E501 in `test_trellis_validate_plan.py`) | AC-11 §9.3 `ruff check .` not green; unrelated to sync code |
| O-2 manifest | **closed** — `implement.jsonl` L69 + manifest tests green | Was blocking strict Tier C at Execute handoff; no longer |
| O-3 Tier C ignore | **partially closed** — manifest ignore unnecessary; trellis ignore still required | See adjudication above |

---

### §8 step → AC trace (summary)

| §8 | Deliverable | Primary AC | Green evidence |
|----|-------------|------------|----------------|
| 8.1 | migration 006 | AC-7 | `8.1-green.txt` |
| 8.2 | jobs / six types | AC-1, AC-2 | `8.2-green.txt` |
| 8.3 | orchestrator shell | AC-4 | `8.3-green.txt` |
| 8.4 | ResourceGuard | AC-5 | `8.4-green.txt` |
| 8.5 | incremental E2E | AC-2, AC-6, AC-8 | `8.5-green.txt` |
| 8.6 | backfill sharding | AC-3 | `8.6-green.txt` |
| 8.7 | reconcile | AC-2, AC-6 | `8.7-green.txt` |
| 8.8 | registry bootstrap | AC-10 | `8.8-green.txt` |
| 8.9 | smoke | AC-9 | `8.9-green.txt` |
| 8.10 | docs | AC-12 (handoff) | `8.10-green.txt` |
| 8.11 | Tier C gates | AC-11 | `8.11-final-gates.txt`, `8.11-handoff.txt` |

---

### A5 dimension verdict inputs

- **PASS threshold (AUDIT §4):** all AC trace ≥4 → **met** (AC-11 = 4).
- **§4.3 candidates:** O-1 (ruff), O-3 residual (strict §9.3 / Trellis meta), `8.8-green.txt` pass-count imprecision.
- **No P0 traceability gap** on Batch D acceptance criteria AC-1…AC-10, AC-12.
