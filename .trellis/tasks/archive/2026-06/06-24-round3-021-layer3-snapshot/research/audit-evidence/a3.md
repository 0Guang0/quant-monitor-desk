# A3 audit-security — 静态威胁面

**维度：** A3 · security-and-hardening  
**判定：** PASS

## 任务专属检查（AUDIT.plan §2）

| 检查                 | 命令                                                                                                                         | 结果                                       |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| live fetch / URL     | `rg "https?://" backend/app/layer3_chains/`                                                                                  | 无匹配                                     |
| production_live 启用 | `rg "enable.qmt\|production_live" backend/app/layer3_chains/`                                                                | 无匹配（拒绝逻辑在 builder manifest 校验） |
| DB 写 / migration    | `rg "duckdb\|WriteManager\|INSERT\|UPDATE" backend/app/layer3_chains/`                                                       | 无匹配                                     |
| subprocess/eval      | `rg "subprocess\|eval\(\|exec\(" backend/app/layer3_chains/`                                                                 | 无匹配                                     |
| 三 registry 写       | `git diff specs/contracts/snapshot_lineage_contract.yaml docs/AUDIT_DEFERRED_REGISTRY.md docs/UNRESOLVED_ISSUES_REGISTRY.md` | 无 diff                                    |

## 信任边界

- **输入：** 仅 staged fixture YAML/JSON + `IndustryChainLoadResult`（020 loader 已校验 bundle）
- **拒绝：** `source_mode != staged_fixture_only` → `Layer3SnapshotError`（`snapshot_builder.py:145-150`）
- **as_of：** `_reject_future_bar` fail-closed（`snapshot_builder.py:198-204`）
- **无网络、无 DuckDB、无 WriteManager**

## GitNexus

- `query("layer3 snapshot live fetch production database write")` — 返回 Layer1 ingestion / WriteManager 流程，**与 layer3_chains 无交叉**；确认新 builder 不在生产写路径。

## DOUBT（三类隐蔽威胁）

| 类别        | 发现                                |
| ----------- | ----------------------------------- |
| 硬编码 URL  | 无发现 — `layer3_chains/` 全目录 rg |
| JWT/API key | 无发现                              |
| SQL 拼接    | 无发现 — 本任务无 SQL               |

**风险等级：** 无 P0–P2 发现。
