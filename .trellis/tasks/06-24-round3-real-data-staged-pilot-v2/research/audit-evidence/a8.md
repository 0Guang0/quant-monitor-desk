# A8 — audit-test-gap（QA / testing-guidelines）

**维度：** pytest-isolated + audit-prod-path · **判定：** PASS

## Red Flag / AC 覆盖

| Red Flag / AC | 覆盖 | 证据 |
| ------------- | ---- | ---- |
| R3Y-MUT-PROOF-001 | 4 条 `test_mutationProof_*` | §8.8 |
| AUD-08 closeout gate | `test_stagedPilotV2_closeoutRequiresHashAndRowCountsUnchanged` | §8.9 |
| staged-only | `test_production_live_pilot_policy.py` 9 测 | policy 文件 |
| caps fail-closed | `test_stagedPilotV2_capsExceedingMaxSymbols_rejects` | §8.1 |

## docstring「目的」抽检（通俗中文）

- `tests/test_staged_pilot.py`：**40/40** `def test_*` 含「覆盖范围」「测试对象」「目的/目标」。
- v2 新增 14 测示例：`目的/目标：AC-MUT-001 — 不得将 hash-only 漂移标为 VERIFIED。`（非工程师可懂：防止文件改了但行数没变就误判安全）。
- **违规：** 0

## AUDIT §2 边界补测

| 场景 | Execute 是否列 | Audit 结论 |
| ---- | -------------- | ---------- |
| 非 KEY 表 count 变 | `test_mutationProof_mutationDetectedWhenNonKeyTableRowCountChanges` | 已覆盖 |
| hash 变 / count 不变 | `test_mutationProof_inconclusiveWhenHashChangesKeyCountUnchanged` | 已覆盖（含非 KEY 内容漂移语义） |

**无需 Audit 补测**；未新增 pytest（遵守只读）。

## pytest 复跑

```
tests/test_staged_pilot.py ........................ 40 passed
tests/test_production_live_pilot_policy.py .........  9 passed
合计 49 passed, exit 0
```

## tautological 断言

抽检 v2 测均断言业务字段 / enum / `proof_status`，非 `assert True`。
