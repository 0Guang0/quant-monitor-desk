# A6 Performance / Resource Adversarial Audit

## 结论

**PASS**

A6 维度通过：三次 micro-fetch 的记录耗时均小于 30s/request；请求规模被 `max_rows` 与代码级截断限制住；route preview 与 service path 均经过 ResourceGuard；审计复跑命令只写入 `.audit-sandbox/r3b275-audit`，未写 production/data。

重要限定：Execute 原始 `phase45_perf_budget.json` 仍是 `RE_DEFERRED`，不是 smoke PASS 证据。本 A6 PASS 依赖本审计会话在 audit sandbox 追加复跑的 smoke 结果，以及现有 Phase 3/4 文件证据。A9 汇总时应保留这个证据来源差异。

## 性能/资源检查表

| 检查项                                       | 判定 | 证据                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| micro-fetch elapsed <= 30s/request eco       | PASS | `phase3_raw_micro_fetch_evidence.json`: req1 `latency_ms=894`, req2 `latency_ms=1405`, req3 `latency_ms=9480`，均小于 30000ms。                                                                                                                                                                                  |
| 请求数据量 bounded                           | PASS | Phase 3: `max_rows` 为 10/10/20，`row_count` 为 9/9/20；`backend/app/ops/live_pilot.py` 定义 `MAX_PILOT_ROW_CAP=100` 并拒绝越界；`live_pilot_fetch_ports.py` 对 baostock 循环达到 `max_rows` 后 break，对 akshare 使用 `tail(self.max_rows)`。                                                                   |
| ResourceGuard 在 route/fetch 路径可见        | PASS | Phase 2/3 route preview 均记录 `resource_guard_decision="OK"`；`preview_live_pilot()` 调用 `DataSourceService.check_resource_guard()`；`DataSourceService.fetch()` 在 adapter fetch 前执行 `ResourceGuard(con=con).check()`，PAUSE/HARD_STOP 会阻断。                                                            |
| eco resource contract                        | PASS | `configs/resource_limits.yaml` 与 `specs/contracts/resource_limits.yaml` 默认 profile 为 `eco`；eco 限制为 `max_threads=2`, `process_rss_pause_mb=1200`, `process_rss_hard_stop_mb=1800`, `duckdb_memory_max_mb=1536`, `max_parallel_non_core_jobs=1`。                                                          |
| production-equivalent smoke in audit sandbox | PASS | 复跑命令：`.\.venv\Scripts\python.exe scripts\production_equivalent_smoke.py --use-service-path --data-root .audit-sandbox\r3b275-audit`，并将 `TMP/TEMP` 与 pytest `--basetemp` 指到同一 sandbox；结果 `ALL PASS`, `elapsed_s=8.08`, `pytest_steps=5`, `guard_status="observable"`, `shard_count_benchmark=3`。 |
| 审计 sandbox 不污染 production/data          | PASS | smoke 使用 `QMD_DATA_ROOT=C:\Users\Guang\Desktop\quant-monitor-desk\.audit-sandbox\r3b275-audit`；Phase 3/4 evidence 均记录 production DB hash / row counts unchanged；本审计未运行任何写 production/data 的命令。                                                                                               |
| 真实数据量级合理                             | PASS | Phase 3 live raw evidence 为三 authorized requests，单标的/单指标、recent window、小行数：baostock 9 行、akshare sidecar 9 行、akshare macro 20 行；closeout 是 `PILOT_FAIL_SOURCE`，未将其扩大为 production-live readiness。                                                                                    |

## 证据缺口

- Execute `phase45_perf_budget.json` 为 `status="RE_DEFERRED"`、`elapsed_seconds=0`，说明 Execute 阶段没有完成 perf smoke；本 A6 的 smoke PASS 是审计阶段复跑证据，不是 Execute 原始证据。
- `phase45_perf_budget.json` 的 `closure_test` 写的是 `uv run python scripts/production_equivalent_smoke.py --data-root .audit-sandbox/batch275-perf-smoke`，未包含 `--use-service-path`；脚本只有 `--use-service-path` 分支会运行 `tests/test_resource_guard.py` 并将 `guard_status` 置为 `observable`。本审计已用更严格命令补测。
- `AUDIT_DEFERRED_REGISTRY.md` 没有 Round 3 Batch 2.75 的 `R3-B25-PERF-BUDGET-01` / `R3-B25-HYG-03` 明细行；这些 OPEN 项只在 `final_registry_update.md` / `final_pilot_closeout.md` 中保留。A6 不改 registry，但建议 A9/registry 维度确认是否需要补入 SSOT。

## 发现项

| 严重级别 | 发现                                                                                                                           | 是否进 §4.3 | 理由                                                                                                                                                            |
| -------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Medium   | Execute perf artifact 仍为 `RE_DEFERRED`，且 closure_test 缺少 `--use-service-path`，不能单独证明 ResourceGuard smoke 已完成。 | No          | A6 已在审计 sandbox 用 `--use-service-path` 成功复跑并补足本维度判定；这是证据来源/后续清理问题，不是当前 A6 阻断。若 A9 要求 Execute evidence 自包含，可升级。 |
| Low      | `AUDIT_DEFERRED_REGISTRY.md` 未列出 Batch 2.75 perf OPEN 项的明细行。                                                          | No          | `final_registry_update.md` 与 closeout 已保留 OPEN/closure test；本项属于 registry consistency 交叉检查，交由 A5/A9 判定。                                      |
| Low      | GitNexus live graph 不可用。                                                                                                   | No          | 不影响本 A6 的文件证据和 sandbox smoke 判定，但不能把 live `query()` / `impact()` / `detect_changes()` 当作绿色证明。                                           |

## GitNexus 可用性限制

`research/gitnexus-audit-summary.md` 记录本 Codex 会话没有暴露 GitNexus MCP resources；`node .gitnexus/run.cjs status` 在 network sandbox 下因 npm registry `EACCES` 失败。我也调用 `list_mcp_resources` 确认返回空列表。因此本 A6 仅使用冻结索引摘要、被动 surfaced related-symbol 提示、源码/证据读取和 audit-sandbox smoke；没有 live GitNexus `query()` / `impact()` / `detect_changes()` 结果。
