# A1 audit-spec — B01-023 Layer5 证据链规格审计

**维度：** A1 · trellis-check + doubt-driven-development  
**模型：** composer-2.5  
**模式：** 只读  
**Worktree：** `quant-monitor-desk-wt-023-layer5` · `feature/round3-023b-evidence-chain-full`  
**任务：** `.trellis/tasks/round3-023b-evidence-chain-full/`  
**判定：** **FAIL**（1 项 BLOCKING）

---

## 执行摘要

| 检查轴 | 结果 |
|--------|------|
| 023A 未推翻 | **PASS** |
| Layer5 独占 / forbidden 路径 | **PASS**（核心）；**NON-BLOCKING** 越界附带改动 |
| playbook §8.4 Tier A | **PASS** |
| 契约 vs 实现一致性 | **FAIL（BLOCKING）** |
| Trace Authority | **PASS** |

**BLOCKING：** `layer5_evidence_contract.yaml` 将 `deferred_to_023b` 清空为 `[]`，但 `futures_bar_daily` / `options_chain_snapshot` / `event_registry` 及 financial/valuation staged 校验器与测试均未交付；与契约 `models` 段及 AC-023-2 文案不一致。

---

## trellis-check 压缩步骤（1–7）

### 1. 变更范围

```text
git diff --name-only master...HEAD（worktree，2026-06-25）
```

**023b 核心（allowed）：**

- `backend/app/layer5_evidence/evidence_chain.py`（新建）
- `backend/app/layer5_evidence/evidence_validator.py`（新建）
- `backend/app/layer5_evidence/instrument_registry.py`（新建）
- `backend/app/layer5_evidence/models.py`（扩展 +50 行）
- `backend/app/layer5_evidence/ports.py`（新建）
- `specs/contracts/layer5_evidence_contract.yaml`
- `tests/test_layer5_evidence_chain.py`
- `tests/fixtures/layer5_staged_evidence/minimal_bundle.json`
- `docs/adr/ADR-023-layer5-conflict-review-path.md`

**023A 基线（未改）：**

- `backend/app/layer5_evidence/foundation.py` — `git diff master...HEAD` 空
- `backend/app/layer5_evidence/lineage.py` — 空
- `tests/test_layer5_evidence_foundation.py` — 空

**forbidden 目录（未写）：**

- `backend/app/layer3_chains/**` — 无 diff
- `backend/app/layer4_markets/**` — 无 diff
- `backend/app/ops/data_health.py` 等 ops 主体 — 无 diff

**计划外 diff（`research/worktree-slices.md` allowed 表外）：**

| 路径 | 性质 |
|------|------|
| `tests/test_ops_data_health.py` | 仅改 `_V2_EVIDENCE` 指向 `tests/fixtures/data_health/v2_integration_bundle` |
| `tests/fixtures/data_health/v2_integration_bundle/**` | 新增 fixture 副本 |
| `docs/quality/coordination/BATCH_01_MAIN_SESSION_COORDINATOR_PLAYBOOK.md` | worktree 自 master 开枝时 playbook 尚未入 master，整文件新增 |
| `docs/generated/docs_specs_index.generated.md` | loop 索引刷新 |
| `tests/test_catalog.yaml` | catalog 登记 |

### 2. 任务工件

| 工件 | 状态 |
|------|------|
| `prd.md` | 存在 |
| `MASTER.plan.md` | §2 AC-023-1..6、§3.3 forbidden、§8.4 映射完整 |
| `AUDIT.plan.md` | A1 矩阵：scope 无泄漏；023A 未推翻 |
| `plan.freeze.md` | 存在 |

### 3–4. 包上下文 / Spec Quality

- 触及包：`backend/app/layer5_evidence`（Layer5 证据）
- `layer5_evidence` 内无 `storage` / `ops` / `layer3` / `layer4` import（`rg` 仅 `ports.py` 文档字符串提及 storage）
- 无 live fetch、无 DuckDB/WriteManager 写路径

### 5. 项目检查（playbook §8.4）

```bash
# 本审计会话执行（Tier A）
uv run pytest tests/test_layer5_evidence_chain.py tests/test_layer5_evidence_foundation.py -q
# → 12 passed, exit 0

# Tier B（Execute 证据，未在本会话重跑全库）
# research/execute-evidence/full-pytest-green.txt → 全库绿（含 skip）
```

### 6. 跨层

- 触及层：Layer5 生产 + L5 测试；上游 L3/L4 **只读引用**（`LayerContextBundle.upstream_snapshot_ids` 测试常量）
- 无 Storage→Service→API 新链路；无 import 环

### 7. manifest vs diff

| manifest | 点名文件 | diff 一致 |
|----------|----------|-----------|
| `audit.jsonl` | `layer5_evidence_contract.yaml` | ✅ |
| `audit.jsonl` | `layer5_security_evidence.md` | 只读对照 |
| `audit.jsonl` | `023_implement_layer5_evidence_chain.md` | 只读对照 |
| `check.jsonl` | playbook §8.4 | ✅（分支内新增） |

---

## Trace Authority（AUDIT.plan §0.1 / source-index §A–§C）

| 条目 | 核对问题 | 结果 | 证据 |
|------|----------|------|------|
| 原始任务卡 | scope/AC/Red Flags 进入 MASTER？ | PASS | `research/original-plan-trace.md`；MASTER §2、§7 |
| task README / input index | Plan 入口合规？ | PASS | `research/source-index.md` §A–§C 索引完整 |
| unresolved coverage | R3-PARTIAL-4 / R2-RISK-2 有 defer/ADR？ | PASS | `ADR-023-layer5-conflict-review-path.md`；`ports.py`（R2-RISK-2）；registry 主会话 defer |
| round map | batch/out-of-scope 与 MASTER §4 一致？ | PASS | Track B · Wave D；`source-index.md` §A |
| source-index | manifest 血缘完整？ | PASS | §B 与 `implement.jsonl` 对齐 |
| omission-check | 地图倒查无遗漏？ | PASS（Plan 级） | SCI-C 低/中风险已登记 §16 pytest gate |
| integration-ledger | context packing 一致？ | PASS | `context_pack.json` + ledger |

---

## §3.1 检查项表

| 检查项 | 结果 | 证据 |
|--------|------|------|
| trellis-check 步骤 1–7 | PASS | 本节各小节 |
| diff vs audit/check manifest | PASS | `audit.jsonl` 点名文件已覆盖 |
| Trace Authority 继承 | PASS | `research/source-index.md` §A–§C |
| 023A 未推翻 | **PASS** | `foundation.py`/`lineage.py`/`test_layer5_evidence_foundation.py` 零 diff；foundation pytest 绿 |
| Layer5 独占（§2.5） | **PASS** | L3/L4/ops 主体无写；`layer5_evidence/**` 独占实现 |
| forbidden：live / prod 写 / registry 直接闭合 | **PASS** | 无 mutation 代码；ADR 声明 registry 主会话 |
| playbook §8.4 Tier A | **PASS** | chain+foundation pytest exit 0 |
| playbook §8.4 Tier B | PASS（Execute 证据） | `full-pytest-green.txt` |
| 契约 `deferred_to_023b` vs 实现 | **FAIL** | 见下文 BLOCKING |
| AC-023-2 bar/event/financial/valuation | **FAIL** | 仅 `SecurityBarDaily` + `test_securityBar_rejectsFutureTradeDate` |
| Agent 非事实源（AC-023-3） | PASS | `test_evidenceChain_rejectsAgentTextAsFact`；复用 `EvidenceFoundationValidator` |
| severe→queued（AC-023-4） | PASS | ADR-023 + `test_evidenceChain_severeConflictQueuesManualReview` |
| GitNexus query | PASS（已查） | `layer5 evidence chain` → 无 L5→storage 执行流；`Layer5LineageBuilder` 定义存在未改 |

---

## BLOCKING 详情：契约过度闭合

**权威：** `specs/contracts/layer5_evidence_contract.yaml`（audit.jsonl 点名；对抗性权威第一级）

**变更：**

```yaml
# master（023A 后）
deferred_to_023b:
  - security_bar_daily ingestion
  - futures_bar_daily ingestion
  - options_chain_snapshot ingestion
  - event_registry ingestion
  - evidence_chain_builder full pipeline

# 本分支
deferred_to_023b: []
```

**实现对照：**

| 契约 `models` 键 | staged 模型/校验器 | 测试 |
|------------------|-------------------|------|
| `instrument_registry` | `InstrumentRegistryValidator` | `test_instrumentRegistry_uniqueIds` ✅ |
| `security_bar_daily` | `SecurityBarDaily` + `StagedEvidenceValidator` | `test_securityBar_rejectsFutureTradeDate` ✅ |
| `futures_bar_daily` | **无** | **无** |
| `options_chain_snapshot` | **无** | **无** |
| `event_registry` | **无** | **无** |
| financial / valuation（module doc §3.5–3.6） | **无** | **无** |
| `evidence_chain` builder | `EvidenceChainBuilder`（staged，非 ingestion pipeline） | 4 项 chain 测试 ✅ |

**与 MASTER 张力：** MASTER §3.2 将 financial/valuation **全量 ingestion** defer 为 staged-only；但 §2 AC-023-2 仍写「bar/event/financial/valuation staged 行符合 contract」。Execute 仅兑现 `security_bar` 切片，却在 §9.6 将契约 defer 列表清空，造成 **契约 SSOT 声称 023b 已全部兑现，而代码/测试未覆盖契约所列多数 model**。

**修复方向（供 Execute/主会话，非本审计写入）：**

1. 恢复或部分保留 `deferred_to_023b`（列出未交付项），**或**
2. 补齐 futures/options/event 最小 staged dataclass + validator + 各 1 条 contract 测试，**或**
3. 在契约中显式将未实现 model 标为 `staged_validator_deferred` 并更新 `layer5_security_evidence.md` 交叉引用。

---

## 计划外发现

| ID | 严重度 | 发现 | 证据 |
|----|--------|------|------|
| OOB-1 | NON-BLOCKING | `test_ops_data_health.py` 改 fixture 路径（B01-DH2 测试域） | `_a7_diff_ops.txt`；未改 `data_health.py` 主体 |
| OOB-2 | NON-BLOCKING | 分支内整文件新增 playbook（worktree 基线早于 master playbook 提交） | `_a7_diff_playbook.txt`；合并时须与主会话协调 rebase |
| OOB-3 | NON-BLOCKING | `v2_integration_bundle` fixture 副本入仓 | diff `tests/fixtures/data_health/` |
| OOB-4 | 建议 | `validate_bar_from_bundle` 零调用方；`build_from_port` 未走 bar 校验 | `evidence_chain.py:85-89`（与 A2 交叉） |

**已对抗搜索：** `backend/app/layer5_evidence/**` 全文；`rg financial|valuation|futures|event_registry`；`git diff` forbidden 树；契约 vs `models.py` / `evidence_validator.py`。

---

## DOUBT

| 问题 | 结论 |
|------|------|
| 原始 scope 是否进入 MASTER/AUDIT？ | 是；`023` §16/§17、`R3D-023-01`..`05`、playbook §8.4 均已映射 |
| 023A 是否被平行第二套 identity 推翻？ | 否；`EvidenceChainBuilder` 注入 `EvidenceFoundationValidator`；023A 文件零 diff |
| 若只跑 MASTER §5.3 用例，生产仍会漏什么？ | 契约所列 futures/options/event/financial/valuation 行校验未实现；`deferred_to_023b: []` 掩盖缺口 |
| Track B 是否混 Batch 01 PR 声称？ | MASTER/AUDIT 均显式 Track B；diff 无 Batch 01 六卡 runtime 改动 |

---

## A1 checklist

- [x] trellis-check 步骤 1–7 有证据
- [x] diff vs audit/check manifest
- [x] Trace Authority 已继承
- [x] Plan omission（地图/轮次/索引）无阻断遗漏
- [x] GitNexus `query` 已执行（`layer5 evidence chain builder upstream snapshot`）

---

## Verdict

**FAIL** — 契约 `deferred_to_023b: []` 与 staged 实现/测试覆盖不一致（**BLOCKING**）。023A 基线、Layer5 文件锁、playbook §8.4 Tier A 与 forbidden 路径均 **PASS**；附带 `test_ops_data_health` / playbook fixture 越界为 **NON-BLOCKING**，合并前由主会话协调。

**解除 FAIL 最低条件：** 契约 defer 列表与实现/测试对齐（恢复 defer 或补齐 model 覆盖 + 测试），并更新 `layer5_evidence_contract.yaml` 与 MASTER §2 AC-023-2 映射一致。
