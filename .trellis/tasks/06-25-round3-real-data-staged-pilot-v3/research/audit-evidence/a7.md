# A7 — audit-ops（DuckDB / 运维）

**维度：** cli-sandbox + audit-prod-path · **判定：** PASS  
**Worktree：** `quant-monitor-desk-wt-b01-sp3` · **分支：** `feature/round3-real-data-staged-pilot-v3`  
**基线：** `f3163bf5` · **审计模式：** 只读（无 migration / 无 DB write / 无 pytest 执行）

## 检查项

| 检查 | 命令/证据 | 结果 |
| ---- | --------- | ---- |
| diff 无 schema/migration | `git diff master --name-only -- specs/schema backend/app/db/ scripts/init_db.py` | **空** — 无变更 |
| diff 无 migration 文件 | `git diff master --name-only` 不含 `specs/schema/**`、`backend/app/db/migrate.py` | PASS |
| v3 无 production clean write | `rg` v3 符号区（L2493+）：`allow_clean_write: False`、`production_clean_write: false`、`clean_write_attempted: false`；无新增 `apply_migrations`/`writer()` 调用 | PASS |
| v3 测试不触 migration | `tests/test_real_data_staged_pilot_v3.py` 无 `apply_migrations`/`init_db`；`test_v3_conflict_dry_run_no_clean_write` patch `WriteManager.write` 断言未调用 | PASS |
| sandbox 写隔离 | `run_staged_pilot_raw_only` 经 `_ensure_sandbox_db(sandbox_root)` 写 **sandbox** `quant_monitor.duckdb`；`DEFAULT_SANDBOX_ROOT_V3 = .audit-sandbox/r3e-staged-pilot-v3` | PASS |
| 工作区无 prod DuckDB | `Test-Path data/duckdb/quant_monitor.duckdb` → **NOT FOUND**；`data/` 仅 `logs/failed_write_audit.ndjson`（9081 B，非 duckdb） | PASS |
| audit-prod-path hash | 审计前后均无 `data/duckdb/` → **N/A**（无可污染对象） | N/A |
| mutation proof 语义 | `build_production_mutation_proof` 在 DB 缺失时 `proof_status=INCONCLUSIVE`、`reason=production_db_file_missing`（`mutation_proof.py` L97–108）；Execute `no_mutation_proof_v3.md` 一致 | PASS（fail-closed） |
| AUDIT_PROD_ROOT | `.audit-sandbox/r3e-pilot-v3-audit-prod/` 未创建（本维只读未跑 audit-prod-path 复制树） | 待 A5 复跑；本维无 prod 面风险 |

## GitNexus

| 动作 | 结果 |
| ---- | ---- |
| `query("staged pilot v3 duckdb write migration")` | 命中 layer2 writer / `WriteManager` 通用流；**未**索引 `run_full_staged_pilot_v3`（greenfield，见 `gitnexus-execute-summary.md`） |
| `context("run_full_staged_pilot_v3")` | not found（索引滞后；不影响本维静态结论） |

v3 入口 `run_full_staged_pilot_v3` 仅：caps →（可选）sandbox raw fetch → taxonomy/conflict JSON → `write_no_mutation_proof_v3` → closeout；**不**调用 production `ConnectionManager.writer()`。

## DOUBT

| 质疑 | 结论 |
| ---- | ---- |
| 第二次 init/migrate 幂等？ | 本任务未改 schema/migration → **不适用** |
| kill 中途 migration 恢复？ | 未触及 migration 路径 → **不适用** |
| `closeout_pass: false` 是否运维 FAIL？ | **否**。`pilot_v3_closeout.json` 因 prod DB 缺失致 `mutation_proof_status=INCONCLUSIVE`；`production_live_readiness_claim=false`、`sandbox_clean_write_rehearsal=false`；符合 staged-only 契约，非 prod 写风险 |
| live fetch 是否污染 prod？ | `run_staged_pilot_raw_only` 在 fetch 前后对 `DEFAULT_PRODUCTION_DB` 做 **只读** hash/count 快照；写路径绑定 `sandbox_db`（L731–756） |

## 计划外发现

| ID | 严重性 | 发现 |
| -- | ------ | ---- |
| — | — | 已对抗搜索 diff、`staged_pilot.py` v3 区、mutation_proof 契约、data/ 目录；**无计划外 prod DB/migration 面** |

## 结论

B01-SP3 staged pilot v3 为 **证据 JSON + sandbox raw-only** 路径；diff 无 schema/migration；工作区无 production DuckDB → hash 审计 **N/A**；运维面无 prod clean write 风险。**A7 PASS**。
