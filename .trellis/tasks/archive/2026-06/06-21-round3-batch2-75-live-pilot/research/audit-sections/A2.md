# A2 Ponytail Audit — 过度工程

## 结论

PASS

A2 找到多处可简化点，且满足 audit-skill-registry 的对抗触发器：至少 1 处 >=20 行可简化/删除代码。未发现必须进入 Audit §4.3 的阻塞型 bloat；现有复杂度主要来自 Batch 2.75 live pilot 的 fail-closed 授权、sandbox/no-production-mutation 证据、HITL 与 Request 2 sidecar 边界，不能为了短代码而删掉安全/数据防护。

## 可简化项表

| ID    | 文件/行                                                                      | 估算可删行 | 风险       | 是否进 §4.3 | A2 判断                                                                                                                                                                                                                                          |
| ----- | ---------------------------------------------------------------------------- | ---------: | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A2-S1 | `tests/test_batch275_live_pilot_gate.py` L72-L155                            |      35-45 | Low        | No          | 三个授权 gate 测试重复手写同一个 `LivePilotRequest` 和 no-fetch patch 结构。可用 `approved_pilot_requests()[0]` + `dataclasses.replace(...)`，并抽一个 `_assert_blocks_before_fetch(request, exc_type)` helper；保留 fail-closed/no-fetch 断言。 |
| A2-S2 | `backend/app/ops/live_pilot.py` L291-L325, L443-L524, L813-L932, L1132-L1149 |      25-35 | Medium     | No          | Phase 1/2/3/4 都重复生产 DB row-count/hash snapshot 与 mutation proof dict。可提取 `_snapshot_db_state(db_path)` + `_mutation_proof(before, after, *, db_path, phase_key=None)`；注意不得弱化 production DB unchanged 证据。                     |
| A2-S3 | `backend/app/ops/live_pilot.py` L545-L557                                    |       8-12 | Low        | No          | `capture_phase3_raw_evidence` 手动复制 `LivePilotRequest` 字段。已有 `dataclasses.replace` import，可直接 `replace(request, dry_run=False, raw_only=True, write_target="sandbox", allow_clean_write=False)`。                                    |
| A2-S4 | `backend/app/ops/live_pilot_fetch_ports.py` L140-L231                        |      20-30 | Low-Medium | No          | 两个 akshare port 重复 import/call/error/empty/tail/to-json 包装。可抽 `_akshare_frame_payload(fetch_fn, *, empty_message, metadata, max_rows)`；保留 equity/macro endpoint 与 payload 语义分开，避免把 Request 2 与 DGS10 shape probe 混同。    |
| A2-S5 | `scripts/production_equivalent_smoke.py` L100-L157                           |      30-45 | Medium     | No          | `--use-service-path` 分支像可选 runbook scaffolding；Batch 2.75 实际 Phase 4.5 已 re-defer，默认路径才是当前引用点。若后续 A6 不再使用 service-path smoke，可删该分支；本轮不建议进 §4.3，因为脚本是既有生产等价 smoke 入口。                    |

最大单项可简化块：A2-S1，测试重复 request/no-fetch scaffolding 约 35-45 行，可在不降低安全断言的前提下删除。估算净 shrink：约 118-167 LOC，其中本轮最安全可做的是 A2-S1 + A2-S3。

## 保留复杂度理由

| 复杂度                                                                                                                           | 保留理由                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `validate_authorization` 的文件名、批准标记、approved triple、raw_only、sandbox、row cap 检查                                    | 这是 fail-closed 授权边界，不能合并成宽松配置或删掉分支。                                                           |
| `run_live_pilot_raw_only` 中 HITL、route READY、fixture FetchPort rejection、sandbox path containment、production mutation proof | 这些是 018B/MASTER 明确要求的数据防护；缩短时只能抽 helper，不能删除检查。                                          |
| `_InlineConnectionFileRegistry`                                                                                                  | 这是同一 writer connection 下避免嵌套写锁的窄适配，不是单实现 interface/factory；删除可能破坏 raw fetch 注册路径。  |
| Request 2 `stock_zh_a_hist` vs Sina sidecar 分类、conflict inspect 文案                                                          | 这是防止 sidecar evidence 错关原请求的安全/语义边界，不能按“只是报告文字”删除。                                     |
| 三个 live FetchPort class                                                                                                        | 它们实现既有 FetchPort 协议的不同 vendor/operation 行为，不是本任务新造的抽象层；可抽重复包装，但不应合并业务语义。 |
| `production_equivalent_smoke.py` 默认 isolated `QMD_DATA_ROOT` 与 metrics                                                        | 保证 smoke 不触碰项目 DB/source tree；这类隔离逻辑不是 bloat。                                                      |

## GitNexus 可用性限制

- 已读 `research/gitnexus-audit-summary.md`：本地索引存在，indexed commit `43ce2ae65a262f35e8e2790b0db54cc91b0765d1`，6263 symbols / 10281 relationships / 276 flows。
- 本 Codex 会话未暴露 GitNexus MCP resources；`node .gitnexus/run.cjs status` 在网络 sandbox 下因 npm registry `EACCES` 失败。
- 因此 A2 未能执行 live `query()` / `impact()` / `detect_changes()`；本审计基于 frozen GitNexus 摘要、Execute GitNexus artifacts、`git status`、`rg` 与源码逐读。
- A2 结论不依赖实时图谱为绿；若 A9 要把任何 shrink 提升为 repair，repair 前仍应按项目规则恢复 GitNexus impact/detect_changes 或记录同等限制。
