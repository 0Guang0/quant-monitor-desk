# completion-check

- 角色：`audit`
- 日期：`2026-07-11`
- 对应 plan：`task/task-01-source-registry/task_plan.md`
- 对象范围：`task-01-source-registry` 的 Source Registry / Source Capability / 平台启用矩阵；入口为 `qmd-sync-registry`（同 `scripts.sync_registry:main`）、`DataSourceService` 路由预览与 `qmd data` 路径；档位为默认本地产品配置与隔离 DuckDB 验证，不把 replay、fixture 或 live 请求当作同一档位。
- 声称：`本子模块已按 MIGRATION_MAP 索引设计 100% 落地，可作为 R4 最终成品发布`
- 权威：`MIGRATION_MAP.md` 数据源注册与路由索引；`task/task-01-source-registry/README.md` 列出的 design 文件；该票 README 的运行时 SSOT/契约清单。
- 正式入口：`qmd-sync-registry` / `scripts.sync_registry:main`；数据使用入口 `DataSourceService → SourceRoutePlanner → SourceCapabilityRegistry`；`qmd data` 相关 CLI。
- 声称档位：`默认本地产品配置（未授权源必须保持禁用）`；registry 同步在 `C:\tmp\qmd-task01-audit` 隔离数据根复现。

## 逐对象关账记录

| CC        | 具体场景示范                                                                                  | 本对象运行事实                                                                                                                                                                                                                                                                            | 证据 / 反证                                                                                                                                                                                                                                                                                         | Verdict | 闭环控制                                                                                                                                         |
| --------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| CC-0 对靶 | 用户要 registry/capability 成为后续路由与抓取的单一配置地基，不能只证明 YAML 能解析           | 任务 README、MIGRATION_MAP 与 design 一致界定了本票：源、能力、平台启用策略；RoutePlan 的运行时选路明确属于 task-02。                                                                                                                                                                     | 完整读取 README、MIGRATION_MAP 的本模块索引及全部列名 design；反证：未把 route-plan、实际 fetch、clean-write 的下游能力误计为 task-01 完成。                                                                                                                                                        | PASS    | 本票目标、范围、权威、入口与档位已明确；后续 FAIL 不以范围歧义抵消。                                                                             |
| CC-1 证伪 | 测试用 patch 打开禁用源，生产默认仍关闭                                                       | 定向套件 exit 0，registry CLI 在隔离库同步 25 行；但 `tests/service_path_support.py` 用 monkeypatch 打开 source/domain/capability，生产/CLI 共享的增量辅助也以同类内存覆盖使路由变绿。                                                                                                    | 复现：Python 载入默认配置得到 `cn_equity_daily_bar=READY/baostock`、`cn_equity_minute_bar=DISABLED_SOURCE`。反证：移除 runtime override 时默认禁用保持；`enabled_source_registry`、`_platform_allows=lambda` 可使同一未配置源的路径不再反映 YAML/平台门。测试绿灯因此不能证明正式配置约束仍被守住。 | FAIL    | 拒绝成品声称。需删除/替换测试与正式路径的专属注入，以未修改的正式入口覆盖 enabled、disabled、授权缺失、capability 缺失及平台不支持的可观察结果。 |
| CC-2 验真 | 权威要求 capability 是细粒度机器权威，产物仍声明 draft/implementation gap                     | `SourceRegistry` 已有加载、角色校验、禁用域和 DuckDB 同步；`SourceCapabilityRegistry` 可检查 source/domain/operation。可是 capability SSOT 顶层仍为 `status: draft`，并明载 adapter 域名与 registry 域名的 production alignment 尚未完成。                                                | `source_capabilities.yaml:2,12`；运行时读取显示 25 sources、44 domain roles、root status `draft`。反证：该文件自身的 `implementation_gap` 即表明拿掉“完整实现”假设后，能力-适配器对齐尚无正式闭环。                                                                                                 | FAIL    | 将 capability machine contract 提升为已验收状态，并实现/验证完整适配器域对齐或有明确兼容映射；以正式 fetch 前 gate 的全量反证重新验收。          |
| CC-3 同路 | CLI/增量入口在内存把默认禁用配置改为可用                                                      | 常规 `DataSourceService` 使用 YAML registry、capability registry 与 platform matrix；`backend/app/cli/data_commands.py` 和 `backend/app/ops/macro_incremental_common.py` 另走 `enabled_source_registry`，改写 frozen record、替换 `get_domain_roles` 并把 `_platform_allows` 强制设为真。 | `data_commands.py:304-323,793-796`；`macro_incremental_common.py:133-155,429-438`；反证：同一分钟线请求默认返回 `DISABLED_SOURCE/user_authorization_required`，覆盖路径可绕过 source/domain/platform 配置。                                                                                         | FAIL    | 所有 CLI、job、preview 和测试改为读取同一 registry/matrix，并仅通过经过授权的配置变更启用源；同参横测应得同 route status、候选及错误码。         |
| CC-4 验档 | 默认禁用/需授权的配置被 fixture、dry-run 或 runtime override 升格                             | 默认路由预览正确把 `cn_equity_minute_bar` 标为 `DISABLED_SOURCE`，而部分增量路径标为 staged fixture 并强制开放 registry/platform。                                                                                                                                                        | 运行时预览实测见 CC-1；`macro_incremental_common.py` 的 `staged_fixture_mode=True` 与 platform override。反证：未授权、缺配置并未始终维持同一产品档位与资格。                                                                                                                                       | FAIL    | 将 replay/fixture/dry-run 结果与默认产品配置严格分离；任何发布/成品证据只能来自未覆盖的正式档位，低档必须显式标注且不得授权默认启用。            |
| CC-5 对表 | 设计规定 source_registry/source_capabilities 是下游只读 SSOT，未经配置的 QMT/xqshare 必须关闸 | 设计规定 Primary/Validation/FallbackPolicy、禁止 silent fallback、QMT/xqshare 默认禁用且缺授权须返回诚实状态；基础 registry 行为基本实现，但 runtime override 与 capability draft/gap 同权威偏离。                                                                                        | design `data_sources.md` §5.3、默认启用与 domain gating；`qmt_xqshare_setup.md` §3-4；README:32-34。反证：在默认配置中 minute route 正确关闭；覆盖路径却改变了同一规则的形、义和行为。                                                                                                              | FAIL    | 先按 design 修复单一 SSOT 接线、授权/平台 gate 和 capability 对齐，再以完整配置—路由—fetch 前 gate 对照证明形义行为效果一致。                    |
| CC-6 清债 | 临时调用方补丁一直保留为 runtime 行为                                                         | 代码注释明确说明“until registry reconcile / Wave 3 P7”，但补丁位于正式 `backend/app/ops` 与 CLI 路径，且 task_plan 仍为未开工，没有本票可验收的移除闭环。                                                                                                                                 | `fred_incremental_watermark.py:17-44`、`macro_incremental_common.py:133-155`；task_plan Phase 1-5 全 pending。反证：共享根因未修，至少多个调用方各自开启源/绕平台。                                                                                                                                 | FAIL    | 不得把此问题登记后宣称本票完成。须从共享 registry 授权接缝消除调用方补丁、在正式配置中表达所需状态，并重新运行受影响 CLI/job/route 证据。        |
| CC-7 守闸 | 缺用户授权的 QMT/xqshare 应诚实未完成，不可用替身开绿                                         | 默认路径对未授权 QMT 维持 `DISABLED_SOURCE`，这是正确的诚实关闸；但其它有效入口可强制开启 source/domain 并绕开平台资格，因此未完成状态不一致。                                                                                                                                            | 默认分钟路由实测；`platform_source_matrix.yaml` 与 `qmt_xqshare_setup.md` 规则；CC-3 的反证。                                                                                                                                                                                                       | FAIL    | 维持所有未授权源在全入口的 `DISABLED_SOURCE` / `USER_AUTH_REQUIRED`，只允许用户授权配置解除，并以同一正式入口再验收。                            |

## Summary

- 首个决定性缺口：`CC-1` — 通过 runtime registry/platform override 与 fixture/staged 路径，测试/增量绿灯无法证伪“YAML 与平台资格是唯一启用策略”的生产保证。
- 最终状态：`OPEN`
- 声称结论：`denied`
- 闭环控制：`先消除 CLI/job/test 对 registry、domain role、platform gate 的内存覆盖；完成 capability 适配器域对齐并删除 source_capabilities.yaml 的 draft/implementation_gap；随后在隔离数据根通过未覆盖的 qmd-sync-registry、route preview、CLI/job 正式入口重跑 enabled/disabled/auth/capability 边界及全量 pytest。`
