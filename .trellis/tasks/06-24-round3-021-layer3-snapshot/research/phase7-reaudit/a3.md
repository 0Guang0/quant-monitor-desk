# A3 audit-security — Phase 7 复审计

| 字段             | 值                                                                                                                                                                                                           |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 任务             | `06-24-round3-021-layer3-snapshot`                                                                                                                                                                           |
| 审计基线         | `wt-r3-pilot-v2` @ `fb044931`（B-19 staged pilot v2 收口）                                                                                                                                                   |
| 当前分支         | `feature/round3-021-layer3-snapshot` @ `f3ec38d`                                                                                                                                                             |
| 维度             | A3 · security-and-hardening + doubt-driven-development                                                                                                                                                       |
| 派发模型         | composer-2.5                                                                                                                                                                                                 |
| 权威已读         | `agents/audit-adversarial-authority.md`、`agents/security-auditor.md`、`021_implement_layer3_snapshot_builder.md`、`R3Y_real_data_staged_pilot_v2.md` §9、`snapshot_lineage_contract.yaml`、AUDIT.plan §2 A3 |
| 模式             | 只读；无 commit                                                                                                                                                                                              |
| **判定**         | **PASS**                                                                                                                                                                                                     |
| **BLOCKING**     | 0                                                                                                                                                                                                            |
| **NON-BLOCKING** | 4                                                                                                                                                                                                            |

---

## 1. 审计范围

### 1.1 变更文件（021 Execute 威胁面）

| 路径                                            | 状态           | 性质                              |
| ----------------------------------------------- | -------------- | --------------------------------- |
| `backend/app/layer3_chains/snapshot_builder.py` | `??` untracked | 新增 — 核心 builder               |
| `backend/app/layer3_chains/lineage.py`          | `??` untracked | 新增 — lineage 封装               |
| `backend/app/layer3_chains/models.py`           | `M`            | 修改 — snapshot/lineage dataclass |
| `tests/test_layer3_snapshot_builder.py`         | `M`            | 修改 — AC 安全相关测试            |
| `tests/fixtures/layer3_l5_staged_bars/**`       | `??` untracked | staged L5 fixture                 |

**相对 `fb044931` 的生产代码增量（021 专属）：** `models.py` +54 行；`snapshot_builder.py` / `lineage.py` / fixture 为工作区未跟踪交付物。`loader.py`（020）本分支未改。

**未触及（MASTER §3.2 / §3.3 / R3Y 禁止路径）：**

| 路径                                             | `git diff fb044931`                 |
| ------------------------------------------------ | ----------------------------------- |
| `specs/contracts/snapshot_lineage_contract.yaml` | 零变更                              |
| `docs/AUDIT_DEFERRED_REGISTRY.md`                | 零变更                              |
| `docs/UNRESOLVED_ISSUES_REGISTRY.md`             | 零变更                              |
| `specs/layer3_global_industry_chains/**`         | 零变更                              |
| `backend/app/datasources/`                       | 零变更（021 scope 外）              |
| `backend/app/ops/staged_pilot.py`（HEAD commit） | 与 `fb044931` 一致；B-19 能力未回退 |

### 1.2 威胁面（本任务）

本地 staged-only 数据路径：YAML fixture → `yaml.safe_load` / `json.loads` → 内存 dataclass → lineage envelope。**无** HTTP API、**无** ops CLI 入口、**无** DuckDB / WriteManager、**无** live vendor fetch、**无** subprocess / eval。

R3Y 任务卡 §9 禁止项（production clean write、live fetch、QMT/xqshare、unbounded network）在本模块均不适用 — 021 未引入网络或写库面。

---

## 2. AUDIT.plan §2 任务专属静态检查

| 检查项                          | 命令 / 方法                                                                                                                              | 结果                                                       |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| live fetch / 硬编码 URL         | `rg "https?://" backend/app/layer3_chains/`                                                                                              | **无匹配**                                                 |
| production_live / qmt / xqshare | `rg "enable\.qmt\|enable\.xqshare\|--enable-qmt\|production_live\|requests\.\|httpx\.\|urllib" backend/app/layer3_chains/`               | **无匹配**                                                 |
| 三 registry 写                  | `git diff fb044931 -- docs/AUDIT_DEFERRED_REGISTRY.md docs/UNRESOLVED_ISSUES_REGISTRY.md specs/contracts/snapshot_lineage_contract.yaml` | **无 diff**                                                |
| DB 写 / migration               | `rg "duckdb\|WriteManager\|writer\(\|apply_migrations\|INSERT \|UPDATE \|DELETE " backend/app/layer3_chains/`                            | **无匹配**                                                 |
| SQL 拼接                        | `rg 'f".*SELECT\|f'"'"'.*SELECT\|\.format\(.*SELECT\|execute\(f' backend/app/layer3_chains/`                                             | **无匹配**                                                 |
| 密钥 / token 模式               | `rg "api[_-]?key\|secret\|token\|password\|Bearer \|JWT" backend/app/layer3_chains/`                                                     | **无匹配**                                                 |
| subprocess / eval / exec        | `rg "subprocess\|os\.system\|eval\(\|exec\(" backend/app/layer3_chains/`                                                                 | **无匹配**                                                 |
| 不安全反序列化                  | `rg "yaml\.load\(\|pickle\|marshal" backend/app/layer3_chains/`                                                                          | **无匹配**（`yaml.safe_load` @ `snapshot_builder.py:140`） |
| fixture 硬编码 URL              | `rg "https?://" tests/fixtures/layer3_l5_staged_bars/`                                                                                   | **无匹配**                                                 |

---

## 3. 信任边界与控制点

### 3.1 双闸门：staged-only

**020 loader**（输入侧，本分支未改）：

```53:58:backend/app/layer3_chains/loader.py
        mode = str(manifest.get("loader_mode", ""))
        if mode != "staged_fixture_only":
            raise IndustryChainLoadError(
                f"loader_mode {mode!r} is not staged_fixture_only; "
                "production-live registry load is forbidden on 020"
            )
```

**021 builder**（L5 bar 侧）：

```145:150:backend/app/layer3_chains/snapshot_builder.py
    mode = str(raw.get("source_mode", ""))
    if mode != "staged_fixture_only":
        raise Layer3SnapshotError(
            f"source_mode {mode!r} is not staged_fixture_only; "
            "non-staged L5 source is forbidden on 021"
        )
```

测试：`test_layer3Snapshot_nonStagedL5Source_rejects` 将 `source_mode` 改为 `production_live` 并断言拒绝。

### 3.2 as_of 未来数据 fail-closed

```198:204:backend/app/layer3_chains/snapshot_builder.py
def _reject_future_bar(*, as_of: datetime, anchor_id: str, bar: dict[str, Any]) -> None:
    visibility = _parse_ts(bar["as_of_timestamp"])
    if visibility > as_of:
        raise Layer3SnapshotError(
            f"future input blocked for {anchor_id!r}: "
            f"as_of_timestamp {visibility!s} > snapshot as_of {as_of!s}"
        )
```

测试：`test_snapshotRejectsFutureInput`。契约 `no_future_data` 对齐 `snapshot_lineage_contract.yaml:22`。

### 3.3 Lineage 契约校验（复用 L2 内核）

`Layer3LineageBuilder` 调用 `validate_source_dataset_ids`（拒绝 agent 文案进 `source_dataset_ids`）与 `assert_lineage_fields_complete`（`backend/app/core/snapshot_lineage.py`）。无 DuckDB 写回；`upstream_snapshot_ids` 默认空元组（MASTER §3.2 ADV-R3X defer 边界 intact）。

### 3.4 任务卡 Red Flags 对照（021 §13）

| Red Flag                     | 021 实现                               | 结论                  |
| ---------------------------- | -------------------------------------- | --------------------- |
| Agent 自由 SQL / 联网 / 写库 | 无 SQL、无网络、无 WriteManager        | PASS                  |
| 绕过 WriteManager            | 无 clean table 写                      | PASS                  |
| 大查询无 ResourceGuard       | 小型 staged fixture 扫描；无全市场路径 | PASS（见计划外 NB-3） |
| 改无关模块                   | diff 限于 `layer3_chains` + tests      | PASS                  |

### 3.5 数据流

```
STAGED_LAYER3_BUNDLE_DIR (020) ──► IndustryChainLoadResult
STAGED_LAYER3_L5_BARS_DIR (021) ──► manifest.yaml (safe_load)
         │                                    │
         └──────── IndustryChainSnapshotBuilder.build() ──► 内存 tuple 结果
```

---

## 4. GitNexus 证据

| 调用                                                                  | 结果                                                           | 解读                          |
| --------------------------------------------------------------------- | -------------------------------------------------------------- | ----------------------------- |
| `query("layer3 snapshot builder security write path staged fixture")` | 返回 Layer1 ingestion / WriteManager / phase4 clean write 流程 | **与 `layer3_chains` 无交叉** |
| `context("IndustryChainSnapshotBuilder", file_path=…)`                | Symbol not found                                               | 新文件未入索引                |
| `context("Layer3LineageBuilder", file_path=…)`                        | Symbol not found                                               | 同上                          |

**索引说明：** greenfield 交付物尚未被 GitNexus 收录；静态结论以 `rg` + 源码阅读为准。合并前建议 `node .gitnexus/run.cjs analyze`。

---

## 5. DOUBT — 三类隐蔽威胁

| 类别                   | 搜索范围                                                                  | 结论                           |
| ---------------------- | ------------------------------------------------------------------------- | ------------------------------ |
| **硬编码 URL 变体**    | `backend/app/layer3_chains/**`、`tests/fixtures/layer3_l5_staged_bars/**` | **无发现**                     |
| **JWT / API key 模式** | 同上 + 密钥 rg 基线                                                       | **无发现**                     |
| **SQL 拼接**           | `backend/app/layer3_chains/**`                                            | **无发现** — 模块无 DuckDB/SQL |

**对抗性追问（计划未写）：**

| 追问                                         | 搜索                               | 结论                                                    |
| -------------------------------------------- | ---------------------------------- | ------------------------------------------------------- |
| `l5_bundle_dir` 能否指向任意可读路径？       | `build()` 参数 + `Path.resolve()`  | 可读任意 YAML manifest；**仅读、无写** → P3（见计划外） |
| `event_only` 锚点是否绕过 future bar 校验？  | `snapshot_builder.py:64-80`        | 设计如此（无 bar 输入）；非 production_live 旁路        |
| 能否通过畸形 YAML 触发代码执行？             | `yaml.safe_load` only              | **无发现**                                              |
| MASTER 未列的 registry / contract mutation？ | `git diff fb044931` forbidden 路径 | **无发现**                                              |

---

## 6. 发现清单（§3.3 格式）

| 威胁                       | 发现                                       | 等级   | 证据                                             |
| -------------------------- | ------------------------------------------ | ------ | ------------------------------------------------ |
| 未授权 live fetch          | 双 `staged_fixture_only` 闸门 + 测试       | —      | `snapshot_builder.py:145-150`；`loader.py:53-58` |
| 三 registry / contract 写  | diff 为空                                  | —      | `git diff fb044931`                              |
| 生产 DB 写                 | 无 WriteManager/duckdb                     | —      | `rg` 无匹配                                      |
| SQL 注入                   | 无 SQL 层                                  | —      | `rg` 无匹配                                      |
| R3Y production clean write | 021 无写路径                               | —      | 数据流 §3.5                                      |
| 错误信息路径泄露           | `Layer3SnapshotError` 含 manifest 绝对路径 | **P3** | `snapshot_builder.py:138,142`                    |
| `l5_bundle_dir` 任意路径读 | `resolve()` 后读 YAML，无白名单            | **P3** | `snapshot_builder.py:55`                         |

**P0–P2：无。**

---

## 计划外发现

> 对抗性搜索已完成；下表为 MASTER §8 / AUDIT.plan §2 未显式列出、但静态审阅与 R3Y/契约信任边界对照后的项。

| ID   | 发现                                                                                                                  | 严重度 | 阻断         | 证据 / 说明                                                                                  |
| ---- | --------------------------------------------------------------------------------------------------------------------- | ------ | ------------ | -------------------------------------------------------------------------------------------- |
| UF-1 | `l5_bundle_dir` 调用方可传入任意可读目录，builder 仅 `resolve()` 后 `read_text` manifest                              | P3     | NON-BLOCKING | `snapshot_builder.py:53-56`；本任务无 CLI/API 入口；Batch 6 ops 切片应加 PROJECT_ROOT 白名单 |
| UF-2 | 异常消息嵌入 `manifest_path` 绝对路径                                                                                 | P3     | NON-BLOCKING | `snapshot_builder.py:138,142`；当前仅本地库；未来 HTTP/ops 暴露时需 redaction                |
| UF-3 | L5 manifest `yaml.safe_load` 无文件大小/锚点数量上限                                                                  | P3     | NON-BLOCKING | `snapshot_builder.py:140`；ponytail 天花板；恶意超大 YAML 可导致内存尖峰（本地信任边界）     |
| UF-4 | 021 模块无专属 `agent_outputs_not_source` pytest；依赖 `core/snapshot_lineage.validate_source_dataset_ids` 运行时校验 | P3     | NON-BLOCKING | `lineage.py:41`；L1 已有同类测试；建议 A8 补 layer3 负面用例（非 A3 阻断）                   |
| UF-5 | 核心交付物 `snapshot_builder.py` / `lineage.py` / fixture 仍为 untracked                                              | 流程   | NON-BLOCKING | `git status`；合并前须纳入版本控制；不影响静态威胁判定                                       |
| UF-6 | GitNexus 未收录新符号                                                                                                 | 流程   | NON-BLOCKING | `context()` Symbol not found                                                                 |

**已搜索且无计划外安全问题的领域：** live URL 变体、JWT/API key 明文、SQL 拼接、subprocess/eval、pickle/yaml.load、三 registry 写、WriteManager 旁路、QMT/xqshare enablement。

---

## 7. 结论

021 Execute 在 A3 静态威胁面下 **PASS**（BLOCKING 0）：

1. **无** registry 写、**无** lineage contract 写、**无** §3.2 forbidden 路径变更；R3Y §9 禁止项未在本模块引入。
2. **无** live fetch、**无** production DB / WriteManager、**无** SQL 面。
3. staged-only 与 as_of 边界在代码与测试中均有 fail-closed 控制；双闸门对齐 BATCH3 staged downstream gate。
4. GitNexus 查询未将新 builder 连入 Layer1 生产写路径；索引滞后记为 NB，不阻断 PASS。

**建议（非阻断）：** 合并后刷新 GitNexus 索引；未来 `layer3 build-snapshot` CLI 对 `l5_bundle_dir` 做根目录白名单；ops 暴露错误前做路径 redaction。

---

_审计员：A3 security-auditor 模板 · Phase 7 复审计 · 基线 `fb044931` · 2026-06-24_
