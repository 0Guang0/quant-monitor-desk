# A3 安全审计证据 — R3G-01 Sandbox Clean-Write Rehearsal

| 字段     | 值                                                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 任务     | `06-27-06-27-round3g-sandbox-rehearsal`                                                                                                                                     |
| 维度     | A3（security）                                                                                                                                                              |
| 模型     | composer-2.5                                                                                                                                                                |
| 模式     | 只读（未改业务代码）                                                                                                                                                        |
| 权威     | `agents/audit-adversarial-authority.md` · `agents/security-auditor.md` · `AUDIT.plan.md` §1 A3 · `reference_adoption_guardrails.yaml` · `sandbox_clean_write_contract.yaml` |
| 审计范围 | `backend/app/ops/sandbox_clean_write/**` · `backend/app/cli/data_commands.py`（`sandbox_clean_write_rehearse`）· CLI 接线 `backend/app/cli/main.py`                         |

---

## 执行证据

### AUDIT.plan §1 A3 命令

```text
rg "参考项目" backend/app/ops/sandbox_clean_write backend/app/cli/data_commands.py
→ 0 matches（无 runtime 参考项目 import / 路径字面量）
```

### A3 基线静态扫描（审计范围内）

| 模式                                                 | 范围                                          | 结果      |
| ---------------------------------------------------- | --------------------------------------------- | --------- |
| `api_key\|secret\|token\|password\|https?://`        | `sandbox_clean_write/**`                      | 0 matches |
| `sys.path\|参考项目\|openbb\|order_value\|run_daily` | `sandbox_clean_write/**`                      | 0 matches |
| `f\".*SELECT\|execute(f`                             | `sandbox_clean_write/**`                      | 0 matches |
| `enable.qmt\|qmt_auto\|captcha`                      | `sandbox_clean_write/**`                      | 0 matches |
| `r3g03\|R3G_03\|limited.production`                  | `sandbox_clean_write/**` · `data_commands.py` | 0 matches |

### GitNexus

| 调用                                                                | 结果                                                                                           |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `impact(run_sandbox_clean_write_rehearsal, upstream)`               | **Symbol not found**（索引未收录新符号）                                                       |
| `impact(sandbox_clean_write_rehearse, upstream)`                    | **Symbol not found**                                                                           |
| `context(run_sandbox_clean_write_rehearsal)`                        | **Symbol not found**                                                                           |
| `query("sandbox clean write rehearsal production mutation bypass")` | 返回 live_pilot / staged_pilot / mutation_proof 相关流程，**未**命中 R3G-01 新模块（索引滞后） |

> **注：** GitNexus 索引落后于本 diff；静态审阅以源码 + pytest 守卫为准。建议 merge 前 `node .gitnexus/run.cjs analyze` 刷新索引。

### 契约对照（任务卡 Red Flags）

| 检查项                    | 证据                                                                                                   | 判定                |
| ------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------- |
| 无参考项目 runtime import | rg 0 + `test_r3g01SandboxCleanWrite_noReferenceProjectRuntimeImport`                                   | ✅                  |
| 无生产 mutation           | CLI + runner 双 fail-closed（见下）                                                                    | ✅                  |
| FRED auth fail-closed     | `validate_fred_authorization` + 默认 artifact 路径                                                     | ✅（见 P2-01 细项） |
| cap 不可静默扩大          | `validate_source_caps` 读契约 YAML；候选集冻结 `r3g_p0`                                                | ✅                  |
| 无密钥泄露                | 源码无硬编码 key；`.audit-sandbox/.../fred_user_authorization.yaml` 仅引用 `api_key_env: FRED_API_KEY` | ✅                  |
| R3G-03 未提前打开         | 无 r3g03 / limited production 入口                                                                     | ✅                  |

---

## P0–P3 发现表

| ID  | 等级 | BLOCKING | 标题                      | 位置 | 摘要 |
| --- | ---- | -------- | ------------------------- | ---- | ---- |
| —   | —    | —        | _本审计范围内无 P0/P1 项_ | —    | —    |

| ID    | 等级 | BLOCKING     | 标题                                                            | 位置                                                 | 摘要                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | ---- | ------------ | --------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2-01 | P2   | NON-BLOCKING | FRED 排练授权未校验 `FRED_API_KEY` 环境变量                     | `rehearsal_plan.py:130-164`                          | `validate_fred_authorization` 仅校验 YAML artifact（`authorization_present`、series 白名单、`allow_production_clean_write: false`、cap），**不**像 `fred_sandbox_pilot.validate_authorization` 那样要求 `api_key_env` 已设置。当前 runner **不发起** live FRED HTTP，dry-run 用 fixture，故无即时 exploit；若未来 `--allow-live-fetch` 接线 live port，存在授权语义缺口。 |
| P2-02 | P2   | NON-BLOCKING | 排练内 validation_report 为合成 PASSED，与 data-health 结果脱钩 | `rehearsal_runner.py:130-159` · `304`                | `_ensure_validation_report` 硬编码 `status=PASSED` / `can_write_clean=True`；报告字段 `validation_status` 恒为 `"PASSED"`，不反映 `dh_report.overall_status`。sandbox 内仍经 `WriteManager`+`DbValidationGate`，但**不能**证明真实 DH→validation→write 闭环；R3G-02 adversarial audit 须识别此排练捷径。                                                                  |
| P3-01 | P3   | NON-BLOCKING | `--allow-live-fetch` 暴露但 runner 未接 live fetch 链           | `rehearsal_runner.py:328-329` · `346` · `main.py:76` | `allow_live_fetch` 仅影响 `DataSourceService(staged_fixture_mode=...)`；数据加载走 `load_rehearsal_bundle`，`dry_run=false` 时要求 `evidence_dir`，**无**网络 fetch。旗标为将来扩展预留，当前无 bypass，但增加误用面。                                                                                                                                                    |
| P3-02 | P3   | NON-BLOCKING | 生产 DB 拒绝列表为固定两路径 resolve 相等                       | `rehearsal_runner.py:77-85`                          | 拒绝 `DEFAULT_PRODUCTION_DB` 与 `DATA_ROOT/duckdb/quant_monitor.duckdb` 的 **resolve 后相等**。对 symlink/hardlink 到生产库的路径可拦截；对「拷贝生产库到新路径作 sandbox」不拦截（属运维选择，非生产 mutation）。与全仓库 `DEFAULT_PRODUCTION_DB` 定义一致。                                                                                                             |
| P3-03 | P3   | NON-BLOCKING | GitNexus 未索引新符号                                           | GitNexus MCP                                         | `run_sandbox_clean_write_rehearsal` / `sandbox_clean_write_rehearse` impact/context 均 not found；不影响运行时安全，影响变更 blast-radius 自动化。                                                                                                                                                                                                                        |

---

## 计划外发现

> 对抗性搜索：除 AUDIT.plan 点名项外，额外对照契约信任边界、mutation bypass、密钥旁路、cap 扩大、R3G-03 越界。

| ID     | 等级 | BLOCKING     | 发现                                                          | 证据                                                                                                | 建议                                                                                                 |
| ------ | ---- | ------------ | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| OOB-01 | P2   | NON-BLOCKING | 合成 validation 可误导 R3G-02「validation gate 已实证」结论   | `rehearsal_runner._ensure_validation_report` + `build_rehearsal_report(validation_status="PASSED")` | R3G-02 审计须将 `quality_flags=r3g01_sandbox_rehearsal` 标为排练专用；BLOCK 若有人将其复用到生产路径 |
| OOB-02 | P3   | NON-BLOCKING | `load_rehearsal_bundle` 对非 fred 源有额外 `len>3` 硬编码守卫 | `rehearsal_loader.py:158-159`                                                                       | 与 `validate_source_caps` 冗余但无害；保持单一 cap SSOT 在 `rehearsal_plan` 即可                     |
| OOB-03 | P3   | NON-BLOCKING | SQL 拼接类 DOUBT：范围内无 `execute(f"...")`                  | rg 0 matches                                                                                        | 无发现                                                                                               |
| OOB-04 | P3   | NON-BLOCKING | URL/API key DOUBT：范围内无硬编码 URL 或密钥                  | rg 0 matches                                                                                        | 无发现                                                                                               |
| OOB-05 | —    | —            | 无 agent-triggered write / 交易 API / OpenBB runtime copy     | guardrails tests + 静态扫描                                                                         | 无发现                                                                                               |

---

## 关键控制点审阅（PASS 依据）

### 1. 生产 mutation fail-closed（双闸）

**CLI 闸** — 缺 `--no-production-mutation` 即失败：

```265:271:backend/app/cli/data_commands.py
    if not no_production_mutation:
        raise CliFailure(
            error_code="USER_AUTH_REQUIRED",
            message="--no-production-mutation is required for sandbox clean-write rehearsal",
            ...
        )
```

**Runner 闸** — 同名标志 + 生产 DB 路径拒绝：

```77:85:backend/app/ops/sandbox_clean_write/rehearsal_runner.py
def assert_sandbox_db_allowed(sandbox_db: Path, *, no_production_mutation: bool) -> Path:
    if not no_production_mutation:
        raise RehearsalRunnerError("--no-production-mutation is required for R3G-01")
    resolved = _resolve_path(sandbox_db)
    prod = DEFAULT_PRODUCTION_DB.resolve()
    default_prod = (DATA_ROOT / "duckdb" / "quant_monitor.duckdb").resolve()
    if resolved in {prod, default_prod}:
        raise RehearsalRunnerError("production DB path refused for sandbox rehearsal")
```

排练结束写入 `production_db_no_mutation_proof.md`（`build_production_mutation_proof`），`production_mutation_allowed: false` 贯穿 payload。

### 2. FRED fail-closed

- 候选集含 fred 时 **必须** 通过 `validate_fred_authorization`（`rehearsal_plan.py:188-193`）。
- 缺失 artifact → `RehearsalPlanError`（测试 `test_RehearsalPlan_fredRequiresAuthorizationArtifact`）。
- `allow_production_clean_write: true` → 拒绝（`rehearsal_plan.py:145-146`）。
- 超 cap series/window → 拒绝。
- 默认 artifact：`.audit-sandbox/round3g/fred_user_authorization.yaml`（无密钥，仅 `api_key_env` 名）。

### 3. Cap 与候选集边界

- 仅注册 `r3g_p0` 冻结三元组（baostock 3 symbols / cninfo 1 / fred 3 series）。
- `validate_source_caps` 动态读取 `sandbox_clean_write_contract.yaml` 的 `r3g01_*` 字段。
- 无用户自定义 symbol 列表 API；未知 `candidate_set` → `RehearsalPlanError`。

### 4. 参考项目采纳守卫

- 模块内无 `参考项目` 字面量、无 `sys.path` 变异、无 OpenBB runtime import。
- `WriteManager` / `DbValidationGate` / `ResourceGuard` / `DataSourceService` 组合调用，无 `layer1_axes.ingestion` 写路径（测试 `test_RehearsalRunner_doesNotCallLayer1IngestionWrite`）。

### 5. 密钥与日志

- 排练代码不读取/打印 `FRED_API_KEY`。
- 报告 JSON 含路径与 write_id，不含 env 秘密。

---

## 正面观察

- CLI 默认 `dry_run=True`（`BooleanOptionalAction`），与 sandbox 排练 posture 一致。
- SQL 均参数化（`INSERT ... VALUES (?, ...)`）。
- 契约 `required_report_fields` 在 `build_rehearsal_report` 构建时强制校验。
- `test_reference_adoption_guardrails.py` 已扩展 R3G-01 专用扫描（import / 交易 API / OpenBB / production_live_claim）。
- R3G-03 limited production entry **未**在本 diff 中出现。

---

## 结论

| 项                      | 判定                                            |
| ----------------------- | ----------------------------------------------- |
| **A3 总评**             | **PASS**                                        |
| P0                      | 0                                               |
| P1                      | 0                                               |
| P2（NON-BLOCKING）      | 2（P2-01 FRED env 语义；P2-02 合成 validation） |
| P3（NON-BLOCKING）      | 3                                               |
| 计划外                  | 5 项已记录；无 BLOCKING                         |
| R3G-03 越界             | 未发现                                          |
| 参考项目 runtime import | 未发现                                          |
| 生产 mutation bypass    | 未发现（双 fail-closed + mutation proof）       |

**合并门禁建议：** A3 不阻断 R3G-01 Execute handoff。建议在 R3G-02 adversarial audit 中显式追踪 P2-02（合成 validation 捷径）与 P2-01（若 live fetch 接线须补齐 `FRED_API_KEY` fail-closed）。合并前刷新 GitNexus 索引以便后续变更 impact 分析。

---

_生成：Trellis Audit A3 · composer-2.5 · 2026-06-27 · 只读_
