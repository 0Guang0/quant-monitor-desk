# A5 — audit-completion（AC 追溯 + 抽检）

**维度：** trace-ac + cli-sandbox + audit-prod-path · **判定：** PASS

## AC 评分（1–5）

| AC#        | 追溯链                                                                                         | 分  | 证据                                                       |
| ---------- | ---------------------------------------------------------------------------------------------- | --- | ---------------------------------------------------------- |
| AC-SP2-01  | R3Y → §8.1 → `test_stagedPilotV2_capsJson_*` → `pilot_v2_caps.json`                            | 5   | execute-evidence + pytest                                  |
| AC-SP2-02  | R3Y → §8.2 → `test_stagedPilotV2_baostockExpanded_*` → `raw/staging_evidence_manifest_v2.json` | 5   | 同上                                                       |
| AC-SP2-03  | R3Y → §8.3 → `test_stagedPilotV2_cninfoExpanded_*` → `cninfo_schema_notes_v2.md`               | 5   | 同上                                                       |
| AC-SP2-04  | R3Y → §8.4 → `test_stagedPilotV2_akshareValidation_*` → `akshare_validation_taxonomy_v2.json`  | 5   | 同上                                                       |
| AC-SP2-05  | R3Y → §8.5 → `test_stagedPilotV2_routePreviewMatrixV2_*` → `route_preview_matrix_v2.json`      | 5   | 同上                                                       |
| AC-SP2-06  | R3Y → §8.6 → `test_stagedPilotV2_validationReportV2_*` → `validation_report_v2.json`           | 5   | 同上                                                       |
| AC-SP2-07  | R3Y → §8.7 → `test_stagedPilotV2_conflictSummaryV2_*` → `conflict_check_summary_v2.json`       | 5   | 同上                                                       |
| AC-SP2-08  | AUD-04 → §8.8 → MUT tests → `no_mutation_proof_v2.md`                                          | 4   | md 含 hash/count 字段；prod DB 缺失 → INCONCLUSIVE（预期） |
| AC-SP2-09  | R3Y → §8.9 → closeout tests → `pilot_v2_closeout.json`                                         | 5   | per_source 矩阵完整                                        |
| AC-MUT-001 | AUD-04 → §8.8 → 4× `test_mutationProof_*`                                                      | 5   | `MUTATION_DETECTED` Literal 已加                           |

## §10 最弱 2 行抽检

| 原 §10 行           | 复跑命令                                                      | exit | 与 Execute        |
| ------------------- | ------------------------------------------------------------- | ---- | ----------------- |
| Tier A staged pilot | `uv run pytest tests/test_staged_pilot.py -q`                 | 0    | 一致（40 passed） |
| production policy   | `uv run pytest tests/test_production_live_pilot_policy.py -q` | 0    | 一致（9 passed）  |

## execute-evidence 真实性

| 文件                      | 判定       | 说明                                                       |
| ------------------------- | ---------- | ---------------------------------------------------------- |
| `8.8-green.txt`           | 弱但可复现 | 仅 `....` + EXIT:0；Audit 独立复跑 4 测绿                  |
| `8.9-green.txt`           | 弱但可复现 | 仅 `..` + EXIT:0；closeout 测绿                            |
| `no_mutation_proof_v2.md` | PASS       | 含 `proof_status`、R3Y-MUT-PROOF-001 说明，非占位符        |
| `pilot_v2_closeout.json`  | PASS       | 含 `db_hash_unchanged`/`row_counts_unchanged`/`per_source` |

## audit-sandbox

`uv run pytest tests/test_staged_pilot.py -q --basetemp=.audit-sandbox/r3y-pilot-v2-audit/pytest` → **40 passed, exit 0**

## audit-prod-path

- 工作区 **无** `data/duckdb/quant_monitor.duckdb` → 无法做 hash 前后对比。
- `uv run pytest tests/test_staged_pilot.py tests/test_production_live_pilot_policy.py -q` → **49 passed**；无 prod 数据可污染。
- **结论：** N/A（无 prod 库副本）；staged-only 约束由 policy 测守卫。

## DOUBT

九切片 §8.1–8.9 green.txt 均为短点阵 → 建议 §4.3 记录为 evidence 格式改进项，**不阻断** AC 闭环。
